name: CTO PR-target forensic New-main closure — RETIRED
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'RETIRED: historical pull_request_target New-main writer disabled.'
          echo 'No target mutation. No reconstruction. No production writes.'