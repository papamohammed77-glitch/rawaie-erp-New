name: P163 exact Main2 ownership surgery — RETIRED

# Retired after forensic review.
# The historical executor could rewrite Current/PWA/New-main.
# P163 closure evidence is retained in Git history and reports; verification is now read-only.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired writer notice
        run: |
          echo 'RETIRED: historical P163 target-writing executor disabled.'
          echo 'P163 historical evidence remains preserved in Git history.'
          echo 'Use the read-only exact New-main verifiers for fresh verification.'
          echo 'No target mutation. No reconstruction. No production writes.'
