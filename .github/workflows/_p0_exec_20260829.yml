name: RAWAEA Main PWA P1 Forensic Repair (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired: replaced by serialized Gold reconstruction gates. This workflow must never mutate main or main1 automatically.'
