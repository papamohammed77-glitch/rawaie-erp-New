# Frozen legacy executor — retained for audit/history; manual invocation only.
name: CTO Whole New-main Gold Diamond Closure — Frozen Legacy
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'FROZEN: superseded by authoritative structural closure runner.'