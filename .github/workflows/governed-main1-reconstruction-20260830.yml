name: RAWAEA MAIN1 Governed Reconstruction 20260830 — ARCHIVED VERIFY ONLY

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'Current/PWA/main/main1.md'

permissions:
  contents: read

jobs:
  verify-only:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify archived fragment only
        shell: bash
        run: |
          set -euo pipefail
          test -s Current/PWA/main/main1.md
          ! grep -q "git push" .github/workflows/governed-main1-reconstruction-20260830.yml
          echo "ARCHIVED_VERIFY_ONLY_PASS"
