# TRIGGER FINAL EXECUTION 2026-09-03-NOW
name: Medhat auth login verification
on:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  verify:
    if: ${{ !contains(github.event.head_commit.message, '[CTO-EXECUTED]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 2
      - name: Execute current governed eleven-part builder
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/run_final_main_reconstruction_20260831.py
      - name: Gold/Diamond ownership and structural gates
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import re,hashlib,subprocess,tempfile
          p=Path('Current/PWA/New-main'); s=p.read_text(encoding='utf-8')
          required=['window.RW_Auth','window.RW_Navigation','window.RW_Views','window.RW_OwnerLicense','window.RW_Dashboard','window.RW_Items','window.RW_POS','window.RW_Orders','window.RW_Runsheets','window.RW_Purchases','window.RW_Warehouse','window.RW_Finance','window.RW_Reports','window.RW_HR','window.RW_CRM','RW_SUPABASE_CLIENT','var supabase = RW_SUPABASE_CLIENT']
          missing=[x for x in required if x not in s]
          if missing: raise SystemExit('CURRENT_CONTRACT_MISSING:'+repr(missing))
          compat='/* RAWAEA MAIN2 COMPATIBILITY */'; auth='/* RAWAEA MAIN2 AUTHORITATIVE MODULE */'; version="window.RW_PWA_RECONSTRUCTION_VERSION='MAIN2-COMPLETE-SURGICAL-v1';"; governed='// MAIN2_GOVERNED_CLOSED:v1'
          if compat in s: raise SystemExit('P163_COMPAT_REMAINS')
          if s.count(auth)!=1 or s.count(version)!=1 or s.count(governed)!=1: raise SystemExit('P163_OWNER_CLOSURE_COUNTS')
          if re.search(r'window\.RW_Dashboard\s*=\s*\{\s*render\s*:\s*renderDashboard\s*\}',s): raise SystemExit('P163_DASHBOARD_ALIAS_REMAINS')
          if re.search(r'window\.RW_Items\s*=\s*\{\s*render\s*:\s*renderItems\s*\}',s): raise SystemExit('P163_ITEMS_ALIAS_REMAINS')
          if s.count('window.RW_Dashboard=RW_Dashboard;')!=1 or s.count('window.RW_Items=RW_Items;')!=1: raise SystemExit('P163_AUTH_EXPORT_COUNTS')
          if 'actions' not in s or 'main1Delegation' not in s: raise SystemExit('MAIN1_DELEGATION_CONTRACT_MISSING')
          if 'MAIN3' not in s: raise SystemExit('MAIN3_CONTRACT_MISSING')
          if s.lower().count('<script')!=s.lower().count('</script>') or s.lower().count('<style')!=s.lower().count('</style>'): raise SystemExit('DOCUMENT_BALANCE_FAIL')
          if s.count("navigator.serviceWorker.register('./sw.js',{scope:'./'})")!=1: raise SystemExit('SERVICE_WORKER_CANONICAL_FAIL')
          app=[m.group(1) for m in re.finditer(r'<script(?![^>]*\bsrc\s*=)[^>]*>([\s\S]*?)</script>',s,re.I) if 'serviceWorker.register' not in m.group(1)]
          if len(app)!=1: raise SystemExit('INLINE_RUNTIME_COUNT')
          js=Path(tempfile.gettempdir())/'new-main.js'; js.write_text(app[0],encoding='utf-8')
          r=subprocess.run(['node','--check',str(js)],capture_output=True,text=True)
          if r.returncode: print(r.stderr); raise SystemExit('JS_SYNTAX_FAIL')
          if re.search(r"\.from\(['\"]stock_branches['\"]\)[\s\S]{0,1200}\.(?:insert|update|upsert|delete)\(",s): raise SystemExit('DIRECT_STOCK_WRITER')
          if re.search(r"\.from\(['\"]inventory_log['\"]\)[\s\S]{0,800}\.insert\(",s): raise SystemExit('DIRECT_INVENTORY_LOG_WRITER')
          print('GOLD_DIAMOND_SOURCE_PASS',hashlib.sha256(s.encode()).hexdigest(),len(s.encode()))
          PY
      - name: Browser Gold/Diamond smoke on exact target
        shell: bash
        run: |
          set -euo pipefail
          npm init -y >/dev/null 2>&1
          npm install --no-audit --no-fund playwright@1.55.0 >/dev/null 2>&1
          npx playwright install --with-deps chromium >/dev/null 2>&1
          rm -rf /tmp/rawaea-final && mkdir -p /tmp/rawaea-final
          cp -r Current/PWA/. /tmp/rawaea-final/
          cp Current/PWA/New-main /tmp/rawaea-final/New-main.html
          python3 -m http.server 8123 --directory /tmp/rawaea-final >/tmp/rawaea-final.log 2>&1 &
          node <<'NODE'
          const {chromium}=require('playwright');
          (async()=>{const b=await chromium.launch({headless:true});const p=await b.newPage();const e=[];p.on('pageerror',x=>e.push('page:'+x.message));p.on('console',m=>{if(m.type()==='error')e.push('console:'+m.text())});await p.goto('http://127.0.0.1:8123/New-main.html',{waitUntil:'domcontentloaded',timeout:30000});await p.waitForTimeout(1800);const r=await p.evaluate(async()=>{const S=window.RW_STATE,N=window.RW_Navigation,flat=[];(function w(a){(a||[]).forEach(x=>{if(x.view)flat.push(x.view);if(x.submenu)w(x.submenu)})})(N&&N.menuTree);let denied=false;if(S&&S.app&&N&&N.navigate){S.app.currentUser={isOwner:false};try{await N.navigate('license')}catch(x){denied=x.message==='OWNER_ONLY'}}const req=['dashboard','telesales','customers','online-store','pos','orders','runsheets','suppliers','purchase-pos','purchases','receiving','items','branches','picking','loading','delivery','return','unloading','transfer','direct-sale','direct-return','supplier-return','vouchers','vehicle-count','branch-count','general-count','settlement','reports-dashboard','reports-detailed','reports-comprehensive','hr','crm','users','roles','license','settings','audit-log','notifications'];return{ar:document.documentElement.lang==='ar',body:!!document.body,state:!!S,auth:!!window.RW_Auth,nav:!!N,views:!!window.RW_Views,shell:!!window.RW_ShellContext,owner:!!window.RW_OwnerContract,critical:req.every(v=>flat.includes(v)),denied,count:flat.length,main2:window.RW_PWA_RECONSTRUCTION_VERSION||null}});console.log(JSON.stringify({r,e}));if(e.length||!r.ar||!r.body||!r.state||!r.auth||!r.nav||!r.views||!r.shell||!r.owner||!r.critical||!r.denied||r.count<30||r.main2!=='MAIN2-COMPLETE-SURGICAL-v1')process.exit(2);await b.close()})().catch(x=>{console.error(x);process.exit(2)});
          NODE
      - name: Persist verified target and state
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import hashlib
          target=Path('Current/PWA/New-main').read_text(encoding='utf-8')
          sha=hashlib.sha256(target.encode()).hexdigest(); b=len(target.encode())
          state=Path('CURRENT_STATE.md'); st=state.read_text(encoding='utf-8')
          marker='P163 CLOSED — MAIN2 ownership surgery + governed New-main reconstruction'
          if marker not in st:
              state.write_text(st.rstrip()+f'''\n\n## CTO CLOSURE — 2026-09-03\n\n{marker}\n- Target: `Current/PWA/New-main`\n- Verified SHA-256: `{sha}`\n- Verified bytes: `{b}`\n- Reconstructed from `Current/PWA/main/main1.md` through `main11.md`.\n- Main2 compatibility ownership removed; authoritative Main2 owner retained.\n- Main1 dashboard/items legacy aliases removed.\n- `actions`, `main1Delegation`, and MAIN3 preserved.\n- Static Gold/Diamond gates passed.\n- Browser Gold/Diamond smoke passed against the exact target.\n- No production database writes.\n- Historical `Original/PWA/main/*` remained untouched.\n''')
          report=Path('doc/Draft/Reprots/تقرير26.md')
          if not report.exists(): report.write_text(f'''# RAWAEA ERP — CTO GOLD/DIAMOND CLOSURE\n\nDate: 2026-09-03\nTarget: `Current/PWA/New-main`\nStatus: CLOSED — P163\n\nVerified SHA-256: `{sha}`\nVerified bytes: `{b}`\n\nReconstructed from `Current/PWA/main/main1.md` through `main11.md`. Main2 compatibility ownership was removed, the authoritative owner was retained, Main1 legacy dashboard/items aliases were removed, and actions/main1Delegation/MAIN3 were preserved. Static and browser Gold/Diamond gates passed. No Production database writes were performed. Historical Original parts were not modified.\n''',encoding='utf-8')
          for q in ['.github/workflows/_cto_close_newmain_20260903.yml','.github/workflows/cto_executor_temp_20260903.yml','_cto_trigger_20260903.txt']:
              Path(q).unlink(missing_ok=True)
          PY
          git config user.name 'RAWAEA CTO Executor'; git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Current/PWA/New-main CURRENT_STATE.md doc/Draft/Reprots/تقرير26.md .github/workflows/_cto_close_newmain_20260903.yml .github/workflows/cto_executor_temp_20260903.yml _cto_trigger_20260903.txt
          git diff --cached --check
          git commit -m '[CTO-EXECUTED] New-main Gold/Diamond closure'
          git push origin HEAD:main
