name: CTO New-main Gold Diamond Executor — RETIRED

# Historical executor retained for audit/history only.
# It previously had contents:write and mutated Current/PWA/New-main plus CURRENT_STATE.
# The historical source is preserved in Git history.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'RETIRED: legacy New-main Gold Diamond executor disabled.'
          echo 'No target mutation. No reconstruction. No production writes.'