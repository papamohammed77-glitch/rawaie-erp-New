name: CTO PR-target forensic New-main closure v2 — RETIRED
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'RETIRED: historical pull_request_target New-main writer v2 disabled.'
          echo 'No target mutation. No reconstruction. No production writes.'