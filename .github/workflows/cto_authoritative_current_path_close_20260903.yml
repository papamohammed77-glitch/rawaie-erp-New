name: CTO authoritative Current-PWA New-main closure — RETIRED

# Retired after forensic stabilization. Preserved as evidence only.
# It no longer mutates Current/PWA/New-main and is not used as a persistence authority.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archive notice
        run: echo 'Authoritative target writer retired; exact New-main verifier is read-only.'
