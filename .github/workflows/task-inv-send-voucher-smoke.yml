name: TASK-INV send-stock-voucher smoke
on:
  push:
    branches:
      - task-028-loading-unloading-refactor
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Invoke staging harness
        run: |
          set -euo pipefail
          curl --fail-with-body --silent --show-error --max-time 120 \
            'https://hfzznsiprnwkpayskzhu.supabase.co/functions/v1/task-inv-send-voucher-harness?token=T028-INV-HARNESS-8f2c6d1a' \
            | tee result.json
          python - <<'PY'
          import json
          with open('result.json') as f:
              r=json.load(f)
          assert r.get('success') is True, r
          print(json.dumps(r, indent=2))
          PY
