name: CTO Direct Exact Target Runner — RETIRED

# Retired after forensic review.
# Historical runner could mutate Current/PWA/New-main and push to main.
# Preserved in Git history as evidence only.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired runner notice
        run: |
          echo 'RETIRED: direct target writer disabled.'
          echo 'No target mutation. No reconstruction. No production writes.'
