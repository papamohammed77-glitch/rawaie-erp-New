name: CTO Final New-main Surgical Closure — RETIRED
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archive notice
        run: echo 'Broad New-main surgical writer retired after forensic review; target may only change through explicitly scoped surgery.'
