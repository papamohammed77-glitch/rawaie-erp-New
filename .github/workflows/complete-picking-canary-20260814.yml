name: complete-picking HTTP canary — RETIRED

# Historical reconstruction/persistence workflow retained for audit/history only.
# It previously had contents:write and could rebuild/persist Current/PWA/New-main.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'RETIRED: legacy picking reconstruction canary disabled.'
          echo 'Historical behavior remains available in Git history.'
          echo 'No target mutation. No reconstruction. No production writes.'