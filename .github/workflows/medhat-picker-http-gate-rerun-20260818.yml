name: Medhat picker Production HTTP gate rerun — RETIRED

# Retired after forensic review.
# Historical workflow could reconstruct and persist Current/PWA/New-main and CURRENT_STATE.
# Historical content is preserved in Git history; this workflow is no longer a persistence authority.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired writer notice
        run: |
          echo 'RETIRED: historical picker target writer disabled.'
          echo 'No target mutation. No reconstruction. No production writes.'