name: CTO Single-File New-main Closure — RETIRED

# Retired after forensic stabilization. Preserved as historical evidence only.
# It no longer mutates Current/PWA/New-main.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archive notice
        run: echo 'Single-file New-main target writer retired; use the read-only exact-target verifier.'
