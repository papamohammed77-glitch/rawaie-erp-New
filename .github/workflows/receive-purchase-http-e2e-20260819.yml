name: receive-purchase Production HTTP E2E 20260819
on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [ctorepair-receive-purchase-http-e2e2-20260819]
permissions: {}
jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Execute Production HTTP harness
        run: |
          set -euo pipefail
          echo 'HARNESS_HEALTH_V3'
          curl --silent --show-error --max-time 180 \
            'https://fiilmooggumokxanwiyx.supabase.co/functions/v1/receive-purchase-runtime-e2e-20260819?token=RPO-CTO-E2E-20260819' | tee result.json
          python - <<'PY'
          import json
          r=json.load(open('result.json'))
          print(json.dumps(r,ensure_ascii=False,indent=2))
          assert r.get('success') is True, r
          assert r.get('health') is True, r
          PY
