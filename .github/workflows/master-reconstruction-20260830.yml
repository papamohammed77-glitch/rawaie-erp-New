name: RAWAEA TEMP TARGET-ONLY CTO EXECUTOR — RETIRED

# Historical reconstruction executor retained for audit/history only.
# It previously had contents:write and could reconstruct/patch/push Current/PWA/New-main.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'RETIRED: historical master reconstruction writer disabled.'
          echo 'Historical source remains preserved in Git history.'
          echo 'No target mutation. No reconstruction. No production writes.'