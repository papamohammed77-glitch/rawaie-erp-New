name: CTO Final Exact-Target Forensic Closure — RETIRED

# Retired after forensic review.
# This workflow previously had a target-writing path that could mutate Current/PWA/New-main.
# It is preserved as historical evidence only and is no longer an application persistence authority.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired writer notice
        run: |
          echo 'RETIRED: historical target-writing closure executor disabled.'
          echo 'Use cto_final_runtime_closure_20260903.yml or cto_final_observable_20260903.yml for read-only verification.'
          echo 'No target mutation. No reconstruction. No production writes.'
