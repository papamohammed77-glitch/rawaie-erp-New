name: CTO PWA Runtime Gold 20260903
on:
  workflow_dispatch:
permissions:
  contents: read

jobs:
  retired-verifier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Explain canonical executor
        run: |
          echo 'This workflow is retained as a historical/manual verifier.'
          echo 'Canonical automatic PWA closure executor: _cto_pwa_gold_direct_20260903.yml'
          echo 'Target: Current/PWA/New-main'
