name: CTO New-main Gold Now 20260903
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Retired destructive executor
        run: |
          echo 'Retired: historical baseline restore executor disabled.'
          echo 'Target-preserving closure is owned by cto_trusted_artifact_closure_20260903.'
