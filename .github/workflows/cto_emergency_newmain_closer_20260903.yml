name: CTO Emergency New-main Closure (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by CTO directive; New-main remains deferred until Current Main1-Main11 functional closure.'