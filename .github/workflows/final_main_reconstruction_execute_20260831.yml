name: RAWAEA TARGET-ONLY GOLD DIAMOND CLOSURE

# RETIRED PUSH EXECUTOR
# Preserved for historical/manual reproduction only.
# Canonical automatic New-main assembly is governed by:
# .github/workflows/forensic_main_assembly.yml

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closure:
    runs-on: ubuntu-latest
    steps:
      - name: Retired executor notice
        run: |
          echo "RETIRED_PUSH_EXECUTOR"
          echo "Use forensic_main_assembly.yml for governed automatic New-main reconstruction."
