name: RAWAEA P131 New-main Navigation — RETIRED
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archive notice
        run: echo 'P131 target-writer retired; preserved only as forensic evidence.'
