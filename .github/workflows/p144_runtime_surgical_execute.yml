name: P144 runtime surgical repair — New-main only

# RETIRED AFTER ONE-SHOT EXECUTION.
# Preserved as forensic evidence; never auto-runs again.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archive notice
        run: echo "P144 surgical executor retired; see Current/CTO/20260901_P144_RUNTIME_SURGICAL_REPAIR.json"
