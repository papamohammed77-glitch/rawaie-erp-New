name: CTO executor temporary 20260903
# trigger-v3: execution is driven by the existing PR CI on the main branch.
on:
  push:
    branches: [cto-new-main-gold-20260903]
permissions:
  contents: write
jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0
      - run: echo 'executor is intentionally dormant; main P0 PR workflow performs the gated operation'
