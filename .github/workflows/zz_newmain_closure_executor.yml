name: CTO New-main surgical closure executor — RETIRED

# Historical executor retained for audit/history only.
# It previously had contents:write and could mutate the target through an automatic push path.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'RETIRED: legacy New-main surgical closure executor disabled.'
          echo 'No target mutation. No reconstruction. No production writes.'