# RETIRED 2026-09-03
# This historical diagnostic executor is intentionally disabled.
# It previously had contents: write and could mutate Current/PWA/New-main.
# Use cto_final_runtime_gate_20260903.yml for read-only verification.
name: ACTIONS DIAGNOSTIC 20260831 — RETIRED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow retired
        run: echo "RETIRED — no repository mutation permitted"
