name: CTO New-main Surgical Guard — RETIRED
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy New-main writer retired; no automatic target mutation permitted.'
