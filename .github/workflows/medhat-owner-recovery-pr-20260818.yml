name: RAWAEA TARGET-ONLY GOLD DIAMOND EXECUTOR — RETIRED

# Retired after forensic review.
# Historical owner-recovery executor had contents:write and rewrote Current/PWA/New-main on push.
# Its history remains preserved for audit; it is no longer a persistence authority.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired writer notice
        run: |
          echo 'RETIRED: historical owner-recovery target writer disabled.'
          echo 'No target mutation. No reconstruction. No production writes.'
          echo 'Use read-only exact New-main verifiers.'
