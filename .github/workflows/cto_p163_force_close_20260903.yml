# Frozen legacy executor — retained for audit/history; manual invocation only after new evidence.
name: CTO P163 Force Close — Frozen Legacy
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'FROZEN: P163 is already verified closed; this legacy writer is intentionally not automatic.'