name: CTO PR-target Final Forensic Exact-Target Closure — RETIRED

# Retired after forensic review.
# pull_request_target + contents:write was an unsafe competing persistence path for New-main.
# Historical file is preserved in Git history; this workflow is now inert and read-only.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired writer notice
        run: |
          echo 'RETIRED: historical pull_request_target writer disabled.'
          echo 'No target mutation. No reconstruction. No production writes.'
          echo 'Use read-only exact New-main verification workflows.'
