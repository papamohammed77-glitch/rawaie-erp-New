name: RAWAEA TEMP GOLD DIAMOND TARGET EXECUTOR

# RETIRED AUTO-EXECUTOR
# Kept for historical reproducibility and manual investigation only.
# The canonical production-authoritative path is:
# .github/workflows/forensic_main_assembly.yml
#
# This workflow MUST NOT auto-run on every push because its P135 assumptions
# target an obsolete New-main shape and can preempt the canonical reconstruction.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  target:
    runs-on: ubuntu-latest
    steps:
      - name: Retired executor notice
        run: |
          echo "RETIRED_AUTO_EXECUTOR"
          echo "Use forensic_main_assembly.yml for governed New-main reconstruction."
