# Archive / Excluded Material

This folder is intentionally not populated with unverified or conflicting operational data.

## Source material deliberately excluded from operational baseline

### 1. `SQL_Evidence/diagnostics/رد حول التعريفات.md`
Reason: contains an earlier schema description presented from memory before the actual Production index evidence was obtained. It is not safe as an authoritative data source.

### 2. `supabase/migrations/20260810_manual_voucher_core_v1_RELEASE.sql`
Reason: explicitly marked `NOT EXECUTED IN PRODUCTION` and contains Target behavior alternatives that conflict with current Production/current code for DirectSale and DirectReturn. It remains a reference candidate, not Production truth.

### 3. Old draft assistant replies
Draft replies are not admitted when later persisted Production Evidence supersedes them.

The original review repository remains the historical archive. This repository is the clean operational baseline.
