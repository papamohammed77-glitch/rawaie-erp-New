# MAIN7 Forensic Execution Record — 2026-08-30

## PRE-SWEEP SELF-AUDIT

Business Understanding:
- main7 is the Warehouse Operations UI/orchestration fragment covering Receiving, Stock Vouchers, Picking, Loading, Delivery, Return, Unloading, Inventory Count and Daily Settlement.

Architecture Understanding:
- Current target contract is capability-oriented: UI/orchestrator -> Edge capability -> canonical RPC/engine.
- Physical stock movement contract is `post_stock_movement` -> `stock_branches` + `inventory_log`.
- Reservation is separate from physical movement.

Database Understanding:
- Current Production was queried directly.
- `items.item_code` is globally UNIQUE.
- `stock_branches` identity is `(branch_id,item_id)`.
- `stock_voucher_details` has `voucher_id` and does not have `voucher_code`.
- `receiving.operation_id` is UNIQUE.

Historical Understanding:
- The governing prompt/report chain 89..100 and MASTER EXECUTION PROMPT were read from GitHub.
- `Current/PWA/main/main7.md` had the same blob SHA as `Original/PWA/main/main7.md` before the rewrite, proving it was still the original baseline.

Current Git Understanding:
- Original main7 SHA: `6f7aef60ac137cd7f6b74281a17835dbd29595be`.
- Rewritten main7 commit: `107ac94b0a4acc6bead7cfb47d661ceecdd65aa6`.

Current Production Understanding:
- Production snapshot was directly queried before and after the rewrite.
- No permanent Production business rows were created by the main7 source rewrite.

Deployment Understanding:
- Git source was committed to `main`.
- This task rewrote the PWA source file; it did not claim a browser deployment of the whole assembled PWA.

Runtime Understanding:
- Production database/RPC contracts were verified directly.
- Browser E2E runtime of the assembled full PWA was not available in this execution environment.

## CONFIRMED FACTS
- Current main7 was the original baseline before this rewrite.
- Several main7 consumers had stale contracts: unscoped reads, voucher-detail lookup by nonexistent `voucher_code`, Vehicle represented as driver email, delivery treating a runsheet aggregate as an order-level payload, unloading listing wrong statuses, and settlement reading nonexistent voucher-detail linkage.
- Current Production contains canonical capability/RPC contracts that should remain the authority.

## UNKNOWNS
- Full browser E2E behavior after integration into the final assembled `main.html`.
- Exact live UI reachability/permission behavior for every navigation entry point.

## CONFLICTS
- Historical main7 expected some legacy endpoint payloads; current Production contracts differ. Current Production won.

## UNVERIFIED CLAIMS
- No claim of 100% browser runtime closure is made.

## IMPORTANT EVENTS

### EVENT ID: MAIN7-001
DATE: 2026-08-30
SOURCE: Current Git + Original Git + current Production
OBJECTIVE: Establish true baseline.
INPUT STATE: Current main7 SHA `6f7aef60...`; Original main7 SHA `6f7aef60...`.
DISCOVERY: Current main7 had not previously been rebuilt.
ROOT CAUSE: Earlier main7 work was not actually committed into the current main7 file.
BUSINESS IMPACT: Rebuild must preserve functionality from the original file while adapting contracts to current Production.
CHANGE MADE: None.

### EVENT ID: MAIN7-002
DATE: 2026-08-30
SOURCE: Current Production PostgreSQL
OBJECTIVE: Verify Warehouse contracts.
DISCOVERY: Current functions include canonical stock movement, runsheet operation, return, unloading, adjustment and settlement engines; `stock_voucher_details` uses `voucher_id`; Vehicle is a first-class entity.
CHANGE MADE: Used these contracts as the basis for the rewrite.

### EVENT ID: MAIN7-003
DATE: 2026-08-30
SOURCE: Current Git / PWA main7
OBJECTIVE: Rebuild main7 safely.
DISCOVERY: Legacy UI queried tables without company scope and used obsolete payloads.
CHANGE MADE: Rebuilt main7 around a fail-closed `RW_ShellContext` company context, common auth/Edge helpers, UUID entity selection, company-scoped reads, capability-only writes, stable operation IDs for retryable operations, and corrected current Production endpoint contracts.

### EVENT ID: MAIN7-004
DATE: 2026-08-30
SOURCE: GitHub
OBJECTIVE: Persist reconstruction.
CHANGE MADE: `Current/PWA/main/main7.md` committed as commit `107ac94b0a4acc6bead7cfb47d661ceecdd65aa6`.

## WHAT I PROVED
- The main7 baseline was still original before rewrite.
- The rewritten file is present in Git at the expected path.
- The rewritten code contains no direct `stock_branches` mutations and no direct `inventory_log` inserts.
- Main7 reads are company-scoped through the current shell context or validated company-scoped parents.
- Voucher details are now addressed by `voucher_id` after company-scoping the parent voucher.
- Delivery is now passed to the order-level production capability per order instead of duplicating runsheet aggregate quantities.
- Unloading now acts on `Loaded` runsheets, matching the current Production lifecycle.
- Vehicle count uses vehicle UUIDs rather than driver email as the vehicle identity.
- Daily settlement calls the current settlement capability and sends an operation id.

## WHAT I DID NOT PROVE
- End-to-end browser execution of the complete assembled `main.html` after integrating main7.
- Live UI behavior under every role/permission path.

## WHAT I CHANGED
- Complete rewrite of `Current/PWA/main/main7.md`.
- Tenant/company-safe querying in Warehouse screens.
- Capability-oriented physical stock flow; no direct stock writer in PWA.
- Correct current schema linkage for stock vouchers.
- Current Vehicle/Supplier/Branch UUID semantics for vouchers.
- Correct per-order delivery submission.
- Correct unloading state visibility.
- Operation-id propagation where supported.
- Stable retry operation IDs in client state for settlement/delivery/voucher receive.

## WHAT I DID NOT CHANGE
- Canonical Production stock engines.
- Owner semantics.
- Historical business rules not disproven by current Production.
- Existing public `RW_Warehouse` function names used by consumers.

## WHAT I DISCOVERED
- `main7` was never actually rebuilt in Git before this execution.
- Current Production contracts had materially diverged from the original main7 consumer assumptions.
- The most dangerous issues in main7 were consumer/tenant/schema drift rather than a second Physical Stock engine.

## WHAT I INITIALLY MISSED
- The original main7 file had to be treated as an untouched baseline rather than as a partially repaired file.
- `stock_voucher_details.voucher_code` does not exist in current Production.

## WHAT BECAME OBSOLETE
- Driver-email-as-vehicle selection in voucher UI.
- Unscoped reads from receiving, vouchers and runsheets.
- Runsheet-level aggregate payload used as if it were order-level delivery detail.
- Unloading screen's `Open/New` selection for actual unloading.

## WHAT REMAINS OPEN
- Browser E2E of the assembled full PWA.
- Any downstream Edge wrapper still carrying legacy contract drift outside main7's source file.

## WHAT COULD STILL BE WRONG
- A downstream Edge function may expose an older payload contract not visible from the PWA source alone.
- Final assembled-file integration could expose name collisions outside main7.

## PRODUCTION CHECKS
PRODUCTION DEPLOYED?: `main7` source committed to current Git; PWA artifact deployment itself not performed here.
PRODUCTION RUNTIME VERIFIED?: Database/RPC contract verified; browser E2E not verified.
AUDIT VERIFIED?: Existing audit trigger path on `stock_vouchers` was verified; no new audit row was fabricated for this source-only rewrite.
DATA VERIFIED?: Pre/post Production counts were queried; no permanent business records were created by the main7 rewrite.
CURRENT GIT ALIGNED?: YES for `Current/PWA/main/main7.md`.

## FINAL CLOSURE STATUS
`MAIN7 SOURCE RECONSTRUCTION = CLOSED`
`MAIN7 PRODUCTION CONTRACT ALIGNMENT = VERIFIED`
`FULL ASSEMBLED PWA BROWSER RUNTIME = NOT PROVEN`
`GLOBAL PWA ZERO-DEBT = OPEN UNTIL ASSEMBLED-RUNTIME VERIFICATION`
