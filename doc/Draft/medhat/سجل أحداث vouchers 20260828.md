# RAWAEA ERP — Vouchers Forensic Closure Log
## 2026-08-28

### Scope
Critical task restricted to `Current/PWA/vouchers.html` and the Production contracts required by its warehouse workflow. Picker and unrelated work intentionally excluded.

### Evidence policy
No prior report, assistant memory, or historical percentage was accepted as proof. Evidence was reconstructed from current Production PostgreSQL/Supabase, currently deployed Edge Functions, current Git `main`, Git commit history, and current schema/permissions.

### Governing source
`doc/Draft/medhat/برومبت 72` and the governing-principles report were read before implementation. The controlling rule was: understand historical/current contract first, then make a surgical change, verify it in Production, and do not convert theoretical/staging results into Production PASS.

### Historical findings
The Git history showed repeated voucher repairs during 2026-08-20/21, including branch-scoped picker work, vehicle/representative workflow repair, syntax repair, and a forensic closure. After that period the page stopped changing while Production continued receiving newer backend versions. This was treated as source/runtime drift, not as a reason to blindly reuse the previous file.

### Current Production findings
1. Current manual voucher contract is:
   - Transfer: Branch -> Branch
   - DirectSale: Branch -> Vehicle
   - DirectReturn: Vehicle -> Branch
   - SupplierReturn: Branch -> Supplier
2. Current `send_stock_voucher_atomic` performs the physical DirectSale/SupplierReturn movement at SEND. They therefore do not have a RECEIVE stage.
3. Current `complete_manual_stock_voucher_atomic` supports completion from Sent/Received.
4. `post_manual_stock_voucher_atomic` routes physical movements through `post_stock_movement`; RECEIVE was hardened to Transfer/DirectReturn only.
5. `items.item_code` is globally UNIQUE in Production, so item identity may use item_code where that contract is respected.
6. No Manual vouchers currently exist in Production; no historical manual-voucher backlog was found that required data repair.
7. No orphan VAN branches were found by the current branch/vehicle relation query.
8. Production `vehicles` currently has no rows, so DirectSale/DirectReturn cannot be operationally exercised with a real vehicle until vehicle master data is legitimately present. No vehicle was invented or fabricated to make the test pass.
9. Vehicle public-ALL access was removed. A company-scoped authenticated SELECT policy is now used.
10. `stock_vouchers`, `stock_voucher_details`, and `stock_branches` were added to `supabase_realtime` with FULL replica identity.

### Changes applied
#### Production
Migration `vouchers_live_sync_workflow_integrity_20260828`:
- removed unsafe public-all vehicle policy;
- added company-scoped authenticated vehicle SELECT;
- enabled Realtime for voucher/detail/stock tables;
- hardened manual voucher RECEIVE to Transfer/DirectReturn only.

Migration `vouchers_server_route_authorization_20260828`:
- hardened manual voucher creation server-side;
- validates active actor within company;
- DirectSale requires active Direct Sales role, assigned vehicle, and branch permission;
- DirectReturn validates active vehicle and direct-sales ownership when applicable;
- SupplierReturn requires an existing supplier and a documented supplier/branch purchase-order relationship;
- preserves the existing voucher workflow and delegates no Physical Stock mutation outside the stock core.

#### Git
`Current/PWA/vouchers.html` was replaced by a single canonical controller rather than stacked IIFE/override layers.
Latest commit: `9bdf511839594c6a350f80a840a9b3c738638431`
Latest blob SHA: `81af65a4bba61771e19120b60a2e68bee2f1b288`
Build marker: `RAWAEA-VOUCHERS-CANONICAL-2026-08-28-R2`

The page now:
- uses company-scoped references;
- follows the current Branch/Vehicle/Supplier route contract;
- shows RECEIVE only for Transfer/DirectReturn;
- shows COMPLETE for Sent DirectSale/SupplierReturn;
- preserves receive operation identity via local storage;
- subscribes to live changes for voucher/detail/stock data;
- refreshes on online/focus/visibility changes;
- removes direct stock mutation from the UI;
- retains warehouse scan/search/cart workflows.

### Production transactional verification
A real Production database transaction was used for a temporary Transfer:
Create -> Send -> Receive -> Complete returned `Completed`, with quantity and received quantity both 1. The transaction was then rolled back.
Post-rollback verification returned:
- voucher rows: 0
- detail rows: 0
- inventory log rows: 0

A deliberate DirectSale negative test from the current direct-sales user to a nonexistent vehicle was rejected server-side before document creation.

### Global writer discovery
Production function discovery found only these direct `stock_branches` references outside `post_stock_movement`:
- `reserve_stock` — reservation only
- `release_stock_reservation` — reservation only
- `setup_van_stock` — initializes missing VAN rows at zero; not a stock movement engine

No other Production function directly inserts into `inventory_log` outside `post_stock_movement`.

### Final status
Physical Stock movement centralization: CLOSED by current Production evidence.
Voucher workflow contract: CLOSED for the currently provisioned master data.
Live synchronization infrastructure: CLOSED at database publication/configuration level.
Direct-sales runtime with a real vehicle: NOT EXECUTABLE in the current Production data because no current vehicle master rows exist. This is a data-state limitation, not a fabricated PASS.

### Explicit non-claims
No Git CI PASS was claimed because the new direct commit had no associated workflow/status result. Production DB runtime was verified transactionally; Edge HTTP end-to-end runtime for a real authenticated user was not claimed where the current data did not permit a safe real-world scenario.
