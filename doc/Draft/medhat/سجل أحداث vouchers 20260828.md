# RAWAEA ERP — Vouchers Forensic Closure Log
## 2026-08-28 — R3 Forensic Update

### Scope
Critical work restricted to the Vouchers warehouse workflow and the Production contracts required by it. Picker and unrelated work remain excluded.

### Evidence policy
No prior report, assistant memory, or historical percentage was accepted as proof. Current evidence was reconstructed directly from Production PostgreSQL/Supabase, currently deployed Edge Functions, current Git `main`, historical Git branches/files, and schema/permissions.

### Current Vouchers contract proven from Production
- Transfer: Branch → Branch; SEND performs outbound physical movement; RECEIVE performs inbound physical movement; COMPLETE closes the document.
- DirectSale: Branch → Vehicle; physical movement occurs at SEND; then COMPLETE.
- DirectReturn: Vehicle → Branch; physical outbound from the vehicle occurs at SEND; physical inbound to the branch occurs at RECEIVE; then COMPLETE.
- SupplierReturn: Branch → Supplier; physical movement occurs at SEND; then COMPLETE.
- `post_stock_movement` remains the only Physical Stock mutation engine.
- `reserve_stock` / `release_stock_reservation` modify `allocated_qty` only and are not Physical Stock writers.

### Current Production data facts
- `stock_vouchers` source=`Manual`: 0 rows.
- `vehicles`: 0 rows.
- No legitimate live vehicle master exists in the current Production dataset; no vehicle was fabricated as permanent data.
- `items.item_code` is globally UNIQUE in Production.
- Current stock integrity checks: `negative_qty=0`, `negative_allocated=0`, `allocated_gt_qty=0`, duplicate `inventory_log.idempotency_key=0`.
- The only non-central functions containing direct `stock_branches` references are `reserve_stock`, `release_stock_reservation`, and `setup_van_stock`; the first two are reservation-only and `setup_van_stock` only initializes missing VAN balance rows at zero. No other direct `inventory_log` writer was found outside `post_stock_movement`.

### Parent PWA vehicle integration forensic target
Current `Current/PWA/main.html` was re-verified to lack `vehicles` view/RW_Vehicles while Production provides `vehicles` + `create_vehicle_atomic`. The requested correction is native Parent-PWA integration only; no new standalone vehicle file or permanent vehicle test data.

### 2026-08-29 execution trigger
A temporary use of the existing forensic workflow is being used only as a surgical writer for `Current/PWA/main.html`. The workflow must commit only that file and is to be restored immediately afterward. No new application file is being created.

### Trigger touch
The runner was hardened to add the native Vehicle Master capability inside `Current/PWA/main.html`, including company-scoped data loading, smart search, permission mapping, and the existing `create_vehicle_atomic` contract. A commit touching only this existing forensic log now triggers the runner. Parent `main.html` remains unclaimed as changed until directly verified.

### 2026-08-29 forensic execution note
Pre-change review confirmed: current `main.html` is the parent ERP shell with existing Dashboard, master-data, sales, procurement, warehouse, finance, HR, CRM, reports, permissions, audit and routing modules; current source is approximately 10.3K lines rather than the older 12K+ historical estimate. A full rewrite was rejected as unsafe because it could silently drop historical/hidden behavior. The implemented direction is surgical native integration, preserving existing routes and capabilities.

### 2026-08-29 — P0 execution authorization
This existing event log is being touched solely to trigger the repository's audited P0 parent-shell repair runner against the current `main` tree. The runner is required to restore its workflow file immediately after execution and to commit only the intended `Current/PWA/main.html` change plus its execution record. No application behavior is changed by this log edit.
