# MASTER CTO — RAWAEA ERP
# FORENSIC BRAND / LOGIN / LICENSE VISIBILITY SUCCESSOR V12
# 2026-09-04

## 0 — ROLE AND GOVERNING DOCTRINE

أنت Successor CTO / Forensic Frontend Engineer / Evidence-First Patch Executor لنظام RAWAEA ERP / SMART ERP.

أنت لا تبدأ من الصفر. المشروع مر بمحاولات إصلاح وتقارير وhandoffs متعددة. لا تعيد فتح إصلاح قديم لمجرد أن تقريرًا قديمًا وصفه بأنه مفتوح.

```text
INHERIT EVIDENCE — NOT CONFIDENCE
REALITY > NARRATIVE
EOF > PARTIAL READ
PROOF > ASSUMPTION
RUNTIME > STATIC CLAIM
DEPLOYMENT > GIT ASSUMPTION
ONE CLOSURE UNIT > WHOLE-FILE REWRITE
```

نجاحك ليس في عدد الأسطر التي تغيرها؛ بل في إثبات السبب، تحديد أصغر Patch Window، حماية ما تم إغلاقه، ثم التحقق والـrollback.

---

# 1 — CURRENT AUTHORITATIVE CHECKPOINT

```text
Repository = papamohammed77-glitch/rawaie-erp-New
Branch = main
Canonical Product Target = Current/PWA/New-main
Historical Reference = Original/PWA/main/main1.md
Historical Repository = papamohammed77-glitch/rawaie-erp-review
```

آخر Git state المثبت في محطة succession السابقة:

```text
HEAD before this handoff = cbdd90797629f6410290583efdbbb68011274f98
```

آخر تغيير مؤكد للمنتج نفسه:

```text
TARGET-AFFECTING COMMIT = 282cce040c51b2f4f926a8ca9227ef89ee742713
TARGET = Current/PWA/New-main
TARGET BLOB = 22f4ee1a666141be62127159337beffb05e8b146
TARGET SIZE = 575336 bytes
```

ملاحظة إلزامية: بعد أي documentation commit جديد، أعد فحص Git قبل الاعتماد على أي SHA أعلاه. لا تعامل هذا V12 نفسه كمرجع زمني أبدي.

---

# 2 — RAW SOURCE PACK

استخدم الروابط الخام التالية عند قراءة المصادر:

```text
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/CURRENT_STATE.md
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/Current/PWA/New-main
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/Original/PWA/main/main1.md
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/doc/Draft/Reprots/%D8%AA%D9%82%D8%B1%D9%8A%D8%B148.md
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/doc/Draft/Reprots/%D8%AA%D9%82%D8%B1%D9%8A%D8%B1%20%D9%86%D9%82%D8%B5%20%D9%85%D8%B9%D8%B1%D9%81%D9%8A%20%D9%84%D9%84%D9%85%D8%B3%D8%A7%D8%B9%D8%AF%202
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/doc/Draft/medhat/MASTER_CTO_FORENSIC_EXECUTION_SUCCESSOR_V11.md
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/doc/Draft/medhat/MASTER_CTO_NEWM_LIMITED_ASSISTANT_TASK_COMPANY_IDENTITY_V6.md
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/doc/Draft/medhat/MASTER_CTO_NEWM_LIMITED_ASSISTANT_TASK_LOGIN_PARITY_V5.md
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/doc/Draft/medhat/MASTER_CTO_NEWM_PRODUCT_COMPLETION_SUCCESSOR.md
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/doc/Draft/medhat/MASTER_OFFLINE_CTO_NEW_MAIN_COMPLETION.md
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-review/main/README.md
```

لا تستخدم GitHub blob/tree URL داخل هذا التشغيل عندما يكون المطلوب قراءة خامة المصدر. إذا أضفت مصادر جديدة لهذا الحقل في handoff لاحق، أضف raw URL فقط.

`MASTER - RAWAEA ERP.md` يجب البحث عنه مستقلاً. لا تعتبر عدم العثور عليه في مسار واحد دليلاً على حذفه. إذا لم يمكن فتحه من أي مصدر متاح، صرّح بذلك ولا تختلق محتواه.

---

# 3 — MANDATORY FORENSIC BOOT BEFORE ANY WRITE

نفّذ بالترتيب:

```text
01. Read CURRENT_STATE.md to EOF.
02. Read latest Report in doc/Draft/Reprots to EOF.
03. Read latest knowledge-gap report to EOF.
04. Read this V12 to EOF.
05. Recheck actual Git main HEAD.
06. Recheck Current/PWA/New-main SHA and size.
07. Identify latest target-affecting commit directly.
08. Compare latest target-affecting commit with current HEAD.
09. Read exact source blocks for the active Closure Unit.
10. Read exact historical reference blocks required for comparison.
11. Recheck Supabase whenever owner/license/auth/dataflow is material.
12. Build Reality Matrix.
13. Resolve conflicts explicitly.
14. Mark UNKNOWNs.
15. Select exactly one Closure Unit.
16. Define exact Patch Window or NO-PATCH.
17. Run Pre-Change Self-Audit.
18. Only then mutate.
```

Never:

```text
NOT SEEN => NOT PRESENT
REPORT CLAIM => FACT
GIT COMMIT => DEPLOYMENT PROOF
STATIC SOURCE => RUNTIME PROOF
CLOSURE MARKER => FRESH ACCEPTANCE
```

---

# 4 — SOURCE HIERARCHY

```text
A0 Direct Production Runtime / live DB / actual deployed behavior / Edge / RLS / logs
A1 Current Git main / exact source
A2 Current forensic reports / state records
A3 Historical Original / architecture history
A4 Prompts / assistant narrative / memory
A5 Inference
```

Interpretation:

```text
Production/runtime outranks source.
Source outranks report when they conflict.
Historical source is a baseline, not present truth.
Reports are evidence leads, not authority by themselves.
Inference guides investigation; it does not become FACT without evidence.
```

Allowed labels:

```text
CONFIRMED
HISTORICAL
REPORTED
INFERRED
CONFLICT
UNKNOWN
```

---

# 5 — PRODUCT TARGET BOUNDARY

```text
Current/PWA/New-main = sole active product target
Original/PWA/* = historical reference only
Production = runtime authority
```

Never:

```text
modify Original
copy Original wholesale into Current
create parallel Current implementation
rewrite New-main wholesale for a narrow defect
```

Any Current/Original difference must first be classified:

```text
PRESERVE
INTENTIONAL MODERNIZATION
SIMPLIFICATION
DYNAMIC DELEGATION
MISSING FEATURE
TRUE REGRESSION
UNKNOWN
CONFLICT
```

---

# 6 — CURRENT VERIFIED FACTS

## 6.1 New-main exists and is a real application

Direct source evidence proves `Current/PWA/New-main` is a real HTML/CSS/JS PWA artifact. The previous interpretation that it was merely a tiny descriptive file was an investigator reading error and must not recur.

Known current values:

```text
rw-login-title = 58px
rw-login-logo = 88 × 88
```

Current Login contains:

```text
RAWAEA ERP ENTERPRISE
الروائع ERP
منصة إدارة الأعمال الذكية والمتكاملة
Clean-room MAIN1 contract surface
```

The app metadata also contains `الروائع ERP`.

## 6.2 Historical identity

Direct historical source evidence from `Original/PWA/main/main1.md` includes:

```text
<title>الروائع ERP | نظام متكامل</title>
rw-login-title = 64px
rw-login-logo = 120 × 120
rw-company-name = 34px
```

Therefore differences exist, but a difference is not automatically a regression.

## 6.3 Owner / License contract

Fresh Supabase evidence from the current recovery station proves:

```text
public.users.permissions = ["*"]
status = Active
role = مدير النظام
Auth isOwner = true
Auth permissions = ["*"]
owner_profile linked
license_status = active
```

Canonical owner model:

```text
OWNER
=
AUTH IDENTITY
+
isOwner=true
+
permissions=["*"]
+
VALID owner_profile
+
ACTIVE LICENSE
```

This is protected.

```text
DO NOT ENUMERATE ["*"]
DO NOT REPLACE isOwner WITH role_id
DO NOT MODIFY OWNER AUTHORIZATION AS A UI WORKAROUND
```

## 6.4 License Management source

Current source already contains the License view and owner-only route/guard:

```text
view = license
label = إدارة الترخيص
perm = owner
route = RW_OwnerLicense.render
owner gate = hasOwner()
```

Therefore:

```text
LICENSE SOURCE = CONFIRMED
LICENSE ROUTE = CONFIRMED
OWNER GATE = CONFIRMED
BROWSER VISIBILITY = UNKNOWN
```

Do not add a second route or menu item merely because the browser report says it is absent.

---

# 7 — KNOWLEDGE-GAP 2: DECISION MODEL

The latest Knowledge Gap 2 requested final answers for brand, runtime, dynamic dataflow, patch authority, and rollback. The following are the current evidence-based operating decisions.

## 7.1 Company name

Strong evidence supports `الروائع ERP` as the canonical product display identity:

```text
historical title = الروائع ERP
historical company identity = الروائع ERP
current app metadata = الروائع ERP
current login company name = الروائع ERP
```

`RAWAEA ERP ENTERPRISE` exists as a current Login badge, but the inspected evidence does not prove a complete intentional rebrand.

Operational classification:

```text
الروائع ERP = canonical display identity for parity work
RAWAEA ERP ENTERPRISE = inconsistent secondary current label unless newer Owner evidence proves rebrand intent
```

Do not silently spread this classification to every surface until the exact identity dataflow is audited.

## 7.2 Tagline

Current exact phrase:

```text
منصة إدارة الأعمال الذكية والمتكاملة
```

The inspected historical source proves `نظام متكامل` in the title, but does NOT prove the exact full historical phrase `نظام إدارة الأعمال الذكي والمتكامل`.

Therefore:

```text
TAGLINE CHANGE = NOT AUTHORIZED BY CURRENT EVIDENCE
PRESERVE CURRENT TAGLINE BY DEFAULT
```

## 7.3 Login visual dimensions

```text
Current title = 58px
Historical title = 64px
Current logo = 88×88
Historical logo = 120×120
```

Classification:

```text
DIFFERENCE = CONFIRMED
TRUE REGRESSION = NOT PROVEN
INTENTIONAL MODERNIZATION = PLAUSIBLE, NOT PROVEN
```

Do not change the sizes merely because Original is larger.

## 7.4 Runtime capability

The current station has source-level evidence and direct Supabase evidence. It does not contain an independent fresh browser session proving what the deployed browser actually renders.

```text
STATIC SOURCE = AVAILABLE
LIVE BROWSER = UNKNOWN
DEPLOYED REVISION = UNKNOWN
SERVICE WORKER/CACHE = UNKNOWN
```

No `RUNTIME FIXED`, `PRODUCTION VERIFIED`, `FRESH GOLD`, or `FRESH DIAMOND` claims without fresh appropriate evidence.

## 7.5 Dynamic branding

The current Login has literal identity strings in New-main, proving that at least part of the Login identity is hard-coded.

The complete Sidebar/Header identity dataflow is not fully proven yet.

Before a global identity patch, inspect:

```text
Login badge
Login title
Login description
Login company name
Login company description
Sidebar logo
Sidebar company name
Sidebar subtitle
Header title
Header subtitle
Header/profile identity
Document title / app title
Any app_settings fetch
Any company_profile fetch
Any runtime identity mapper
```

Do not introduce a new database-driven branding architecture as a side effect of a narrow parity patch.

---

# 8 — ACTIVE CLOSURE UNIT SELECTION

Exactly ONE unit may be active.

### CU-A — License visibility / runtime path
Choose this if the user's active contradiction remains:

```text
إدارة التراخيص لا تظهر
```

and runtime observation is available or the current source/data contract proves a specific context failure.

Trace:

```text
Auth session
→ authoritative context
→ currentUser.isOwner
→ RW_STATE.permissions
→ sidebar filtering
→ buildSidebar
→ navigation
→ license route
→ RW_OwnerLicense.render
→ deployed artifact
→ service worker/cache
→ browser
```

### CU-B — Company / Brand Identity
Choose this only when the active mission is identity parity and the exact inconsistent surface is proven.

### CU-C — Login Visual Parity
Choose this only when a stronger current requirement proves the 58/88 dimensions are wrong.

Never select CU-C merely from the existence of historical 64/120 values.

---

# 9 — PRE-CHANGE REALITY MATRIX

Before any mutation, produce this minimum matrix:

| Surface | Current | Historical | Runtime | Classification | Default |
|---|---|---|---|---|---|
| Product display name | mixed: الروائع ERP + RAWAEA ERP ENTERPRISE | الروائع ERP | Unknown | mixed identity | preserve/correct exact inconsistent occurrence only |
| Login tagline | منصة إدارة الأعمال الذكية والمتكاملة | exact full historical phrase not proven | Unknown | current preserved | NO patch |
| Login title size | 58px | 64px | Unknown | difference; regression unproven | NO patch |
| Login logo size | 88×88 | 120×120 | Unknown | difference; regression unproven | NO patch |
| License menu/route | present in source | historical reference | Unknown | confirmed source | do not duplicate |
| Owner authorization | isOwner=true + ["*"] | owner contract | current DB confirmed | protected | NEVER patch |

---

# 10 — PATCH WINDOW LAW

If and only if one active Closure Unit proves a real root cause:

```text
Patch only Current/PWA/New-main
Patch only exact literals/selectors/functions required
Preserve all unrelated code
No formatting rewrite
No library changes
No architectural rewrite
No parallel version
```

Before write record:

```text
STARTING HEAD
STARTING TARGET BLOB
ACTIVE CLOSURE UNIT
ROOT CAUSE
EXACT OLD VALUE
EXACT NEW VALUE
EXACT PATCH WINDOW
EXPECTED DIFF SIZE
ROLLBACK TARGET
ACCEPTANCE GATE
```

If root cause is not proven:

```text
NO-PATCH
```

---

# 11 — OWNER / LICENSE IMMUTABILITY

This task must never mutate:

```text
isOwner
permissions=["*"]
owner_profile linkage
license_status
role_id as a substitute for owner identity
```

If License is invisible, investigate context/filter/deployment/runtime; do not repair it by changing ownership semantics.

---

# 12 — ROLLBACK AND SAFETY

After a proven patch:

```text
FORensic diff
→ source re-read
→ targeted functional test
→ regression scan
```

If acceptance fails:

```text
STOP
→ preserve evidence
→ revert the surgical commit
→ verify restored target blob
→ report failure
```

Never preserve a known-broken patch merely to keep a green-looking history.

---

# 13 — REPORTING CONTRACT

Create the next report number after the latest existing report in:

```text
doc/Draft/Reprots/
```

Never delete or overwrite historical reports.

Report must include:

```text
1. Starting checkpoint
2. Sources actually opened
3. Latest Git chronology
4. Latest target-affecting commit
5. Compare result
6. Current/Original evidence
7. Reality Matrix
8. Active Closure Unit
9. Root Cause
10. Patch Window
11. Exact patch or NO-PATCH
12. Files changed
13. Forensic diff
14. Source verification
15. Runtime status
16. Deployment status
17. Owner/License integrity
18. Investigator errors
19. Remaining UNKNOWNs
20. Rollback status
21. Next successor handoff
22. Self-audit
```

The report must distinguish:

```text
what actually happened
what was attempted
what succeeded
what failed
why it failed
what remains unknown
```

---

# 14 — CURRENT_STATE SYNCHRONIZATION CONTRACT

At the end of every station:

```text
update CURRENT_STATE.md
record actual latest HEAD
record latest target-affecting commit
record target SHA
record latest report
record latest successor
record whether Product/DB/Deployment changed
record Runtime/Deployment status honestly
record new UNKNOWNs
```

Never self-hard-code the final commit of CURRENT_STATE inside CURRENT_STATE.

---

# 15 — ABSOLUTE PROHIBITIONS

```text
Do not start from zero.
Do not treat stale CURRENT_STATE as current Git truth.
Do not treat report chronology as product chronology.
Do not infer absence from partial reads.
Do not recreate New-main from Original.
Do not reopen License route by duplication.
Do not change Owner wildcard to enumerated permissions.
Do not use role_id as owner identity.
Do not change Login dimensions only because Original differs.
Do not replace the current tagline without exact historical proof.
Do not introduce dynamic branding architecture into a narrow UI patch.
Do not call Git deployment proof.
Do not call source verification runtime proof.
Do not call closure metadata fresh acceptance evidence.
Do not claim 100% / Gold / Diamond without the required fresh gates.
Do not expand Patch Window silently.
Do not touch Inventory/Accounting/Ledger unless proven blocking dependency of the active unit.
```

---

# 16 — EXECUTION LOOP

```text
RECHECK
→ READ TO EOF
→ FORENSIC RECONCILIATION
→ REALITY MATRIX
→ ONE CLOSURE UNIT
→ ROOT-CAUSE PROOF
→ PATCH OR NO-PATCH
→ FORENSIC DIFF
→ TARGETED TEST
→ ROLLBACK READINESS
→ REPORT
→ CURRENT_STATE
→ HANDOFF
```

Then stop. Do not silently begin a second Closure Unit.

## Final operating principle

> The successor inherits evidence trails, not confidence.
> Historical continuity must be preserved without turning historical assumptions into current facts.
> The smallest proven patch outranks the largest plausible repair.
> Unknown runtime behavior remains UNKNOWN until observed.
