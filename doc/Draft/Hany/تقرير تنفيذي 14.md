# RAWAEA ERP — تقرير تنفيذي 14
## P133 — Forensic Runtime Authentication Repair, Controlled Merge & Deployment Gate

**التاريخ:** 2026-09-01  
**الحالة:** SOURCE REPAIR MERGED / PRODUCTION RUNTIME UNVERIFIED  
**المسار:** `doc/Draft/Hany/`  

## 1. هدف الجولة

هذه الجولة لم تبدأ من الصفر. تمت إعادة بناء الحالة من المصادر المباشرة، ثم فصل المشكلات التي ظهرت في Runtime إلى أسباب مستقلة بدل افتراض سبب واحد.

الأدلة الداخلة كانت:

- `MAIN11_SUPABASE_UNAVAILABLE`
- `401 Unauthorized`
- `AuthApiError: Invalid API key`
- Service Worker `404` على `/companies/sw.js`

## 2. الذاكرة واسترجاع الحالة

تمت قراءة `MASTER - RAWAEA ERP.md` بالكامل قبل Product Mutation، ثم تمت قراءة `CURRENT_STATE.md` قبل التعديل.

تمت مراجعة `doc/Draft/Hany` مباشرة. كان `تقرير تنفيذي 13.md` آخر تقرير مرئي قبل الجولة.

تم طلب:

`doc/Draft/medhat/برومبت 124+ ملحق تقرير`

مباشرة من Git، وكانت النتيجة `404 Not Found`. كما لم توجد commits مرتبطة بالمسار نفسه. لم يتم اختلاق Prompt 124.

القاعدة المستخدمة كانت: Git/Production/Runtime الحالي أعلى من التقارير والذاكرة، وworkflow success لا يساوي runtime success.

## 3. التحقيق في New-main

تم فحص `Current/PWA/New-main` مباشرة.

### 3.1 Supabase credential

الهدف كان يحتوي على تعيينين لـ`RW_SUPABASE_ANON_KEY`: placeholder قديم ثم hard-coded legacy key.

تمت مقارنة الـhard-coded key مباشرة مع المفتاح الحالي المنشور من Production Supabase project `fiilmooggumokxanwiyx`.

النتيجة: المفتاح داخل target غير مطابق للمفتاح الحالي.

هذا دليل مصدر مباشر يفسر `Invalid API key`.

### 3.2 Supabase client reference

الهدف ينشئ client في المتغير `supabase`، بينما MAIN11 يستخدم `window.supabase.auth` في check/session وبعض عمليات Auth.

هذا mismatch حقيقي بين SDK namespace وبين client object الذي أنشأه التطبيق، ومتسق مع:

`MAIN11_SUPABASE_UNAVAILABLE`

## 4. Service Worker

يوجد في Current:

- `Current/PWA/sw.js`
- `Current/PWA/register-sw.js`

و`register-sw.js` الحالي يستخدم `sw.js` ضمن نفس بنية `Current/PWA`.

لكن Runtime المبلغ عنه يطلب:

`/companies/sw.js`

ويحصل على `404`.

لم يتم تعديل `sw.js` عشوائيًا؛ لأن هذا الفرق قد يكون دليلًا على أن الـartifact المنشور أو base path مختلف عن target الحالي.

## 5. التجربة الجراحية الأولى

تم فتح PR #66 واستخدام existing surgical execution channel.

Run:

`33533313638`

فشل قبل target mutation برسالة:

`P133_WRONG_GLOBAL_AUTH_REFERENCE_REMAINS`

### سبب الفشل

الـverifier الأول افترض عددًا محدودًا من `window.supabase.auth` references، بينما target يحتوي على سبعة references.

### أثر الخطأ

الـguard أوقف العملية قبل إنشاء target mutation commit.

وهذا منع partial/unsafe mutation.

## 6. التجربة الثانية

تم تصحيح الـexecutor ليعالج جميع السبعة references بعد التحقق من أن التطبيق يستخدم created client نفسه.

Run:

`33533396610`

النتيجة:

`success`

والـlog سجل:

`WRONG_AUTH_REFS_REPAIRED=7`

و:

`1 file changed, 8 insertions(+), 9 deletions(-)`

Target mutation commit:

`573def3`

Target blob:

`6c94560ef22f6778fe0ecf91757afe500f4f1c63`

Static verification:

- Node syntax: PASS.
- MAIN11 boot uses `supabase.auth`.
- session restore uses `supabase.auth.getSession()`.
- no `window.supabase.auth` remains inside `Current/PWA/New-main`.

## 7. PR verification والدمج

تمت مراجعة PR #66 مباشرة.

الـfinal diff كان ملفًا واحدًا فقط:

`Current/PWA/New-main`

مع:

- 8 additions
- 9 deletions

تمت استعادة التعديلات المؤقتة على surgical workflow إلى محتواها الأصلي قبل الدمج.

تم دمج PR بنجاح إلى `main`:

`5d5de8419cfd751220adb324fceb7a659d0d1941`

ثم تمت إعادة قراءة target من `main` والتأكد أن blob أصبح:

`6c94560ef22f6778fe0ecf91757afe500f4f1c63`

`Current/PWA/main.html` لم يتغير.

## 8. Production Supabase

المشروع:

`fiilmooggumokxanwiyx`

اسم المشروع الظاهر:

`SMART ERP`

تمت قراءة المشروع والمفتاح المنشور مباشرة عبر Supabase connector.

لم يتم تنفيذ أي تغيير على:

- Database
- Auth configuration
- Schema
- Edge Functions

محاولة raw HTTP من container فشلت عند DNS قبل الوصول إلى API، ولذلك لم تُستخدم كدليل ضد Supabase.

## 9. Deployment lineage

تمت مراجعة:

`Current/CTO/20260831_PHASE10_DEPLOYMENT_LINEAGE.md`

والوثيقة نفسها تجعل Cloudflare Pages artifact/commit lineage نقطة غير مثبتة.

تمت محاولة فتح production URL:

`https://rawaea-erp.pages.dev/companies/`

لكن بيئة الأدوات لم تستطع جلبه بسبب cache/network limitation.

كما أن repository search لم ينتج deployment identifier عام يمكن من خلاله إثبات الـartifact المنشور.

إذن لا توجد حتى نهاية هذه الجولة شهادة مستقلة بأن Cloudflare Pages تخدم blob `6c94560e...` بالفعل.

## 10. لماذا لم نعلن Closed 100%

لأن لدينا الآن ثلاث حالات منفصلة:

`SOURCE_FIXED = YES`

`MAIN_MERGED = YES`

`PRODUCTION_RUNTIME_CERTIFIED = NO`

لا يجوز دمج هذه الحالات في حالة واحدة.

نجاح الـworkflow يثبت فقط نجاح العملية التي نفذها الـworkflow، ولا يثبت أن Browser/Cloudflare runtime يخدم نفس artifact.

## 11. Service Worker الحكم النهائي لهذه الجولة

Service Worker `404` لم يُغلق.

الدليل المتاح يقول إن:

- المصدر الحالي يحتوي `sw.js`.
- `register-sw.js` الحالي يستخدم المسار المحلي الصحيح.
- runtime يطلب `/companies/sw.js`.

الاستنتاج الآمن: يجب أولًا إثبات production artifact/base path قبل تعديل هذا الجزء.

## 12. سجل الأخطاء والتجارب

### الخطأ 1 — verifier assumption

تم افتراض وجود مرجعين Auth فقط بدل السبعة الحقيقيين.

النتيجة: fail-safe stop.

الإجراء التصحيحي: عدّ references الفعلية ثم توسيع الإصلاح ليغطيها كلها.

### الخطأ 2 — محاولة raw network من بيئة التنفيذ

فشلت DNS resolution.

التقييم: environment limitation وليس Supabase failure.

### المبدأ الصحيح

لم يتم استخدام أي من الخطأين كذريعة لإعلان نجاح غير مثبت.

## 13. ما تم إثباته

### مثبت

- Invalid/stale target Supabase credential.
- Duplicate key assignment.
- Seven wrong `window.supabase.auth` references.
- MAIN11 client-reference problem.
- Static syntax integrity after patch.
- Target mutation persisted to main.
- Main protected artifact untouched.
- Historical Hany reports preserved.

### غير مثبت

- Cloudflare deployment artifact identity.
- Fresh production browser login after this merge.
- disappearance of the 401 in actual production browser.
- Service Worker `/companies/sw.js` closure.
- Gold.
- Diamond.
- Closed 100%.

## 14. الخطة القادمة — P134

### P134-01
إثبات Cloudflare Pages deployment SHA/artifact وعلاقته مباشرة بالـtarget blob:

`6c94560ef22f6778fe0ecf91757afe500f4f1c63`

### P134-02
إجراء browser test على production تشمل:

boot → session → login → tenant context → console.

والتحقق من اختفاء:

`MAIN11_SUPABASE_UNAVAILABLE`

و:

`Invalid API key`

### P134-03
تحديد سبب `/companies/sw.js` بعد إثبات artifact/base path.

### P134-04
إعادة regression test لمسارات Auth/session/tenant ومسارات reports التي أصلحها P131.

### P134-05
إغلاق Gold/Diamond فقط إذا استوفت الأدلة شروطهما بالفعل.

## 15. الحكم التنفيذي

هذه الجولة أصلحت بالفعل العطلين المباشرين الموجودين في source والتي يتوافقان مع الخطأ المبلغ عنه:

1. credential mismatch.
2. wrong Supabase client reference.

والتغيير وصل إلى `main` بنجاح.

لكن لم يتم إثبات وصوله إلى Cloudflare production runtime من الأدوات المتاحة، لذلك لا يجوز القول إن المستخدم لن يرى `Invalid API key` مرة أخرى إلا بعد browser verification على artifact المنشور.

**الحالة النهائية:**

`SOURCE AUTH DEFECT = FIXED`  
`MAIN = UPDATED`  
`PRODUCTION RUNTIME = UNVERIFIED`  
`SERVICE WORKER = OPEN`  
`GOLD = UNPROVEN`  
`DIAMOND = UNPROVEN`  
`CLOSED 100% = NO`

## 16. حفظ التاريخ

هذا الملف هو:

`doc/Draft/Hany/تقرير تنفيذي 14.md`

تم إنشاء التقرير الجديد دون حذف أو overwrite لأي تقرير سابق.
