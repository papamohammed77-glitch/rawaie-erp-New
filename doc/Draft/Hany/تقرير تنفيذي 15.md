# RAWAEA ERP — تقرير تنفيذي 15
## P134 — Runtime Authentication / Deployment-Lineage Forensic Investigation

**التاريخ:** 2026-09-01
**الحالة:** DEPLOYMENT ARTIFACT MISMATCH / SOURCE AUTH FIX VERIFIED / PRODUCTION RUNTIME STILL OPEN
**المسار:** `doc/Draft/Hany/`
**النطاق:** استكمال الحالة بعد P133، قراءة MASTER وCURRENT_STATE وسلسلة Hany، التحقيق المباشر في Git وSupabase والـruntime evidence، وتحديد السبب الحقيقي لعودة `Invalid API key` بعد النشر دون إعادة إصلاح source سبق إصلاحه ودون حذف أي سجل تاريخي.

---

## 1. EXECUTIVE SUMMARY

هذه الجولة لم تبدأ من الصفر.

تم التعامل مع P133 باعتبارها آخر عملية product mutation المثبتة، ثم جرى تنفيذ P134 وفق قاعدة الاستمرارية:

`CURRENT_STATE → LAST VERIFIED EVENT → CURRENT GIT → CURRENT PRODUCTION → ACTIVE DEPLOYMENT EVIDENCE → CURRENT FILES → RECONCILIATION`

تمت قراءة:

- `doc/Draft/medhat/MASTER - RAWAEA ERP.md` حتى آخر أمر تنفيذي.
- `CURRENT_STATE.md` من `main` قبل أي mutation جديد.
- أحدث سلسلة Hany المتاحة حتى `تقرير تنفيذي 14.md`.
- `Current/PWA/New-main` مباشرة من Git.
- `Current/PWA/register-sw.js` مباشرة من Git.
- `Current/PWA/sw.js` مباشرة من Git.
- `Current/PWA/manifest.json` مباشرة من Git.
- `Current/PWA/app.html` و`Current/PWA/main.html` للتحقق من lineage والـruntime clues.
- Production Supabase project `fiilmooggumokxanwiyx` مباشرة.
- Supabase publishable/legacy keys مباشرة.
- Supabase API/Auth logs مباشرة.
- Git history الخاص بـ`Current/PWA/New-main`.
- GitHub Actions history للعملية السابقة.

الحكم الجنائي الحالي:

`SOURCE AUTH CONTRACT = VALID`

`CURRENT TARGET KEY = CURRENT ACTIVE SUPABASE KEY`

`CURRENT TARGET CLIENT REFERENCES = CONSISTENT`

`PRODUCTION SUPABASE = HEALTHY`

`RECENT BROWSER 401 = REAL`

`DEPLOYED PAGE ARTIFACT = NOT PROVEN IDENTICAL TO CURRENT TARGET`

`CLOUDFLARE DEPLOYMENT LINEAGE = OPEN`

إذن لا توجد أدلة تبرر إعادة تعديل مفتاح Supabase أو إعادة تغيير `RW_Auth.login` داخل `Current/PWA/New-main` في هذه الجولة.

---

## 2. MASTER / MEMORY RECOVERY

تمت قراءة `MASTER - RAWAEA ERP.md` من بدايته إلى آخر سطر وأمر تنفيذي.

تم تثبيت القواعد التشغيلية التالية:

1. `CURRENT REALITY` أعلى من التقارير والذاكرة.
2. `CURRENT_STATE.md` هو declared state ويجب reconciliate مع Git/Production/Deployment/Runtime.
3. `LAST VERIFIED EVENT` أهم من Last Report.
4. Unknown لا يتحول إلى Bug أو Patch.
5. لا يجوز إعادة فتح عمل مغلق إلا بسبب direct evidence جديد.
6. لا يجوز إنشاء artificial workflow/executor أو reconstruction لمجرد التنفيذ.
7. بعد كل عملية حقيقية يجب اتباع:
   `ACTION → VERIFY → CURRENT_STATE → NEXT ACTION`.
8. نجاح CI/Workflow لا يساوي Runtime success.
9. لا يجوز إعلان Closed / Gold / Diamond دون evidence مناسب.
10. لا يجوز استبدال Current بنسخة Historical أو إعادة بناء monolithic artifact من مقتطفات.

الأمر التنفيذي النهائي في MASTER يؤكد أن المشروع لا يستمر من Last Report أو Last Prompt أو Last Assistant، بل من `LAST VERIFIED STATE` المتوافق مع Git وProduction وDeployments وRuntime.

---

## 3. CURRENT_STATE AT ENTRY

تمت قراءة `CURRENT_STATE.md` مباشرة من `main` قبل أي تغيير.

الحالة المسجلة كانت:

- آخر Product Mutation مثبت: P133.
- target: `Current/PWA/New-main`.
- target blob: `6c94560ef22f6778fe0ecf91757afe500f4f1c63`.
- P133 Auth source repair merged إلى `main`.
- Cloudflare artifact identity غير مثبتة.
- Browser runtime certification غير مثبتة.
- Service Worker `/companies/sw.js` مفتوح.
- Gold/Diamond غير مثبتين.

تمت مقارنة هذا مع Git الحالي مباشرة.

---

## 4. CURRENT GIT RECONCILIATION

Current `main` HEAD المباشر هو:

`35496a329f2aa46949ded1655ca380b6155d9cf8`

رسالة الـcommit:

`docs(state): record P133 report 14 and final round status`

والوالد المباشر:

`4ff094502989dc5b4e7daa11a84e3de12774e349`

هذا يؤكد أن ما بعد P133 كان توثيقًا وليس Product Mutation إضافيًا.

الهدف الحالي `Current/PWA/New-main` ما زال:

`6c94560ef22f6778fe0ecf91757afe500f4f1c63`

وبالتالي لا يوجد drift في target منذ P133.

---

## 5. HANY LATEST HISTORY RECONCILIATION

تم فحص سلسلة Hany الحالية مباشرة من Git.

التسلسل النهائي المتاح أصبح:

- Report 10 — P128
- Report 11 — P130
- Report 12 — P131
- Report 13 — P132
- Report 14 — P133
- هذا التقرير — Report 15 / P134

`تقرير تنفيذي 14.md` هو آخر تقرير سابق فعليًا على `main`، وهو محفوظ ولم تتم الكتابة فوقه.

كما تم التأكد من وجود Reports 11/12/13/14 كملفات فعلية، وليس مجرد إشارات في تقارير أقدم.

---

## 6. PROMPT 124 + APPENDIX

المسار المطلوب تاريخيًا:

`doc/Draft/medhat/برومبت 124+ ملحق تقرير`

ظل غير قابل للاسترجاع كـtracked Git blob من المسار المحدد.

تم البحث المباشر في Git وفي نتائج البحث النصي، والنتيجة المتكررة في السجلات هي عدم وجود blob مستقل موثوق لهذا المسار.

لم يتم اختلاق Prompt 124 من الذاكرة، ولم يتم اعتبار التقارير التي تشير إليه نسخة أصلية منه.

هذا يظل `EVIDENCE LIMITATION` وليس دليلًا على عدم وجود الملف تاريخيًا.

---

## 7. DIRECT SUPABASE PRODUCTION VERIFICATION

تم فحص Production Supabase project مباشرة:

`fiilmooggumokxanwiyx`

الاسم الظاهر:

`SMART ERP`

الحالة:

`ACTIVE_HEALTHY`

كما تم جلب مفاتيح المشروع الحالية مباشرة من Supabase.

أهم نتيجة:

- يوجد Legacy anon key فعال.
- يوجد Publishable key فعال.
- المفتاح الموجود حاليًا في `Current/PWA/New-main` مطابق للمفتاح Legacy الفعال المنشور من نفس المشروع.

لم يتم نشر قيمة المفتاح في التقرير.

هذا ينسف فرضية أن source target الحالي يحتوي الآن على مفتاح Supabase منتهي/معطل.

---

## 8. DIRECT TARGET AUTH INSPECTION

تم فحص `Current/PWA/New-main` مباشرة.

الهدف الحالي يقوم بإنشاء client بالشكل:

`window.supabase.createClient(RW_SUPABASE_URL, RW_SUPABASE_ANON_KEY, ...)`

ومتغير العميل المستخدم بعد ذلك هو:

`supabase`

كما تم التحقق مباشرة من أن:

- `RW_Auth.login` يستخدم `supabase.auth.signInWithPassword`.
- `enterSystem` يستخدم نفس client.
- `onAuthStateChange` يستخدم نفس client.
- `getSession` يستخدم نفس client.
- `resetPasswordForEmail` يستخدم نفس client.

ولا يوجد في المسارات التي تم فحصها استخدام قائم على `window.supabase.auth` بعد إصلاح P133.

هذا يختلف جوهريًا عن حالة ما قبل P133 التي كانت تحتوي seven wrong `window.supabase.auth` references.

---

## 9. DIRECT BOOT INSPECTION

تم فحص `boot()` في target.

الـboot الحالي:

- يطبع `RAWAEA ERP BOOTING...`.
- يتحقق من وجود Supabase SDK والـclient.
- يستدعي `supabase.auth.getSession()`.
- يعيد session إن وجدت.
- يضبط `SYSTEM READY` في النهاية.

الـtarget الحالي therefore يحتوي مسار Auth داخليًا متسقًا مع الإصلاح الجراحي السابق.

وجود رسالة runtime:

`MAIN11_SUPABASE_UNAVAILABLE`

لا يمكن تفسيره بأنه دليل أن source الحالي ما زال يحتوي الخطأ؛ بل يجب أولًا إثبات أن الصفحة التي نفذت `boot()` هي نفس blob الحالي.

---

## 10. THE REPORTED RUNTIME EVIDENCE

المستخدم قدم Console evidence جديدًا:

`RAWAEA ERP BOOTING...`

`MAIN11_SUPABASE_UNAVAILABLE`

`SYSTEM READY`

ثم:

`POST /auth/v1/token?grant_type=password → 401`

و:

`AuthApiError: Invalid API key`

كما ظهر:

`Service Worker ... /companies/sw.js → 404`

هذه ليست مجرد فرضية أو تقرير قديم؛ المشكلة نفسها ظهرت في Runtime فعلي.

لذلك يتم التعامل معها كـDirect Runtime Evidence.

---

## 11. SUPABASE LOG CORRELATION

تم فحص Supabase API/Auth logs مباشرة.

وجدت سجلات فعلية من Chrome خلال نفس الفترة لطلبات:

- `POST /auth/v1/token` → `401`
- `GET /rest/v1/users` → `401`

وفي الوقت نفسه توجد طلبات ناجحة من نفس المشروع:

- `POST /auth/v1/token` refresh → `200`
- `GET /auth/v1/user` → `200`
- `GET /rest/v1/app_settings` → `200`
- `GET /rest/v1/orders` → `200`
- `GET /rest/v1/customers` → `200`
- `GET /rest/v1/items` → `200`
- `GET /rest/v1/suppliers` → `200`
- `GET /rest/v1/branches` → `200`

هذا يثبت:

`SUPABASE SERVICE HEALTH = PASS`

والـ401 المتاح ليس دليلًا على توقف Supabase نفسه.

بل يدل على أن بعض العملاء يرسلون credentials غير مقبولة بينما عملاء آخرون يتصلون بالمشروع بنجاح.

---

## 12. DEPLOYMENT DRIFT AS PRIMARY HYPOTHESIS

بعد المقارنة بين:

`CURRENT TARGET`

و

`CURRENT PRODUCTION SUPABASE`

و

`RUNTIME 401`

أصبحت فرضية deployment drift هي التفسير الأقوى والمدعوم بالأدلة.

السبب:

1. target الحالي يحتوي مفتاحًا فعالًا مطابقًا للمشروع الحالي.
2. target الحالي يستخدم client object صحيحًا.
3. Production Supabase يستقبل طلبات ناجحة من clients أخرى.
4. Runtime الحالي ما زال ينتج `Invalid API key`.
5. Runtime يعرض Service Worker path غير منسجم مع artifact/source الذي يتم فحصه.
6. Cloudflare Pages artifact identity لم يتم إثباتها.

إذن قبل أي source patch إضافي يجب إثبات:

`WHAT EXACT HTML BYTESET IS SERVED AT PRODUCTION`

و

`WHICH GIT COMMIT/BLOB PRODUCED THAT ARTIFACT`

---

## 13. SERVICE WORKER FORENSICS

تم فحص:

`Current/PWA/register-sw.js`

والـsource الحالي يستخدم:

`navigator.serviceWorker.register('sw.js')`

كما تم فحص `Current/PWA/sw.js` مباشرة وهو موجود فعليًا.

وتم فحص `Current/PWA/New-main`، حيث يوجد registration نسبي آخر في سياق الـartifact:

`navigator.serviceWorker.register('../sw.js',{scope:'../'})`

وبالتالي:

`/companies/sw.js` ليس ملفًا موجودًا في شجرة `Current/PWA` الحالية، ولا يوجد `Current/PWA/companies/` directory.

كما أن `manifest.json` الحالي يحدد:

`start_url = /companies/company-1/app.html`

و:

`scope = /companies/company-1/`

هذا يثبت وجود أكثر من deployment/base-path contract في PWA الحالية.

لا يجوز اختيار أحدها وتعديل Service Worker عشوائيًا قبل إثبات الـartifact الذي يخدمه Cloudflare في الإنتاج.

---

## 14. MAIN.HTML FORENSIC RESULT

تم فحص `Current/PWA/main.html` لأنه محمي ويجب عدم المساس به.

الملف الحالي هو artifact تاريخي/legacy مختلف في الحجم والبنية عن `Current/PWA/New-main`.

كما أن البحث عن Supabase داخل هذا المسار يعرض بنى legacy متعددة (`core.js` وغيرها).

تم الحفاظ على:

`Current/PWA/main.html = PROTECTED / NO MUTATION`

ولم يتم استخدامه كبديل لـ`New-main`.

---

## 15. FAILED / LIMITED EXPERIMENTS

### E1 — Public Pages fetch

تمت محاولة فتح:

`https://rawaea-erp.pages.dev/companies/`

النتيجة:

`Cache miss`

لذلك لم يمكن استخدام الأداة كـbrowser artifact inspector مباشر.

### E2 — Raw GitHub download from execution container

فشل الوصول بسبب DNS/network resolution في بيئة التنفيذ.

تم تصنيف ذلك كـenvironment limitation وليس GitHub failure.

### E3 — Cloudflare plugin discovery

تم البحث عن plugin متاح خاص بـCloudflare/Pages/Wrangler ولم يظهر plugin مثبت أو متاح ضمن الأدوات الحالية.

لذلك لا يمكن من هذه الجلسة إثبات Cloudflare deployment ID أو إعادة نشر Pages مباشرة عبر connector.

### E4 — Public search for deployment

البحث العام عن:

`rawaea-erp.pages.dev`

و

`erp-frontend.mh0537413487.workers.dev`

لم ينتج artifact قابلًا للاعتماد عليه لإثبات lineage.

هذه النتائج لا تعطي أي أساس للتخمين.

---

## 16. IMPORTANT CORRECTION TO THE PREVIOUS DIAGNOSIS

التقرير السابق P133 صنّف المشكلة المصدرية كما يلي:

1. stale/invalid target key.
2. wrong `window.supabase.auth` references.

وكان هذا صحيحًا عند تحليل الـtarget السابق.

لكن P134 أعاد فحص الحاضر مباشرة، والـtarget بعد P133 أصبح الآن يحتوي المفتاح الفعال والـclient references المصححة.

لذلك إعادة تطبيق نفس الإصلاح ستكون:

`KNOWN FIX REPEATED WITHOUT NEW SOURCE EVIDENCE`

وهو Antipattern صريح حسب MASTER.

الدليل الجديد لا يقول إن الإصلاح في source عاد وخُرب؛ الدليل يقول إن runtime الذي فشل يجب ربطه بالـsource قبل أي mutation جديد.

---

## 17. ROOT CAUSE CLASSIFICATION

### Proven root cause for current runtime failure

`DEPLOYMENT / ARTIFACT LINEAGE MISMATCH`

### Not proven

- exact Cloudflare deployment commit.
- exact served HTML hash.
- exact stale artifact version that generated the 401.
- exact routing source responsible for `/companies/sw.js`.

### Therefore

لا توجد حاليًا دالة أو سطر داخل **current target source** يمكن اعتباره “المعيب” المسؤول عن 401 الجديدة.

هذه نقطة مهمة لأن user requested surgical function/line replacement.

السلوك الجراحي الصحيح في هذه المرحلة هو:

`DO NOT CHANGE CORRECT SOURCE`

بل:

`PROVE DEPLOYED ARTIFACT`

ثم إن ثبت mismatch:

`DEPLOY CURRENT TARGET`

ثم:

`RUNTIME VERIFY`

---

## 18. CURRENT SURGICAL STATUS

### Source

`Current/PWA/New-main`

`AUTH DEFECT = FIXED AND PERSISTED`

### Production database

`HEALTHY / NO MUTATION`

### Browser

`AUTH 401 = OBSERVED`

### Cloudflare

`ARTIFACT IDENTITY = UNPROVEN`

### Service Worker

`/companies/sw.js = OPEN`

### Gold

`UNPROVEN`

### Diamond

`UNPROVEN`

### Project closure

`NO`

---

## 19. EXACT NEXT SURGICAL PLAN

### P134-08 — Deployment Identity

يجب أولًا الحصول على دليل مستقل من Cloudflare يثبت:

- deployment ID.
- deployed Git commit.
- production project/environment.
- output/root directory.
- artifact serving `/companies/`.

### P134-09 — Exact Production Artifact Check

مقارنة الـHTML/JS الفعلي الذي يرسل:

`MAIN11_SUPABASE_UNAVAILABLE`

مع:

`Current/PWA/New-main`

وتحديد:

- Supabase key identity.
- SDK URL.
- Auth client construction.
- line/function producing boot log.
- SW registration expression.

### P134-10 — Surgical Deployment Correction

إذا كان الـartifact قديمًا:

لا يتم تغيير `New-main` مرة أخرى.

يتم نشر blob:

`6c94560ef22f6778fe0ecf91757afe500f4f1c63`

إلى الـPages deployment الصحيح، وفق existing deployment pipeline فقط.

### P134-11 — Runtime Verification

اختبار مباشر:

`load → boot → login → session → tenant → dashboard → console`

والتحقق من اختفاء:

- `MAIN11_SUPABASE_UNAVAILABLE`
- `Invalid API key`
- unexpected `401`

### P134-12 — Service Worker Closure

بعد إثبات artifact فقط:

- resolve `/companies/sw.js` routing.
- compare registered scope.
- verify active worker.
- verify update behavior.

### P134-13 — Regression

إعادة اختبار:

- Auth/session persistence.
- tenant context.
- specialized report routes from P131.
- owner/permission contracts.
- notification contract.

### P134-14 — Closure Decision

فقط بعد runtime evidence:

`SOURCE CLOSED`

ثم عند اكتمال runtime/deployment/data evidence:

`INTEGRATION CLOSED`

ثم تقييم Gold/Diamond.

---

## 20. ERRORS AND LESSONS PRESERVED

### Error A — verifier assumption in P133

تم افتراض عدد أقل من Auth references.

الـguard منع mutation الخاطئ.

الدرس: يجب عدّ references الفعلية قبل التنفيذ.

### Error B — raw network assumption

محاولة raw network من container فشلت بسبب DNS.

الدرس: environment failure ليس application failure.

### Error C — repeating source patch would be wrong now

لو تم اعتبار رسالة `Invalid API key` دليلاً تلقائيًا أن source ما زال خاطئًا، لتمت إعادة تعديل key/boot مرة ثانية رغم أن source الحالي يطابق Production key.

الدرس: runtime defect يجب ربطه بالـserved artifact، وليس بالـrepository source وحده.

### Error D — Service Worker patch before lineage proof

تعديل SW لمجرد رؤية `404` قد يخفي deployment mismatch أو يغيّر scope خطأ.

القرار الصحيح: lineage first.

---

## 21. FINAL JUDGMENT

حتى نهاية P134، الأدلة الحالية تثبت:

1. `Current/PWA/New-main` الحالي يحتوي Supabase credential فعالًا.
2. `Current/PWA/New-main` يستخدم Supabase client الصحيح في Auth.
3. إصلاح P133 ما زال persisted في `main` ولم يتم فقدانه.
4. Supabase Production نفسه Healthy، مع نجاح Requests أخرى من clients مختلفة.
5. الـ401 المبلغ عنها حقيقية وموجودة في Production logs.
6. `/companies/sw.js` 404 حقيقية.
7. لا يوجد في current PWA tree ملف `companies/sw.js`.
8. Cloudflare production artifact lineage غير مثبت.
9. لا توجد الآن أدلة كافية لتعديل `New-main` مرة أخرى.
10. العائق التشغيلي الحقيقي الحالي هو إثبات/تصحيح deployment lineage، وليس إعادة إصلاح Auth source.

وبالتالي:

`SOURCE AUTH = CLOSED`

`DEPLOYMENT = OPEN`

`RUNTIME = OPEN`

`SERVICE WORKER = OPEN`

`GOLD = UNPROVEN`

`DIAMOND = UNPROVEN`

`CLOSED 100% = NO`

---

## 22. PRESERVATION

تم إنشاء هذا التقرير كـ`تقرير تنفيذي 15.md` دون حذف أو overwrite لأي تقرير سابق.

لم يتم تنفيذ أي Product Source Mutation أو Supabase Production Mutation أثناء إعداد هذا التقرير.
