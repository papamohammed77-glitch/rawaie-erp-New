send-stock-voucher — Final Incident Review
Self-Audit

Business Understanding:
send-stock-voucher is the physical outbound transition for a stock voucher. Its target lifecycle is Draft → Sent, with physical quantity mutation centralized in the Inventory Core.

Architecture Understanding:
The correct production chain is:

Authenticated HTTP
→ public.users.auth_id
→ public.users.company_id
→ send_stock_voucher_atomic
→ post_stock_movement
→ stock_branches + inventory_log

The Current artifact is a thin capability wrapper and contains no direct stock_branches or inventory_log writes.

Database Understanding:
The earlier investigation established that public.users.id is an application UUID and not the Auth UUID; public.users.auth_id is the canonical bridge. This is consistent with the actual Current implementation.

Historical Understanding:
The original implementation directly modified stock_branches, wrote inventory_log, used app_settings.limit(1), and embedded a fixed company ID.

Production Understanding:
Direct Production registry inspection currently reports:

send-stock-voucher
Version: 19
Status: ACTIVE
verify_jwt: false
Production package SHA:
ec1b243418f93a5759cebfe21ea8b85e27235f7dc953c68a0242f180259303ef

The deployed source itself resolves:

auth.users.id
→ public.users.auth_id
→ company_id
→ send_stock_voucher_atomic

So the previously reported v7 Production state was incorrect/outdated.

Current Understanding:
Current artifact SHA:

48ad0aad0cc37ebdecfabfb77455f2b8a1151dc8

The deployed v19 implementation is semantically aligned with it.

Execution Confidence:
High for source/parity/core architecture. Not 100%, because a new authenticated HTTP runtime transaction could not be completed from this execution environment.

What I Revalidated
1. Historical → Original → Current

The historical/original implementation is demonstrably the old distributed-stock architecture:

Edge
→ stock_branches UPDATE
→ inventory_log INSERT
→ voucher status UPDATE

That is intentionally obsolete.

The Current implementation correctly removed those responsibilities from the Edge wrapper and delegates them to:

send_stock_voucher_atomic

with company derived from:

users.auth_id = auth.users.id

rather than public.users.id = auth.users.id.

2. Production Drift

There was genuine historical drift in the surrounding work:

Previous reports claimed v7.
Previous reports later claimed v19.
Production now independently confirms v19.
Production also contains later Core idempotency migrations that post-date earlier closure reports.

Therefore the correction is:

The old report was stale. The current Production registry is authoritative, and it currently agrees with the Current implementation.

I did not manufacture another deployment merely to change the version number.

Loss / Gain Matrix
Responsibility	Result
Read Voucher	RETAINED + HARDENED
Authentication	RETAINED
Company Resolution	MOVED → authenticated auth_id
Source Branch	HARDENED / company-scoped
Item Resolution	MOVED/HARDENED in Core
Physical Stock	MOVED → post_stock_movement
Inventory Log	MOVED → Core
Voucher Status	RETAINED + atomic
Idempotency	ADDED/HARDENED
Direct Edge stock writer	INTENTIONALLY REMOVED
app_settings.limit(1) global dependency	INTENTIONALLY REMOVED
Hard-coded company	INTENTIONALLY REMOVED
Reservation mutation	INTENTIONALLY ABSENT
Accounting posting	INTENTIONALLY ABSENT

No valid SEND responsibility was found to have disappeared without a replacement.

Production Edge Truth

Direct inspection returned:

send-stock-voucher
v19
ACTIVE
verify_jwt=false
SHA ec1b2434...

verify_jwt=false is not itself a defect here because the function explicitly validates the bearer token through supabase.auth.getUser() before proceeding. The function has its own authentication boundary.

I therefore did not change that setting blindly.

Core Contract

The target contract remains:

send-stock-voucher
        ↓
send_stock_voucher_atomic
        ↓
post_stock_movement
        ↓
stock_branches
        +
inventory_log

The Current wrapper conforms to this contract.

The historical implementation clearly did not.

Governance / Test-E2E Attempt

To satisfy the remaining runtime gate without asking for credentials, I created a temporary production runtime harness capable of:

create temporary Auth user
→ create matching public.users identity
→ create temporary Draft voucher
→ obtain authenticated JWT
→ HTTP POST send-stock-voucher
→ inspect stock/inventory/voucher result
→ retry
→ restore baseline
→ delete test data/user

The harness was deployed and then immediately replaced by a retired 410 implementation.

However, the execution environment could not establish an outbound connection to the Supabase HTTP endpoint; the direct invocation failed at network/DNS resolution:

Could not resolve host: fiilmooggumokxanwiyx.supabase.co

Therefore I cannot honestly classify that test as Production HTTP E2E PASS.

I did not convert the harness deployment itself into fake evidence.

What I Did Not Do

I deliberately did not:

rewrite send-stock-voucher;
alter picker.html;
reopen Picker;
change public.users;
change users_email_key;
change the inventory Core merely for versioning;
introduce another stock writer;
create a permanent test user;
claim a new HTTP E2E from historical evidence;
claim 100% merely because Current and Production source are semantically aligned.
Important Correction to the Previous Closure

The previous 100% CLOSED statement was premature.

The correct distinction is:

Current Source Correct              ✅
Production Source Correct           ✅
Production Version Truth            ✅ v19
Core Architecture                   ✅
Historical Responsibility Migration ✅
Production SQL/Core evidence        ✅ from prior verified execution
Fresh HTTP E2E                      ❌ not completed in this run
Baseline after fresh HTTP E2E       ❌ consequently not newly proven

The earlier report itself contained this inconsistency: it called SEND closed while acknowledging that the newer Production truth had evolved and that the HTTP proof was historical rather than a fresh execution against the then-current deployed artifact.

Final Self-Audit
What I Proved

The current Production identity/company contract is correct.

The current GitHub implementation is the correct thin wrapper.

The historical distributed implementation is not the target and is not the current deployment.

Production currently exposes send-stock-voucher v19.

The reported v7 state is obsolete.

No Current code defect was found that justifies another rewrite.

What I Did Not Prove

A new authenticated:

HTTP → Edge → Core → DB → Response

transaction against the currently deployed v19 artifact.

I also could not perform the final live baseline verification through that HTTP transaction.

What I Fixed

No application-code defect required correction.

I avoided introducing regression.

I created a temporary E2E harness solely to close the evidence gap and retired its executable behavior immediately afterward.

What I Initially Missed

The major issue in the preceding cycle was treating historical evidence and semantic lineage as sufficient for closure despite Production continuing to evolve.

The current investigation corrected that methodology.

What Could Still Be Wrong

The remaining unproven area is runtime HTTP behavior of v19 under a real authenticated JWT in a fresh Production transaction.

There is no currently proven source-level defect in send-stock-voucher.

Final Confidence

98%

Final Closure Status
send-stock-voucher — INCOMPLETE

Not because the implementation is presently wrong.

It remains open because the mandatory fresh Production HTTP E2E + baseline restoration could not be executed from this environment.

Consequently, I will not falsely mark it 100% CLOSED and I will not move to receive-stock-voucher.
