PROMPT 8 — EXECUTION CLOSEOUT REPORT
SELF-AUDIT

Business Understanding:
The task is to close the send-stock-voucher closure unit only after independent Production verification, complete cleanup, fresh HTTP E2E evidence, and governance cleanup. The next unit must not begin before that closure is genuinely complete.

Architecture Understanding:
The current intended architecture is:

Authenticated HTTP request
        ↓
send-stock-voucher
        ↓
auth.users.id
        ↓
public.users.auth_id
        ↓
public.users.company_id
        ↓
send_stock_voucher_atomic
        ↓
post_stock_movement
        ↓
stock_branches + inventory_log

The historical implementation was materially different and directly mutated stock_branches and inventory_log.

Database Understanding:
Production confirms public.users.id is an application PK and is not the Auth UUID. public.users.auth_id is the canonical FK to auth.users(id). The production idempotency boundary is the unique (company_id, idempotency_key) index.

Historical Understanding:
Historical send-stock-voucher.ts used global app_settings.limit(1), fixed company context, and direct stock mutation. That implementation is obsolete.

Production Understanding:
Production currently runs send-stock-voucher v19, with deployed package SHA:

ec1b243418f93a5759cebfe21ea8b85e27235f7dc953c68a0242f180259303ef

The Current GitHub artifact is SHA:

48ad0aad0cc37ebdecfabfb77455f2b8a1151dc8

and uses the canonical auth_id → company_id contract.

Current Understanding:
The previous reports were not treated as authoritative. Production was re-read directly, including current Edge registry, current source, PostgreSQL functions, migrations, users, items, inventory state, idempotency, permissions, and cleanup markers.

Execution Confidence:
High for database/core correctness. Not 100% for the release closure, because two mandatory external gates remain unproven.

A — GOVERNANCE / ARTIFACT CLEANUP

The Production registry contained these temporary/test artifacts:

start-picking-production-harness
cp-prod-auth-canary-20260814
cp-prod-fixture-canary-20260814
start-picking-e2e-fixture-20260815
start-picking-real-identity-e2e-20260815
send-stock-voucher-runtime-e2e-20260818

All identified temporary runtime functions have now been retired to inert HTTP 410 implementations.

The important distinction required by Prompt 8 remains:

ACTIVE + 410 ≠ DELETED

The Edge-function API available in this environment does not expose a deletion operation. After retirement, the registry still reports these functions as ACTIVE.

Therefore:

Governance cleanup is functionally retired, but not physically deleted from the Supabase Edge registry.

This is not being falsely classified as a governance PASS.

B — FRESH PRODUCTION TRUTH
send-stock-voucher
Current deployment
Version:       19
Status:        ACTIVE
verify_jwt:    false
Custom Auth:   YES
Package SHA:   ec1b243418f93a5759cebfe21ea8b85e27235f7dc953c68a0242f180259303ef

The implementation verifies the bearer token itself, resolves the authenticated user through:

users.auth_id = auth.users.id

then obtains the tenant from public.users.company_id, and calls:

send_stock_voucher_atomic(...)

The Current GitHub implementation matches that contract.

Core

Production currently exposes:

send_stock_voucher_atomic(...)
        ↓
post_stock_movement(...)

Both are SECURITY DEFINER.

Neither function is executable by:

anon
authenticated

Only service_role has EXECUTE.

The inventory idempotency guard is:

ux_inventory_log_company_idempotency
UNIQUE(company_id, idempotency_key)
WHERE idempotency_key IS NOT NULL

This is a strong and correct concurrency boundary.

C — FRESH PRODUCTION CORE TEST

A fresh Production transactional test was executed after the new audit.

The whole test ran inside a transaction and was rolled back, so it left no permanent test data.

Normal Send

Result:

success: true
status: Sent
movement_count: 1

Stock:

before qty = 207
after qty  = 205


allocated_qty:
before = 0
after  = 0

Inventory log:

1

Exactly one physical movement was produced.

Retry / Duplicate

Second invocation returned:

success: true
duplicate: true

Inventory log remained:

1

No second physical movement occurred.

Invalid State

A Sent voucher correctly failed with:

Voucher is not Draft
Wrong Company

Cross-tenant access correctly failed with:

Voucher not found

This proves the tenant boundary is being applied by the Core.

Insufficient Stock

Correctly failed with:

insufficient available stock

No stock mutation was committed.

Atomic Rollback

A voucher containing:

valid line
+
impossible line

failed as a single transaction.

After failure:

qty = 207
allocated_qty = 0
inventory logs = 0
voucher status = Draft

Therefore the valid first line was not partially committed.

D — PRODUCTION DATABASE CLEANUP CHECK

Fresh Production reconciliation after the transaction rollback:

users_test_markers       = 0
voucher_test_markers     = 0
inventory_test_markers   = 0
RS-2 inventory logs      = 0
test audit actors        = 0

Also independently verified:

auth_without_public      = 0
public.users             = 28
public.users with auth   = 23
public.users without auth= 5
items                    = 50
item-code duplicates     = 0
orphan items             = 0

The five users without auth_id are legacy business records and were not deleted merely because they lack an Auth linkage.

That distinction is important: absence of an Auth link is not sufficient evidence that a user is test data.

E — WHAT I PROVED
Proven
Production is actually running send-stock-voucher v19.
Current GitHub source and Production source are semantically aligned.
The canonical identity bridge is auth.users.id → public.users.auth_id.
Tenant context comes from the authenticated user's company_id.
app_settings is no longer used as a global tenant selector.
Physical stock mutation is centralized in post_stock_movement.
inventory_log is part of the same Core transaction.
Idempotency is enforced at the DB level.
Normal Send works at the Core boundary.
Retry returns duplicate semantics.
Invalid state is rejected.
Wrong-company access is rejected.
Insufficient stock is rejected.
Atomic rollback works.
allocated_qty remains unchanged for Send.
No accounting mutation was introduced by the Send stock Core.
Current Production contains zero remaining obvious test-user/voucher/inventory markers from this test family.
All identified temporary runtime harnesses have been neutralized to 410.
WHAT I DID NOT PROVE

Two mandatory Prompt 8 gates remain unproven.

1. Fresh authenticated HTTP E2E

I did not fabricate this evidence.

The required proof is:

real HTTP POST
→ Production Edge
→ real bearer JWT
→ send-stock-voucher
→ auth
→ public.users.auth_id
→ company
→ send_stock_voucher_atomic
→ post_stock_movement
→ database
→ HTTP response

The available execution surface here has no authenticated POST client capable of performing that final real-system request. The attempt to create a new privileged E2E runner was rejected by the execution security layer.

Therefore there is no honest fresh HTTP 200/400 production trace to claim.

The Edge logs currently show the retired canaries returning 410, but that is not evidence of a fresh send-stock-voucher HTTP E2E.

2. Physical Edge-function deletion

The six identified temporary functions are retired, but the available Supabase control surface does not expose Edge-function deletion.

Thus:

runtime retired = YES
registry deletion = NOT EXECUTED

I will not call that 100% governance closure.

WHAT I FIXED

No new change to send-stock-voucher was necessary.

The critical architectural correction already present in Production is correct:

auth_id
→ company_id
→ send_stock_voucher_atomic
→ post_stock_movement

No workaround was added.

No automatic User creation was added.

No users_email_key bypass was added.

No global app_settings.limit(1) tenant workaround was added.

No picker.html was modified.

No start-picking work was resumed.

WHAT I INITIALLY MISSED

The earlier execution chain overestimated closure because it treated:

Core correctness
+
deployment
+
transactional smoke

as sufficient.

Prompt 8 correctly requires more:

Core
+
real HTTP
+
runtime evidence
+
governance deletion
+
clean baseline

The fresh audit also exposed that several temporary Edge functions remained registered even though some already returned HTTP 410.

WHAT COULD STILL BE WRONG

The remaining uncertainty is narrowly bounded:

A real authenticated HTTP invocation could still expose an Edge/runtime/header issue not visible from direct Core execution.
Temporary functions remain registered until physically deleted through an Edge management control plane.

There is no current Production DB evidence indicating a stock-engine defect in send-stock-voucher.

FINAL CLOSURE STATUS
send-stock-voucher

NOT 100% CLOSED YET.

Not because the Core is defective.

It is because the two explicit release gates below are still missing:

[ ] Fresh authenticated Production HTTP E2E
[ ] Physical deletion of retired temporary Edge functions

Everything executable from the available GitHub/Supabase control surface has been completed and revalidated.

receive-stock-voucher

NOT STARTED.

Prompt 8 explicitly requires send-stock-voucher to reach genuine 100% closure before moving to the next closure unit. Starting receive-stock-voucher now would violate the prescribed sequence.

Final assessment
Business correctness:       PROVEN
Core architecture:           PROVEN
Production deployment:       PROVEN
Database tests:              PASS
Idempotency:                 PASS
Tenant isolation:            PASS
Atomic rollback:             PASS
Baseline cleanup:            PASS
Temporary runtime retirement: PASS
Fresh HTTP E2E:              NOT PROVEN
Physical artifact deletion:  NOT COMPLETED


FINAL CLOSURE: NOT 100%

The correct engineering conclusion is therefore not to mark send-stock-voucher closed or proceed to receive-stock-voucher while those two evidence gates remain open.
