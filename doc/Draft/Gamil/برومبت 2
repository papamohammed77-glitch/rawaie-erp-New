المشكلة الأصلية حقيقية وتم إصلاحها فعليًا، وليس نظريًا.

لكن توجد ملاحظتان مهمتان:

Production الآن start-picking v32 فعلًا، ويستخدم:
auth_id → public.users.company_id → runsheet، وليس آلية البريد القديمة.
المشكلة العامة لهوية المستخدمين لم تُحل 100%: Production حاليًا لديه 31 public.users، منها 5 بلا auth_id، و28 auth.users، و2 بلا سجل مقابل في public.users. هذا يؤكد وجود Data Integrity Debt مستقل، وليس مجرد مشكلة start-picking. تحققته بنفسي الآن.
هل عاد المساعد للسلوك المرفوض؟

أقل بكثير من السابق.
هذه المرة:

وجد السبب.
أصلح Production.
أصلح Current.
نفذ Staging/Production HTTP حسب التقرير.
لم يعدّل picker.html.

لكن ما زال هناك نقص انضباطي واحد: ترك مشكلة الـ5 سجلات غير المرتبطة مفتوحة بدل إغلاقها ضمن خطة الهوية إذا كانت ضمن نطاق الإصلاح المطلوب.

وما المطلوب الآن؟

لا نعدل picker.html.
الخلل Backend/Identity Mapping.

لكن لا يكفي أن نصلح start-picking ونترك الـ5 سجلات. يجب التعامل مع Identity Mapping Debt بطريقة مستقلة ومثبتة، بعد التأكد من كل سجل ومسار استخدامه، دون إنشاء مستخدمين وهميين أو كسر الـusers_email_key.

البرومبت الإلزامي للمساعد
MANDATORY EXECUTION DIRECTIVE
START-PICKING + IDENTITY MAPPING ZERO-DEBT REPAIR

هذه المهمة إصلاح فعلي وليست مرحلة تقرير.

الخطأ الأصلي:

duplicate key value violates unique constraint "users_email_key"

تم إثباته في Production، والسبب كان:

email + company_id lookup
→ not found
→ automatic INSERT
→ users_email_key violation

والتصميم الصحيح أصبح:

JWT auth.users.id
→ public.users.auth_id
→ public.users.company_id
→ company-scoped runsheet
1. ممنوع تعديل picker.html

picker.html Contract الحالي صحيح:

POST /functions/v1/start-picking
Authorization: Bearer <token>
{ runsheet_code }

لا تغيّر الـPWA إلا إذا أثبتت من الأدلة وجود Contract defect مستقل.

2. الإنتاج الحالي يجب اعتباره الواقع

Production الحالي:

start-picking = v32

ويجب فحص مصدره الفعلي قبل أي تغيير.

لا تعتمد على التقرير السابق.

3. أصلح مشكلة Identity Mapping كاملة

Production الآن يحتوي:

public.users = 31
public.users without auth_id = 5
auth.users = 28
auth.users without public.users = 2

هذه ليست معلومة نظرية؛ تم التحقق منها مباشرة.

المطلوب

لكل واحد من الـ5:

حدد من هو.
هل له Auth Account؟
هل يمكن ربطه بأمان؟
ما الـcompany_id الصحيح؟
هل له Consumers أو بيانات مرتبطة؟
هل هو Legacy/System User؟
هل يجب ربطه أم إبقاؤه غير مرتبط؟
أثبت القرار بالمصادر.

ولكل واحد من الـ2 في auth.users بلا public.users:

حدد هل هو مستخدم ERP فعلي.
حدد الـcompany.
لا تنشئ سجلًا تلقائيًا بلا contract.
لا تحذف الحساب.
4. لا تعالج المشكلة بالترقيع

ممنوع:

ignore missing auth_id

أو:

fallback by email

أو:

automatic INSERT

أو:

app_settings.limit(1)

أو أي fallback يخفي المشكلة.

5. Start-Picking Contract

يجب أن تكون:

Authenticated user
→ public.users.auth_id
→ public.users.company_id
→ company-scoped runsheet

والوظيفة:

تبدأ Open/Confirmed → Picking
تضبط picker_id
تضبط picker_start
لا تعدّل stock_branches.qty
لا تستدعي post_stock_movement
لا تستدعي reserve_stock ما لم يكن ذلك جزءًا من العقد الفعلي المعتمد لـStart Picking
لا تنشئ public.users
6. التحقيق قبل التعديل

افتح فعليًا:

Historical
Original
Current
Production
PWA consumer
PostgreSQL schema
Constraints
Triggers

وطابق:

Historical
vs
Original
vs
Current
vs
Production
vs
Target

وسجل Loss/Gain/Retained/Moved/Hardened/Missing.

7. الاختبار

نفذ فعليًا:

Existing valid user
Open → Picking
Confirmed → Picking
Wrong company
Missing auth linkage
Inactive user
Invalid status
Duplicate request
Concurrent request

ثم نفذ المسار الحقيقي عبر:

picker.html
→ HTTP
→ Auth
→ Edge
→ DB

إن تعذر Browser automation، استخدم نفس HTTP contract من Runner/Environment متاح، ولا تجعل غياب أداة بعينها سببًا للتوقف.

8. بعد الإصلاح

يجب التحقق من:

Production v(final)
Current SHA
Production source
Users identity mapping
Runsheet state
picker_id
company_id
No unintended stock mutation
No duplicate user creation
No users_email_key errors
9. Self-Audit إلزامي

في المقدمة:

Business Understanding
Architecture Understanding
Database Understanding
Historical Understanding
Production Understanding
Current Understanding
Execution Confidence

Confirmed Facts
Unknowns
Conflicts
Unverified Claims

وفي النهاية:

What I Proved
What I Did Not Prove
What I Fixed
What I Initially Missed
What Could Still Be Wrong
Final Confidence
Final Closure Status

أي Unknown أو Conflict أساسي يمنع 100%.

10. ممنوع السلوك السابق

ممنوع:

BLOCKED كحالة توقف.
تقرير بدل الإصلاح.
نقل المشكلة للدالة التالية.
100% بدون إثبات.
fallback مؤقت.
إنشاء بيانات اصطناعية لإخفاء المشكلة.
ترك نقص الهوية معروفًا دون قرار موثق.

إذا واجهت عائقًا:

حدد العائق
→ استنفد البدائل
→ أصلح ما تستطيع
→ حدد الإجراء اليدوي المطلوب فقط
→ واصل التنفيذ
11. الإغلاق

لا تعتبر:

start-picking = 100% CLOSED

إلا بعد:

Code
+
Core
+
Identity
+
Consumers
+
Staging
+
Production
+
HTTP E2E
+
Baseline
+
Governance

ثم أغلق هذه الوحدة بالكامل قبل الانتقال لأي Closure Unit أخرى.

الهدف

إزالة الخطأ الحقيقي.

إغلاق Identity Mapping Debt ذات الصلة.

تثبيت:

PRODUCTION = CURRENT = VERIFIED TARGET

ثم الانتقال إلى الوحدة التالية دون حمل دين.
