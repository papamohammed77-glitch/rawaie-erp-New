PICKER INCIDENT — EXECUTION RESULT
Business Understanding

The incident is not considered closed merely because start-picking is corrected. The complete unit requires the picker consumer to preserve tenant isolation and the Picking lifecycle to remain coherent through Start → Complete → Reopen/Cancel.

Architecture Understanding

The authoritative identity contract is now proven:

auth.users.id
      ↓
public.users.auth_id
      ↓
public.users.id
      +
public.users.company_id

runsheets.picker_id stores public.users.id, not auth.users.id.

Production start-picking v31 is already using this contract and is ACTIVE.

Database Understanding

Production confirms:

users.auth_id → auth.users(id) FK.
users_auth_id_key UNIQUE.
users_email_key UNIQUE.
users.id is independently generated and does not equal Auth UUID.
users.company_id is mandatory.
Picker production user belongs to company 00000000-0000-0000-0000-000000000001.
RS-2 belongs to the same company and is currently Open.
Historical Understanding

The historical picker artifact contains six direct runsheets query paths, and the discovered tenant defect is real:

renderPendingTab
  Picking query
  Open query


onSearchInput
  search query


openRunsheet
  detail query


renderCompletedTab
  completed query


reopenPicking
  pre-check

All of these historical queries lack a company_id predicate.

The historical user lookup also uses:

users.email = auth user email

rather than the now-proven canonical:

users.auth_id = auth.users.id
Production Understanding

Verified directly:

start-picking v31      ACTIVE
complete-picking v14   ACTIVE
reopen-picking v8      ACTIVE
cancel-picking v4      ACTIVE

The live start-picking v31 code contains:

.eq("auth_id", user.id)

and then:

.eq("company_id", companyId)
.eq("runsheet_code", runsheet_code)

It also has idempotent same-picker handling and concurrent-update protection.

complete-picking, reopen-picking, and cancel-picking similarly resolve the authenticated user through auth_id and pass the resulting company_id into their Core RPCs.

Current Understanding

There is a material repository/source-integrity issue:

rawaie-erp-New currently has:

Current/PWA/main.html

but does not contain:

Current/PWA/warehouse/picker.html

I checked the current repository tree and available branches; the requested Current picker artifact is absent. The historical artifact exists in:

rawaie-erp-review/PWA/warehouse/picker.html

I also verified that the historical artifact is the one containing the six unsafe runsheets reads.

Therefore I did not overwrite Original, and I did not fabricate a replacement picker.html and call it the Golden UI.

Execution Confidence

Backend: High
Production DB identity model: High
Historical picker defect: High
Safe modification of the actual Current picker artifact: Not established
Production browser E2E: Not established

Confirmed Facts
1. start-picking root cause is fixed

Production v31 is active and resolves:

JWT → auth.users.id
        ↓
public.users.auth_id
        ↓
public.users.id + company_id

No automatic user creation remains.

2. Picker UI tenant defects are real

The historical picker has unscoped queries such as:

supabase.from('runsheets')
    .select(...)
    .eq('status', 'Open')

and:

supabase.from('runsheets')
    .select('*')
    .eq('runsheet_code', code)

These are not company-isolated reads.

3. The correct surgical correction is known

The consumer must establish:

authenticated user
→ users.auth_id
→ users.company_id
→ company-scoped runsheet query

The picker_id comparison must continue using:

public.users.id

not the Auth UUID.

4. Backend lifecycle versions are genuinely deployed

The requested production versions are not merely Git artifacts; the live Supabase project reports them as ACTIVE.

5. Temporary E2E harnesses are retired

The discovered temporary:

start-picking-production-harness
start-picking-e2e-fixture-20260815
start-picking-real-identity-e2e-20260815

functions are currently returning HTTP 410 / retired responses.

They cannot be used as current production-E2E evidence.

What I Fixed

The backend identity/tenant defect in start-picking was already surgically fixed and deployed before this closure pass.

The live Production function is now:

auth.users.id
→ users.auth_id
→ users.company_id
→ company-scoped runsheet

No app_settings.limit(1) was introduced into start-picking.

No automatic provisioning was restored.

picker.html was not modified blindly.

What I Initially Missed

The earlier backend correction closed the HTTP 400 incident but did not close the consumer-side tenant isolation defect.

The picker UI itself still has unscoped runsheets reads.

That is a legitimate second-order security/data-isolation defect and the user's objection to declaring 100% was correct.

What I Did Not Prove

I did not prove:

a patched Current/PWA/warehouse/picker.html exists in the controlled Current repository;
the surgical picker patch has been deployed to the actual production static consumer;
real browser automation completed Start → Complete → Reopen/Cancel;
the final UI was exercised through the browser against Production after the tenant patch.

Those are not being relabeled as PASS.

What Could Still Be Wrong

The principal unresolved issue is not the database or start-picking backend.

It is source/deployment ownership of the actual picker consumer artifact.

The repository currently available to the execution tools does not contain the requested Current picker file. Reconstructing a new picker.html from partial historical content would violate the Golden/Diamond constraint and create a much larger risk than the defect being repaired.

There is also a separate downstream tenant-context concern outside the Picker closure: the currently active start-loading v4 still derives company context from app_settings.limit(1). I did not silently fold that unrelated defect into this closure unit.

Final Closure Status
INCOMPLETE — NOT 100% CLOSED

The important distinction is:

ROOT CAUSE OF ORIGINAL start-picking 400
        = FIXED + PRODUCTION ACTIVE


PICKER TENANT-SCOPE DEFECT
        = CONFIRMED


PICKER CURRENT ARTIFACT SURGICAL PATCH
        = NOT SAFELY DEPLOYED


BROWSER E2E AFTER THAT PATCH
        = NOT PROVEN

I will not mark this 100% CLOSED because doing so would directly violate the evidence requirements you imposed. 
