# تقرير52 — FORENSIC `Current/PWA/main2/main1.md` SOURCE REPAIR

**Date:** 2026-09-04
**Repository:** `papamohammed77-glitch/rawaie-erp-New`
**Branch:** `main`
**Current repository HEAD at investigation start:** `8049d8980801768453531abdd9164fca0f232457`
**Target file:** `Current/PWA/main2/main1.md`
**Target blob:** `14b12a471c20ad23a2c18f456dbc4d59783a0d1f`
**Target size:** `63126 bytes`
**Target insertion commit:** `c55ce8b3e0ecdfffc5739afdac53910009208f43`

---

# 1. PURPOSE

هذه الجولة لم تبدأ من الصفر.

تم استرجاع الحالة من:

```text
MASTER - RAWAEA ERP.md
CURRENT_STATE.md
Report49
Report50
Report51
MASTER_CTO_NEW_MAIN_CONTINUITY_EXECUTION.md
MASTER_RAWAEA_SYSTEM_PWA_KNOWLEDGE_PACK_2026-09-04.md
MASTER_CTO_FORENSIC_BRAND_PATCH_SUCCESSOR_V12.md
Git chronology
Current/PWA/main2 tree
Current/PWA/main2/main1.md
Current/PWA/main2/main2.md
Current/PWA/main2/main11.md
Production Supabase schema/RLS
```

لم يتم استخدام تقرير سابق كإثبات للحالة الحالية دون إعادة المطابقة.

---

# 2. LAST VERIFIED CONTINUITY RECOVERY

تم إثبات أن الوثيقة الحاكمة `MASTER - RAWAEA ERP.md` هي Engineering Governance Command وليست مرحلة قديمة. القواعد الفاعلة منها:

```text
CURRENT REALITY > CURRENT GIT > CURRENT PRODUCTION > DEPLOYMENTS > DB CONTRACTS > HISTORY > REPORTS > MEMORY

UNKNOWN != BUG
UNKNOWN != REMOVE

DO NOT PATCH BEFORE RECONCILIATION

ONE CLOSURE UNIT AT A TIME

PATCH ONLY THE REAL WINDOW

NO WHOLE-FILE REWRITE FOR A NARROW DEFECT

GIT != DEPLOYMENT PROOF

SOURCE != RUNTIME PROOF

CURRENT_STATE MUST FOLLOW REAL EVENTS
```

كما ثبت من Master continuity أن المشروع يجب أن يستمر من `LAST VERIFIED STATE` وليس من آخر تقرير أو آخر مرحلة تاريخية.

---

# 3. LATEST GIT REALITY

البحث المباشر في commits أثبت أن أحدث سلسلة `main` بتاريخ 2026-09-04 هي:

```text
8049d8980801768453531abdd9164fca0f232457
Delete Current/PWA/main2/Test

c55ce8b3e0ecdfffc5739afdac53910009208f43
Add files via upload

79c4d901f2f305be78340b8d235bdcc4d5215118
Delete Current/PWA/main2/rawaie-erp-New-Original-PWA.zip

73a92e40fa9ed1c2c2f3b19f6150fa82c6e13c9a
Add files via upload

c65dc5a11c2abe5defe0a449ab2c3ede55260064
Add new Test file in main2 directory

da77d91568cea39e91ba7864765d786502833055
[CTO] Update CURRENT_STATE with Report51 and verified PWA knowledge baseline
```

Commit `8049...` حذف ملف اختبار فقط، ولم يغير `main1.md`.

Commit `c55ce8...` أضاف `Current/PWA/main2/main1.md` كملف HTML/JS كامل من 991 سطرًا.

إذن:

```text
main1.md has NOT been modified after its insertion commit.
```

وهذا مهم: أي patch جديد يجب أن يبدأ من blob `14b12a...` وليس من تخمين حول ما يوجد في Git.

---

# 4. WHAT `main2/main1.md` ACTUALLY IS

الفحص المباشر للشجرة يثبت أن `Current/PWA/main2` يحتوي:

```text
main1.md
main2.md
main3.md
...
main11.md
```

وحجم/طبيعة الملفات يثبت أنها أجزاء كبيرة من Parent PWA implementation وليست مجرد أوصاف.

`main1.md` نفسه يحتوي:

```text
HTML shell
CSS
Supabase client bootstrap
RW_Table
RW_Audit_log
RW_Permissions_check
RW_Permissions_applyUI
RW_Workflow
RW_Notification
RW_Audit UI
RW_STATE
RW_Auth
RW_Data
RW_Navigation
```

وينتهي عند تعريف `RW_Navigation` دون إغلاق `<script>`، وهو متسق مع كونه fragment أول في تركيب `main2`.

`main2.md` يبدأ مباشرة بـ`RW_Dashboard`، confirming the fragment boundary rather than a standalone application boundary.

---

# 5. PRODUCTION DATABASE REALITY RELEVANT TO MAIN1

Production RLS was checked directly for the tables consumed by Main1.

Verified current policies include:

```text
app_settings:
  SELECT is company-scoped through current authenticated user's company.

items:
  SELECT is company-scoped through current authenticated user's company.

customers:
  SELECT is company-scoped through current authenticated user's company.

branches:
  SELECT is company-scoped through current authenticated user's company.

users:
  authenticated user can select own row through auth_id = auth.uid().

audit_log:
  SELECT is owner permission gated.

notifications:
  SELECT/UPDATE/DELETE are own-user scoped.

notification_templates:
  current contract allows authenticated SELECT globally.

workflow_rules:
  current contract allows authenticated SELECT globally.
```

Production also exposes the helper:

```text
app_private.current_user_company_id()
```

which derives the current company from `users.auth_id = auth.uid()`.

Therefore Main1 can and should establish a real company context from the authenticated user before operational reads.

---

# 6. FORENSIC FINDINGS — REAL DEFECTS IN MAIN1

## DEFECT A — SECURITY-SENSITIVE DEFAULT WILDCARD

Current code:

```javascript
RW_STATE.permissions = meta.permissions || ['*'];
```

Impact:

إذا وصل مستخدم Authenticated بدون `permissions` metadata، فإن الواجهة تتحول إلى wildcard permission set.

هذا يتعارض مع العقد المثبت للمالك:

```text
OWNER wildcard is an explicit owner contract:
permissions = ["*"]
+
isOwner=true
+
owner_profile
+
active license
```

وليس default fallback لجميع المستخدمين.

Classification:

```text
DEFECT = CONFIRMED
ROOT CAUSE = unsafe frontend authorization default
PATCH = AUTHORIZED
```

---

## DEFECT B — COMPANY CONTEXT IS NOT DERIVED FROM THE AUTHORITATIVE USER ROW

Current login flow uses only Auth metadata:

```javascript
var meta = user.user_metadata || {};
...
RW_STATE.app.currentUser = {...}
RW_STATE.permissions = ...
RW_STATE.app.company = {...}
```

ولا يستخرج `company_id` من `public.users` رغم أن Production contract يثبت أن:

```text
users.auth_id -> users.company_id
```

Classification:

```text
DEFECT = CONFIRMED
ROOT CAUSE = missing authoritative tenant bootstrap in this fragment
PATCH = AUTHORIZED
```

---

## DEFECT C — GLOBAL `app_settings` LOOKUP

Current code in `enterSystem()`:

```javascript
RW_SUPABASE_CLIENT
    .from('app_settings')
    .select('*')
    .limit(1)
```

ثم:

```javascript
var row = r.data[0];
```

This is a global/ambiguous lookup.

The table has an explicit `company_id` column and Production policy is company-scoped.

The backend pattern already uses company-scoped settings resolution.

Classification:

```text
DEFECT = CONFIRMED
ROOT CAUSE = unscoped settings selection
PATCH = AUTHORIZED
```

---

## DEFECT D — BOOTSTRAP READS RELY ON RLS ALONE AND DO NOT CARRY EXPLICIT CURRENT COMPANY CONTEXT

Current:

```javascript
supabase.from('items').select('*')
supabase.from('customers').select('*')
supabase.from('branches').select('*')
supabase.from('suppliers').select('*')
```

RLS currently protects these operational reads, so this is not equivalent to a proven cross-tenant leak.

However, the fragment has no explicit application-level tenant context and is therefore harder to reason about and less deterministic than the established architecture.

Classification:

```text
HARDENING = AUTHORIZED
SECURITY INCIDENT = NOT PROVEN
```

---

# 7. FINDINGS DELIBERATELY NOT PATCHED

The following were investigated and intentionally left alone because the evidence does not prove that they are wrong for this closure unit.

### Login visual sizes

`main1` has:

```text
64px title
120x120 logo
34px company name
```

Historical Original shows the same values.

No need for a visual patch here.

### Brand identity

Current source includes:

```text
الروائع ERP
RAWAEA ERP ENTERPRISE
منصة إدارة الأعمال الذكية والمتكاملة
```

Historical/current differences do not prove a regression or owner-approved rebrand.

No brand patch in Main1.

### Owner wildcard semantics

Not removed.

Only the unsafe default fallback is corrected.

### Workflow rules

`workflow_rules` has no `company_id` column in current Production schema and its current policy is globally readable by authenticated users.

No speculative tenant field or architecture was introduced.

### Notification templates

The current schema also lacks `company_id` on `notification_templates` and the current policy explicitly permits authenticated SELECT.

No speculative change was introduced.

### Audit log

The current owner-only RLS contract is preserved.

No attempt was made to invent company scoping for the owner audit surface.

---

# 8. SURGICAL PATCH WINDOW FOR MAIN1

The exact intended changes are only these.

## PATCH 1 — Add authoritative company id to RW_STATE

### OLD

```javascript
const RW_STATE = { app: { initialized: false, authenticated: false, loading: false, currentView: 'dashboard', currentUser: null, company: { name: 'الروائع ERP', logo: 'ر' } }, data: { items: [], customers: [], suppliers: [], branches: [] }, permissions: [], ui: { sidebarOpen: false, sidebarCollapsed: false } };
```

### NEW

```javascript
const RW_STATE = { app: { initialized: false, authenticated: false, loading: false, currentView: 'dashboard', currentUser: null, company: { id: null, name: 'الروائع ERP', logo: 'ر' } }, data: { items: [], customers: [], suppliers: [], branches: [] }, permissions: [], ui: { sidebarOpen: false, sidebarCollapsed: false } };
```

---

## PATCH 2 — Resolve authoritative company context during login

### REPLACE THIS BLOCK

```javascript
var user = authRes.data.user;
var meta = user.user_metadata || {};

RW_STATE.app.authenticated = true;
RW_STATE.app.currentUser = {
    name: meta.name || user.email,
    email: user.email,
    role: meta.role || 'مدير النظام',
    isOwner: meta.isOwner === true || meta.isOwner === 'true'
};
RW_STATE.permissions = meta.permissions || ['*'];
RW_STATE.app.company = {
    name: meta.companyName || 'الروائع ERP',
    logo: meta.companyLogo || 'ر'
};

RW_Audit_log('login', 'auth', user.id, null, { email: user.email, role: meta.role || 'مدير النظام' });

self.enterSystem();
```

### WITH

```javascript
var user = authRes.data.user;
var meta = user.user_metadata || {};

return RW_SUPABASE_CLIENT
    .from('users')
    .select('company_id, status')
    .eq('auth_id', user.id)
    .maybeSingle()
    .then(function(profileRes) {
        if (profileRes.error) throw profileRes.error;
        if (!profileRes.data || !profileRes.data.company_id) {
            throw new Error('بيانات سياق الشركة للمستخدم غير مكتملة');
        }
        if (profileRes.data.status === 'Inactive') {
            throw new Error('حساب المستخدم غير نشط');
        }

        RW_STATE.app.authenticated = true;
        RW_STATE.app.currentUser = {
            name: meta.name || user.email,
            email: user.email,
            role: meta.role || 'مدير النظام',
            isOwner: meta.isOwner === true || meta.isOwner === 'true'
        };

        RW_STATE.permissions = Array.isArray(meta.permissions) ? meta.permissions : [];
        RW_STATE.app.company = {
            id: profileRes.data.company_id,
            name: meta.companyName || 'الروائع ERP',
            logo: meta.companyLogo || 'ر'
        };

        RW_Audit_log('login', 'auth', user.id, null, {
            email: user.email,
            role: meta.role || 'مدير النظام',
            company_id: profileRes.data.company_id
        });

        return self.enterSystem();
    });
```

### Why this exact shape

```text
Company identity = DB authoritative
Owner wildcard = explicit only
Missing permissions = []
No role/permission invention
No owner semantics rewrite
```

---

## PATCH 3 — Company-scope app_settings lookup

### OLD

```javascript
RW_SUPABASE_CLIENT
    .from('app_settings')
    .select('*')
    .limit(1)
    .then(function(r){

        console.log('APP_SETTINGS_RESULT', r);

        if (!r || !r.data || !r.data.length) return;

        var row = r.data[0];
```

### NEW

```javascript
RW_SUPABASE_CLIENT
    .from('app_settings')
    .select('*')
    .eq('company_id', RW_STATE.app.company.id)
    .order('created_at', { ascending: true })
    .limit(1)
    .then(function(r){

        console.log('APP_SETTINGS_RESULT', r);

        if (!r || !r.data || !r.data.length) return;

        var row = r.data[0];
```

The `LIMIT 1` remains only after explicit company scoping and deterministic ordering; it is not a global tenant lookup anymore.

---

## PATCH 4 — Explicit company scope for bootstrap reads

### OLD

```javascript
return supabase.from('items').select('*').then(function(res) {
```

```javascript
return supabase.from('customers').select('*').then(function(res) {
```

```javascript
return supabase.from('branches').select('*').then(function(res) {
```

and:

```javascript
supabase.from('suppliers').select('*').then(function(r) {
```

### NEW

```javascript
return supabase.from('items').select('*').eq('company_id', RW_STATE.app.company.id).then(function(res) {
```

```javascript
return supabase.from('customers').select('*').eq('company_id', RW_STATE.app.company.id).then(function(res) {
```

```javascript
return supabase.from('branches').select('*').eq('company_id', RW_STATE.app.company.id).then(function(res) {
```

and:

```javascript
supabase.from('suppliers').select('*').eq('company_id', RW_STATE.app.company.id).then(function(r) {
```

The Supplier change is included because Supplier is part of the same bootstrap context and its table belongs to the company domain.

---

# 9. IMPORTANT NON-PATCH DECISION — `RW_Auth` OWNER SEMANTICS

We did **not** change:

```javascript
isOwner: meta.isOwner === true || meta.isOwner === 'true'
```

because the current owner contract explicitly uses Auth `isOwner=true` together with DB wildcard semantics and owner_profile/license state.

We did **not** convert wildcard to a list of permissions.

We only changed the unsafe missing-metadata fallback from:

```text
['*']
```

to:

```text
[]
```

---

# 10. TESTING PLAN

Because this file is currently a working source fragment, not the canonical deployed `New-main` artifact, the correct validation model is:

```text
STATIC PATCH REVIEW
↓
SYNTAX CHECK OF MODIFIED BLOCK
↓
CONCATENATION/INTEGRATION CHECK AGAINST MAIN2..
↓
PARENT ARTIFACT BUILD/ASSEMBLY
↓
DEPLOY ONLY THROUGH THE REAL TARGET PIPELINE
↓
FRESH BROWSER RUNTIME VERIFY
```

A direct Production browser closure cannot honestly be claimed for `main2/main1.md` itself because Git/Current-PWA/main2 is not the verified deployed target `Current/PWA/New-main`.

---

# 11. EXPERIMENTS / FAILURES IN THIS ROUND

## Experiment 1 — Treat reports as current truth

Rejected.

Latest Git is newer than the state recorded in earlier documentation.

Lesson:

```text
CURRENT GIT must always be rediscovered.
```

## Experiment 2 — Treat Original as desired target

Rejected.

The governing documents explicitly forbid turning historical differences into automatic patches.

Lesson:

```text
ORIGINAL = HISTORICAL CONTRACT EVIDENCE
not current desired artifact
```

## Experiment 3 — Treat app_settings RLS as sufficient reason to keep global frontend lookup

Rejected.

RLS is a protection boundary, but the frontend still lacked explicit authoritative company context.

Lesson:

```text
Security should be enforced by backend/RLS,
but client execution should still carry explicit tenant context.
```

## Experiment 4 — Whole-file rewrite

Rejected.

The file is 63,126 bytes and is the first fragment of an 11-part composite. The governing rule explicitly forbids whole-file replacement for a narrow defect.

The available repository write operation is whole-file replacement, so using it for this surgical patch would create unjustified risk.

Lesson:

```text
No unsafe wholesale replacement.
Return exact replacement windows instead.
```

---

# 12. WHAT WAS NOT PROVEN

```text
main2/main1.md is not independently proven to be the deployed browser artifact.
Current/PWA/New-main remains the verified canonical product target.
Fresh browser E2E for the Main2 composite is not available in this round.
Production deployment of this specific Main2 fragment was NOT performed.
```

This is intentional; it avoids false `Production PASS` claims.

---

# 13. CURRENT STATUS OF THIS CLOSURE UNIT

```text
TARGET IDENTIFIED                 = CONFIRMED
SOURCE FULLY INSPECTED            = CONFIRMED
PRODUCTION CONTRACT CHECKED       = CONFIRMED
ROOT CAUSE PROVEN                 = CONFIRMED
PATCH WINDOW DEFINED              = CONFIRMED
SURGICAL REPLACEMENTS WRITTEN     = CONFIRMED
SOURCE FILE MUTATED               = NO
PRODUCTION DEPLOYMENT             = NO
BROWSER RUNTIME VERIFIED          = NO
```

Therefore the exact honest closure status is:

```text
SOURCE FORENSIC ANALYSIS = CLOSED
SURGICAL PATCH DESIGN    = CLOSED
SOURCE APPLICATION      = PENDING MANUAL SURGICAL REPLACEMENT
PRODUCTION/RUNTIME       = OPEN
```

This is **not** a claim that the Main1 file itself is fully repaired.

---

# 14. NEXT AUTHORIZED ACTION

بعد تطبيق PATCH 1–4 يدويًا على `Current/PWA/main2/main1.md` مع الحفاظ على كل ما عداه:

```text
1. Re-read the patched Main1 from line 1 to EOF.
2. Verify no duplicate RW_STATE/RW_Auth bootstrap was introduced.
3. Concatenate/check Main1 + Main2 + later fragments in the official assembly method.
4. Run static syntax validation on the resulting parent artifact.
5. Only then move to Main2 closure.
```

Do not reopen historical Login/Brand/License patches unless fresh evidence proves regression.

---

# 15. FINAL SELF-AUDIT

## What I Proved

```text
- Current repository HEAD after the latest commits.
- Main1 exact current blob and insertion chronology.
- Main2 fragment boundary.
- Production tenant/RLS contract relevant to Main1.
- Unsafe wildcard default.
- Missing authoritative company bootstrap.
- Global app_settings lookup.
- Explicit bootstrap scoping opportunity.
```

## What I Did Not Prove

```text
- That Main2/Main1 is currently deployed to browser Production.
- That a fresh runtime browser session consumes this exact fragment.
- That brand/tagline differences are regressions.
- That the eleven-fragment assembly is already the canonical deployed New-main.
```

## What I Fixed

```text
No source bytes were changed in this closure unit.
Exact surgical replacement windows were prepared.
```

## What I Initially Missed / Corrected

```text
- Earlier work had focused on New-main as canonical product target.
- The user task now correctly targets the separate Main2 11-part source pack.
- Treating Main2/Main1 as identical to New-main would have been a target-selection error.
```

## What Could Still Be Wrong

```text
- Additional defects may exist in Main1 outside the current tenant/auth closure.
- Those must be investigated only after applying and re-reading the surgical patch.
- No percentage is assigned.
```

## Final Confidence

```text
FORENSIC IDENTIFICATION = HIGH
ROOT-CAUSE CONFIDENCE = HIGH for Defects A–C
RUNTIME CONFIDENCE = NONE / NOT CLAIMED
PRODUCTION CLOSURE = NOT CLAIMED
```

---

# 16. GOVERNANCE MEMORY

```text
DO NOT REPEAT:

- stale CURRENT_STATE as current HEAD
- historical commit as deployment proof
- Original-vs-Current as automatic regression
- missing Auth metadata as wildcard permission
- global app_settings lookup for tenant identity
- whole-file rewrite for a 4-window surgical patch
- Runtime/Production closure claims without fresh runtime proof
```
