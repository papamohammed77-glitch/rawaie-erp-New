# تقرير15 — P154 / Forensic Comparison: Original main1 vs Current main.html

**التاريخ:** 2026-09-02
**النوع:** Forensic continuity + Original/Current application comparison + surgical owner instructions
**الحالة:** COMPARISON COMPLETE / CURRENT MAIN.HTML NOT MODIFIED / SURGICAL PATCH PLAN ISSUED / INVENTORY CLOSURE STILL OPEN

---

## 1. نطاق الجولة والقاعدة الحاكمة

بدأت الجولة من `MASTER - RAWAEA ERP.md` ثم حالة المشروع والتقارير التنفيذية الأخيرة، ثم جرت قراءة مباشرة لملفي المقارنة:

```text
Original/PWA/main/main1.md
Current/PWA/main.html
```

كما تمت مطابقة النقاط الحرجة مع:

- `CURRENT_STATE.md`
- `doc/Draft/Reprots/تقرير14.md`
- Current Edge Function sources ذات الصلة
- Production PostgreSQL contracts / functions / constraints / grants
- Production runtime logs المتاحة

المعيار ليس: "أي ملف أجمل؟" فقط، بل:

```text
FUNCTIONALITY
+
PRIVILEGES / AUTHORIZATION
+
VISUAL / UX CONTRACT
+
BUSINESS CONTRACT
+
CURRENT ARCHITECTURE
+
CENTRAL CORE DIRECTION
+
PRODUCTION SAFETY
```

قاعدة الحوكمة بقيت:

```text
UNDERSTAND
→ HISTORICAL CONTRACT
→ CURRENT BEHAVIOR
→ DATA/AUTH FLOW
→ TARGET ARCHITECTURE
→ ACTUAL GAP
→ SURGICAL CHANGE
→ VERIFY
```

ولم يتم تعديل `Current/PWA/main.html` في هذه الجولة.

---

## 2. استرجاع الحالة قبل المقارنة

آخر حالة تنفيذية موثقة قبل هذا التقرير هي P153 / تقرير14. التقرير14 أثبت أن `CURRENT_STATE.md` كان قد تأخر عن تقرير13 ثم تمت مصالحة الحالة، وأثبت كذلك أن:

- `Current/PWA/New-main` هو الـauthorized product target في خط النشر الحالي.
- `Current/PWA/main.html` ملف منفصل ومحمي ولا يجوز استبداله أو دمجه آليًا معه بسبب تشابه الاسم.
- `main.html` الحالي يحتوي بالفعل على `Idempotency-Key` و`operation_id` في تدفق `receive-purchase`.
- لا يوجد أساس لتغيير هذا الجزء يدويًا.
- Inventory Core Integrity لم يصل إلى إغلاق 100%.

هذه الحقائق اعتمدت كـcontinuity baseline، ثم أعيد فحص ما يخص المقارنة الحالية مباشرة.

---

## 3. الملفات والـSHA

### Original

```text
Original/PWA/main/main1.md
SHA = 14b12a471c20ad23a2c18f456dbc4d59783a0d1f
```

### Current

```text
Current/PWA/main.html
SHA = 27b777528665dcc985809648f006452c861ae36e
```

الملفان ليسا نفس الـartifact ولا نفس المرحلة المعمارية.

---

## 4. الخلاصة التنفيذية

### الحكم الرئيسي

`Current/PWA/main.html` **أكثر اكتمالًا وظيفيًا وبنيانيًا بكثير** من `Original/PWA/main/main1.md`.

لكن `Original/PWA/main1.md` يحتفظ بعدة عناصر من **الرؤية البصرية والعقد التاريخي للواجهة** أكثر ثراءً من النسخة الحالية، خصوصًا:

- Login hero الأكبر والأكثر تعبيرًا.
- عناصر إظهار كلمة المرور.
- Login footer.
- كثافة بصرية ومساحات أوسع في الـsidebar والـheader.
- بعض العقود الأصلية الواضحة الخاصة بـworkflow/notification/UI-permission orchestration.

وفي المقابل فإن `Current/main.html` يحتوي على وحدات حقيقية متقدمة لم تكن موجودة كتنفيذ كامل في `main1.md`، مثل:

```text
Dashboard
Items + Stock Matrix
Customers
Suppliers
Branches
Users / Permissions / Assignments
POS
TeleSales
Orders
Runsheets
Online Store
Purchasing / Receiving
Warehouse Operations
Vouchers
Picking
Loading
Delivery
Returns
Unloading
Settlement
Finance / Accounting UI
Reports / Analytics
HR
CRM
Owner / License
```

وبالتالي:

```text
لا يجوز استبدال Current بـOriginal.

ولا يجوز نسخ Original على Current ككتلة واحدة.
```

الاستراتيجية الصحيحة هي:

```text
CURRENT = BASE
ORIGINAL = HISTORICAL UX / CONTRACT REFERENCE
        ↓
SURGICAL PARITY + SAFE ENHANCEMENT
```

وهذا يتفق مع مبدأ القلب المركزي؛ لأن Current يحتوي بالفعل على مسارات delegated إلى Edge/RPC ويحتوي على وحدات warehouse/orchestration أوسع بكثير من Original.

---

# 5. المقارنة الوظيفية التفصيلية

| المجال | Original main1 | Current main.html | الحكم |
|---|---|---|---|
| Login shell | موجود وذو عرض بصري غني | موجود ومحسن بصريًا بصورة أكثر compact | Current أكمل وظيفيًا، Original أغنى بصريًا |
| Supabase Auth | `signInWithPassword` | `signInWithPassword` مع bootstrap/context أحدث | Current أفضل |
| Owner semantics | `isOwner` + wildcard fallback في metadata | يحتفظ بـ`isOwner` ولا يستبدل semantics بترجمة صريحة عمياء | Current أكثر أمانًا |
| Permission checker | موجود | موجود ويستخدم routing/module guards | Current يتفوق وظيفيًا |
| UI permission applier | موجود كـutility مستقل في Original | لا توجد مساواة 1:1 مثبتة في القراءة الحالية | Parity تحتاج قرارًا؛ لا تنقل utility بلا حاجة |
| Workflow engine | عقد صريح في Original | ليس من الحكمة اعتبار غيابه مؤكدًا من دون trace مستقل لكل Current source | UNVERIFIED — لا تخمين |
| Notifications | عقد كامل في Original | Current لديه notification UI surface، لكن parity الكامل لا يثبت بمجرد وجود الزر | يحتاج parity verification مستقل |
| Audit | واجهة + pagination + detail | Current لديه route مستقل لـaudit-log وguard owner | Current route أفضل/أوضح |
| Dashboard | shell محدود | Dashboard حقيقي مع KPIs/charts/date filtering | Current يتفوق بقوة |
| Items | shell/route | list + movement + matrix + stock status + filters | Current يتفوق |
| Customers | بيانات/UI أساسية | CRUD + search + sort + pagination | Current يتفوق |
| Suppliers | route/menu | CRUD + search + sort + balances | Current يتفوق |
| Branches | route/menu | CRUD + company-scoped UI | Current يتفوق |
| Users | route/menu | users + roles + permissions + branch/customer assignments | Current يتفوق |
| POS | موجود كمسار تاريخي | تنفيذ فعلي وربط Edge | Current يتفوق |
| TeleSales | route | module فعلي | Current يتفوق |
| Orders | route | module فعلي + filters | Current يتفوق |
| Runsheets | route | module فعلي | Current يتفوق |
| Purchasing | route | purchase orders + purchase POS + receive flow | Current يتفوق |
| Warehouse ops | menu intent | receiving/picking/loading/delivery/return/unloading/settlement + vouchers | Current يتفوق بفارق كبير |
| Finance | menu intent | Finance module / journal / treasury / receipts / payments / transfers / reports | Current يتفوق |
| HR | menu intent | employee UI/records/salary data/document placeholders | Current أكمل، مع أجزاء ما تزال قيد التطوير |
| CRM | menu intent | customer followups / contacts surface | Current يتفوق |
| Reports | menu intent | Dashboard + detailed + comprehensive reports | Current يتفوق |
| Owner/license | menu/route history | dedicated owner/license module | Current يتفوق |

### النتيجة

الـOriginal ليس "نسخة أفضل من Current"؛ بل هو **أحد مصادر الـhistorical shell والـcontract surface** الذي يمكن أخذ ما فقد منه أو ما ثبت أنه ما يزال مرغوبًا.

---

# 6. المقارنة المعمارية

## 6.1 Original

`main1.md` يجمع بين:

- UI shell
- Auth bootstrap
- Data loaders
- Permissions
- Workflow
- Notification
- Audit
- Navigation

لكن أسلوبه أقرب إلى monolithic historical shell، وبعض عمليات القراءة فيه global/unscoped أو ذات fallback قديم، مثل `app_settings.limit(1)` وloaders عامة.

هذا مهم جدًا لأننا **لا نستطيع اعتبار كل سطر في Original contract صحيحًا للتطبيق الحديث**.

## 6.2 Current

`Current/main.html` أعيد تشكيله إلى modules واضحة نسبيًا داخل الملف:

```text
MAIN3 → Users / permissions
MAIN4 → POS
MAIN6 → Online Store / Purchasing
MAIN7 → Warehouse
MAIN8 → Finance
MAIN9 → Reports
MAIN10 → Owner / License
```

كما أن Warehouse module يعلن صراحة أن دوره orchestration/UX وأن Physical Stock mutation يجب أن يبقى خارج الواجهة وفي canonical Edge/RPC capability.

وهذا متوافق مباشرة مع الخطة المركزية:

```text
PHYSICAL STOCK MOVEMENT
        ↓
post_stock_movement
        ↓
stock_branches + inventory_log
```

إذن إعادة بناء Current اعتمادًا على Original بالكامل **ستكون خطوة للخلف**.

---

# 7. مقارنة الصلاحيات والامتيازات

## Original

Original يعرّف:

```javascript
RW_Permissions_check
RW_Permissions_applyUI
```

كما يحتوي Auth bootstrap على:

```javascript
RW_STATE.permissions = meta.permissions || ['*'];
```

هذا الجزء لا يجوز نقله إلى Current لمجرد أنه موجود في Original؛ لأن `['*']` كـfallback عام يمكن أن يوسّع privilege semantics بطريقة غير مقصودة.

## Current

Current يحتفظ بالـOwner semantics صراحة، ويستخدم `RW_Views` كطبقة route guard، مثل:

```text
audit-log → owner
```

كما أن `RW_Users` يعرض permissions وallowed branches وcustomer assignments وfield constraints.

### الحكم

```text
AUTH / OWNER SEMANTICS = Current أفضل
LEGACY UI PERMISSION UTILITY = Original أكثر صراحة
```

الحل ليس نقل `['*']` ولا نسخ utility عميانًا؛ بل الحفاظ على Current authorization contract وإكمال parity فقط إذا أثبتنا أن هناك UI behavior مفقودًا فعليًا.

---

# 8. المقارنة البصرية

## Original أقوى في

1. Login Hero أكبر وأكثر cinematic.
2. عنوان login = 64px.
3. Login logo = 120px.
4. بطاقة login أكثر فخامة ومساحة.
5. وجود login footer.
6. وجود password visibility utility بصريًا.
7. sidebar/header أكثر spacious.

هذه العناصر موثقة مباشرة في Original.

## Current أقوى في

1. Compact modern shell.
2. Responsive behavior أبسط وأوضح.
3. Mobile breakpoint منظم.
4. sidebar أصغر وأكثر عملية.
5. كثافة معلومات أفضل للـERP التشغيلي.
6. cards/table styling أكثر توحيدًا بين الوحدات.
7. login copy يعكس النظام الحالي بشكل أدق مثل المخزون والرانشيتات والحسابات والتقارير.

### الحكم البصري النهائي

```text
Luxury / Presentation = Original
Operational ERP Density = Current
Responsive Compactness = Current
Historical Visual Identity = Original
Functional Screen Coverage = Current
```

لذلك التوصية ليست إعادة CSS بالكامل، بل **استعادة عناصر UX المفقودة ذات القيمة فقط**.

---

# 9. الفجوات التي ثبتت أو أصبحت صالحة للتعديل الجراحي

## GAP-01 — إظهار/إخفاء كلمة المرور في Login

### الدليل
Original لديه contract بصري واضح للـpassword controls، بينما Current login الحالي يحتوي على:

```html
<input id="rw-password" class="rw-input" type="password" autocomplete="current-password">
```

ولا يظهر في البنية الحالية button مماثل داخل Login.

### القرار
هذه فجوة UI حقيقية ولا تمس authorization أو backend.

### تعليمات المالك — لا تعديل تلقائي مني

افتح:

```text
Current/PWA/main.html
```

وابحث عن:

```html
<div class="rw-form-group"><label class="rw-form-label" for="rw-password">كلمة المرور</label><input id="rw-password" class="rw-input" type="password" autocomplete="current-password"></div>
```

احذف هذا المقطع فقط، واستبدله بالكامل بـ:

```html
<div class="rw-form-group">
  <label class="rw-form-label" for="rw-password">كلمة المرور</label>
  <div class="rw-password-wrap">
    <input id="rw-password" class="rw-input" type="password" autocomplete="current-password">
    <button
      type="button"
      class="rw-password-toggle"
      aria-label="إظهار كلمة المرور"
      title="إظهار كلمة المرور"
      onclick="(function(b){var i=document.getElementById('rw-password');if(!i)return;var s=i.type==='password';i.type=s?'text':'password';b.textContent=s?'🙈':'👁';b.setAttribute('aria-label',s?'إخفاء كلمة المرور':'إظهار كلمة المرور');b.setAttribute('title',s?'إخفاء كلمة المرور':'إظهار كلمة المرور');i.focus()})(this)"
    >👁</button>
  </div>
</div>
```

ثم ابحث داخل `<style>` عن:

```css
.rw-login-options
```

وأضف **فوقه مباشرة**:

```css
.rw-password-wrap{position:relative;width:100%}
.rw-password-wrap .rw-input{padding-left:52px}
.rw-password-toggle{position:absolute;left:8px;top:50%;transform:translateY(-50%);width:38px;height:38px;border:0;border-radius:10px;background:transparent;color:#64748b;display:grid;place-items:center;cursor:pointer;font-size:17px}
.rw-password-toggle:hover{background:#f1f5f9;color:#2563eb}
```

هذا Patch بصري محلي، لا يغيّر الـAuth contract ولا الـAPI.

---

## GAP-02 — Login Footer

### الدليل
Original يعرّف `.rw-login-footer` ويستخدمه، بينما Current لا يظهر فيه footer مماثل داخل Login form.

### القرار
فجوة UX منخفضة المخاطر.

### تعليمات المالك

في `Current/PWA/main.html` ابحث عن:

```html
<div id="rw-login-error" class="rw-notice rw-notice-bad hidden"></div></form>
```

استبدله بـ:

```html
<div id="rw-login-error" class="rw-notice rw-notice-bad hidden"></div><div class="rw-login-footer">منصة الروائع ERP — إدارة المبيعات والمخزون والتوزيع والحسابات من نظام واحد.</div></form>
```

ثم أضف داخل `<style>` **فوق `.rw-main-shell` مباشرة**:

```css
.rw-login-footer{margin-top:4px;text-align:center;color:#64748b;font-size:11px;font-weight:700;line-height:1.8}
```

### ملاحظة
لا ننقل footer Original حرفيًا إذا كان يحتوي نصًا تاريخيًا لم يعد يعكس النظام؛ نستخدم النص المتوافق مع Current.

---

# 10. أشياء لا يجب تعديلها رغم ظهورها في Original

## DO NOT COPY — Permission wildcard fallback

لا تستبدل Current authorization logic بـ:

```javascript
meta.permissions || ['*']
```

لأن هذا يعيد semantics قديمة أوسع من اللازم.

## DO NOT COPY — global app_settings lookup

لا تنقل من Original:

```javascript
.from('app_settings').select('*').limit(1)
```

إلى Current.

Current يجب أن يبقى company-scoped.

## DO NOT COPY — global data loaders

لا تنقل loaders Original التي لا تقيد company context إلى Current.

## DO NOT COPY — full Original CSS

لا تحذف CSS Current وتستبدله بـCSS Original؛ Current هو الـoperational baseline الحالي.

## DO NOT COPY — old module structure

لا تعِد تجميع Current إلى ملف Original-style واحد بدون سبب معماري.

---

# 11. GAP-03 — Dashboard "صافي الربح"

هذه النقطة **ليست جاهزة للـmanual patch الآن** رغم أنها لافتة بشدة.

Current يحسب:

```text
Net = Orders Total - Purchase Orders Total
```

وهذا واضح من المصدر الحالي.

هذا ليس بالضرورة تعريف الربح المحاسبي الحقيقي للنظام؛ قد يتعارض مع:

```text
COGS
Inventory valuation
Journal entries
Period posting
Returns
Discounts
Tax
```

### القرار الحاكم

```text
FOUND = YES
ROOT CAUSE = NOT YET PROVEN
PATCH = NOT AUTHORIZED
```

قبل إعطاء المالك replacement code يجب تتبع الـFinancial reporting contract الحالي وتحديد المصدر الرسمي للـP&L في Production.

هذه ليست حالة نريد فيها تخمينًا رياضيًا داخل Dashboard.

---

# 12. GAP-04 — UI permission parity

Original يحتوي `RW_Permissions_applyUI` كآلية مستقلة لتطبيق شروط على العناصر.

Current يعتمد بشكل أكبر على:

```text
route/view guards
+
module-level checks
+
Owner checks
```

ولا توجد من القراءة الحالية أدلة كافية تسمح بالقول إن Current "فقد" الوظيفة بالكامل؛ لأنه قد تم نقل المسؤولية إلى طبقة أخرى.

### الحكم

```text
Historical function = PROVEN
Current absence = NOT PROVEN
Responsibility transfer = PARTIALLY PROVEN
```

إذًا لا نضيف utility جديدة الآن.

---

# 13. GAP-05 — Workflow parity

Original يحتوي Workflow evaluator + workflow log orchestration.

Current لديه modules كثيرة ولكن لا يجوز إعلان workflow missing من عدم ظهور الاسم في مقتطف واحد.

### القرار

```text
Workflow parity = NEEDS DIRECT TRACE
PATCH = NOT AUTHORIZED
```

يجب لاحقًا البحث في Current كاملًا عن:

```text
RW_Workflow
workflow_rules
workflow_log
evaluate
trigger
```

ثم مقارنة كل responsibility بمسارها الحالي.

---

# 14. GAP-06 — Notification parity

Current يحتوي notification button في shell، بينما Original يحتوي notification module contract واضحًا.

وجود زر لا يثبت وجود كل الوظائف، وغياب module name من مقتطف لا يثبت الغياب.

### القرار

```text
Notification parity = UNVERIFIED
PATCH = NOT AUTHORIZED
```

يجب tracing الوظائف الفعلية قبل كتابة replacement code.

---

# 15. Business / Architecture compatibility

المقارنة مع خطة القلب المركزي تؤكد:

```text
Current/main.html
        ↓
UI / Orchestration
        ↓
Edge / RPC capabilities
        ↓
Canonical engines
```

وWarehouse module الحالي يعلن أنه لا يملك Physical Stock mutation مستقلًا، بل يعتمد على canonical capabilities.

هذا أفضل من إعادة بناء Current على أسلوب Original الذي يحتوي تاريخيًا على direct/global data access أكبر.

إذن:

```text
CURRENT ARCHITECTURAL DIRECTION = CORRECT BASE
ORIGINAL ARCHITECTURE = HISTORICAL REFERENCE ONLY
```

---

# 16. Inventory continuity impact

هذه المقارنة لا تغلق Global Inventory Sweep.

Production forensic state ما زال يحتوي على anomalies جرى رصدها سابقًا:

```text
143 stock_branches cross-company item metadata relationships
86 inventory_log item/company mismatches
```

ولم يتم حذفها أو إعادة توزيعها دون trace إضافي.

كما أن مبدأ inventory المركزي يبقى:

```text
post_stock_movement
    ↓
stock_branches + inventory_log
```

لذلك فإن Current module richness لا تعني Inventory Core closure.

---

# 17. أخطاء/محاولات سابقة يجب ألا تتكرر

## الخطأ 1 — الادعاء أن Idempotency-Key غير موجود

ثبت لاحقًا أن `main.html` الحالي يرسله بالفعل.

التصحيح: لا manual patch.

## الخطأ 2 — الخلط بين Current/main.html وCurrent/New-main

الـSHA مختلف والعقد مختلف.

التصحيح: عدم النسخ بينهما دون evidence.

## الخطأ 3 — اعتبار DB repair = browser/runtime closure

تقرير13 لم يثبت Browser Retry 200.

التصحيح: runtime يبقى boundary منفصل.

## الخطأ 4 — الاعتماد على report بدل current source

تم تصحيح المسار بالرجوع إلى Git/Production.

## الخطأ 5 — تجربة Receive Purchase السابقة

تمت تجربة عملية receiving داخل transaction اختبارية، وظهر أن ترتيب duplicate/idempotency validation كان يمكن أن يمنع retry الصحيح، وأن operation identity المبني جزئيًا على `qty_received_before` ليس أفضل identity مستقل.

التجربة لم تُترك كبيانات دائمة، ولم يُبن عليها patch تخميني داخل الواجهة.

---

# 18. الاختبارات المطلوبة بعد تنفيذ الـmanual UI patches

بعد تعديل GAP-01 وGAP-02 فقط:

### Login

1. افتح Login.
2. اكتب كلمة المرور.
3. اضغط 👁.
4. تحقق أن النوع يتحول إلى `text`.
5. اضغط مرة ثانية.
6. تحقق أنه يعود إلى `password`.
7. تحقق أن `aria-label/title` يتغيران.
8. نفذ Login عادي.

### Regression

تحقق من:

```text
Owner login
Normal user login
Navigation
POS
Purchases
Receiving
Warehouse
Finance
Reports
CRM
HR
```

### Security

لا يجب أن يؤدي زر show/hide إلى إرسال كلمة المرور أو تغيير payload.

---

# 19. جدول الـSurgical Work Packages

| ID | الملف | التعديل | الحالة |
|---|---|---|---|
| SP-01 | `Current/PWA/main.html` | إضافة password visibility | READY FOR OWNER MANUAL PATCH |
| SP-02 | `Current/PWA/main.html` | إضافة login footer | READY FOR OWNER MANUAL PATCH |
| SP-03 | `Current/PWA/main.html` | إصلاح dashboard net profit | NOT AUTHORIZED — يحتاج Financial trace |
| SP-04 | `Current/PWA/main.html` | UI permission parity | NOT AUTHORIZED — يحتاج Current-wide trace |
| SP-05 | `Current/PWA/main.html` | Workflow parity | NOT AUTHORIZED — يحتاج Current-wide trace |
| SP-06 | `Current/PWA/main.html` | Notification parity | NOT AUTHORIZED — يحتاج Current-wide trace |
| SP-07 | Original → Current full merge | ممنوع | REJECTED |

---

# 20. هل الملف الثاني يحتاج "استكمالًا" من الأول؟

نعم، لكن **ليس بالطريقة التقليدية**.

المقصود بالاستكمال الصحيح هو:

```text
استعادة الوظائف/العقود التي ثبت أنها ذات قيمة
        ↓
حفظ Current architecture
        ↓
حفظ Current tenant/auth behavior
        ↓
حفظ canonical Edge/RPC ownership
        ↓
تحسين UX فقط حيث يوجد فقد حقيقي
```

وليس:

```text
Original
   ↓
Copy all
   ↓
Replace Current
```

---

# 21. القرار النهائي للمقارنة

```text
ORIGINAL MAIN1
= HISTORICAL UX / CONTRACT REFERENCE

CURRENT MAIN.HTML
= CURRENT OPERATIONAL UI BASE

CURRENT FUNCTIONAL COMPLETENESS
= SUPERIOR

ORIGINAL VISUAL RICHNESS
= SUPERIOR IN LOGIN / SPACING

CURRENT ARCHITECTURAL ALIGNMENT
= SUPERIOR

FULL MERGE
= REJECTED

SURGICAL ENHANCEMENT
= APPROVED
```

---

# 22. ما تم فعله فعليًا في هذه الجولة

```text
READ MASTER                         = YES
READ CONTINUITY STATE               = YES
READ REPORT14                       = YES
READ ORIGINAL                       = YES
READ CURRENT                        = YES
CHECK PRODUCTION                    = YES
CHECK CURRENT EDGE CONTEXT          = YES
COMPARE VISUAL                      = YES
COMPARE FUNCTIONAL                  = YES
COMPARE PRIVILEGES                  = YES
COMPARE ARCHITECTURE                = YES
CURRENT MAIN.HTML MODIFIED          = NO
PRODUCTION DATA MODIFIED             = NO (FOR THIS COMPARISON)
HISTORICAL REPORTS DELETED           = NO
```

---

# 23. FINAL SELF-AUDIT

```text
Business Understanding          = VERIFIED FOR COMPARISON
Architecture Understanding      = VERIFIED FOR EXAMINED CURRENT MODULES
Historical Understanding        = VERIFIED FROM ORIGINAL + REPORT14 CONTINUITY
Database Understanding          = VERIFIED FOR RELEVANT CONTRACTS
Production Understanding        = VERIFIED FOR RELEVANT CONTRACTS
Current Understanding           = VERIFIED FOR TARGET FILE

Confirmed Facts                 = RECORDED
Unknowns                        = FULL CURRENT-WIDE WORKFLOW/NOTIFICATION/PERMISSION PARITY
Conflicts                       = NONE THAT JUSTIFY FULL MERGE
Unverified Claims               = FULL UI parity outside inspected responsibilities

Original Opened                 = YES
Current Opened                  = YES
Production Opened               = YES
Historical Continuity Opened    = YES

Current/PWA/main.html Modified  = NO
Original/PWA/main/main1.md     = READ ONLY

Surgical Patches Issued         = 2 low-risk UI patches
Core Business Patches Issued    = 0
Inventory Core Closed           = NO
```

## FINAL JUDGMENT

The correct engineering decision is to **retain Current as the operational baseline** and use Original only as a historical/UX contract source.

Two safe UI parity patches are ready for manual application by the owner:

```text
SP-01 Password visibility
SP-02 Login footer
```

No backend, accounting, workflow, permission, notification, or inventory patch was issued where the underlying responsibility transfer was not proven.

The next technically valuable step is **not a visual rewrite**; it is a dedicated Current-wide trace of Workflow / Notifications / UI permission application, followed by the Financial-source trace for Dashboard P&L, while Inventory Writer closure continues independently under its one-writer-at-a-time governance.
