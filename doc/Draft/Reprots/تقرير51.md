# تقرير51 — VERIFIED SYSTEM / PWA KNOWLEDGE COMPLETION

**Date:** 2026-09-04
**Repository:** `papamohammed77-glitch/rawaie-erp-New`
**Branch:** `main`

---

## 1. Mission

استكمال المهمة من آخر checkpoint، لا من الصفر، وإغلاق النقص المعرفي الذي سجله `النقص المعرفي للمساعد الجديد` فيما يخص دور النظام الأم، حدود تطبيقات PWA المنفصلة، الملفات التاريخية والخطط، وعلاقة هذه الطبقات بـSupabase Production.

لم أعتبر أي تقرير سابق حقيقة إلا بعد مقارنة ما أمكن منه بالمصادر المباشرة.

---

## 2. Latest Git Reality

التحقق المباشر من commits يثبت أن أحدث `main` observed head عند كتابة هذا التقرير هو:

```text
67fd5b327139be15964e4ecaa98218b0b7345ccf
[CTO] Refresh PROJECT_MEMORY with verified GitHub/Supabase review 2026-09-04
```

ويسبقه:

```text
4b93e9178397028b6345c3a863d65e659a696331
[CTO] Sync CURRENT_STATE with Report50 and corrected main ancestry

391de3fb38cb63f5c9ea6a3e62a91a5a3727a1a
[CTO] Add Report50 forensic PWA consumer inventory and state reconciliation
```

**Important:** قبل هذه الجولة كان `CURRENT_STATE.md` متوقفًا عند `4b93...`. لذلك تم اكتشاف drift توثيقي جديد سببه commit `67fd...` نفسه، وهو لا يثبت تغييرًا في المنتج؛ بل تحديث `PROJECT_MEMORY.md`.

---

## 3. Recovery of Previous Knowledge-Gap

التقرير السابق `النقص المعرفي للمساعد الجديد` سجّل 8 UNKNOWN/GAP، أهمها:

1. اعتقاد أن `Current/PWA/New-main` قد يكون ملفًا وصفيًا قصيرًا.
2. عدم معرفة مكان CSS الخاص بواجهة الدخول.
3. عدم التأكد من runtime الحقيقي.
4. عدم معرفة العلاقة الدقيقة بين الملفات الحالية والـhistorical files.
5. عدم إثبات نية فروق الـbranding.
6. عدم معرفة مكان CSS إن كان خارجيًا.
7. عدم معرفة أثر التعديلات على بقية التطبيق.
8. عدم إثبات سلسلة commits بشكل مباشر.

التدقيق الحالي أغلق 1 و2 و3 جزئيًا و4 و6 و8، وأبقى runtime proof وbranding intent ضمن الحدود المفتوحة.

---

## 4. Direct Source Investigation

### 4.1 `Current/PWA/New-main`

الفحص المباشر للـ`main` الحالي يثبت أن `New-main` **HTML/CSS/JS كامل**، وليس النص الوصفي الذي تسبب في UNKNOWN السابق.

الدليل المباشر يتضمن:

```text
<!doctype html>
<html lang="ar" dir="rtl">
<style> ... full application CSS ... </style>
<script ... Supabase ...></script>
<link rel="manifest" href="./manifest.json">
<script id="RAWAEA_TARGET_SW_COORDINATOR" src="./register-sw.js"></script>
```

كما يظهر مباشرة داخل نفس الملف:

```text
rw-login-title = 58px
rw-login-logo = 88 × 88
RAWAEA ERP ENTERPRISE
الروائع ERP
منصة إدارة الأعمال الذكية والمتكاملة
```

إذن:

```text
UNKNOWN-1 = RESOLVED
UNKNOWN-6 = RESOLVED
```

### 4.2 `Current/PWA/core.js`

المصدر المباشر يثبت النواة المشتركة:

```text
RW_Auth
RW_DB
RW_API
RW_UI
```

ويثبت تحديدًا أن authorization front-end يقرأ Auth metadata وأن:

```text
isOwner=true -> full frontend permission
permissions contains '*' -> full frontend permission
```

ويثبت أن `RW_API` يرسل bearer token إلى Edge Functions، وأن `RW_DB` طبقة Dexie/local persistence وsync وليست database SSOT.

### 4.3 Historical `Current/PWA/main/main1.md`

الفحص المباشر يثبت أنه جزء كامل من Parent/Main PWA contract/history، وليس مجرد عنوان.

كما يثبت قيم Original/previous contract مثل:

```text
<title>الروائع ERP | نظام متكامل</title>
rw-login-title = 64px
rw-login-logo = 120 × 120
rw-company-name = 34px
```

لكن لا يوجد دليل كافٍ لتحويل الفرق إلى regression.

---

## 5. System Role: What Is the "Mother System"?

التدقيق يثبت أن "النظام الأم" يجب فهمه كنظام الأعمال الكامل، وليس على أنه ملف HTML منفرد.

النموذج الحالي:

```text
RAWAEA ERP BUSINESS SYSTEM
    |
    +--> PWA consumers / interfaces
    |
    +--> shared runtime
    |
    +--> Edge Functions / RPC
    |
    +--> PostgreSQL / Supabase SSOT
```

والـPWA layer ليست منافسًا للنظام الأم ولا database authority مستقلة.

`Current/PWA/New-main` هو **canonical current application target** لمسار استكمال واجهة المنتج، وليس بديلًا عن Supabase business authority.

---

## 6. PWA Consumers — Current Map

الـcurrent consumer inventory الموثق في `Current/PWA` هو:

### Parent / Main
```text
New-main
main.html
```

### Sales / Orders
```text
telesales.html
order-taker.html
van-sales.html
pos.html
```

### Warehouse / Physical Operations
```text
picker.html
loader.html
receiver.html
unloader.html
warehouse.manager.html
warehouse.supervisor
Returns.html
```

### Driver / Delivery
```text
driver.html
driver.supervisor.html
```

### Purchasing / Finance / Management
```text
buyer.html
buyers.supervisor.html
accountant.html
finance-manager.html
general-manager.html
hr.html
sales.manager.html
sales.supervisor.html
```

### Owner / General Entry
```text
owner.html
counter.html
index.html
app.html
```

### Online Store
```text
store.index.html
store.track.html
```

### Vouchers
```text
vouchers.html
```

هذه كلها **consumers/entry surfaces**. لا يوجد من هذه القائمة ما يبرر اعتبار تطبيق PWA مستقلًا business authority مستقلًا.

---

## 7. Shared Support Layer — Not Applications

```text
core.js
sw.js
register-sw.js
manifest.json
New-main.manifest.json
schema-validator.js
vouchers-gold-master-ui.js
New-main-icon.svg
_headers
```

داخل:

```text
Current/PWA/main/
```

 توجد `main1.md` إلى `main11.md` ومشغلات/مواد تاريخية مرتبطة بالـParent PWA؛ لا يتم عدها كتطبيقات مستقلة.

---

## 8. Production Supabase Investigation

التحقق المباشر يثبت:

```text
Project = SMART ERP
Ref = fiilmooggumokxanwiyx
Status = ACTIVE_HEALTHY
Region = eu-west-1
PostgreSQL = 17.6
```

كما تم التحقق من وجود business Edge Functions فعّالة تغطي:

```text
items/customers/suppliers/branches/employees/settings/roles
orders/sales
runsheet/picking/loading/delivery/return
stock vouchers
purchasing
accounting/reporting
audit/supporting workflows
```

ومن أمثلة versions المنشورة حاليًا:

```text
save-sales-invoice       v15
create-runsheet          v26
start-picking            v34
complete-picking         v17
start-loading            v5
complete-loading         v11
start-delivery           v7
complete-delivery        v4
complete-return          v25
unload-runsheet          v6
send-stock-voucher       v20
receive-stock-voucher    v22
receive-purchase         v12
save-journal-entry       v8
```

وجود version منشورة = **PRODUCTION DEPLOYED evidence** فقط؛ لا يساوي 100% functional closure.

---

## 9. Owner / License Contract

Production DB verification:

```text
owner email         = owner@alrawae.com
permissions         = ["*"]
status              = Active
role                = مدير النظام
role_id             = e8a76adb-8efe-4bc5-b760-0ee660f10e9f
auth_id             = 0a6089e6-0c33-4cf9-9aa0-31fc42774b89
owner_profile       = linked
license_status      = active
```

العقد الحاكم:

```text
OWNER
=
AUTH OWNER IDENTITY
+
isOwner=true
+
permissions=["*"]
+
VALID owner_profile
+
ACTIVE LICENSE
```

هذا العقد محمي. لا يجوز تحويل `*` إلى role enumeration.

---

## 10. License Management Visibility

Current source contract موجود:

```text
view = license
label = إدارة الترخيص
perm = owner
route = RW_OwnerLicense.render
owner gate = hasOwner()
```

ومع `core.js` فإن owner أو `*` يتجاوز checkPermission على الواجهة.

الحكم الحالي:

```text
SOURCE CONTRACT = VERIFIED
OWNER CONTRACT = VERIFIED
BROWSER VISIBILITY = NOT VERIFIED
DEPLOYED REVISION = NOT VERIFIED
SERVICE WORKER CACHE = NOT VERIFIED
```

لذلك لا يوجد أساس لإضافة route جديد قبل Runtime evidence.

---

## 11. Historical / Current / Production Boundaries

تم تثبيت التصنيفات التالية:

```text
THEORETICAL
CURRENT ONLY
STAGING VERIFIED
PRODUCTION DEPLOYED
PRODUCTION RUNTIME VERIFIED
100% CLOSED
```

ومن أهم الأخطاء التي تم منع تكرارها:

```text
Git source != browser runtime
Commit message != deployment proof
Report != production truth
Original != regression automatically
Source route != visible tab automatically
```

---

## 12. Current Risks Confirmed by Production Evidence

تبقى المخاطر التالية مفتوحة لأنها مثبتة مباشرة:

```text
SECURITY DEFINER EXECUTE exposure on:
  public.create_item_with_opening_stock(...)
  public.sync_company_main_branch_projection(...)

Supabase Auth leaked-password protection = disabled

verify_jwt=false historical/test/recovery/E2E function surface remains active

owner-recovery-20260818 returns repeated HTTP 410 in logs

unindexed foreign keys / RLS init-plan / permissive-policy / unused-index advisories exist

CI combined status can be empty; empty != PASS
```

هذه المخاطر لا تعني أن PWA consumer map خاطئ؛ هي separate closure units.

---

## 13. Investigator Errors Already Committed / Corrected

### Error A — stale CURRENT_STATE HEAD

تم اكتشاف أن `CURRENT_STATE` كان يشير إلى `4b93...` بينما ظهر لاحقًا commit `67fd...`.

### Error B — treating a short read as file absence

التقرير السابق اعتبر New-main ملفًا قصيرًا. الفحص المباشر الجديد أثبت عكس ذلك.

### Error C — substituting role permissions for owner wildcard

تم تثبيت العقد التاريخي والصحي الصحيح:

```text
permissions = ["*"]
isOwner = true
```

### Error D — treating documentation commit as product mutation

`67fd...` و`4b93...` تحديثات continuity/documentation؛ لا يوجد منهما بحد ذاته دليل deployment جديد للمنتج.

---

## 14. Deliverable Created

تم إنشاء:

```text
doc/Draft/Reprots/MASTER_RAWAEA_SYSTEM_PWA_KNOWLEDGE_PACK_2026-09-04.md
```

وهو الحزمة المطلوبة لتعليم أي Successor CTO:

```text
- دور النظام الأم
- حدود PWA consumers
- shared runtime
- backend / SSOT
- owner/license contract
- historical/original relationship
- production evidence boundaries
- known risks
- successor boot sequence
- raw source index
```

---

## 15. Next Gate

لا يوجد Product Patch جديد في هذا التقرير.

المحطة التالية يجب أن تكون Closure Unit واحدة فقط بناءً على Runtime/Production evidence جديد. الاحتمالات المباشرة:

```text
A. Fresh browser/runtime proof for New-main + License visibility
B. Security closure for exposed SECURITY DEFINER functions
C. verify_jwt=false lifecycle classification
D. stale owner-recovery caller forensic closure
E. CI evidence restoration
```

اختيار أي منها يجب ألا يعيد فتح Knowledge baseline الذي تم إغلاقه هنا.

---

## 16. Raw Link

```text
https://raw.githubusercontent.com/papamohammed77-glitch/rawaie-erp-New/main/doc/Draft/Reprots/MASTER_RAWAEA_SYSTEM_PWA_KNOWLEDGE_PACK_2026-09-04.md
```

هذا هو الرابط الذي يجب تسليمه للمساعد الجديد مع `CURRENT_STATE.md` وآخر Report كحزمة الاستمرارية الأساسية.
