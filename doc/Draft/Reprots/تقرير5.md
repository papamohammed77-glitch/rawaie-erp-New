# تقرير5 — P138 Forensic Deployment Artifact Mismatch & Login Blocker

**التاريخ:** 2026-09-01  
**المرحلـة:** P138 — DIRECT RUNTIME / DEPLOYMENT RECONCILIATION  
**النطاق:** `Current/PWA/New-main` + `Current/PWA/sw.js` + Supabase Auth/API evidence + Git deployment lineage  
**الحالة:** `SOURCE CORRECT / SERVED ARTIFACT STALE-OR-MISMATCHED / DEPLOYMENT GATE OPEN`

## 1. نقطة الانطلاق

تم تنفيذ الجولة وفق `MASTER - RAWAEA ERP.md` ومنهج:

`UNDERSTAND → PROVE → DESIGN → IMPLEMENT → VALIDATE → CLOSE`

ولم يبدأ التحقيق من الصفر. تمت مراجعة `CURRENT_STATE.md` أولًا، ثم سجل Git، ثم التقارير الموجودة في `doc/Draft/Reprots`، ثم Production Supabase، ثم الملفات الحالية ذات الصلة.

التقارير السابقة محفوظة ولم يتم حذفها أو استبدالها.

## 2. الحالة التاريخية المباشرة قبل P138

### P135

تم إصلاح guard في MAIN11 لأنه كان يفحص `window.supabase` بدل client المحلي، وإضافة marker:

`RAWAEA_P135_TARGET`

لكن لم يكن ذلك كافيًا لأن MAIN11 داخل IIFE منفصل.

### P136

تم إثبات السبب الحقيقي الثاني: مشكلة JavaScript scope. تم تصدير نفس client إلى:

`window.RW_SUPABASE_CLIENT`

وأصبح MAIN11 يستخدم:

`var sb = window.RW_SUPABASE_CLIENT || null;`

ويستدعي `sb.auth.getSession()`.

الـartifact الحالي في Git ما زال يحتوي هذا الإصلاح.

### P137

تم تحديث `Current/PWA/sw.js` إلى النسخة `2.2 FINAL` مع:

`RAWAEA_SW_P137_FINAL`

وتم رفع cache namespace إلى `rw-static-v3`.

الملف صحيح من ناحية المصدر، لكن وجوده في Git لم يكن دليلًا على وجوده في URL الإنتاج.

## 3. الأدلة الحالية المباشرة

### 3.1 Git الحالي

Current `main` HEAD:

`7d15d9c8722ffb56c394054d8c28cce00720489d`

والـhistory المباشر يثبت:

- `65b2ef057cba3f2f507495a37f124f6d9225ac39` = P136 target repair.
- `d36e7783e3934628bb63e7f2c5f3e95e0e5301ac` = P137 Service Worker source repair.
- بعد P136 لا يوجد commit يعيد `Current/PWA/New-main` إلى P135.
- مقارنة `65b2ef...` حتى `7d15d9c...` أظهرت أن التغييرات اللاحقة لم تعدل `Current/PWA/New-main`، وإنما شملت التوثيق و`sw.js`.

Current `Current/PWA/New-main` blob:

`5a4cc333b24de8fe66b79329db97a997ffa8ec3b`

Current `Current/PWA/sw.js` هو P137 FINAL.

### 3.2 Runtime evidence المقدم للمشكلة

المتصفح الحالي يسجل:

`RAWAEA_P135_TARGET New-main`

ثم:

`MAIN11_SUPABASE_UNAVAILABLE`

ثم:

`POST /auth/v1/token?grant_type=password → 401`

مع:

`AuthApiError: Invalid API key`

وكذلك:

`/companies/sw.js → 404`

هذا runtime لا يتوافق مع حالة Git الحالية التي تحتوي إصلاح P136.

## 4. Supabase Production verification

تم فحص Production مباشرة، وليس اعتمادًا على التقارير.

### 4.1 Auth keys

Production يحتوي مفتاح legacy anon فعالًا، ويوجد أيضًا publishable key فعال.

لم يتم تدوير أو تغيير أي مفتاح في هذه الجولة.

### 4.2 Auth logs

Production يسجل نشاطًا ناجحًا من runtime آخر، بما في ذلك:

- `POST /auth/v1/token?grant_type=refresh_token → 200`
- `GET /auth/v1/user → 200`
- Login event ناجح للمالك.

وفي المقابل يسجل runtime الحالي طلبات password login بنتيجة:

`401`

### 4.3 API logs

نفس الفترة تحتوي على:

- `GET /rest/v1/users?... → 401` للـruntime الفاشل.
- وطلبات REST ناجحة `200` من runtime عامل.

إذن Supabase نفسه ليس متوقفًا، ولا يوجد دليل مباشر على أن الخدمة أو حساب المالك هو سبب التوقف.

## 5. OWNER / LICENSE verification

تم فحص المالك مباشرة من قاعدة البيانات.

النتيجة المؤكدة:

- `public.users.permissions = ["*"]`
- `public.users` wildcard = true
- Auth `isOwner = true`
- Auth `permissions = ["*"]`
- `owner_profile.auth_user_id` يطابق Auth user
- `license_status = active`
- حالة المستخدم Active

إذًا تبويب إدارة الترخيص لا يحتاج إعادة بناء صلاحيات الدور، ولا توجد حاجة لأي تعديل إضافي على wildcard أو Owner role في هذه الجولة.

## 6. Service Worker contract

`Current/PWA/New-main` يعمل في سياق nested deployment ويستخدم التسجيل النسبي:

`../sw.js`

بينما `manifest.json` يحدد:

`start_url = /companies/company-1/app.html`

`scope = /companies/company-1/`

بالتالي العنوان الطبيعي الناتج من التسجيل هو:

`/companies/sw.js`

Runtime يعيد:

`404`

وهذا يتفق مع عدم ثبوت placement في Cloudflare artifact.

المهم:

`Git file exists ≠ deployed URL exists`

## 7. CONFLICT RECONCILIATION

### Conflict A — Runtime version mismatch

**Source A:** Git الحالي يحتوي P136 target.  
**Source B:** Browser runtime يظهر P135 marker ولا يظهر سلوك P136 المتوقع.  
**الحكم:** Runtime ليس serving current Git target، أو توجد طبقة deployment/cache/lineage مختلفة.

### Conflict B — Service Worker placement

**Source A:** `Current/PWA/sw.js` موجود ومصحح إلى P137.  
**Source B:** `/companies/sw.js` يعيد 404.  
**الحكم:** مشكلة artifact placement/routing/deployment، وليست مشكلة محتوى `sw.js` مثبتة من الدليل الحالي.

### Conflict C — Supabase API key

**Source A:** current Git target يحمل configuration مطابقة للمفتاح legacy النشط في Production.  
**Source B:** Runtime الحالي يبلغ `Invalid API key`.  
**الحكم:** هذا يرجح بقوة أن الـserved artifact/client configuration مختلف عن target الحالي. لم يتم تغيير المفتاح لأنه لا يوجد دليل يبرر تغييره في Git.

## 8. الأخطاء السابقة التي تم إثباتها

1. **P135:** محاولة أولى للـexecutor استخدمت مطابقة نصية افتراضية افترضت عددًا أقل من مراجع `window.supabase`؛ انتهت بخطأ verifier رغم بقاء العيب الحقيقي.
2. **P135/P136:** إصلاح guard النصي دون معالجة JavaScript scope لم يكن كافيًا.
3. **P137:** خطر الاستنتاج الخاطئ بأن وجود `sw.js` في Git يساوي توفر `/companies/sw.js` في الإنتاج. تم رفض هذا الاستنتاج صراحة.
4. الخطأ الأخطر الذي كان يمكن ارتكابه الآن هو إجراء patch جديد للمفاتيح أو Service Worker بينما artifact المنشور نفسه غير مثبت. لم يتم ارتكاب هذا الخطأ.

## 9. ما نجح وما فشل

### نجح

- P136 source repair موجود في Git الحالي.
- P137 Service Worker source النهائي موجود في Git الحالي.
- Node syntax validation نجح في جولات الإصلاح السابقة.
- Supabase Production healthy وقابل لتسجيل دخول ناجح من runtime آخر.
- Owner wildcard/license state سليمة مباشرة في DB/Auth.
- Historical reports بقيت محفوظة.

### لم يغلق المشكلة

- Cloudflare Pages served artifact identity غير مثبتة.
- deployed commit غير مثبت.
- deployment output/root path غير مثبت.
- `/companies/sw.js` runtime fetch ما زال 404.
- browser login على `rawaea-erp.pages.dev` ما زال 401.
- Gold/Diamond غير مثبتين.

## 10. ROOT CAUSE CLASSIFICATION

الحالة الحالية تصنف كالتالي:

`DEPLOYMENT_ARTIFACT_MISMATCH`

بدرجة ثقة عالية، مع بقاء تفاصيل Cloudflare lineage التالية غير مثبتة:

`DEPLOYED_COMMIT`
`DEPLOYMENT_OUTPUT_ROOT`
`SERVED_ARTIFACT_IDENTITY`
`PATH_MAPPING`

الـsource defects P135/P136 ليست التفسير الحالي الكافي، لأن Git الحالي يحتوي إصلاح P136 بينما browser runtime ما زال يعرض P135 behavior/marker.

## 11. القرار الجراحي الصحيح

**لا يوجد Patch جديد معتمد لـ`Current/PWA/New-main` أو Supabase أو `Current/PWA/sw.js` في هذه اللحظة.**

الإصلاح المطلوب الآن هو في طبقة النشر:

`Current/PWA/New-main`
`+`
`Current/PWA/sw.js`
`↓`
`EXISTING LEGITIMATE CLOUDFLARE PAGES DEPLOYMENT PATH`
`↓`
`/companies/company-1/main.html`
`/companies/sw.js`

ويجب إثبات ذلك قبل تغيير أي مصدر آخر.

## 12. سبب عدم تنفيذ redeploy من داخل هذه الجولة

تم البحث عن Cloudflare/Wrangler integration متاح، ولم توجد أداة/Plugin Cloudflare قابلة للاستخدام في الجلسة الحالية.

كما لم يظهر في المستودع مسار نشر Pages موثق يوفر لنا deployment ID حاليًا.

لذلك لم يتم اختلاق deployment أو تعديل workflow لإخفاء هذه الفجوة.

## 13. NEXT AUTHORIZED ACTIONS

1. الوصول إلى قناة Cloudflare Pages الشرعية المستخدمة للمشروع.
2. استخراج deployment ID والحالة وcommit المرجعي وoutput/root directory.
3. إثبات أن deployment يخدم `Current/PWA/New-main` الحالي وليس نسخة P135.
4. إعادة نشر نفس الـtarget الحالي دون تغيير business logic أو credentials.
5. إثبات مباشرة:
   - `/companies/company-1/main.html` يحتوي P136 runtime.
   - `/companies/sw.js` يعيد HTTP 200 ويحتوي `RAWAEA_SW_P137_FINAL`.
6. فتح المتصفح في جلسة نظيفة والتحقق من:
   - عدم ظهور `MAIN11_SUPABASE_UNAVAILABLE`.
   - ظهور P136 marker في runtime state.
   - نجاح login.
   - session restoration.
   - tenant resolution.
   - dashboard.
   - إدارة الترخيص.
7. عند نجاح ذلك فقط يتم إغلاق Auth/Deployment gate ثم إعادة اختبارات Gold/Diamond.

## 14. FINAL JUDGMENT

`SOURCE_AUTH_FIX = PRESENT`

`SOURCE_SCOPE_FIX = PRESENT`

`OWNER_WILDCARD = PRESENT`

`OWNER_LICENSE = ACTIVE`

`SUPABASE_SERVICE = HEALTHY`

`CURRENT_GIT_TARGET = P136`

`CURRENT_SW_SOURCE = P137 FINAL`

`BROWSER_RUNTIME = P135-LIKE / STALE OR DIFFERENT ARTIFACT`

`SUPABASE_401 = REAL`

`/companies/sw.js 404 = REAL`

`CLOUDFLARE_ARTIFACT_LINEAGE = OPEN`

`NEW_SOURCE_PATCH_AUTHORIZED = NO`

`REDEPLOY_CURRENT_TARGET = NEXT AUTHORIZED ACTION`

`GOLD = UNPROVEN`

`DIAMOND = UNPROVEN`

`CLOSED 100% = NO`
