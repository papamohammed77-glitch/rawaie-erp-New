# تقرير55 — التحقيق الجنائي واستعادة الاستمرارية ومراجعة Main1
## RAWAEA ERP — 2026-09-05

## 0. الحالة التنفيذية

هذه الجولة لم تبدأ من الصفر. تم استعادة آخر سلسلة موثقة من `CURRENT_STATE.md` وGit والتقارير، ثم أعيد فحص الادعاءات الأساسية من المصدر المباشر ومن Production.

النتيجة النهائية:

```text
MAIN1 SOURCE PATCHES 1–4 = VERIFIED IN CURRENT GIT
MAIN1 SESSION-RESTORE FIX = VERIFIED IN CURRENT SOURCE
MAIN1 BRANDING SOURCE = APP_SETTINGS / COMPANY CONTEXT
MAIN1 PRODUCTION RUNTIME = NOT PROVEN IN THIS ROUND
MAIN1 FULL CLOSURE = NOT PROVEN
MAIN2 EXECUTION = NOT STARTED
```

سبب عدم إعلان 100% ليس وجود Patch قديم ناقص، بل غياب إثبات مستقل وحالي لمسار Runtime الكامل مع Production، بالإضافة إلى اكتشاف configuration coverage يجب ألا يُحل بتحويل القيم إلى static constants.

---

## 1. آخر نقطة وصل إليها الجميع قبل هذه الجولة

آخر HEAD معروف في Git قبل هذه الجولة:

```text
8649758ca535e273f979fa349181d7a14b7380f7
```

والسلسلة المباشرة الأخيرة تضمنت:

```text
ed4e91ec595234ba7ede3f08558c660c1b100d3e
Actual Main1 source mutation — Patch 1–4

8399ff50664b76c776f01a9e941cc2bdfd247b59
External Main1 analysis update

4baace12f19ab70a063d985a06488ca53cf2e799
CURRENT_STATE reconciliation

d26695ccca675fb85936bda1707398326ce882ec
Report54 forensic reconciliation

e829b747d811e40b402f0e6ba00c366818939a6f
Unified continuity/governance command

8649758ca535e273f979fa349181d7a14b7380f7
Final CURRENT_STATE checkpoint
```

هذه السلسلة تؤكد أن Patch 1–4 ليست pending؛ إعادة تنفيذها كانت ستكرر إصلاحًا قديمًا.

---

## 2. ما أثبته Git عن Main1

الملف الحالي:

```text
Current/PWA/main2/main1.md
```

والـsource mutation المنفذ في `ed4e91...` يتضمن:

```text
1. RW_STATE.app.company.id
2. users lookup by auth_id → company_id + status
3. app_settings company-scoped lookup
4. company-scoped bootstrap reads for items/customers/branches/suppliers
```

كما أن source الحالي يحتوي على مسار Session Restore يعيد تثبيت user metadata ثم يدخل للنظام، ولذلك لا يجوز اعتبار فجوة session-restore القديمة ما زالت مفتوحة دون دليل regression.

---

## 3. التحقيق في تقرير المساعد الخارجي

التقرير الخارجي كان مفيدًا كـHistorical / Architecture reference، لكنه احتوى على توصيف قديم لـMain1 على أنه Login/static فقط.

Direct source inspection الحالي يثبت أن `main1.md` في Main2 ليس مجرد Login-only fragment، بل يحتوي runtime/shared application responsibilities، ويستمر منطقيًا مع أجزاء Main2 اللاحقة.

إذن:

```text
External report = historical design/reference
Current Git = current source evidence
```

ولا يجوز استخدام التقرير الخارجي لإعادة فتح Patch تم إثباته في Git.

---

## 4. مراجعة System Settings / Branding

تم فحص نقطة الإعدادات تحديدًا لأن هناك خطرًا من تفسير القيم الثابتة في HTML على أنها Source of Truth.

المبدأ الصحيح:

```text
Authenticated user
→ company_id
→ app_settings(company_id)
→ company_name / company_logo / system settings
```

وفي Main1 الحالي، `enterSystem` يقرأ `app_settings` بـ`company_id` ثم يكتب `company_name` و`company_logo` إلى واجهة الـParent.

أما القيم الموجودة داخل HTML/State كـfallback مثل اسم الشركة أو الحروف الافتراضية، فهي لا تصبح مصدر الحقيقة طالما أن المسار الديناميكي يطبق بيانات `app_settings` عند الدخول.

لكن يجب التمييز بين:

```text
Fallback for presentation
```

و:

```text
Authoritative system configuration
```

ولا يجوز تحويل fallback إلى بديل عن سجل `app_settings`.

---

## 5. Production configuration evidence

التحقيق المباشر على Production الحالي أعاد:

```text
Companies table currently visible = 1 company
Users for that company = 24
app_settings rows for that company = 1
```

وسجل `app_settings` الحالي يحتوي:

```text
company_id = 00000000-0000-0000-0000-000000000001
company_name = الروائع
company_logo = NULL
main_branch_id = a38332b6-6cea-480a-ada1-6eb6ab0590db
delivery_fee = 0.00
tax_rate = 0.00
min_invoice_amount = 0.00
```

هذا يثبت أن `app_settings` هو مصدر بيانات فعلي، وليس مجرد design claim.

كما أن التحقيق الحالي لم يثبت وجود Companies إضافية في Production الحالية، ولذلك لا يجوز إعادة استخدام أرقام قديمة من snapshots تاريخية وإعلان وجود multi-tenant data في اللحظة الحالية.

---

## 6. Main1 runtime gate

لم يتم العثور في الأدلة الحالية على Browser/Production runtime run يثبت تشغيل `Current/PWA/main2/main1.md` بعد آخر mutation `ed4e91...`.

كما أن فشل Browser gate السابق المرتبط بـ`Current/PWA/New-main` كان:

```text
page.goto: Download is starting
```

ولا يوجد أساس فني لنسب هذا الفشل إلى Main1.

بالتالي:

```text
Source success ≠ Runtime success
New-main browser failure ≠ Main1 browser failure
```

ومع عدم توفر Runtime evidence مستقل حاليًا، لا يمكن إعلان Full Closure.

---

## 7. أخطاء الجولة السابقة التي تم تصحيحها

### الخطأ 1 — إعادة فتح session restore

تم في تاريخ سابق تفسير الحالة كأن session restore لا يزال مكسورًا. Direct current source يثبت أن المسار تم إصلاحه في Main1.

الدرس:

```text
لا تعِد فتح Bug مغلق دون Regression evidence.
```

### الخطأ 2 — ربط فشل New-main بـMain1

هذا كان استنتاجًا غير صالح من نطاق الاختبار.

### الخطأ 3 — اعتبار التقرير الخارجي Current Truth

التقرير الخارجي كان Historical Reference وليس Production/Current proof.

### الخطأ 4 — الخلط بين static fallback وdynamic source

وجود fallback في الواجهة لا يعني أن branding authority static.

### الخطأ 5 — اعتبار `company_id + item_code` دائمًا المفتاح الوحيد

Production schema أثبت أن `items.item_code` مـUNIQUE عالميًا، لذلك Item identity يجب أن يحترم الـschema بدلاً من فرض company-local identity على شيء عالمي.

---

## 8. ما الذي تم اكتشافه خارج Main1 أثناء التحقيق

تم خلال continuity/inventory cross-check اكتشاف وجود historical/fixture-like stock rows في snapshots أقدم، وكان بينها cross-company-looking combinations. لكن هذا لا يبرر تعديلها في هذه الجولة لأن Production الحالية يجب أن تكون المصدر الأول، والبيانات الحالية لم تُثبت أن تلك anomaly ما زالت بنفس الشكل.

أي data repair يجب أن يعاد تشغيله على Production الحالية قبل لمس أي row.

---

## 9. ما لم أفعله عمدًا

لم أغيّر:

```text
Main1 source Patch 1–4
historical visual values 64px / 120px / 34px
Owner wildcard semantics
Main2
Core.js
```

لأنه لا يوجد دليل جديد يثبت أن إعادة تعديلها الآن آمنة أو مطلوبة لإغلاق Main1.

لم أضف أي Company/static branding fallback إلى Production بديلًا عن `app_settings`.

---

## 10. حكم Main1

### Source Closure

```text
CLOSED
```

### Historical reconciliation

```text
CLOSED for the issues addressed by Patch 1–4
```

### System Settings source contract

```text
CONFIRMED: app_settings is the authoritative source
```

### Production data/configuration readiness

```text
PARTIALLY PROVEN
```

### Independent current Runtime

```text
NOT PROVEN
```

### Full Main1 closure

```text
OPEN
```

### Main2 start authorization

```text
NOT AUTHORIZED YET
```

---

## 11. ما يلزم لإغلاق Main1 فعليًا

الـnext closure unit ليس إعادة كتابة Main1.

المطلوب فقط إثبات:

```text
Current source at HEAD
+
actual deployment of that source
+
current runtime of the deployed artifact
+
production company/configuration compatibility
+
no regression in login/session/branding/navigation
```

ثم تحديث `CURRENT_STATE.md` من نفس اللحظة.

بعدها فقط يعاد تقييم Main2.

---

## 12. Unified governance artifact

تم إنشاء:

```text
doc/Draft/medhat/MASTER - RAWAEA ERP FORENSIC CONTINUITY GOVERNANCE v2.md
```

وهو successor command يجمع:

```text
continuity recovery
historical reconstruction
production truth
Main1 contract
System Settings dynamic contract
Tenant identity
Owner semantics
Inventory contract
safe production testing
failure memory
CURRENT_STATE governance
final self-audit
```

---

## 13. Final Self-Audit

```text
BUSINESS UNDERSTANDING = CONFIRMED FOR MAIN1 BOUNDARY
ARCHITECTURE UNDERSTANDING = CONFIRMED
DATABASE UNDERSTANDING = CONFIRMED FOR APP_SETTINGS / COMPANY STATE
HISTORICAL UNDERSTANDING = RECONCILED
CURRENT SOURCE UNDERSTANDING = CONFIRMED
PRODUCTION UNDERSTANDING = PARTIAL BUT DIRECTLY VERIFIED
DEPLOYMENT UNDERSTANDING = NOT SUFFICIENT FOR MAIN1 FULL CLOSURE
RUNTIME UNDERSTANDING = NOT PROVEN
```

### Confirmed Facts

```text
ed4e91 exists
Patch 1–4 exist in current source
session restore fix exists in current source
branding path uses company-scoped app_settings
current Production contains one visible Company
current Production contains one corresponding app_settings row
```

### Unknowns

```text
independent current Main1 browser/runtime pass
exact deployed artifact equivalence to current Main2 source
```

### Conflicts resolved

```text
Historical external report vs Current Main1 source
Old multi-company snapshot vs current Production snapshot
Previous Main1 runtime assumption vs current lack of runtime proof
```

### What I Proved

```text
Main1 source Patch 1–4 are real and current
Main1 is not a login-only static artifact in the current lineage
System Settings authority remains app_settings
Main1 should not be re-patched without regression evidence
```

### What I Did Not Prove

```text
Main1 100% runtime closure
Full Parent application runtime closure
Main2 readiness
```

### Final Closure Status

```text
MAIN1 = SOURCE-CLOSED / FULL-CLOSURE-OPEN
MAIN2 = NOT STARTED
```
