# تقرير7 — P140 Forensic Runtime / Source / Deployment-Gate Reconciliation

**التاريخ:** 2026-09-01  
**المرحلة:** P140 — DIRECT FORENSIC RECONCILIATION  
**النطاق:** `MASTER - RAWAEA ERP.md` + `CURRENT_STATE.md` + `doc/Draft/Reprots` + Current PWA + Production Supabase + supplied Browser Console evidence + Cloudflare availability checks  
**الحالة:** `CURRENT_SOURCE VERIFIED / PRODUCTION DATA VERIFIED / SERVED RUNTIME UNRECONCILED / DEPLOYMENT EXECUTION EXTERNALLY BLOCKED`

## 1. Executive Judgment

بدأت الجولة وفق أمر `MASTER - RAWAEA ERP.md` ولم تبدأ من الصفر. تم التعامل مع `CURRENT_STATE.md` كسجل حالة معلن، ثم أُعيدت مطابقة الحالة مع Current Git وProduction Supabase والملفات الحالية والأدلة التشغيلية المقدمة.

النتيجة:

1. `Current/PWA/New-main` الحالي يحتوي إصلاح P136 بالفعل.
2. `Current/PWA/sw.js` الحالي هو مصدر P137 FINAL بالفعل.
3. Production Supabase وبيانات المالك/الترخيص سليمة في التحقق المباشر.
4. Browser runtime المقدم لا يطابق Current target؛ فهو يطبع `MAIN11_SUPABASE_UNAVAILABLE` ويعيد `401 Invalid API key`.
5. Browser runtime يطلب `/companies/sw.js` ويحصل على `404`.
6. داخل Current Git نفسه يوجد تناقض مساري يجب عدم تحويله إلى patch عشوائي: `New-main` يسجل Service Worker كـ`../sw.js`، بينما `sw.js` موجود في جذر `Current/PWA`، والـmanifest يستخدم nested scope `/companies/company-1/`.
7. لا توجد قناة Cloudflare Pages شرعية متاحة لنا في هذه الجلسة لتحديد deployment ID أو artifact أو تنفيذ redeploy.

بالتالي لا يوجد تعديل جديد آمن ومثبت في `New-main` أو Supabase أو `sw.js` يمكنه وحده إغلاق المشكلة الحالية دون إثبات كيف يُنشر `Current/PWA` إلى الـURL الفعلي.

## 2. Master Governance Followed

تمت قراءة `MASTER - RAWAEA ERP.md` من Git الحالي حتى نهاية الأمر، وتطبيق مبادئه الأساسية:

- `CURRENT_STATE` نقطة الدخول وليس حقيقة تلقائية.
- `LAST VERIFIED EVENT` أهم من آخر تقرير.
- `UNKNOWN ≠ BUG`.
- لا يجوز تحويل Unknown إلى Guess ثم Patch.
- Production/runtime لا يساوي Git، وGit لا يساوي deployment.
- لا تُفتح أعمال مغلقة دون دليل مادي جديد.
- لا يُنشأ duplicate/shadow implementation لمجرد تجاوز blocker.
- لا يُعلن `FIXED/CLOSED/VERIFIED` بلا evidence object.

## 3. Current-State Recovery

`CURRENT_STATE.md` الحالي يسجل P131 → P139 ويحدد أن P139 انتهى إلى:

`CURRENT_SOURCE_P136 = VERIFIED`  
`CURRENT_SW_P137 = VERIFIED`  
`SUPABASE_PRODUCTION = HEALTHY`  
`OWNER_WILDCARD = VERIFIED`  
`OWNER_LICENSE = ACTIVE`  
`BROWSER_LOGIN = OPEN / 401`  
`COMPANIES_SW = OPEN / 404`  
`CLOUDFLARE_ARTIFACT_ID = UNKNOWN`  
`DEPLOYMENT_ROOT_MAPPING = UNKNOWN`

تمت إعادة إثبات البنود ذات الصلة في P140، ولم يُقبل سجل P139 لمجرد وجوده.

## 4. Historical Reports Review

تم الحفاظ على السلسلة كاملة:

- `تقرير1` محفوظ.
- `تقرير2.md` يحفظ P135 والـexecutor/guard failure history.
- `تقرير3.md` يحفظ P136 ويثبت shared-client scope repair.
- `تقرير4.md` يحفظ P137 ويثبت Service Worker source finalization.
- `تقرير5.md` يحفظ P138 ويصنف المشكلة كـdeployment artifact mismatch.
- `تقرير6.md` يحفظ P139 ويمنع إنشاء patch جديد قبل publication lineage proof.
- هذا الملف `تقرير7.md` هو P140 ولا يستبدل أي تقرير سابق.

التقارير استُخدمت كخيوط تاريخية فقط، بينما الحسم الحالي مبني على Current Git وSupabase والأدلة التشغيلية.

## 5. Current Git — Direct Evidence

### 5.1 Current Target

`Current/PWA/New-main` blob الحالي:

`5a4cc333b24de8fe66b79329db97a997ffa8ec3b`

والملف يحتوي بالفعل على contract P136:

```js
window.RW_SUPABASE_CLIENT=supabase;
window.__RAWAEA_P136_SCOPE_REPAIR__='shared-client';
var sb = window.RW_SUPABASE_CLIENT || null;
var sessionResult = await sb.auth.getSession();
```

كما يحتفظ بـ`RAWAEA_P135_TARGET` كـcontinuity marker قديم؛ هذا الـmarker لا يثبت أن المصدر الحالي هو P135.

### 5.2 Service Worker

`Current/PWA/sw.js` موجود في Current Git، ويحتوي:

`RAWAEA_SW_P137_FINAL`

وcache namespace:

`rw-static-v3`

وNetwork Only للـHTML وSupabase/API والـruntime code.

### 5.3 Headers

`Current/PWA/_headers` يضع no-cache/no-store/must-revalidate على `/sw.js` و`/**/sw.js` و`manifest.json` وHTML files.

لكن فعالية ذلك في Production مشروطة بأن يكون `_headers` داخل output root الفعلي في Cloudflare.

### 5.4 Manifest

`Current/PWA/manifest.json` يحدد:

`start_url = /companies/company-1/app.html`  
`scope = /companies/company-1/`

## 6. New Current Finding — Service Worker Path Mismatch Inside Source Layout

تمت إعادة قراءة `Current/PWA` نفسه بدل الاعتماد على التقرير السابق.

الموجود فعليًا:

`Current/PWA/sw.js`

ولا توجد في الشجرة الحالية:

`Current/PWA/companies/sw.js`

وتوجد `New-main` التي تعمل في nested URL بحسب الأدلة السابقة.

`New-main` يستخدم التسجيل النسبي:

```js
navigator.serviceWorker.register('../sw.js', ...)
```

من صفحة nested من نمط:

`/companies/company-1/main.html`

هذا يجعل المتصفح يطلب:

`/companies/sw.js`

وهو بالضبط الـURL الذي يظهر في Console كـ404.

هذه ملاحظة مصدر حقيقية، لكن **لا يجوز إصلاحها بإنشاء `Current/PWA/companies/sw.js`**؛ لأن P139 وضع ذلك صراحة ضمن المسارات الممنوعة ما لم يثبت deployment root والـtarget contract.

كما أن تعديل registration path نفسه لن يعالج `401 Invalid API key` أو يثبت أن المتصفح يستخدم Current P136 artifact.

لذلك تم تصنيفها:

`SOURCE/PATH CONTRACT INCONSISTENCY — REQUIRES DEPLOYMENT ROOT PROOF`

وليس `PATCH NOW`.

## 7. Supabase Production — Direct Reconciliation

تم استخدام SQL مباشر على Production project:

`fiilmooggumokxanwiyx`

وبعد اصطدام الاستعلام الأول بعمود غير موجود `users.is_active`، تم قراءة schema أولًا ثم استخدام الأعمدة الحقيقية.

النتيجة المباشرة للمالك:

- `users.email = owner@alrawae.com`
- `users.status = Active`
- `users.permissions = ["*"]`
- `users.auth_id` مربوط بحساب Auth.
- `owner_profile.license_status = active`
- `owner_profile.auth_user_id = users.auth_id`

إذًا Owner/Licensing ليس هو سبب توقف الـlogin الحالي.

لم تُنفذ أي mutation في Supabase خلال P140.

## 8. Browser Runtime Evidence

المستخدم قدم الأدلة التالية من `rawaea-erp.pages.dev`:

```text
RAWAEA ERP BOOTING...
RAWAEA_P135_TARGET New-main
MAIN11_SUPABASE_UNAVAILABLE
POST https://fiilmooggumokxanwiyx.supabase.co/auth/v1/token?grant_type=password 401
AuthApiError: Invalid API key
/companies/sw.js 404
```

هذه الأدلة حاسمة من ناحية runtime لأنها تصف ما ينفذه المتصفح فعليًا.

لكن لا يجوز تفسير `RAWAEA_P135_TARGET` وحده على أنه إثبات أن Git source هو P135، لأن Current source الحالي يحتفظ بذلك marker لأغراض continuity.

الدليل الفارق هو أن Current source يحتوي P136 shared-client marker، بينما runtime evidence لا يظهره ويظهر بدلًا منه `MAIN11_SUPABASE_UNAVAILABLE`.

إذًا يوجد تعارض حقيقي بين:

`CURRENT GIT TARGET`
و
`SERVED BROWSER RUNTIME`

## 9. Why the 401 Is Not a Justified Supabase Credential Repair

Production Supabase ليس متوقفًا، والـowner identity والـlicense contract سليمين مباشرة.

كما أن سجل الحالة السابق يذكر وجود Auth requests ناجحة من runtime آخر.

مع ذلك، يجب التمييز بين:

`Supabase service healthy`

وبين:

`this browser artifact using the correct current key`

وجود `401 Invalid API key` في browser لا يبرر تدوير المفتاح طالما أن Current source يحمل configuration معتمدة، ولا يوجد دليل أن الـserved artifact هو نفس هذا المصدر.

لذلك:

`NO CREDENTIAL ROTATION`

## 10. Cloudflare / Deployment Evidence

تمت محاولة التحقق من runtime URL عبر قناة web المتاحة. القراءة المباشرة لـ`/companies/sw.js` لم توفر artifact صالحًا للتحليل وأعادت `Cache miss` من القناة المتاحة هنا.

تم أيضًا البحث عن Cloudflare/Wrangler plugin متاح للتثبيت، ولم يتم العثور على plugin قابل للاستخدام.

وبذلك لا توجد في الجلسة الحالية قناة شرعية تمكّننا من إثبات:

- deployment ID
- deployed commit
- Pages project output root
- artifact identity
- `/companies/` path mapping
- whether `_headers` was included

هذه ليست حجة لتغيير source؛ بل هي `EXTERNAL EXECUTION / LINEAGE BLOCKER`.

## 11. Failed / Rejected Approaches

### 11.1 Source guard repair loop

P135 أصلح guard أوليًا لكنه لم يحل scope separation.

### 11.2 Scope-only repair

P136 أصلح JavaScript scope عبر shared client، وهو موجود فعليًا في Current Git.

### 11.3 Treating Git SW as proof of URL availability

P137 أثبت source finalization فقط. وجود `Current/PWA/sw.js` لا يثبت أن `/companies/sw.js` موجود في Production.

### 11.4 Credential rotation

تم رفضها لأن Production service فعال، والـcurrent configuration لا يوجد دليل مباشر أنه خاطئ.

### 11.5 Duplicate `/companies/sw.js`

تم رفض إنشاء نسخة موازية لأن deployment root لم يُثبت، ولأن ذلك يخالف P139 surgical decision ويخلق shadow path.

### 11.6 New JavaScript rewrite

تم رفض إعادة تعديل `New-main` لأن current source يحتوي الإصلاح الحالي بالفعل، والمشكلة الظاهرة في runtime هي artifact/publication mismatch.

### 11.7 Direct Git clone from execution container

فشل بسبب DNS/network restriction للبيئة (`Could not resolve host: github.com`). تم تجاهله كقيد بيئي والاعتماد على GitHub connector.

## 12. Root Cause Classification

### Primary confirmed operational blocker

`DEPLOYMENT_ARTIFACT_MISMATCH / RUNTIME_LINEAGE_OPEN`

### Secondary source-level inconsistency requiring deployment-root evidence

`SERVICE_WORKER_RELATIVE_PATH ↔ /companies/sw.js`

### Not root causes in current evidence

- Supabase outage
- Owner wildcard loss
- Owner license inactive
- P136 source regression
- Missing P137 source

## 13. Surgical Repair Decision

في هذه الجولة **لا يوجد source patch جديد معتمد**.

الـsurgical action الصحيح هو:

```text
PUBLISH EXACT CURRENT TARGET
+
PROVE URL → ARTIFACT MAPPING
+
THEN TEST RUNTIME
```

وبالتحديد:

1. نشر `Current/PWA/New-main` إلى الـURL الذي يستخدمه المستخدم فعليًا كـmain application artifact.
2. نشر `Current/PWA/sw.js` في الموضع الذي يطابق deployment root الفعلي.
3. التأكد من نشر `Current/PWA/_headers` إذا كان output root يعتمد عليه.
4. بعد النشر فقط يتم الحكم على ما إذا كان registration `../sw.js` صحيحًا أو يحتاج تعديلًا.

لا يجوز اختيار `/sw.js` أو `/companies/sw.js` بالتخمين قبل إثبات output root.

## 14. Exact Manual Publication Set

بسبب أن المستخدم أوضح أن `Current/PWA` هو النسخة المطابقة للملفات المنشورة يدويًا، فإن الملفات التي يجب أن تدخل تجربة النشر الحالية هي، دون تغيير محتواها في هذه الجولة:

```text
Current/PWA/New-main
Current/PWA/sw.js
Current/PWA/manifest.json
Current/PWA/_headers
```

ولا يتم إضافة:

```text
Current/PWA/companies/sw.js
```

ولا تدوير مفاتيح Supabase.

ولا تعديل `Current/PWA/main.html`.

## 15. Required Post-Publication Verification

بعد النشر، لا يكفي فتح الصفحة فقط. يجب إثبات التالي في نفس جلسة browser نظيفة:

```text
1. /companies/company-1/main.html → current target artifact
2. P136 shared-client marker present
3. MAIN11_SUPABASE_UNAVAILABLE absent
4. Supabase login succeeds
5. getSession succeeds
6. tenant/company context resolves
7. dashboard loads
8. owner/license tab appears
9. Service Worker request returns HTTP 200
10. Service Worker body contains RAWAEA_SW_P137_FINAL
11. Effective registration/scope matches manifest contract
```

إذا فشل بند 1 أو 10، لا نعود لتعديل business logic.

## 16. Production Closure Conditions

لا يوجد `CLOSED` بعد.

إغلاق هذه الوحدة يتطلب evidence object يثبت على الأقل:

- deployed artifact identity
- URL mapping
- P136 runtime execution
- successful auth
- P137 SW availability
- owner/license tab visibility

## 17. Mistake Register — P140

الخطأ الذي تم تصحيحه في هذه الجولة هو **عدم الخلط بين runtime symptoms وsource defect**.

كان من السهل أن يتحول `404 /companies/sw.js` إلى إنشاء duplicate file أو تحويل registration path فورًا. لكن هذا كان سيعيد الخطأ التاريخي: إصلاح مسار مجهول قبل إثبات deployment root.

كما أن محاولة استعلام `users.is_active` كانت غير صحيحة لأن schema الفعلي يستخدم `users.status`. تم تصحيح ذلك بعد قراءة `information_schema` ولم ينتج عن الخطأ أي mutation.

الدرس التشغيلي:

`READ SCHEMA → VERIFY → THEN ACT`

## 18. Final Status

```text
MASTER_CONTINUITY_READ            = VERIFIED
CURRENT_STATE_READ                = VERIFIED
REPORT_CHAIN_PRESERVED             = VERIFIED
CURRENT_NEW_MAIN_P136              = VERIFIED
CURRENT_SW_P137                    = VERIFIED
SUPABASE_PRODUCTION                = HEALTHY
OWNER_WILDCARD                     = VERIFIED
OWNER_LICENSE                      = ACTIVE
BROWSER_RUNTIME                    = FAILING
LOGIN_401_INVALID_API_KEY         = OPEN
MAIN11_UNAVAILABLE                = OPEN IN SERVED RUNTIME
COMPANIES_SW_404                   = OPEN
CLOUDFLARE_DEPLOYMENT_ID           = UNKNOWN
CLOUDFLARE_ARTIFACT_ID             = UNKNOWN
DEPLOYMENT_OUTPUT_ROOT             = UNKNOWN
URL_TO_ARTIFACT_MAPPING            = UNKNOWN
SOURCE_PATCH_AUTHORIZED             = NO
MANUAL_PUBLICATION_REQUIRED        = YES
GOLD                              = UNPROVEN
DIAMOND                           = UNPROVEN
CLOSED_100_PERCENT                 = NO
```

## 19. Next Authorized Action

تنفيذ تجربة النشر اليدوي للملفات الأربعة المحددة في القسم 14، ثم إرسال أدلة runtime الجديدة من نفس الـURL.

لا يتم تعديل أي ملف JavaScript أو مفتاح Supabase قبل أن تظهر الأدلة الناتجة عن النشر.
