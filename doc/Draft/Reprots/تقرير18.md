# تقرير18 — P157 / التحقق الجنائي من تنفيذ إصلاحات New-main

**التاريخ:** 2026-09-02
**النطاق:** `Current/PWA/New-main` بعد آخر تعديل منشور
**الحالة:** FORENSIC VERIFICATION COMPLETE / ONE STRUCTURAL CORRECTION REQUIRED / NO FULL ORIGINAL MERGE

---

## 1. منهج الحسم

تم تطبيق Master Continuity Command كما هو:

```text
CURRENT_STATE
→ LAST VERIFIED EVENT
→ CURRENT GIT
→ CURRENT PRODUCTION
→ CURRENT FILE
→ RECONCILE
→ CLASSIFY
```

لم يتم اعتماد `CURRENT_STATE.md` وحده؛ تم مقارنته مباشرة مع أحدث Git commit وملف `Current/PWA/New-main` الحالي.

المصادر المباشرة:

- `doc/Draft/medhat/MASTER - RAWAEA ERP.md`
- `CURRENT_STATE.md`
- `doc/Draft/Reprots/تقرير17.md`
- `doc/Draft/Reprots/تقرير16.md`
- `Current/PWA/New-main`
- Git commit `70aaad76c53b6d8fae045e5868938e718b30ee13`

---

# 2. آخر Verified Event

أحدث commit فعلي متعلق بالهدف هو:

```text
70aaad76c53b6d8fae045e5868938e718b30ee13
Update New-main
2026-09-02 07:18:06 +03:00
```

وهذا يتجاوز زمنيًا الـcommit السابق المذكور في P156/P157 state.

الـcommit يثبت أنه غيّر `Current/PWA/New-main` وأضاف حزم الإصلاح الأربع التي كان `تقرير17.md` قد أعدها يدويًا.

---

# 3. نتيجة مراجعة الحزم الأربع

## SP-NEW-01 — Notifications

### الحكم

```text
APPLIED
```

المصدر الحالي يحتوي:

```text
قراءة الكل
RW_Notification.markAllRead()
RW_Notification._clickNotif(...)
reference_table
reference_id
```

كما أن الـpanel الحالي لم يعد يستخدم `showPanel()` التاريخي البسيط.

### لا يوجد Patch إضافي مطلوب هنا

---

## SP-NEW-02 — Audit

### الحكم

```text
APPLIED
```

المصدر الحالي يحتوي:

```text
RW_AUDIT_PAGE
RW_AUDIT_PAGE_SIZE = 50
RW_AUDIT_TOTAL
count: 'exact'
search
action filter
date from
date to
numbered pagination
previous / next
detail action
old_data
new_data
```

والـassignment الحالي لـ`RW_Audit_renderTab` محفوظ ضمن المصدر.

### لا يوجد Patch إضافي مطلوب هنا

---

## SP-NEW-03 — Password Visibility

### الحكم

```text
FUNCTIONALLY APPLIED
STRUCTURALLY DEFECTIVE
```

تمت إضافة:

```html
<button ... onclick="window.togglePasswordVisibility('rw-password', this)">
```

وهذا يحقق الـUI hook المطلوب، كما أن helper موجود أصلًا في JavaScript.

لكن Commit `70aa...` أضاف قواعد CSS نفسها داخل `<body>` وداخل `<form>` كنص خام، مثل:

```text
</div>.rw-password-wrap{...}
.rw-password-wrap .rw-input{...}
.rw-password-toggle{...}
```

ثم أضيف أيضًا:

```text
.rw-login-footer{...}
```

خارج `<style>`.

إذن هذا ليس CSS صالحًا في موضعه الحالي؛ بل Text Node داخل صفحة الدخول.

### النتيجة

```text
Password toggle HTML = PRESENT
Password toggle helper = PRESENT
Password toggle CSS placement = WRONG
SP-NEW-03 = NOT CLOSED
```

---

## SP-NEW-04 — Login Footer

### الحكم

```text
FUNCTIONALLY PRESENT
CSS PLACEMENT DEFECTIVE
```

الـfooter نفسه موجود داخل form:

```html
<div class="rw-login-footer">منصة الروائع ERP — إدارة المبيعات والمخزون والتوزيع والحسابات من نظام واحد.</div>
```

لكن قواعد `.rw-login-footer` موجودة داخل body كنص خام بدل وضعها داخل `<style>`.

### النتيجة

```text
Footer element = PRESENT
Footer CSS placement = WRONG
SP-NEW-04 = NOT CLOSED
```

---

# 4. التصحيح الوحيد المطلوب

لا تعِد تنفيذ SP-NEW-01 أو SP-NEW-02.

لا تعِد كتابة Module كاملة.

لا تستبدل New-main بـOriginal.

لا تضف helper جديدة.

في `Current/PWA/New-main` يجب تنفيذ الآتي فقط:

## A) حذف النص الخام من body

احذف بالكامل هذا الجزء المحدد:

```text
</div>.rw-password-wrap{position:relative;width:100%}
.rw-password-wrap .rw-input{padding-left:52px}
.rw-password-toggle{position:absolute;left:8px;top:50%;transform:translateY(-50%);width:38px;height:38px;border:0;border-radius:10px;background:transparent;color:#64748b;display:grid;place-items:center;cursor:pointer;font-size:17px}
.rw-password-toggle:hover{background:#f1f5f9;color:#2563eb}
```

ويصبح بعد إغلاق مجموعة كلمة المرور مباشرة:

```html
</div>
<div class="rw-login-options">
```

## B) حذف CSS الخام الخاص بالFooter من body

احذف بالكامل:

```text
.rw-login-footer{margin-top:4px;text-align:center;color:#64748b;font-size:11px;font-weight:700;line-height:1.8}
```

من body.

ويبقى عنصر الـfooter نفسه دون تغيير:

```html
<div id="rw-login-error" class="rw-notice rw-notice-bad hidden"></div><div class="rw-login-footer">منصة الروائع ERP — إدارة المبيعات والمخزون والتوزيع والحسابات من نظام واحد.</div></form>
```

## C) إضافة CSS داخل `<style>` الموجود في `<head>`

قبل `.rw-login-options` أضف بالضبط:

```css
.rw-password-wrap{position:relative;width:100%}
.rw-password-wrap .rw-input{padding-left:52px}
.rw-password-toggle{position:absolute;left:8px;top:50%;transform:translateY(-50%);width:38px;height:38px;border:0;border-radius:10px;background:transparent;color:#64748b;display:grid;place-items:center;cursor:pointer;font-size:17px}
.rw-password-toggle:hover{background:#f1f5f9;color:#2563eb}
.rw-login-footer{margin-top:4px;text-align:center;color:#64748b;font-size:11px;font-weight:700;line-height:1.8}
```

هذه هي **العملية الوحيدة** المطلوبة في New-main في هذه الجولة.

---

# 5. ما لا يجب تغييره

```text
RW_Notification module           = DO NOT REWRITE
markAllRead                      = DO NOT DUPLICATE
_clickNotif                      = DO NOT DUPLICATE
RW_Audit module                  = DO NOT REWRITE
RW_Permissions                   = DO NOT CHANGE
RW_Workflow                      = DO NOT CHANGE
Authentication                   = DO NOT CHANGE
Owner semantics                  = DO NOT CHANGE
RW_Table                         = DO NOT CHANGE
Original/main1                   = DO NOT COPY
```

---

# 6. أخطاء هذه الجولة التي يجب حفظها

## F-06 — تنفيذ تعليمات CSS داخل HTML

الخطأ لم يكن في منطق الإصلاح، بل في موضع إدخال CSS أثناء تنفيذ SP-NEW-03/04.

الدرس:

```text
HTML BODY
≠
STYLE BLOCK
```

وجود CSS كنص داخل الـDOM لا يعني أن CSS تم تطبيقه.

## F-07 — CURRENT_STATE أصبح stale بعد تعديل New-main

Git تقدم إلى:

```text
70aaad76c53b6d8fae045e5868938e718b30ee13
```

بينما `CURRENT_STATE.md` ما زال يصف SP-NEW-01..04 كإجراءات لم ينفذها المالك.

إذن `CURRENT_STATE` يحتاج reconciliation فور هذه الجولة.

---

# 7. Production / Runtime Status

هذا التقرير يثبت المصدر المنشور في Git فقط.

لا توجد في هذه الجولة browser/Cloudflare proof جديدة تثبت أن الـlive served artifact صار مساويًا لهذا commit.

لذلك:

```text
CURRENT GIT                  = VERIFIED
TARGET SOURCE STRUCTURE      = VERIFIED EXCEPT ONE CSS PLACEMENT DEFECT
PRODUCTION RUNTIME          = NOT PROVEN FROM THIS ROUND
CLOUDFLARE SERVED ARTIFACT   = NOT PROVEN
```

لا يُسمح بتحويل Git PASS إلى Runtime PASS.

---

# 8. Self-Audit

```text
MASTER RECOVERY                 = VERIFIED
CURRENT_STATE REVIEW             = VERIFIED
LATEST REPORT                    = VERIFIED
LATEST TARGET COMMIT             = VERIFIED
TARGET FILE                      = OPENED DIRECTLY
SP-NEW-01                        = APPLIED
SP-NEW-02                        = APPLIED
SP-NEW-03                        = APPLIED / CSS STRUCTURE WRONG
SP-NEW-04                        = APPLIED / CSS STRUCTURE WRONG
ORIGINAL MERGE                   = REJECTED
TARGET FULL REPLACEMENT          = NONE
HISTORICAL REPORTS DELETED       = NO
PRODUCTION MODIFIED              = NO
CLOUDFLARE RUNTIME VERIFIED      = NO
CRITICAL UNKNOWN ABOUT REQUIRED PATCH = NO

CONFIRMED:
- The four intended UI changes were inserted by commit 70aa...
- Notification functionality is connected.
- Audit functionality is connected.
- Password toggle element is connected.
- Login footer element is connected.
- CSS for the last two was inserted into the wrong layer.

UNVERIFIED:
- Live Cloudflare served artifact after 70aa...
- Browser behavior after CSS relocation.

FINAL STATUS:
- New-main = NOT YET 100% CLOSED
- Required correction = ONE SURGICAL CSS MOVE
```

---

# 9. Next Authorized Action

```text
Remove the raw CSS from BODY
↓
Place the exact CSS block inside existing <style>
↓
Commit New-main
↓
Re-open New-main
↓
Verify no raw .rw-password / .rw-login-footer CSS remains in BODY
↓
Verify notification + audit patches remain intact
↓
Update CURRENT_STATE
↓
Then proceed to runtime verification
```

لا توجد حاجة لإعادة تنفيذ أي جزء آخر من تقرير17.
