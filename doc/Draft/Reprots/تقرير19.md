# تقرير19 — P158 / التحقق الجنائي النهائي من New-main بعد تصحيح CSS

**التاريخ:** 2026-09-02  
**النطاق:** `Current/PWA/New-main` فقط، مع مراجعة سلسلة الاستمرارية وProduction ذات الصلة  
**الحالة:** FORENSIC VERIFICATION COMPLETE / ONE STRUCTURAL HTML DEFECT REMAINS / NO FALSE CLOSURE

---

## 1. هدف الجولة

هذه الجولة ليست بداية جديدة.

تم تنفيذ استرجاع الذاكرة من المصادر المباشرة، ثم مراجعة:

- `doc/Draft/medhat/MASTER - RAWAEA ERP.md`
- `CURRENT_STATE.md`
- `doc/Draft/Reprots/تقرير16.md`
- `doc/Draft/Reprots/تقرير17.md`
- `doc/Draft/Reprots/تقرير18.md`
- أحدث Git history للملف `Current/PWA/New-main`
- الملف المنشور الحالي `Current/PWA/New-main`
- Production Supabase migration lineage والحالة الحالية ذات الصلة

قاعدة الحسم:

```text
CURRENT REALITY
>
CURRENT GIT
>
CURRENT PRODUCTION
>
CURRENT DEPLOYMENTS / RUNTIME
>
CURRENT DATABASE CONTRACTS
>
CURRENT VERIFIED ARTIFACTS
>
HISTORICAL CONTRACTS
>
REPORTS
>
MEMORY
>
ASSUMPTIONS
```

---

# 2. Master Continuity Recovery

تمت إعادة قراءة `MASTER - RAWAEA ERP.md` من بدايته حتى نهايته.

أهم القواعد التنفيذية التي تحكم هذه الجولة:

```text
CURRENT_STATE
→ LAST VERIFIED EVENT
→ CURRENT GIT
→ CURRENT PRODUCTION
→ ACTIVE DEPLOYMENTS / RUNTIME
→ RECONCILE
→ IDENTIFY REAL CURRENT TARGET
→ RESOLVE UNKNOWN
→ SURGICAL CHANGE
→ VERIFY
→ UPDATE CURRENT_STATE
```

كما يفرض الـMaster:

```text
UNKNOWN ≠ BUG
UNKNOWN ≠ REMOVE

Git PASS ≠ Production PASS
Staging PASS ≠ Production PASS

لا توجد Closure بدون Evidence Object كامل.

بعد كل تغيير حقيقي:
ACTION → VERIFY → CURRENT_STATE UPDATE → NEXT ACTION
```

هذه الجولة التزمت بذلك ولم تعتبر التقرير السابق مصدر الحقيقة الحالي.

---

# 3. Latest Verified New-main Event

المسار:

```text
Current/PWA/New-main
```

أحدث commit فعلي للملف هو:

```text
Commit = c3871ef49e5ed40c550f23359d75c1e380093dbd
Message = Update New-main
UTC = 2026-09-02 04:31:33
Parent = e526f7b69383a553d7a299e3c293baa03bf64a86
```

Git verified blob identity الحالية:

```text
SHA = 963e8a6b498ad4544997339ce5ffbd74b332cb64
```

وهذا يتجاوز الحد التاريخي السابق:

```text
70aaad76c53b6d8fae045e5868938e718b30ee13
```

الحكم:

```text
CURRENT TARGET IDENTITY = RE-VERIFIED
CURRENT TARGET COMMIT   = c3871ef...
CURRENT TARGET BLOB     = 963e8a6...
```

---

# 4. What the latest owner-side correction actually changed

الـcommit `c3871ef...` صحح جزءًا مهمًا من الخطأ الذي سجله تقرير18:

## 4.1 Password CSS

أضيفت داخل `<style>`:

```css
.rw-password-wrap{position:relative;width:100%}
.rw-password-wrap .rw-input{padding-left:52px}
.rw-password-toggle{position:absolute;left:8px;top:50%;transform:translateY(-50%);width:38px;height:38px;border:0;border-radius:10px;background:transparent;color:#64748b;display:grid;place-items:center;cursor:pointer;font-size:17px}
.rw-password-toggle:hover{background:#f1f5f9;color:#2563eb}
```

## 4.2 Footer CSS

أضيفت داخل `<style>`:

```css
.rw-login-footer{margin-top:4px;text-align:center;color:#64748b;font-size:11px;font-weight:700;line-height:1.8}
```

## 4.3 Raw CSS in BODY

المراجعة المباشرة للملف الحالي تثبت أن نصوص CSS الخام التي كانت موجودة داخل login body في `70aa...` لم تعد موجودة هناك.

إذن:

```text
CSS relocation = APPLIED
Raw CSS body text = REMOVED
```

وهذا إصلاح حقيقي لنقطة تقرير18.

---

# 5. Structural defect discovered after the correction

رغم نجاح نقل CSS، توجد الآن مشكلة HTML بنيوية دقيقة.

التركيب الحالي في login form هو فعليًا:

```html
<div class="rw-form-group">
  <label ...>كلمة المرور</label>
  <div class="rw-password-wrap">
    ...
    <button ... onclick="window.togglePasswordVisibility('rw-password', this)">...</button>
  </div>
<div class="rw-login-options">
```

المطلوب حسب العقد كان:

```html
<div class="rw-form-group">
  <label ...>كلمة المرور</label>
  <div class="rw-password-wrap">
    ...
  </div>
</div>
<div class="rw-login-options">
```

إذن هناك `</div>` مفقودة بعد إغلاق `.rw-password-wrap` مباشرة.

## الحكم

```text
Password toggle HTML              = PRESENT
Password toggle helper             = PRESENT
Password CSS                       = CORRECTLY IN <style>
Login footer element               = PRESENT
Footer CSS                         = CORRECTLY IN <style>
Required password form-group close = MISSING
```

هذا ليس نفس خطأ CSS السابق، لكنه يمنع المطابقة البنيوية الكاملة مع التصحيح المطلوب.

---

# 6. SP-NEW-01 — Notifications

المراجعة المباشرة لتغييرات commit `c3871ef...` تثبت استمرار الحزمة.

الموجود:

```text
RW_Notification
markAllRead()
قراءة الكل
_clickNotif(...)
reference_table
reference_id
```

والـpanel يعرض العناصر القابلة للنقر مع reference mapping.

الحكم:

```text
SP-NEW-01 = APPLIED / PRESERVED
```

لا يوجد سبب لإعادة كتابة الـmodule.

---

# 7. SP-NEW-02 — Audit

المراجعة المباشرة لتغييرات commit `c3871ef...` تثبت استمرار:

```text
RW_AUDIT_PAGE
RW_AUDIT_PAGE_SIZE = 50
RW_AUDIT_TOTAL
count = exact
search
اتخاذ filter
from date
to date
numbered pagination
previous / next
detail action
old_data
new_data
```

كما أن guard الـOwner ما يزال موجودًا.

الحكم:

```text
SP-NEW-02 = APPLIED / PRESERVED
```

لا يوجد سبب لإعادة كتابة `RW_Audit` أو `RW_Table`.

---

# 8. Authentication / Owner / Tenant preservation

المراجعة الحالية تثبت عدم استبدال عقد Current بعقد Original التاريخي.

الملف الحالي ما يزال يعتمد على:

```text
users.auth_id
users.company_id
companies.id
owner_profile
Owner semantics
currentCompanyId()
```

والـOwner logic لا يزال قائمًا على سياق المستخدم الحالي وليس على نسخ Legacy `permissions=['*']` العمياء.

الحكم:

```text
AUTH CONTRACT   = PRESERVED
OWNER CONTRACT  = PRESERVED
TENANT CONTEXT  = PRESERVED
```

---

# 9. Historical mistakes retained

هذا التقرير لا يخفي الأخطاء السابقة.

## F-08 — CSS moved but form wrapper closure omitted

أثناء استكمال التصحيح، تم نقل CSS بنجاح من body إلى `<style>`, لكن البنية الناتجة فقدت إغلاق `.rw-form-group`.

الدرس:

```text
Correct CSS placement
≠
Correct HTML structure
```

ويجب دائمًا التحقق من:

```text
DOM nesting
closing tags
parent/child ownership
```

بعد إصلاحات HTML/CSS.

## F-07 — stale CURRENT_STATE

استمر `CURRENT_STATE.md` في الإشارة إلى `70aa...` بعد تقدم Git إلى `c3871ef...`.

تم تصحيح ذلك في سجل الحالة بهذه الجولة.

---

# 10. Production Inventory continuity observed during this round

هذه الجولة لا تعيد فتح Inventory Core كهدف رئيسي، لكن استعادة الحالة المباشرة أكدت وجود lineage إنتاجي حديث في Supabase يشمل إصلاحات Inventory/RPC السابقة، ومنها:

```text
20260818224310 / 20260819014000_close_manual_voucher_create_tenant_and_item_identity
20260818224503 / 20260819015500_close_receive_purchase_idempotency_and_tenant_scope
20260818224518 / 20260819020000_fix_receive_purchase_runtime_definition
20260818224636 / 20260819021000_order_receive_purchase_idempotency_before_quantity_guard
20260818225051 / 20260819022500_receive_purchase_explicit_operation_identity
20260818225202 / 20260819024000_harden_receive_purchase_payload_identity
20260818225304 / 20260819030000_fix_inventory_adjustment_global_item_identity
20260818225616 / 20260819031000_close_legacy_receive_v2_writer
20260819010619 / 20260819042000_centralize_return_delivery_operations
20260819025532 / retire_legacy_receive_purchase_atomic_4arg
20260819031120 / add_sales_order_operation_identity
20260819040708 / retire_post_stock_movement_legacy_overload
20260819050353 / 20260819_inventory_write_boundary_zero_debt
```

كما ما زال العقد المرجعي:

```text
PHYSICAL STOCK MOVEMENT
        ↓
post_stock_movement
        ↓
stock_branches + inventory_log
```

ولا تعتبر هذه القائمة دليل إغلاق Inventory Core؛ هي فقط Current Production lineage تمت قراءتها مباشرة.

---

# 11. Production data truth

آخر direct Production measurements في هذه الاستمرارية ما زالت تتطلب فصل anomaly عن contract قبل أي cleanup:

```text
stock_branches branch/item-company mismatches = 143
inventory_log item/company mismatches         = 86
order_details item/company mismatches         = 6
```

لم يتم حذف أو إعادة كتابة هذه البيانات في هذه الجولة لأن ذلك يخالف:

```text
DETECT
→ TRACE SOURCE
→ TRACE HISTORY
→ TRACE BUSINESS IMPACT
→ TRACE DOWNSTREAM IMPACT
→ DECIDE
→ PRESERVE AUDIT
→ SURGICAL REPAIR
→ VERIFY
```

وهذا مسار مستقل عن مراجعة `New-main` الحالية.

---

# 12. Runtime status

لم يتم إثبات في هذه الجولة:

```text
Cloudflare served artifact parity
Browser runtime after c3871ef...
Live password toggle behavior
Live footer rendering
```

لذلك:

```text
CURRENT GIT           = VERIFIED
SOURCE CSS FIX        = VERIFIED
SOURCE STRUCTURE      = NOT FULLY CLOSED
CLOUDFLARE RUNTIME    = UNPROVEN
BROWSER RUNTIME       = UNPROVEN
```

لا يجوز تحويل Source PASS إلى Runtime PASS.

---

# 13. Final classification

```text
SP-NEW-01 Notifications       = CLOSED AT SOURCE / RUNTIME OPEN
SP-NEW-02 Audit               = CLOSED AT SOURCE / RUNTIME OPEN
SP-NEW-03 Password Visibility = PARTIALLY CLOSED / ONE HTML CLOSURE DEFECT
SP-NEW-04 Login Footer        = SOURCE PRESENT / ONE SHARED HTML CLOSURE DEFECT
```

### Overall New-main state

```text
TARGET FILE                = Current/PWA/New-main
TARGET BLOB                = 963e8a6b498ad4544997339ce5ffbd74b332cb64
LATEST COMMIT              = c3871ef49e5ed40c550f23359d75c1e380093dbd
CSS RELOCATION             = VERIFIED
RAW BODY CSS               = REMOVED
PASSWORD TOGGLE HTML       = VERIFIED
PASSWORD HELPER            = VERIFIED
LOGIN FOOTER               = VERIFIED
NOTIFICATION PATCH         = VERIFIED
AUDIT PATCH                = VERIFIED
HTML NESTING               = ONE CLOSURE ERROR
CLOUDFLARE RUNTIME         = OPEN
BROWSER RUNTIME            = OPEN
100_PERCENT_CLOSED         = NO
```

---

# 14. Exact next authorized action

لا توجد حاجة لإعادة تنفيذ SP-NEW-01 أو SP-NEW-02.

الإجراء التالي الوحيد على `New-main` هو:

```text
Add exactly one missing </div>
after the closing </div> of .rw-password-wrap
and before .rw-login-options
```

ثم:

```text
Re-open target file
→ verify DOM nesting
→ verify no raw CSS remains in BODY
→ verify SP-NEW-01
→ verify SP-NEW-02
→ verify commit/blob
→ then perform runtime verification
→ only then reassess closure
```

ممنوع في الخطوة التالية:

```text
Replace New-main with Original
Rewrite the module
Rewrite RW_Table
Touch Permissions
Touch Authentication
Touch Owner semantics
Touch Inventory Core
```

---

# 15. SELF-AUDIT

```text
MASTER RECOVERY                 = VERIFIED
CURRENT_STATE                   = REVIEWED
REPORT CHAIN                    = REVIEWED
LATEST TARGET COMMIT            = VERIFIED
LATEST TARGET BLOB              = VERIFIED
TARGET FILE                     = OPENED DIRECTLY
SP-NEW-01                       = VERIFIED
SP-NEW-02                       = VERIFIED
SP-NEW-03                       = SOURCE PARTIAL / ONE STRUCTURAL DEFECT
SP-NEW-04                       = SOURCE PARTIAL / ONE STRUCTURAL DEFECT
HISTORICAL REPORTS DELETED      = NO
PRODUCTION DATA MODIFIED        = NO
CLOUDFLARE RUNTIME VERIFIED     = NO
BROWSER RUNTIME VERIFIED        = NO
CRITICAL UNKNOWN                = NONE ABOUT THE REQUIRED HTML FIX
FALSE CLOSURE CLAIM             = REJECTED
FINAL CLOSURE                   = NOT CLOSED
```

---

# 16. Final verdict

التصحيح الذي تم على `New-main` ليس فاشلًا؛ بل نجح في إزالة الخطأ الأصلي الأهم الخاص بوضع CSS داخل body، وحافظ على Notification وAudit.

لكن الملف الحالي **ليس مطابقًا بالكامل للتصحيح المطلوب** بسبب خطأ HTML واحد مثبت بالدليل المباشر:

```text
missing </div>
```

وبالتالي الحكم المهني الصحيح هو:

```text
NEW-MAIN SOURCE VERIFICATION = NOT YET CLOSED
```

ولا يوجد أي أساس صادق لإعلان:

```text
100%
CLOSED
PRODUCTION READY
RUNTIME VERIFIED
```

قبل إصلاح هذا الإغلاق ثم إثبات runtime.
