# تقرير21 — P160 / إعادة التحقيق الجنائي — Original main2 مقابل Current New-main

**التاريخ:** 2026-09-02  
**النطاق:** `Original/PWA/main/main2.md` مقابل `Current/PWA/New-main`، مع استعادة الاستمرارية من `MASTER - RAWAEA ERP.md` + سلسلة التقارير السابقة + `CURRENT_STATE.md` + Git الحالي + الأدلة المباشرة من Production ذات الصلة.  
**سياسة التعديل:** `Current/PWA/New-main` = READ ONLY في هذه الجولة. المالك هو الذي يطبق الجراحة على الملف النهائي.  
**الحالة:** FORENSIC REVALIDATION COMPLETE / P159 CORRECTED WHERE SUPERSEDED / DUPLICATE MODULE OWNERSHIP PROVEN / SURGICAL INSTRUCTIONS READY

---

## 1. قاعدة الحسم

تم التعامل مع هذه الجولة باعتبارها استكمالًا لحالة قائمة، لا بداية جديدة.

ترتيب الحسم:

```text
MASTER GOVERNANCE
→ CURRENT_STATE
→ LAST VERIFIED EVENT
→ CURRENT GIT
→ CURRENT PRODUCTION EVIDENCE
→ CURRENT New-main DIRECT SOURCE
→ ORIGINAL main2 DIRECT SOURCE
→ HISTORICAL REPORTS
→ ACTUAL GAP
```

القواعد الحاكمة:

```text
UNKNOWN ≠ BUG
UNKNOWN ≠ REMOVE
Git/source verified ≠ runtime verified
Runtime verified ≠ Production data repaired
لا نسخ كامل من Original فوق Current
لا حذف بلا إثبات consumer safety
```

---

## 2. استرجاع الحالة السابقة

السجل السابق P159 / تقرير20 كان قد خلص إلى أن `RW_Dashboard` و`RW_Items` موجودان في Current، لكنه سجل Shadowing بين Renderers وRouter/Global aliases.

المراجعة الحالية للمصدر نفسه كشفت أن الصورة الحالية أكثر تعقيدًا:

```text
Current New-main
    ├── MAIN2 COMPATIBILITY block
    │      ├── RW_Dashboard definition #1
    │      └── RW_Items definition #1
    │
    ├── MAIN2 AUTHORITATIVE block
    │      ├── RW_Dashboard definition #2
    │      └── RW_Items definition #2
    │
    ├── MAIN3 authoritative reconstruction
    │      └── routes = window.RW_Dashboard / window.RW_Items
    │
    └── MAIN4 / later modules
```

وبالتالي فإن الحكم الأصح حاليًا ليس "لا يوجد إلا Renderer مختصر"، بل:

```text
هناك ازدواج حقيقي لتعريفات Main2
+
هناك بقايا Main1 compatibility navigation
+
وهناك Main3 routing لاحقة تعتمد على globals
```

---

## 3. آخر Git مثبت

```text
Target: Current/PWA/New-main
Latest commit = da5af424360239c0571bf9c118871a635b96f8de
Commit message = Update New-main
Commit UTC = 2026-09-02T04:40:53Z
Blob = fa7c0fcf78a3b217d781fe543b6e5a5ed7411c63
```

كما أن بداية الملف الحالية تحمل timestamp:

```text
2026-09-02 05:20 UTC
```

وهو دليل أن الملف الجاري فحصه يحمل النسخة الحالية نفسها ذات الـblob المذكور أعلاه.

---

## 4. تعريف Original main2

الـOriginal `main2.md` ليس snapshot كاملًا لكل ERP.

النطاق الذي يثبته الملف هو أساسًا:

```text
RW_Dashboard
RW_Items
```

ولا يجوز استخدامه لإثبات فقدان POS أو Purchases أو Finance أو Users لأنها خارج artifact نفسه.

---

# 5. MAIN2 Dashboard — العقد التاريخي

العقد الأصلي المثبت يشمل:

```text
Welcome / Intro
5 KPI cards
Date range filter
Previous-period comparison
Daily sales chart
Region chart
Top 10 items
Top 10 customers
Region → Orders + area filter
Customer → Orders + customer filter
Item → Items navigation
```

والـhelpers:

```text
render
_destroyCharts
loadAll
shiftDate
renderSalesChart
renderRegionChart
renderTopItemsChart
renderTopCustomersChart
```

هذه القدرات ليست مفقودة من Current source.

---

# 6. MAIN2 Items — العقد التاريخي

العقد التاريخي الكامل هو:

```text
render
_openModal
openItemPage
_switchItemTab
_sort
_openCategoryModal
_addCategory
_editCategory
_deleteCategory
_executeDeleteCategory
_handleSaveFromPage
_handleDeleteFromPage
_viewImage
_showBranchStockMovement
_switchSubTab
_applyFilters
_resetFilters
_renderStockMovementReport
_loadMovementReport
_filterMatrix
_exportMatrixToExcel
_handleFileSelect
_executeUpload
```

والقدرات:

```text
Item search
Barcode search
Category filter
Stock status filter
50-row pagination
Branch quantities
Allocated quantities
Stock status classification
Item editor
Image viewer/upload
Category CRUD
Category replacement flow
Movement report
Branch stock matrix
Excel export
CSV/XLSX preview
Bulk stock adjustment
Stock-to-movement drill-down
```

هذه القدرات كذلك ليست مفقودة من Current source.

---

# 7. النتيجة الأساسية الجديدة: المصدر يحتوي تعريفين لكل Main2 module

المراجعة المباشرة لـ`New-main` أثبتت وجود:

```javascript
var RW_Dashboard = (function() {
    ...
})();
```

مرتين.

وأثبتت وجود:

```javascript
var RW_Items = (function() {
    ...
})();
```

مرتين.

والنسخة الأولى تقع تحت marker:

```text
/* RAWAEA MAIN2 COMPATIBILITY */
```

بينما النسخة الثانية تقع تحت:

```text
/* RAWAEA MAIN2 AUTHORITATIVE MODULE */
```

وهذا يعني أن Main2 لم يُفقد؛ بل تم إدخاله مرتين في نفس الملف.

---

# 8. ازدواج RW_Dashboard

## النسخة الأولى — Compatibility

النسخة الأولى تحتوي على Dashboard كاملًا من الناحية الوظيفية، بما في ذلك:

```text
5 KPI
Date range
sales comparison
orders comparison
daily sales
region
items
customers
```

لكنها نسخة Compatibility وليست النسخة التي يجب أن تبقى كمرجع وحيد.

في هذه النسخة توجد أيضًا اختلافات سلوكية عن النسخة اللاحقة، منها أن بعض chart click handlers أبسط ولا تحمل كل تفاصيل drill-down الموجودة في النسخة authoritative.

## النسخة الثانية — Authoritative

النسخة الثانية تقع بعد:

```text
/* RAWAEA MAIN2 AUTHORITATIVE MODULE */
```

وتستخدم tenant helper:

```javascript
window.RW_ShellContext.getCompanyId()
```

وتحمل الـDashboard الكامل، بما في ذلك drill-down الأكثر اكتمالًا.

من المصدر الحالي يتبين أن النسخة الثانية هي المرجع الأقرب إلى Current target contract.

---

# 9. ازدواج RW_Items

## النسخة الأولى — Compatibility / legacy residue

النسخة الأولى تعيد تعريف `RW_Items` مرة ثانية، ولكنها تحمل بوضوح وظائف ناقصة أو Delegation stubs.

الدوال الناقصة/المختصرة كاملةً هي:

```javascript
function openItemPage(itemCode){
    delegated(itemCode?'تعديل الصنف':'إضافة صنف جديد',
      [['📦','./buyer.html','إدارة الصنف','التعديل التفصيلي']],
      'صفحة الصنف الكاملة مملوكة لوحدة الأصناف المتخصصة.');
}

function _handleSaveFromPage(){
    showToast('حفظ بيانات الصنف يتم عبر save-item في المسار المتخصص','info');
}

function _handleDeleteFromPage(){
    showToast('حذف بيانات الصنف يتم عبر delete-item في المسار المتخصص','info');
}

function _openCategoryModal(){
    showToast('إدارة التصنيفات متاحة من وحدة الأصناف المتخصصة','info');
}

function _addCategory(){
    return _openCategoryModal();
}

function _editCategory(){
    return _openCategoryModal();
}

function _deleteCategory(){
    return _openCategoryModal();
}

function _executeDeleteCategory(){
    return _openCategoryModal();
}

function _exportMatrixToExcel(){
    showToast('تصدير المصفوفة متاح من وحدة الأصناف المتخصصة','info');
}

function _handleFileSelect(){
    return null;
}

function _executeUpload(){
    return null;
}
```

هذه الدوال **عيوب مؤكدة داخل نسخة Compatibility** لأنها لا تنفذ عقد Main2 الحقيقي، بينما النسخة authoritative اللاحقة تحتوي implementations حقيقية لهذه المسؤوليات.

الدليل المباشر من Current يثبت الفرق بين النسخة ذات stubs والنسخة اللاحقة التي تحتوي Category CRUD وUpload وExcel وBranch Movement كاملة. fileciteturn1294file0 fileciteturn1294file2

---

# 10. RW_Items authoritative — الحالة الفعلية

النسخة اللاحقة تحمل implementations حقيقية لـ:

```text
_getStockStatus
_loadStockData
render
_renderSubTabs
_switchSubTab
_renderListView
_applyFilters
_resetFilters
_renderTable
_sort
_renderStockMovementReport
_loadMovementReport
_filterMatrix
_renderBranchStockMatrix
_handleFileSelect
_executeUpload
_exportMatrixToExcel
_openCategoryModal
_addCategory
_editCategory
_deleteCategory
_executeDeleteCategory
_showBranchStockMovement
_handleSaveFromPage
_handleDeleteFromPage
```

كما أن النسخة اللاحقة تعرّف:

```javascript
window.RW_Items = RW_Items;
```

وتوجد بعدها مباشرة علامة:

```text
// MAIN2_GOVERNED_CLOSED:v1
```

إذن **لا يجوز استبدال هذه النسخة بنسخة Original**.

---

# 11. Router الحالي — تصحيح حكم P159

P159 كان قد سجل أن:

```text
actions.dashboard → compact renderDashboard
actions.items     → compact renderItems
```

وهذا ما يزال موجودًا في طبقة Main1 compatibility.

لكن المصدر الحالي يحتوي لاحقًا على Router أحدث يبني routes هكذا:

```javascript
var routes={
  dashboard:[window.RW_Dashboard,'render'],
  items:[window.RW_Items,'render'],
  ...
};
```

كما توجد طبقة navigation لاحقة تستخدم globals الخاصة بالموديولات.

بالتالي لا يجوز اعتبار `actions.dashboard` و`actions.items` وحدهما دليلًا كافيًا على أن Main2 runtime ما زال shadowed.

**الحكم المصحح:**

```text
Legacy Main1 router shadow = PRESENT
Main2 authoritative modules = PRESENT
Later Main3 route table = PRESENT
Final live runtime winner = NOT PROVEN
```

وهذا يجعل P159 نفسه سجلًا تاريخيًا صحيحًا بالنسبة للحالة التي تم تحليلها آنذاك، لكنه لا يكفي لإثبات الحالة النهائية الحالية.

---

# 12. Defect 1 — Duplicate Module Ownership

هذا هو العيب الأقوى المثبت الآن.

لدينا:

```text
RW_Dashboard × 2
RW_Items     × 2
```

في نفس `New-main`.

المشكلة ليست تجميلية.

الـJavaScript `var` يسمح بإعادة تعريف الاسم في نفس السياق، والـdefinition الأخير يطغى على السابق.

وبالتالي:

```text
Editing copy #1
≠
Editing runtime copy #2
```

وهذا يخلق خطرًا حقيقيًا بأن يصل إصلاح لاحق إلى نسخة غير مستخدمة، فتعود المشكلة في المستقبل أو يظهر اختلاف بين source review وruntime behavior.

---

# 13. Defect 2 — Legacy Main1 navigation still coexists

لا يزال لدينا:

```javascript
var actions={
    dashboard:renderDashboard,
    ...
    items:renderItems,
    ...
};
```

ويظهر أيضًا assignment قديم:

```javascript
window.RW_Dashboard={render:renderDashboard};
window.RW_Items={render:renderItems};
```

ثم يأتي بعده لاحقًا Main2 authoritative assignment.

هذا ليس حاليًا دليلًا وحده على runtime failure؛ لكنه **governance/maintenance defect** واضح لأنه يترك ownership مزدوجًا للمسارات نفسها.

---

# 14. Defect 3 — Compatibility functions تحتوي false capability surface

النسخة الأولى من Items تعلن API كاملًا ظاهريًا:

```text
_openCategoryModal
_addCategory
_editCategory
_deleteCategory
_executeDeleteCategory
_exportMatrixToExcel
_handleFileSelect
_executeUpload
```

لكن بعض هذه الدوال لا تنفذ العقد الحقيقي، بل تعرض Toast أو تعيد `null`.

هذا أخطر من غياب الدالة؛ لأنه يعطي انطباعًا بوجود capability بينما التنفيذ الحقيقي موجود في Module آخر.

هذا يسمى هنا:

```text
False Capability Surface
```

وهو دين معماري يجب التخلص منه في الجراحة النهائية بدل إضافة implementations ثالثة.

---

# 15. Defect 4 — Compatibility naming لا يوضح ownership كفاية

لدينا markers:

```text
MAIN2 COMPATIBILITY
MAIN2 AUTHORITATIVE
MAIN3 AUTHORITATIVE
MAIN4 AUTHORITATIVE
```

لكن بعض globals نفسها معرّفة مرة ثانية بأسماء مطابقة.

النتيجة:

```text
Contract ownership
= inferred from file order
```

بدل:

```text
Contract ownership
= explicit and unique
```

وهذا غير مناسب لملف PWA أحادي كبير سيستمر تعديله.

---

# 16. ما الذي **ليس** مفقودًا

تم نفي الآتي من source comparison:

```text
Dashboard 5 KPI              = PRESENT
Date filtering               = PRESENT
Previous comparison          = PRESENT
Daily sales chart            = PRESENT
Region chart                 = PRESENT
Top items chart              = PRESENT
Top customers chart          = PRESENT
Dashboard drill-down        = PRESENT
Items search                 = PRESENT
Barcode search               = PRESENT
Stock status                 = PRESENT
Branch quantities            = PRESENT
Item editor                  = PRESENT
Image handling               = PRESENT
Category CRUD                = PRESENT in authoritative copy
Movement report              = PRESENT
Stock matrix                 = PRESENT
Excel export                 = PRESENT in authoritative copy
Upload preview               = PRESENT
Bulk adjustment execution    = PRESENT
```

Therefore:

```text
SOURCE FEATURE LOSS = 0 PROVEN
```

---

# 17. ما الذي **ينبغي اعتباره ناقصًا/معيبًا** في Current

الجدول النهائي:

| ID | العنصر | الحالة | نوع العيب |
|---|---|---|---|
| D1 | `RW_Dashboard` duplicate definition | مثبت | Duplicate Ownership |
| D2 | `RW_Items` duplicate definition | مثبت | Duplicate Ownership |
| D3 | Compatibility Item CRUD stubs | مثبت | False Capability / Incomplete |
| D4 | Compatibility upload/export stubs | مثبت | False Capability / Incomplete |
| D5 | Legacy `actions.dashboard/items` router | مثبت | Coexisting Legacy Surface |
| D6 | Legacy `window.RW_Dashboard/RW_Items` assignment | مثبت | Global Ownership Residue |
| D7 | Final live winner بعد كل navigation layers | غير مثبت | Runtime Unknown |
| D8 | Browser/served artifact current behavior | غير مثبت | Runtime Unknown |

---

# 18. Owner Surgical Plan — لا تعديل من المساعد

## SURGERY-01 — إزالة duplicate Main2 module #1

في `New-main` يوجد marker:

```text
/* RAWAEA MAIN2 COMPATIBILITY */
```

ثم تعريف `RW_Dashboard` و`RW_Items` الأول.

**المطلوب:** إزالة **كتلة Main2 Compatibility الكاملة الخاصة بـRW_Dashboard وRW_Items الأولى فقط**، من marker:

```text
/* RAWAEA MAIN2 COMPATIBILITY */
```

حتى نهاية:

```text
window.RW_PWA_RECONSTRUCTION_VERSION='MAIN2-COMPLETE-SURGICAL-v1';
```

**على ألا يمتد الحذف إلى:**

```text
/* RAWAEA MAIN2 AUTHORITATIVE MODULE */
```

الهدف:

```text
RW_Dashboard = one authoritative definition
RW_Items     = one authoritative definition
```

لا تُحذف النسخة authoritative الثانية.

---

## SURGERY-02 — إزالة Main1 compatibility router residue بعد ثبوت أن Main3 router هو المالك

المقطع المعني هو:

```javascript
var actions={dashboard:renderDashboard,...,items:renderItems,...};
```

ومعه assignment:

```javascript
window.RW_Dashboard={render:renderDashboard};
window.RW_Items={render:renderItems};
```

**المطلوب الجراحي النهائي:** بعد وجود Main3 route ownership، إزالة هذه الـlegacy route/alias surface كاملة بدل إصلاحها بإضافة alias ثالث.

سبب الإزالة:

```text
لا نريد ثلاثة navigation authorities
```

ولا نريد:

```text
Main1 actions
+
Main2 globals
+
Main3 routes
```

في نفس runtime.

---

## SURGERY-03 — لا تُعاد كتابة RW_Items authoritative

لا تنشئ:

```text
RW_Items جديد
```

ولا تنسخ Original main2.

النسخة authoritative الحالية هي النسخة التي تحتوي implementations الحقيقية للـCRUD/upload/export/movement/stock.

---

## SURGERY-04 — لا تستبدل stubs واحدًا واحدًا

هذه الدوال:

```text
_openCategoryModal
_addCategory
_editCategory
_deleteCategory
_executeDeleteCategory
_exportMatrixToExcel
_handleFileSelect
_executeUpload
```

الموجودة داخل النسخة Compatibility **لا تُصحح منفردة**.

السبب:

```text
المشكلة = duplicate obsolete module
```

وليس:

```text
Missing implementation in authoritative module
```

إصلاح كل stub منفردًا سيصنع نسخة ثالثة من Business Logic.

---

# 19. Owner Patch — إن كانت الجراحة تريد الاحتفاظ بالـCompatibility wrapper

الحالة الحالية تجعل الاختيار الأفضل هو الإزالة، لكن لا يجوز حذف wrapper بدون consumer tracing خاص به.

لذلك إن كان هناك consumer legacy مثبت خارج Main1 routing، يكون البديل الجراحي هو تحويل الـexports فقط:

```javascript
window.RW_Dashboard = RW_Dashboard;
window.RW_Items = RW_Items;
```

دون إعادة تعريف module ودون نسخ أي implementation.

أما إذا لم يوجد consumer، فالإزالة الكاملة للـcompatibility block هي الحل الأنظف.

---

# 20. ما نجح وما فشل في هذه الجولة

## نجح

```text
قراءة Master
قراءة CURRENT_STATE
قراءة سلسلة التقارير السابقة
قراءة Original main2
قراءة Current New-main
تثبيت Git identity
إثبات وجود Main2 modules
إثبات duplicate definitions
إثبات compatibility stubs
إثبات وجود Main3 route table
تصحيح الحكم السابق بشأن final runtime winner
```

## فشل / خطأ تحليلي سابق

### خطأ E1
تقرير20 تعامل مع `actions.dashboard/items` والـglobal aliases باعتبارهما الدليل النهائي للـruntime.

الخطأ:

```text
لم يُؤخذ في الحسبان بالكامل أن Current يحتوي لاحقًا على Main2 authoritative block ثم Main3 route table.
```

### خطأ E2
تم استخدام عبارة "مفقود runtime" بصياغة أقرب للحسم من الأدلة المتاحة.

التصحيح الحالي:

```text
Runtime winner = UNKNOWN until live runtime is traced.
```

### خطأ E3
كان من السهل اقتراح تصحيح الـstubs واحدًا واحدًا.

تم رفض ذلك لأن source يثبت أن authoritative copy موجودة بالفعل.

---

# 21. Production / Inventory Continuity

هذه الجولة لم تُجرِ أي تعديل على `New-main`، ولم تُجرِ إصلاح بيانات Production.

تبقى حالة Inventory السابقة منفصلة:

```text
Physical Stock Contract
→ post_stock_movement
→ stock_branches + inventory_log
```

والـProduction anomalies السابقة تظل كما هي في السجل حتى تُعالج في closure مستقل قائم على الدليل.

لا يجوز خلط Inventory Core مع Main2 PWA surgery.

---

# 22. Self-Audit

```text
MASTER READ                         = DONE
CURRENT_STATE READ                  = DONE
REPORT CHAIN REVIEW                 = DONE
ORIGINAL main2 READ                 = DONE
CURRENT New-main READ               = DONE
LATEST GIT IDENTITY                 = VERIFIED
MAIN2 SOURCE PRESENCE               = PROVEN
RW_DASHBOARD DUPLICATE              = PROVEN
RW_ITEMS DUPLICATE                  = PROVEN
COMPATIBILITY STUBS                 = PROVEN
MAIN3 ROUTE TABLE                   = PROVEN
SOURCE FEATURE LOSS                 = 0 PROVEN
FINAL RUNTIME WINNER                = UNKNOWN
BROWSER RUNTIME                     = NOT VERIFIED
CLOUDFLARE/SERVED ARTIFACT          = NOT VERIFIED
TARGET New-main MODIFIED BY ASSISTANT = NO
HISTORICAL REPORTS DELETED          = NO
NEW REPORT CREATED                  = YES
CURRENT_STATE UPDATED               = PENDING THIS COMMIT
100_PERCENT_CLOSURE                  = NO
```

---

# 23. Final Judgment

الحكم الحالي الوحيد المدعوم بالكامل هو:

```text
MAIN2 FEATURE LOSS
= 0 PROVEN

MAIN2 DUPLICATE OWNERSHIP
= PROVEN

COMPATIBILITY STUBS
= PROVEN

LEGACY ROUTER RESIDUE
= PROVEN

FINAL LIVE RUNTIME WINNER
= NOT PROVEN
```

وبالتالي لا يُصحح `renderDashboard` أو `renderItems` بإعادة بناء جديدة، ولا تُنسخ `main2.md` فوق `New-main`.

الجراحة الصحيحة هي:

```text
REMOVE DUPLICATE MAIN2 COMPATIBILITY OWNERSHIP
↓
KEEP MAIN2 AUTHORITATIVE MODULES
↓
REMOVE/NEUTRALIZE LEGACY MAIN1 ROUTER OWNERSHIP
↓
KEEP ONE NAVIGATION AUTHORITY
↓
THEN PERFORM LIVE RUNTIME VERIFICATION
```

هذا هو المسار الذي يقلل الدين بدل نقله من function إلى function.
