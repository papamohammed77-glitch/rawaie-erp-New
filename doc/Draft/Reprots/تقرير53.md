# تقرير53 — FORENSIC CONTINUITY RECOVERY + MAIN2/MAIN1 SURGICAL EXECUTION CHECKPOINT

**Date:** 2026-09-04
**Repository:** `papamohammed77-glitch/rawaie-erp-New`
**Branch:** `main`
**Latest verified Git HEAD at investigation start:** `2bffd02f09f757424e4bfa38f3411ee9cf74b2ba`
**Last verified event before this report:** `[CTO] Add Report52 forensic Main1 source repair closure`

---

## 1. EXECUTIVE RECOVERY RESULT

هذه الجولة لم تبدأ من الصفر.

تمت إعادة بناء الحالة من:

- `MASTER - RAWAEA ERP.md` حتى EOF.
- `CURRENT_STATE.md`.
- `PROJECT_MEMORY.md`.
- `Report51`.
- `Report52` حتى EOF.
- `MASTER_CTO_FORENSIC_BRAND_PATCH_SUCCESSOR_V12.md`.
- Git chronology المباشرة.
- شجرة `Current/PWA/main2`.
- المصدر الفعلي `Current/PWA/main2/main1.md`.
- المصادر التاريخية/الحالية ذات الصلة.
- Production Supabase / PostgreSQL / RLS / function definitions / grants / triggers حيث كانت مؤثرة.

النتيجة الأهم:

```text
CURRENT_STATE.md كان متأخرًا عن Git الحالي.
آخر Git HEAD ليس Report51.
آخر HEAD مثبت مباشرًا = 2bffd02...
Report52 موجود بالفعل قبل هذه الجولة.
Report52 كان تحليلًا وتصميمًا فقط، ولم يغير main2/main1.
Current/PWA/main2/main1.md ما زال على blob 14b12a...
```

---

## 2. GOVERNANCE RE-VALIDATION

المبدأ الحاكم من `MASTER - RAWAEA ERP.md` تم تطبيقه كما هو:

```text
CURRENT REALITY
→ CURRENT GIT
→ CURRENT PRODUCTION
→ CURRENT DEPLOYMENTS
→ CURRENT DATABASE CONTRACTS
→ CURRENT VERIFIED ARTIFACTS
→ HISTORY
→ REPORTS
→ MEMORY
→ ASSUMPTIONS
```

وقواعد التشغيل الفعلية في هذه الجولة:

```text
UNKNOWN != BUG
UNKNOWN != REMOVE

DO NOT PATCH BEFORE RECONCILIATION
ONE CLOSURE UNIT AT A TIME
PATCH ONLY THE REAL WINDOW
NO WHOLE-FILE REWRITE FOR A NARROW DEFECT
GIT != DEPLOYMENT PROOF
SOURCE != RUNTIME PROOF
```

كما تم تطبيق قاعدة `LAST VERIFIED EVENT` بدل `LAST REPORT` أو `LAST ASSISTANT MESSAGE`.

---

## 3. LATEST GIT CHRONOLOGY ACTUALLY FOUND

الـGit الحالي أثبت سلسلة أحدث من الحالة المسجلة سابقًا:

```text
2bffd02f09f757424e4bfa38f3411ee9cf74b2ba
[CTO] Add Report52 forensic Main1 source repair closure

8049d8980801768453531abdd9164fca0f232457
Delete Current/PWA/main2/Test

c55ce8b3e0ecdfffc5739afdac53910009208f43
Add files via upload

79c4d901f2f305be78340b8d235bdcc4d5215118
Delete Current/PWA/main2/rawaie-erp-New-Original-PWA.zip

73a92e40fa9ed1c2c2f3b19f6150fa82c6e13c9a
Add files via upload

c65dc5a11c2abe5defe0a449ab2c3ede55260064
Add new Test file in main2 directory

da77d91568cea39e91ba7864765d786502833055
[CTO] Update CURRENT_STATE with Report51 and verified PWA knowledge baseline
```

ملاحظات:

- `8049...` حذف ملف اختبار، ولم يغير `main2/main1.md`.
- `c55ce8...` أضاف `main2/main1.md`.
- `2bffd...` أضاف Report52.
- بالتالي Report52 كان آخر حدث توثيقي فعلي قبل هذه الجولة، لكنه ليس إثباتًا أن Main1 تم إصلاحه.

---

## 4. CURRENT_STATE DRIFT

`CURRENT_STATE.md` كان لا يزال يصف Report51 كآخر milestone وبعض chronology أقدم من الواقع الحالي.

تصنيف الحالة القديمة:

```text
STATE = STALE
```

ولذلك تم التعامل معها كـDeclared Current State فقط، وتمت إعادة المطابقة مع Git مباشرة.

---

## 5. TARGET RECONCILIATION

الهدف الذي طلبه المالك في هذه الجولة ليس `Current/PWA/New-main`.

الهدف الحالي لهذه السلسلة هو:

```text
Current/PWA/main2/
```

والمطلوب الحالي هو بدء الجزء الأول:

```text
Current/PWA/main2/main1.md
```

مع اعتبار `Current/PWA/main2/main2.md ... main11.md` بقية أجزاء composite source.

شجرة Main2 مثبتة وتحتوي:

```text
main1.md
main2.md
main3.md
main4.md
main5.md
main6.md
main7.md
main8.md
main9.md
main10.md
main11.md
```

`main1.md` هو fragment أول، وليس تطبيقًا مستقلاً؛ فهو ينتهي عند `RW_Navigation` دون إغلاق `<script>`، بينما يبدأ `main2.md` بوظائف Dashboard.

---

## 6. MAIN1 CURRENT SOURCE FACTS

المصدر المباشر الحالي:

```text
Path = Current/PWA/main2/main1.md
SHA = 14b12a471c20ad23a2c18f456dbc4d59783a0d1f
Size = 63126 bytes
```

ويحتوي على:

```text
HTML shell
CSS
Supabase client bootstrap
RW_Table
RW_Audit_log
RW_Permissions_check
RW_Permissions_applyUI
RW_Workflow
RW_Notification
RW_Audit UI
RW_STATE
RW_Auth
RW_Data
RW_Navigation
```

---

## 7. IMPORTANT COMPARISON WITH `Current/PWA/main/main1.md`

تم التحقق من المصدر المختلف:

```text
Current/PWA/main/main1.md
```

وهذا المصدر يحتوي بالفعل على `RW_ShellContext` الحديث، ويجعل tenant context مشتقًا من `public.users.company_id` ويستخدم company-scoped operational reads.

لكن هذا لا يبرر نسخه كاملًا فوق `main2/main1.md`.

السبب:

```text
Different fragment
Different source lineage
Different composite boundary
```

لذلك تم رفض whole-file copy ورفض إعادة إدخال تغييرات غير مطلوبة.

---

## 8. FORENSIC FINDINGS IN MAIN2/MAIN1

### DEFECT A — Unsafe wildcard permission fallback

المصدر الحالي:

```javascript
RW_STATE.permissions = meta.permissions || ['*'];
```

التصنيف:

```text
CONFIRMED DEFECT
ROOT CAUSE = unsafe frontend authorization default
```

المشكلة ليست في Owner wildcard ذاته.

المشكلة أن wildcard يصبح default عندما تكون metadata ناقصة.

العقد المحمي يبقى:

```text
OWNER
+
isOwner=true
+
permissions=["*"]
+
owner_profile
+
active license
```

ولا يجوز تحويله إلى enumeration.

---

### DEFECT B — Authoritative company context missing from login bootstrap

التسجيل الحالي يعتمد على Auth metadata فقط ولا يستخرج `company_id` من `public.users`.

Production contract يثبت:

```text
users.auth_id
→ users.company_id
```

التصنيف:

```text
CONFIRMED DEFECT
ROOT CAUSE = missing authoritative tenant bootstrap
```

---

### DEFECT C — Global `app_settings` lookup

المصدر الحالي:

```javascript
RW_SUPABASE_CLIENT
    .from('app_settings')
    .select('*')
    .limit(1)
```

ثم يعتمد على أول صف.

التصنيف:

```text
CONFIRMED DEFECT
ROOT CAUSE = unscoped tenant settings lookup
```

---

### DEFECT D — Bootstrap reads lack explicit company scope

المصدر الحالي يستعمل:

```javascript
items
customers
branches
suppliers
```

دون `.eq('company_id', ...)` في طبقة Main1.

RLS يوفر حماية حالية، ولذلك:

```text
SECURITY INCIDENT = NOT PROVEN
HARDENING = AUTHORIZED
```

لكن بعد إنشاء authoritative company context يصبح explicit scoping هو المسار الصحيح.

---

## 9. FINDINGS THAT WERE NOT PATCHED

لا يوجد دليل مباشر يبرر تغيير:

```text
Login title 64px
Login logo 120x120
Company name 34px
```

لأنها متسقة مع التاريخ الموثق.

كما لم يتم تغيير:

```text
RAWAEA ERP ENTERPRISE
منصة إدارة الأعمال الذكية والمتكاملة
```

لأن اختلاف branding ليس وحده دليل regression.

كما لم يتم:

```text
enumerate owner wildcard
invent new workflow tenant column
modify audit owner contract
rewrite the entire fragment
```

---

## 10. EXACT SURGICAL PATCH WINDOWS ALREADY PROVEN

### PATCH 1 — `RW_STATE`

ابحث عن السطر الكامل:

```javascript
const RW_STATE = { app: { initialized: false, authenticated: false, loading: false, currentView: 'dashboard', currentUser: null, company: { name: 'الروائع ERP', logo: 'ر' } }, data: { items: [], customers: [], suppliers: [], branches: [] }, permissions: [], ui: { sidebarOpen: false, sidebarCollapsed: false } };
```

واستبدله بالسطر:

```javascript
const RW_STATE = { app: { initialized: false, authenticated: false, loading: false, currentView: 'dashboard', currentUser: null, company: { id: null, name: 'الروائع ERP', logo: 'ر' } }, data: { items: [], customers: [], suppliers: [], branches: [] }, permissions: [], ui: { sidebarOpen: false, sidebarCollapsed: false } };
```

---

### PATCH 2 — Login authoritative tenant bootstrap

ابحث عن البلوك الذي يبدأ تحديدًا بـ:

```javascript
var user = authRes.data.user;
var meta = user.user_metadata || {};
```

وينتهي بـ:

```javascript
self.enterSystem();
```

في النسخة الحالية للهدف، البلوك المعيب هو:

```javascript
var user = authRes.data.user;
var meta = user.user_metadata || {};

RW_STATE.app.authenticated = true;
RW_STATE.app.currentUser = {
    name: meta.name || user.email,
    email: user.email,
    role: meta.role || 'مدير النظام',
    isOwner: meta.isOwner === true || meta.isOwner === 'true'
};
RW_STATE.permissions = meta.permissions || ['*'];
RW_STATE.app.company = {
    name: meta.companyName || 'الروائع ERP',
    logo: meta.companyLogo || 'ر'
};

RW_Audit_log('login', 'auth', user.id, null, { email: user.email, role: meta.role || 'مدير النظام' });

self.enterSystem();
```

استبدله بالكامل بـ:

```javascript
var user = authRes.data.user;
var meta = user.user_metadata || {};

return RW_SUPABASE_CLIENT
    .from('users')
    .select('company_id, status')
    .eq('auth_id', user.id)
    .maybeSingle()
    .then(function(profileRes) {
        if (profileRes.error) throw profileRes.error;
        if (!profileRes.data || !profileRes.data.company_id) {
            throw new Error('بيانات سياق الشركة للمستخدم غير مكتملة');
        }
        if (profileRes.data.status === 'Inactive') {
            throw new Error('حساب المستخدم غير نشط');
        }

        RW_STATE.app.authenticated = true;
        RW_STATE.app.currentUser = {
            name: meta.name || user.email,
            email: user.email,
            role: meta.role || 'مدير النظام',
            isOwner: meta.isOwner === true || meta.isOwner === 'true'
        };

        RW_STATE.permissions = Array.isArray(meta.permissions) ? meta.permissions : [];
        RW_STATE.app.company = {
            id: profileRes.data.company_id,
            name: meta.companyName || 'الروائع ERP',
            logo: meta.companyLogo || 'ر'
        };

        RW_Audit_log('login', 'auth', user.id, null, {
            email: user.email,
            role: meta.role || 'مدير النظام',
            company_id: profileRes.data.company_id
        });

        return self.enterSystem();
    });
```

---

### PATCH 3 — `app_settings` company scope

ابحث عن:

```javascript
RW_SUPABASE_CLIENT
    .from('app_settings')
    .select('*')
    .limit(1)
    .then(function(r){
```

واستبدل بداية الاستعلام بـ:

```javascript
RW_SUPABASE_CLIENT
    .from('app_settings')
    .select('*')
    .eq('company_id', RW_STATE.app.company.id)
    .order('created_at', { ascending: true })
    .limit(1)
    .then(function(r){
```

لا تحذف `LIMIT 1` بعد إضافة company scope؛ في هذه الحالة أصبح محدودًا داخل الشركة ومحددًا ترتيبًا.

---

### PATCH 4 — Explicit company scope on bootstrap reads

استبدل:

```javascript
return supabase.from('items').select('*').then(function(res) {
```

بـ:

```javascript
return supabase.from('items').select('*').eq('company_id', RW_STATE.app.company.id).then(function(res) {
```

واستبدل:

```javascript
return supabase.from('customers').select('*').then(function(res) {
```

بـ:

```javascript
return supabase.from('customers').select('*').eq('company_id', RW_STATE.app.company.id).then(function(res) {
```

واستبدل:

```javascript
return supabase.from('branches').select('*').then(function(res) {
```

بـ:

```javascript
return supabase.from('branches').select('*').eq('company_id', RW_STATE.app.company.id).then(function(res) {
```

وفي تحميل الموردين استبدل:

```javascript
supabase.from('suppliers').select('*').then(function(r) {
```

بـ:

```javascript
supabase.from('suppliers').select('*').eq('company_id', RW_STATE.app.company.id).then(function(r) {
```

---

## 11. WHY THESE PATCHES ARE SAFE

المسؤوليات لا تختفي:

```text
Auth
→ remains Auth owner

Company identity
→ moves from untrusted/optional metadata to authoritative DB profile

Owner wildcard
→ remains explicit Owner semantics

app_settings
→ remains UI metadata source, now tenant scoped

bootstrap reads
→ remain reads, now explicitly tenant scoped
```

لا يوجد Physical Stock writer داخل هذا الجزء.

لا يوجد تغيير في Business Stock Engine.

لا يوجد تغيير في `post_stock_movement`.

لا يوجد تغيير في Owner license semantics.

---

## 12. MANUAL SURGICAL APPLICATION STATUS

وفقًا للأداة المتاحة في GitHub، الكتابة على الملف تستبدل الملف كاملًا.

ولأن هذا fragment حجمه 63,126 bytes والتوجيه الحاكم يرفض whole-file replacement لمشكلة موضعية، لم يتم تنفيذ `update_file` على `main2/main1.md`.

إذن الحالة الدقيقة:

```text
FORENSIC ANALYSIS = CLOSED
ROOT CAUSE = PROVEN
SURGICAL PATCH WINDOWS = PROVEN
SOURCE BYTES = UNCHANGED
PRODUCTION DEPLOYMENT OF MAIN2 = NOT PERFORMED
BROWSER RUNTIME = NOT VERIFIED
```

هذا ليس عجزًا عن معرفة الحل؛ بل حماية المصدر من الكتابة الكاملة غير الآمنة.

---

## 13. FAILURES / LESSONS FROM THE EXECUTION HISTORY

### Failure / Error 1 — اعتماد الحالة المسجلة في `CURRENT_STATE.md` دون إعادة فحص Git

تم تصحيحه باكتشاف HEAD الحقيقي `2bffd02...`.

### Failure / Error 2 — محاولة التعامل مع `main2/main1` كأنه نفس `Current/PWA/main/main1`

تم رفض ذلك بعد المقارنة المباشرة؛ اختلاف lineage والـfragment boundary مؤكد.

### Failure / Error 3 — محاولة استخدام whole-file write لأجل surgical change

تم رفضه قبل التنفيذ لأن الأداة لا تدعم patch window على الملف مباشرة، والـgovernance تمنع overwrite غير الضروري.

### Failure / Error 4 — محاولة إثبات runtime من source

تم رفض هذا الاستنتاج. Main2 source لم يثبت أنه deployed browser artifact.

---

## 14. WHAT IS PROVEN / WHAT IS NOT

### PROVEN

```text
Latest main HEAD = 2bffd02...
Report52 exists
Main2/main1 exists
Main2/main1 SHA = 14b12a...
Main2/main1 is a fragment
Four tenant/auth defects are proven from source + Production contract
Exact surgical windows are known
No unrelated visual/branding patch is justified
```

### NOT PROVEN

```text
Main2 composite is deployed to production
Fresh browser runtime for Main2
Browser cache/service-worker lineage for Main2
Production deployment of the repaired Main2 source
Final Gold/Diamond UI state
```

---

## 15. NEXT AUTHORIZED GATE

لا تبدأ `main2.md` قبل إغلاق Main1 source gate.

الخطوة الصحيحة بعد هذه الجولة هي:

```text
1. تطبيق PATCH 1–4 يدويًا في Current/PWA/main2/main1.md.
2. إعادة رفع الملف بعد التعديل.
3. إعادة قراءة Main1 كاملًا من أول سطر حتى EOF.
4. مطابقة الـanchors والـfunctions وعدم وجود duplicate bootstrap.
5. إثبات SHA جديد للملف.
6. عمل static syntax / fragment integration check مع main2.md.
7. تسجيل الحدث الجديد في تقرير لاحق.
8. فقط بعدها الانتقال إلى Main2.
```

لا تعُد إلى branding/login visual parity لمجرد اختلاف تاريخي ما لم يظهر دليل مباشر جديد.

---

## 16. FINAL SELF-AUDIT

```text
BUSINESS UNDERSTANDING
= CONFIRMED FOR MAIN1 CLOSURE

ARCHITECTURE UNDERSTANDING
= CONFIRMED FOR MAIN1/MAIN2 FRAGMENT BOUNDARY

DATABASE UNDERSTANDING
= CONFIRMED FOR RELEVANT TENANT CONTRACT

HISTORICAL UNDERSTANDING
= CONFIRMED FOR ORIGINAL/MAIN COMPARISON

PRODUCTION UNDERSTANDING
= CONFIRMED FOR RELEVANT RLS/SCHEMA CONTRACT

CURRENT GIT UNDERSTANDING
= CONFIRMED

EXECUTION CONFIDENCE
= HIGH FOR PATCH DESIGN

RUNTIME CONFIDENCE
= NOT CLAIMED
```

### Confirmed Facts

```text
2bffd02 is latest verified HEAD discovered directly.
main2/main1 remains on SHA 14b12a.
Report52 did not mutate Main1.
Four exact patch windows are valid.
```

### Unknowns

```text
Fresh Main2 browser deployment/runtime.
```

### Conflicts

```text
CURRENT_STATE was stale relative to Git chronology.
Resolved by direct Git reconciliation.
```

### Unverified claims rejected

```text
No "Production Fixed" claim.
No "Runtime Verified" claim.
No "Gold/Diamond" claim.
No whole-file rewrite claim.
```

---

## 17. LAST VERIFIED EVENT FOR SUCCESSOR

```text
EVENT ID:
2bffd02f09f757424e4bfa38f3411ee9cf74b2ba

EVENT TYPE:
GIT DOCUMENTATION / FORENSIC CLOSURE RECORD

UTC DATE:
2026-09-04

SOURCE:
GitHub main chronology

ACTION:
Added Report52 forensic Main1 source repair closure

RESULT:
Forensic identification and surgical design completed; Main1 source bytes intentionally unchanged.

NEXT AUTHORIZED ACTION:
Apply exact PATCH 1–4 manually, then re-read Main1 EOF and verify the resulting SHA before Main2.
```

---

## 18. CLOSURE STATUS

```text
MAIN1 FORENSIC IDENTIFICATION = CLOSED
MAIN1 ROOT CAUSE ANALYSIS = CLOSED
MAIN1 SURGICAL PATCH DESIGN = CLOSED
MAIN1 SOURCE APPLICATION = PENDING MANUAL PATCH
MAIN1 PRODUCTION = OPEN
MAIN1 RUNTIME = OPEN
MAIN2 = NOT STARTED
```

**No historical report was deleted or overwritten.**
