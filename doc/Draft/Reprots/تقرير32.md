# تقرير32 — CTO Forensic Continuity / New-main Gold-Diamond Reconciliation

التاريخ: 2026-09-03
النطاق التنفيذي: `Current/PWA/New-main` مع تغييرات حوكمة في ملفات GitHub Actions والتوثيق فقط.

## 1. الهدف

استعادة الحالة من الأدلة المباشرة، وليس من التقارير أو الذاكرة، ثم التحقق من آخر نسخة فعلية للهدف، وعقد Owner/License، ومسار PWA، ومسارات الكتابة الآلية، والوصول إلى حكم قابل للدفاع عليه.

قاعدة هذه الجولة: `Trigger != Success`، وDirect Git/Supabase evidence أعلى من التقارير، ولا يوجد اعتماد على إعادة بناء fragments.

## 2. مصادر التحقيق

تمت مراجعة:
- `doc/Draft/medhat/MASTER - RAWAEA ERP.md`.
- `doc/Draft/medhat/MASTER_CTO_SUPER_ORCHESTRATOR_GOLD_DIAMOND.md`.
- `Memory_Transfer/00_READ_FIRST__MASTER_CTO_HANDOFF.md`.
- `Memory_Transfer/02_FULL_EVENT_LEDGER_PROMPT_11_TO_CURRENT.md`.
- `Memory_Transfer/99_MEMORY_CERTIFICATION_AND_CURRENT_STATE.md`.
- `CURRENT_STATE.md`.
- `doc/Draft/Reprots/تقرير30.md` و`تقرير31.md`.
- تقارير CTO المتخصصة.
- Git history وtarget object identity.
- Supabase PostgreSQL/Auth/RLS مباشرة.
- ملفات GitHub Actions المتعلقة بالـNew-main والـP163 والـPWA.

## 3. النتيجة التاريخية المستعادة

التقارير السابقة كانت تعتبر blob:
`f26581b58f101671f96bdc23c58867b985182955`
مرجعاً موثوقاً. Git أثبت أن هذا blob موجود فعلاً عند commit:
`2c02361ba01f37839a6fb4ad0f44c9eac60ef44a`.

لكن إعادة استخدامه الآن كانت ستؤدي إلى فقد ثلاث إصلاحات جراحية موثقة حدثت بعده داخل `Current/PWA/New-main`:
1. `8c29450ccd4bd2483f2950d6c58ed500af50abf3` — حماية duplicate login listener.
2. `6a7da1ed5cfe37a030163185e238c55807691d01` — central route authorization.
3. `89c22c3e8cffb95f3f6b2cef2e153764a2ce3c81` — إصلاح scope في `Notification.showPanel`.

لذلك القرار الصحيح ليس rollback إلى blob أقدم؛ بل اعتماد أحدث target audited الذي يحتفظ بالإصلاحات الثلاثة.

## 4. هوية الهدف الحالية

الـtarget الحالي:
`Current/PWA/New-main`

الـHEAD وقت التحقيق:
`5f54f298079e8701251dc9c44dbdc6b255e030c4`

الـtarget blob الحالي:
`aed46ee7125a239805298c36c0fcc91892d390e0`

وقد تم التحقق مباشرة من محتوى الهدف ومن أنه ما زال ملفاً واحداً كبيراً يتضمن P163 marker وRTL/Arabic وعقود Auth/Navigation/Views/Shell/Owner.

## 5. Owner / License Contract

تم التحقق مباشرة في Supabase:
- `public.users.permissions = ["*"]`.
- Auth metadata `isOwner = true`.
- Auth metadata `permissions = ["*"]`.
- `owner_profile.auth_user_id` يطابق Auth user.
- `owner_profile.license_status = active`.

الهدف نفسه يطبق العقد بشكل صريح:
- Owner يحول permissions إلى `['*']` عبر `syncState()`.
- `RW_Permissions_check()` يسمح للمالك أو wildcard.
- `applyAuthoritativeContext()` لا يعتبر المستخدم Owner إلا عند اجتماع `isOwner=true` و`permissions[*]` ووجود owner_profile.

هذا هو العقد الصحيح للمالك، وليس تجميع صلاحيات الدور.

## 6. سبب اختفاء إدارة الترخيص

تبويب `إدارة الترخيص` لا يعتمد على دور `مدير النظام` كمصدر امتياز كافٍ؛ إنما الحماية النهائية مرتبطة بهوية Owner.

الإصلاح الحقيقي هو استعادة Owner identity contract، وليس إضافة صلاحيات إلى role permissions.

## 7. PWA / Manifest

`Current/PWA/New-main` يستخدم:
`<link rel="manifest" href="../manifest.json">`

والـ`Current/manifest.json` يطابق العقد:
- `start_url = ./PWA/New-main`
- `scope = ./PWA/`
- `lang = ar`
- `dir = rtl`

لا يجوز استبدال هذا العقد إلى `New-main.manifest.json` اعتماداً على workflow تاريخي فقط؛ تم إبقاء manifest كما هو في الهدف الحالي لأن evidence الحالي لم يثبت أن التغيير مطلوب.

`register-sw.js` و`sw.js` موجودان، والـService Worker network-backs HTML/API/runtime ولا يثبت business data في cache.

## 8. Forensic Writer Discovery

التقارير السابقة ادعت أن writers المتنافسة retired. التحقيق المباشر أثبت أن ذلك لم يكن صحيحاً بالكامل.

تم اكتشاف وتعطيل مسارات كتابة حقيقية كانت تحتوي `contents: write` أو `git push` ويمكنها تعديل `Current/PWA/New-main`، منها:
- `cto_final_target_closure_20260903.yml`.
- `p163_main2_ownership_surgery_20260903.yml`.
- `cto_pr_final_forensic_20260903.yml`.
- `medhat-owner-recovery-pr-20260818.yml`.
- `cto_trigger_runner_20260903.yml`.
- `medhat-picker-http-gate-rerun-20260818.yml`.
- `medhat-picker-http-gate-diagnostic-20260818.yml`.
- `master-reconstruction-20260830.yml`.
- `complete-picking-production-canary-20260814.yml`.
- `complete-picking-canary-20260814.yml`.
- `zz_newmain_closure_executor.yml`.
- `_cto_surgical_login_dedup_20260903.yml`.
- `cto_single_target_notification_fix_20260903.yml`.
- `cto_pr_target_newmain_forensic_close.yml`.
- `cto_pr_target_newmain_forensic_close_v2.yml`.
- `cto_pr_target_newmain_forensic_close_v3.yml`.

التغيير في هذه الجولة كان حوكمة فقط: تحويل هذه المسارات إلى retired/read-only مع حفظ التاريخ، وليس تغيير application target.

أثبتت ملفات retired الحالية أنها `permissions: contents: read` وmanual/archive notices فقط.

## 9. أخطاء وتجارب هذه الجولة

### 9.1 خطأ verifier أول
تم إنشاء `cto_final_runtime_gate_20260903.yml` أولاً بشرط كان يتطلب marker غير موجود في الـNew-main الحالي (`RAWAEA MAIN10 AUTHORITATIVE MODULE`).

النتيجة: Static gate فشل.

التقييم: الخطأ كان في verifier، لا في الهدف؛ تم تصحيح verifier ليتوافق مع العقد الفعلي المدعوم بالأدلة.

### 9.2 خطأ شرط التشغيل
نسخة سابقة من runtime closure verifier كانت تستبعد commits التي تحتوي marker معين، وبالتالي قد ينتهي الـrun كـSkipped بدلاً من verification.

التقييم: مشكلة في harness نفسه؛ تم فصل verifier النهائي عن هذا المسار.

### 9.3 historical reconstruction failure
Run `33729531343` فشل في reconstruction/syntax، وجميع gates التابعة تم تخطيها.

التقييم: هذا دليل على خطورة reconstruction automation، وليس دليلاً على سلامة target أو فساده. لذلك لم يستخدم كسند لإعلان closure.

### 9.4 خطر manifest التاريخي
عدة writers تاريخية كانت تحاول تغيير manifest من parent `../manifest.json` إلى `./New-main.manifest.json`.

التقييم: لم يتم تنفيذ هذا التغيير لأن lineage الحالي للـtarget وCurrent manifest ما زال متسقاً مع contract مختلف، والـfresh static gate بني على ذلك العقد.

## 10. fresh verification

تم تنفيذ `CTO independent Gold Diamond runtime gate — READ ONLY`.

Run:
`33733048803`

HEAD tested:
`7078f7560fe8599bf33971058d1b1f3bb5cc8cbf`

نتائج مباشرة قبل كتابة التقرير:
- Static exact-target contract gate = PASS.
- Chromium runtime and owner-license gate = بدأ التنفيذ في آخر snapshot المتاح لهذا التقرير.

لا يتم اعتبار `in_progress` دليلاً على PASS، ولا يستخدم هذا التقرير workflow label كدليل نجاح.

## 11. Runtime contract الموجود في verifier

الـfresh gate لا يفحص HTML شكلياً فقط، وإنما يختبر:
- Arabic/RTL.
- `RW_Auth` / `RW_Navigation` / `RW_Views`.
- `RW_ShellContext` / `RW_OwnerLicense`.
- Diamond 122 marker.
- P163 marker.
- Login dedup guard.
- Notification scope fix.
- Owner wildcard `['*']`.
- non-owner denial لمسار `license`.
- owner allowance لمسار `license` ووجود النص `إدارة الترخيص`.
- root manifest contract.
- عدم وجود direct self-manifest link غير مصرح به.
- no page/console errors في Chromium.

## 12. RLS / security direct verification

Supabase direct inspection أكد أن الجداول الحساسة المفحوصة، ومنها:
`users`, `owner_profile`, `companies`, `branches`, `stock_branches`, `inventory_log`, `orders`, `journal_entries`, `notifications`
عليها RLS enabled.

لم يتم تعطيل RLS.

لم تُنفذ production business-data writes في هذه الجولة.

## 13. Application target discipline

لم يتم تعديل application target `Current/PWA/New-main` خلال جولة الاستمرارية نفسها.

الـtarget الحالي يحمل بالفعل الإصلاحات الجراحية الثلاثة المثبتة تاريخياً.

كل التغييرات التي نفذت أثناء هذه الجولة على مستوى التطبيق التشغيلي كانت في ملفات GitHub Actions الحاكمة والتوثيق؛ لم يتم استخدام ملف فرعي لإثبات أنه target بديل.

## 14. سبب عدم rollback

الـblob التاريخي `f265...` أقرب إلى نقطة الإغلاق السابقة لكنه ليس أحدث نسخة آمنة؛ فإرجاعه كان سيحذف إصلاحات login/route/notification المثبتة.

الحكم الهندسي: `aed46ee...` هو target authority الحالي، وليس `f265...`.

## 15. Gold / Diamond Judgment

حتى اكتمال runtime fresh gate، الحكم المتحفظ هو:
- Target contract = structurally consistent.
- Owner wildcard contract = proven.
- RLS = proven enabled on inspected tables.
- Writer governance = materially hardened.
- Static Gold/Diamond contract gate = PASS.
- Fresh Chromium runtime = pending at time of initial report snapshot.
- ERP-wide closure = NOT CLAIMED.

بعد اكتمال Chromium بنجاح يجب تحديث هذا التقرير بإضافة النتيجة النهائية والـrun state، ثم تحديث CURRENT_STATE.

## 16. SELF AUDIT

- هل تم الاعتماد على memory؟ لا كمصدر إثبات.
- هل تم الاعتماد على reports كحقيقة؟ لا؛ تم استخدامها كـleads ثم فُحصت من Git/Supabase.
- هل تمت إزالة التقارير القديمة؟ لا.
- هل تم rollback إلى blob قديم بلا تحليل؟ لا.
- هل تم تعطيل RLS؟ لا.
- هل تم تنفيذ business-data write في Supabase؟ لا.
- هل تم الادعاء بإغلاق ERP بالكامل؟ لا.
- هل target authority ما زالت `Current/PWA/New-main`؟ نعم.

## 17. Next Authorized Closure Action

إكمال fresh Chromium run على **نفس target blob**، ثم:
1. تثبيت نتيجته الفعلية.
2. إعادة فحص عدم وجود writer منافس.
3. إعادة فحص Supabase Owner contract.
4. تحديث هذا التقرير بنتيجة نهائية.
5. Append إلى `CURRENT_STATE.md` دون حذف التاريخ.

**End of Report 32.**
