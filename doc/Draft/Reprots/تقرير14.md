# تقرير14 — P153 Memory Recovery / Current-State Reconciliation / MAIN.HTML Surgical Verification

**التاريخ:** 2026-09-02  
**النوع:** Forensic continuity + current-source reconciliation  
**الحالة:** CURRENT STATE RECONCILED FOR THIS REVIEW / NO MAIN.HTML MANUAL PATCH REQUIRED / INVENTORY CLOSURE STILL OPEN

---

## 1. الهدف ونقطة البداية

تمت هذه الجولة وفق `MASTER - RAWAEA ERP.md` و`برومبت استكمال مهام`، مع الالتزام بقاعدة أن الاستمرار يكون من `LAST VERIFIED STATE` لا من الذاكرة أو آخر رسالة أو آخر تقرير.

تمت مراجعة:

- `doc/Draft/medhat/MASTER - RAWAEA ERP.md`
- `doc/Draft/medhat/برومبت استكمال مهام`
- `doc/Draft/Reprots/تقرير1` حتى `تقرير13`
- `CURRENT_STATE.md`
- `Current/PWA/New-main`
- `Current/PWA/main.html`
- `Current/Edge_Functions/create-stock-voucher`
- `Current/Edge_Functions/receive-purchase`
- Production PostgreSQL functions/signatures
- Production migration history
- Production schema/constraints/triggers/grants

لم يتم الاعتماد على تقرير سابق لإثبات الحالة الحالية دون إعادة التحقق من Git أو Production.

---

## 2. أهم نتيجة في استعادة الذاكرة

التسلسل الفعلي لم يتوقف عند P152 كما قد توحي قراءة `CURRENT_STATE.md` وحدها.

أحدث سجل تنفيذي منشور هو `تقرير13`، وهو يسجل إصلاحًا فعليًا على Production بتاريخ 2026-09-02 لإضافة Compatibility Read Projection في `companies` للحقول `main_branch_id` و`main_branch_code`، مع إبقاء `app_settings.main_branch_id` مصدر الحقيقة. التقرير سجل أيضًا Migration Production:

`20260902023122 / compatibility_company_main_branch_projection_20260902`.

تم تأكيد وجود هذه الـmigration مباشرة في Production migration history.

إذن:

```text
LATEST EXECUTED / DOCUMENTED EVENT
= تقرير13 / Production compatibility projection
```

بينما:

```text
CURRENT_STATE.md
= لم يكن قد سُجل فيه هذا الحدث حتى بدء هذه الجولة
```

وعليه كان `CURRENT_STATE.md` **STALE بالنسبة لتقرير13**، وهو تعارض تم حسمه الآن وتوثيقه هنا.

---

## 3. التاريخ التنفيذي السابق: ما الذي ثبت أنه تم بالفعل

### P135 / تقرير2
تم إصلاح guard في MAIN11، لكن تبين لاحقًا أن المشكلة الأهم كانت JavaScript scope.

### P136 / تقرير3
تم تصدير نفس Supabase client إلى `window.RW_SUPABASE_CLIENT` وجعل MAIN11 يستخدم المرجع المشترك.

### P137 / تقرير4
تم إنهاء مصدر Service Worker الحالي مع marker `RAWAEA_SW_P137_FINAL`، مع بقاء `/companies/sw.js` runtime gate مفتوحًا.

### P138–P140 / تقارير5–7
تم إثبات أن Runtime لا يطابق Current Git، وأن Cloudflare deployment lineage هو الحد الفاصل غير المثبت. تم رفض credential rotation ورفض إنشاء shadow `/companies/sw.js` دون deployment-root proof.

### P143 / تقرير8
تم إصلاح pipeline reconstruction، بعد اكتشاف sentinel HTML داخل JavaScript وتحقق خاطئ على artifact مختلف عن `New-main`.

### P146 / تقرير9
تم إنشاء compatibility routes في `erp-frontend/_redirects` للمسارين:

```text
/companies/manifest.json → /companies/company-1/manifest.json
/companies/sw.js → /companies/company-1/sw.js
```

### P150 / تقرير10
تم تنفيذ عقد auto-update دائم على مستوى SW/register coordinator/CDN headers.

### P151 / تقرير11
تم سد فجوة New-main عبر SW injection للـupdate coordinator. أول نسخة فشلت syntax ثم تم تصحيحها والتحقق منها بـ`node --check`.

### P152 / تقرير12
تمت إعادة مصالحة Auth/Deployment، وتأكد أن Supabase key ليست سببًا مثبتًا، وبقي Cloudflare live artifact غير مقروء.

### تقرير13
تم تنفيذ Production compatibility projection لعقد `companies.main_branch_id/main_branch_code` بدل اختراع source جديد للحقيقة.

هذه السلسلة تؤكد أن المشروع لم يبدأ من الصفر في هذه الجولة، وأن تقرير13 سبق تحديث `CURRENT_STATE.md`.

---

## 4. تقرير13: هل تم التنفيذ الحقيقي أم كان نظريًا؟

الجواب: **نعم، توجد أدلة مباشرة أن تقرير13 لم يكن كلامًا نظريًا**.

تم التحقق من Production migration history مباشرة، وظهر:

```text
20260902023122
compatibility_company_main_branch_projection_20260902
```

كما أن تعريفات Production الحالية تسمح لـ`main.html` بقراءة:

```text
companies.main_branch_id
companies.main_branch_code
```

وكان التقرير13 قد سجل أن المصدر الموثوق يبقى:

```text
app_settings.main_branch_id
→ branches.branch_code
→ companies.* compatibility projection
```

لكن تقرير13 لم يثبت Browser Retry HTTP 200، ولذلك لا يجوز تحويل إصلاح DB إلى إغلاق Runtime كامل.

---

## 5. النقطة التي سببت الالتباس: هل تم تعديل main.html؟

بعد قراءة `Current/PWA/New-main` و`Current/PWA/main.html` مباشرة، والبحث في كود `receive-purchase`، النتيجة الحاسمة هي:

**لا يوجد في الحالة الحالية تعديل يدوي مطلوب على `main.html` بخصوص `Idempotency-Key`.**

السبب أن الكود الحالي يحتوي بالفعل على التصحيح المطلوب.

المقطع الحالي الذي تم إثباته مباشرة هو:

```javascript
var opId=(window.crypto&&typeof window.crypto.randomUUID==='function')?window.crypto.randomUUID():(Date.now().toString(36)+'-'+Math.random().toString(36).slice(2));
showLoader('جاري حفظ الاستلام...');
RW_Main6.token().then(function(t){
  return fetch(RW_SUPABASE_URL+'/functions/v1/receive-purchase',{
    method:'POST',
    headers:{'Content-Type':'application/json','Authorization':'Bearer '+t,'Idempotency-Key':opId},
    body:JSON.stringify({po_code:poCode,itemsReceived:r.value,notes:notes,operation_id:opId})
  });
})
```

وبالتالي `main.html` الحالي يرسل بالفعل:

```text
Idempotency-Key: opId
```

ويحمل أيضًا:

```text
operation_id: opId
```

إلى `receive-purchase`.

### تصحيح خطأ سابق في هذه الجولة

سبق أن قيل إن `main.html` لا يرسل `Idempotency-Key`.

هذا الاستنتاج كان **غير صحيح** بسبب قراءة قديمة/غير متزامنة مع حالة Current الحالية.

تم تصحيح الاستنتاج بعد القراءة المباشرة الحالية، ولم يتم اعتماد أي Patch يدوي على `main.html` بناءً على ذلك الاستنتاج الخاطئ.

---

## 6. لماذا هذا مهم بالنسبة للمالك؟

لأن المطلوب من المالك كان:

> السطر المعيب والسطر المصحح لكي يتم الاستبدال يدويًا.

والنتيجة المهنية الدقيقة هي:

```text
لا يوجد استبدال يدوي مطلوب لهذا الجزء الآن.
```

لا يجب على المالك تعديل `main.html` يدويًا استنادًا إلى الرسالة السابقة، لأن ذلك سيجعل المصدر الموجود فعليًا أقل تطابقًا مع الحالة المثبتة.

---

## 7. ماذا عن `main_branch_id`؟ هل السطر الموجود في main.html معيب؟

الكود الحالي يحتوي داخل `applyAuthoritativeContext` على:

```javascript
var c=await supabase.from('companies').select('id,name,logo_url,main_branch_id,main_branch_code').eq('id',u.data.company_id).maybeSingle();
```

هذا السطر كان يفشل تاريخيًا لأن الأعمدة المطلوبة لم تكن موجودة في `companies`.

لكن تقرير13 أصلح **الاعتماد على مستوى Database Contract** عبر Compatibility Read Projection في Production.

إذًا بعد الإصلاح:

```text
الـlookup نفسه company-scoped وصحيح.
```

ولا يوجد أساس لتغيير هذا السطر في `main.html` الآن.

التصحيح الذي تم هو في Database contract، وليس في هذا السطر.

---

## 8. فرق مهم جدًا بين ملفين باسم main.html

تم إثبات وجود ملفين مختلفين في Git يجب عدم خلطهما:

### Authorized product target

```text
Current/PWA/New-main
SHA = 5bf6907747d807dfa9f10979f5a63685c8bae64e
```

### Separate Current/PWA/main.html

```text
Current/PWA/main.html
SHA = 27b777528665dcc985809648f006452c861ae36e
```

`CURRENT_STATE.md` وP152 يحددان `New-main` كـauthorized product target لـ`erp-frontend/companies/company-1/main.html`، مع اعتبار `Current/PWA/main.html` ملفًا محميًا لا يجوز استبداله يدويًا لمجرد اختلاف SHA.

هذه نقطة lineage مهمة جدًا، وأي نسخ يدوي بين الملفين بدون إثبات contract جديد سيكون مخالفًا للحوكمة.

---

## 9. Current Edge Function state بعد إعادة القراءة

### create-stock-voucher

النسخة الحالية في Git هي RPC-backed، وتستخرج company context من authenticated user، وتحافظ على operation-aware dispatch عند وجود operation id.

هذا يتفق مع سجل حدث `EVENT-20260830-CREATE-STOCK-VOUCHER-CLOSURE` الذي أثبت أن المسار Production الحالي ليس direct Physical Stock writer.

### receive-purchase

النسخة الحالية:

- تستخرج `company_id` من `users.auth_id`؛
- تدعم `operation_id` الصريح؛
- تقرأ `Idempotency-Key` عند الحاجة؛
- تبني fallback deterministic operation id عند غياب الهوية الصريحة؛
- تستدعي `receive_purchase_atomic`.

وهذا متوافق مع الكود الحالي الذي تمت قراءته مباشرة.

---

## 10. Production Database current signature evidence

تم التحقق مباشرة من PostgreSQL أن Production تحتوي حاليًا على:

```text
create_manual_stock_voucher_atomic(10 args)
create_manual_stock_voucher_atomic(12 args, includes p_operation_id)
post_inventory_adjustment_atomic(7 args)
post_manual_stock_voucher_atomic(6 args, includes p_operation_id)
receive_purchase_atomic(5 args, includes p_operation_id uuid)
send_stock_voucher_atomic(3 args)
```

وهذا مهم لأنه يثبت أن طبقة operation-aware في `receive-purchase` ليست مجرد source غير منشور؛ يوجد بالفعل عقد Production مطابق من حيث وجود `p_operation_id`.

---

## 11. ما زال مفتوحًا في Inventory Sweep

هذه الجولة لم تعلن Inventory 100%.

المبدأ الحاكم ما زال:

```text
PHYSICAL STOCK MOVEMENT
        ↓
post_stock_movement
        ↓
stock_branches + inventory_log
```

وما زالت هناك بنود تحتاج Closure Unit مستقلة، منها ما ظهر من Production forensic inspection:

- legacy/manual voucher context guards التي تستخدم `app_settings LIMIT 1` داخل بعض RPCs؛
- ضرورة فصل أي historical/fixture inventory contamination عن البيانات التشغيلية قبل أي تنظيف؛
- متابعة global writer discovery؛
- إكمال verification لكل Writer واحدًا في كل مرة؛
- Runtime verification المستقلة، وعدم تحويل source-only pass إلى Production closure.

هذه البنود لا تُعتبر مغلقة من هذا التقرير.

---

## 12. Production data anomaly التي ظهرت أثناء الـforensics

وجدت القراءة المباشرة وجود:

```text
143 stock_branches rows
```

تحت فرع شركة `da4e…` بينما `items.company_id` لبعض هذه الأصناف ينتمي إلى شركات أخرى.

كما ظهر:

```text
86 inventory_log item/company mismatches
```

لكن لم يتم حذف أو إعادة توزيع هذه البيانات في هذه الجولة، لأن وجود اختلاف company metadata لا يكفي وحده لإثبات أن الصفوف يجب حذفها؛ `items.item_code` لديه UNIQUE عالمي، وهناك مؤشرات تاريخية قوية على وجود fixture/legacy population.

القرار الصحيح الحالي:

```text
DETECT = YES
TRACE = REQUIRED
REPAIR = NOT YET AUTHORIZED
```

لا يوجد تنظيف تخميني.

---

## 13. الأخطاء التي ارتُكبت ويجب حفظها

### خطأ 1 — اعتبار `main.html` ناقص Idempotency-Key

حدث هذا الاستنتاج في جولة سابقة اعتمادًا على قراءة لم تكن متزامنة مع Current source النهائي.

**التصحيح:** direct Git read أثبت أن `Idempotency-Key` و`operation_id` موجودان بالفعل.

### خطأ 2 — محاولة تحويل source discrepancy إلى manual owner patch

كان من الممكن أن يُطلب من المالك تعديل الملف يدويًا بناءً على هذا الاستنتاج.

**القرار المصحح:** لا modification يدوي مطلوب.

### خطأ 3 — الخلط بين `Current/PWA/main.html` و`Current/PWA/New-main`

الـSHA مختلف، والعقد الحالي يجعل `New-main` هو target المنتج المنشور.

**القرار:** لا نسخ بينهما دون دليل جديد.

### خطأ 4 — استخدام تقرير13 وكأنه كافٍ لإثبات Browser Runtime

تقرير13 نفسه يقول إن Browser Retry HTTP 200 غير مثبت.

**القرار:** DB closure ≠ runtime closure.

### خطأ 5 — الخلط بين تقرير13 وCURRENT_STATE

تقرير13 كان أحدث سجل تنفيذي، بينما `CURRENT_STATE.md` ظل يسجل P152 فقط.

**التصحيح:** تسجيل هذا التعارض كـSTALE STATE وتحديث CURRENT_STATE بعد هذا التقرير.

---

## 14. What I Proved

- `MASTER - RAWAEA ERP.md` هو governing command الحالي وتمت قراءته وتحليله حتى نهاية الأمر التنفيذي.
- `برومبت استكمال مهام` هو inventory continuation directive الحالي.
- تاريخ التقارير 1–13 محفوظ ولم يُحذف.
- تقرير13 يمثل تنفيذ Production حقيقيًا على الأقل على مستوى Database migration، وليس تصورًا نظريًا.
- `CURRENT_STATE.md` كان متأخرًا عن تقرير13.
- `Current/PWA/New-main` SHA = `5bf6907747d807dfa9f10979f5a63685c8bae64e`.
- `Current/PWA/main.html` SHA مختلف = `27b777528665dcc985809648f006452c861ae36e`.
- `main.html` الحالي يحتوي بالفعل على `Idempotency-Key` و`operation_id` عند استدعاء `receive-purchase`.
- `companies.main_branch_id/main_branch_code` dependency أصبح مدعومًا بـProduction compatibility projection، مع بقاء `app_settings.main_branch_id` مصدر الحقيقة.
- Production `receive_purchase_atomic` الحالي يحتوي `p_operation_id`.
- Physical Stock contract ما زال مركزيًا حول `post_stock_movement`.

---

## 15. What I Did Not Prove

- لم أثبت أن Cloudflare Worker live artifact يطابق آخر Git deployment.
- لم أثبت أن Browser Login 401 أغلق نهائيًا.
- لم أثبت Inventory Core Integrity = 100%.
- لم أثبت أن كل legacy RPC قد تم إزالته من كل possible consumer.
- لم أثبت أن الـ143/86 data anomalies كلها قابلة للحذف أو النقل؛ لذلك لم أقم بتنظيفها.

---

## 16. Current authorized next actions

وفق الحالة المجمعة الآن:

```text
1. لا تُجرِ أي تعديل يدوي على main.html بسبب Idempotency-Key.
2. لا تستبدل Current/PWA/main.html بـNew-main.
3. لا تدوّر Supabase credentials بناءً على 401 وحدها.
4. استمر في Inventory Writer Closure Unit-by-Unit.
5. عند الوصول إلى Runtime closure، أثبت Cloudflare artifact مباشرة قبل إعلان Runtime PASS.
6. أي Data cleanup يجب أن يأتي بعد source/history/business-impact trace.
```

---

## 17. FINAL SELF-AUDIT

```text
Business Understanding        = VERIFIED FOR THIS REVIEW
Architecture Understanding    = VERIFIED FOR THIS REVIEW
Database Understanding        = VERIFIED FOR THIS REVIEW
Historical Understanding      = VERIFIED THROUGH REPORT13
Production Understanding      = VERIFIED FOR EXAMINED CONTRACTS
Current Understanding         = VERIFIED

Confirmed Facts               = RECORDED
Unknowns                      = CLOUDFLARE LIVE ARTIFACT / FULL INVENTORY CLOSURE
Conflicts                     = CURRENT_STATE LAG VS REPORT13 (RESOLVED BY THIS UPDATE)
Unverified Claims              = BROWSER RETRY / LIVE CLOUDFLARE SERVING

Historical Opened             = YES
Original Opened               = RELEVANT SOURCES OPENED
Production Opened             = YES
Current Opened                = YES
Schema Checked                = YES
Triggers Checked              = YES
Dependencies Checked         = YES FOR TARGETED CLOSURE
Consumers Checked             = PARTIAL / TARGETED
```

### Final judgment

```text
MAIN.HTML_MANUAL_PATCH_REQUIRED = NO
MAIN.HTML_IDEMPOTENCY_FIX        = ALREADY_PRESENT
REPORT13_PRODUCTION_EXECUTION    = PROVEN AT DB/MIGRATION LEVEL
CURRENT_STATE_WAS_STALE          = YES / NOW BEING UPDATED
CLOUDFLARE_RUNTIME               = UNVERIFIED
GLOBAL_INVENTORY_100_PERCENT     = NO
PROJECT_CLOSED                   = NO
```

هذا التقرير لا يغلق شيئًا لم يُثبت إغلاقه، ويحفظ الخطأ السابق بدل إخفائه، ويمنع إعادة نفس الخطأ في الجولة التالية.
