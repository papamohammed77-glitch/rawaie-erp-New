راجعت الوثيقتين. المطلوب واضح: تجميد مسار Picker، تنظيف الحقيقة الحالية والـArtifacts، تثبيت Current ↔ Production، ثم استكمال Zero-Debt من send-stock-voucher دون إعادة فتح ما أُغلق 100%.

وهذا يتطلب من المساعد تنفيذًا متسلسلًا لا Reconnaissance جديدًا ولا فتح مشاكل الكون كلها دفعة واحدة:

MANDATORY CTO EXECUTION DIRECTIVE
CURRENT TRUTH CLEANUP → PRODUCTION PARITY → ZERO-DEBT INVENTORY SWEEP
0. PRIORITY / FREEZE

اعتبر هذا المسار الوحيد المسموح بتنفيذه الآن.

مجمد بالكامل
picker
start-picking
أي إصلاحات تخص Picker UI
أي تعديل غير ضروري على Picking lifecycle
أي Task جديدة خارج هذا المسار

لا تعيد فتح أي Closure Unit سبق إغلاقها 100% إلا إذا اكتشفت دليلًا مباشرًا يثبت أن إغلاقها كان خاطئًا.

1. PHASE A — CURRENT TRUTH CLEANUP

ابدأ أولًا بتثبيت الحقيقة الحالية فعليًا.

افحص Production Supabase مباشرة:

Edge Functions
PostgreSQL Functions
Tables
Constraints
Triggers
RLS
Auth Users
Public Users
Runsheets
Canary / Test artifacts
Test data
Test workflows
Migration state

ثم طابق ذلك مع GitHub:

Historical

rawaie-erp-review/Edge_Functions/

Original

rawaie-erp-New/Original/

Current

rawaie-erp-New/Current/

Git history / commits / rescue branches

لا تعتمد على أي تقرير سابق كحقيقة.

2. إزالة البيانات التجريبية غير المطلوبة

ابحث عن:

Test Users
Canary Users
Test Runsheets
Test Orders
Test fixtures
Temporary Edge Functions
Temporary workflows
Sentinel artifacts
obsolete migrations

لكن:

ممنوع الحذف الأعمى.

لكل Artifact:

IDENTIFY
→ PROVE TEST-ONLY
→ CHECK DEPENDENCIES
→ REMOVE
→ VERIFY ABSENCE

مع الحفاظ الكامل على بيانات المالك والبيانات الحقيقية.

ACTIVE + 410 ≠ Deleted.

3. CURRENT ↔ PRODUCTION PARITY

بعد تنظيف الحقيقة:

لكل Edge Function سبق إصلاحها:

Current source
vs
Production deployed source

حدد:

Current SHA
Production version
Production package SHA
Semantic parity
Deployment parity
أي Drift

خصوصًا:

complete-picking
complete-loading
start-loading
reopen-loading
unload-runsheet
start-picking

ولا تعلن parity من مجرد تشابه الاسم أو الإصدار.

4. لا تعيد فتح ما أُغلق

وفق التقرير الحالي:

complete-picking
complete-loading
start-loading
reopen-loading
unload-runsheet

معتبرة مغلقة وظيفيًا.

لا تعيد فتحها لمجرد أنك تريد تحسين التقرير.

افتحها فقط إذا ظهر Defect مثبت في Production أو Core.

5. PHASE B — ZERO-DEBT INVENTORY SWEEP

بعد تثبيت الحقيقة فقط، ابدأ:

send-stock-voucher

وهذه تصبح Closure Unit الوحيدة النشطة.

ثم بالتتابع:

send-stock-voucher
→ receive-stock-voucher
→ receive-purchase
→ bulk-stock-adjustment
→ save-sales-invoice
→ complete-return
→ complete-order-delivery

ولا تنتقل للدالة التالية قبل إغلاق الحالية 100%.

6. BEFORE EACH FUNCTION — MANDATORY READINESS

قبل تعديل أي Function يجب أن تقدم:

PRE-CHANGE SELF-AUDIT
Business Understanding:
Architecture Understanding:
Database Understanding:
Historical Understanding:
Production Understanding:
Current Understanding:
Execution Confidence:

Confirmed Facts:
Unknowns:
Conflicts:
Unverified Claims:

ثم:

Historical Opened:
Original Opened:
Production Opened:
Current Opened:
Schema Checked:
Triggers Checked:
Dependencies Checked:
Consumers Checked:

ثم تشرح:

مسؤولية الدالة
كل الوظائف الحالية
كل الوظائف التاريخية
كل Consumers
كل Dependencies
Inventory effect
Accounting effect
Lifecycle effect
Target contract
ما يجب الحفاظ عليه
ما يجب نقله إلى Core
ما يجب إزالته عمدًا
أين توجد النسخة الأصلية
أين توجد النسخة النهائية في Current
ما الموجود فعليًا في Production

لا تعديل قبل إثبات هذا الفهم.

7. SURGICAL CHANGE RULE

التعديل يجب أن يكون:

SURGICAL

أي:

الحفاظ على الـHTTP Contract.
الحفاظ على PWA consumers.
الحفاظ على الوظائف الصحيحة.
نقل Physical Stock Logic إلى Core.
عدم إنشاء Business Engine جديد داخل Edge.
عدم تكرار Sources of Truth.
عدم إنشاء Workarounds مؤقتة.

لكن:

السurgical لا يعني عدم إصلاح المشكلة.

إذا احتاجت الدالة إعادة بناء حقيقية للوصول إلى العقد النهائي، نفذها.

8. CENTRAL INVENTORY CONTRACT

العقد النهائي:

Physical Stock Movement
        ↓
post_stock_movement
        ↓
stock_branches
        +
inventory_log

والفصل:

reserve_stock
=
Reservation
release_stock_reservation
=
Reservation Reversal
setup_van_stock
=
Initialization only

أي Function أخرى تتجاوز هذا العقد يجب:

DISCOVER
→ CLASSIFY
→ REWIRE
→ TEST
→ DEPLOY
→ VERIFY

ولا تُترك كـParallel Engine.

9. ACCOUNTING / BUSINESS LOGIC

لا تحذف مسؤولية محاسبية أو تشغيلية.

عند نقل وظيفة:

EDGE
→ CORE

يجب إثبات مكانها الجديد.

واستخدم الأنظمة الناضجة كمرجع في الأمور الثابتة:

SAP
Microsoft Dynamics
Odoo
وغيرها

لكن:

لا تنسخ التصميم أعمى؛ خذ المبدأ وطبقه على RAWAEA Contract.

10. IMPLEMENTATION ≠ REPORT

الـEvidence يجب تصنيفه:

THEORETICAL
CURRENT ONLY
STAGING VERIFIED
DEPLOYED
PRODUCTION RUNTIME VERIFIED
100% CLOSED

ولا تعتبر:

Staging = Production

ولا:

Git = Production

ولا:

Deployed = Runtime Verified
11. العوائق

ممنوع:

FOUND BLOCKER
→ STOP

المطلوب:

FOUND BLOCKER
→ ROOT CAUSE
→ SEARCH ALL SOURCES
→ FIND SOLUTION
→ IMPLEMENT
→ TEST
→ DEPLOY
→ VERIFY

إذا كانت هناك عملية لا تستطيع الأداة تنفيذها:

حدد الإجراء اليدوي المطلوب من المالك بدقة، ثم أكمل كل ما يمكن تنفيذه فورًا.

لا تجعل عائقًا واحدًا يوقف Closure Unit كاملة.

12. ZERO-DEBT RULE

لا تحمل:

Unknown
Unverified Claim
Conflict
Missing Artifact
Current/Production Drift
Legacy Writer
Test Artifact
Governance Debt

إلى الدالة التالية.

كل Closure Unit يجب أن تنتهي:

Remaining Defects = NONE
13. FINAL TEST REQUIREMENTS

لكل Function:

Normal
Partial
Retry
Duplicate
Concurrent
Invalid State
Insufficient Data/Stock
Failure/Rollback
Company Isolation
Consumer Compatibility

ثم:

Production Runtime Verification

ثم:

Baseline Restoration

ثم:

Governance Cleanup
14. FINAL CLOSURE REPORT

في نهاية كل Function:

SELF-AUDIT FINAL
What I Proved
What I Did Not Prove
What I Fixed
What I Initially Missed
What Could Still Be Wrong
Final Confidence
Final Closure Status

ثم:

Current Artifact:
Current SHA:

Production Version:
Production SHA:

Core RPCs:
Dependencies:
Consumers:

Tests:
Production Evidence:

Remaining Defects:
Remaining Governance:
لا تكتب 100% CLOSED

إلا إذا كان:

Code + Core + Dependencies + Consumers + Staging + Production + Governance

كلها مثبتة.

15. EXECUTION ORDER NOW
المرحلة الحالية فقط:
A — Truth Cleanup
↓
B — Remove Test/Canary Debt
↓
C — Current ↔ Production Parity
↓
D — send-stock-voucher
↓
E — receive-stock-voucher
↓
F — receive-purchase
↓
G — bulk-stock-adjustment
↓
H — save-sales-invoice
↓
I — complete-return
↓
J — complete-order-delivery
↓
K — Global Inventory Writer Sweep
↓
L — Final Inventory Rescue Closeout

لا تبدأ send-stock-voucher قبل إغلاق A/B/C.

16. FINAL COMMAND

نفّذ الآن.

لا أريد:

تقريرًا عن النوايا.
قائمة عوائق.
Readiness فقط.
خطة نظرية.
توقفًا عند أول مشكلة.

أريد:

DISCOVER → CLEAN → RECONCILE → REPAIR → TEST → DEPLOY → VERIFY → CLOSE

ثم فقط الانتقال للدالة التالية.

المشروع يحتاج CTO يزيل العوائق، لا CTO يحول العوائق إلى تقارير.

والحل النهائي يجب أن يكون دائمًا، مركزيًا، قابلًا للتوسع، ومتماشيًا مع العقد المعماري الكامل لـRAWAEA، وليس ترقيعًا مؤقتًا.
