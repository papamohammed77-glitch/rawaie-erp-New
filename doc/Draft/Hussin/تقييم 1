النتيجة النهائية

هذه المرة الإصلاح حقيقي وليس نظريًا.

picker@rawaea.com مرتبط فعليًا وصحيحًا:
auth.users.id = public.users.auth_id، والشركة والدور والحالة صحيحة.
ولدينا حاليًا 31 public users، 5 بلا auth_id، و2 auth users بلا public.users؛ أي أن مشكلة الربط العامة لم تُحل للنظام كله بعد. لكن حساب picker@rawaea.com نفسه سليم.
start-picking في Production الآن v32 ACTIVE، ويستخدم:
auth_id → public.users.company_id → runsheet.
وهذا مطابق للـCurrent الحالي أيضًا، مع اختلاف SHA بسبب تاريخ النسخ.
لذلك الخطأ القديم users_email_key عولج فعلًا، والسبب الحالي لو ظهر للمستخدم سيكون شيئًا مختلفًا: عدم وجود public.users مرتبط بـauth_id.
لكن هناك نقطة مهمة

التقرير الأصلي يقول إن المشكلة هي 5 سجلات بلا auth_id، وهذا صحيح فعليًا للنظام عمومًا، لكن ليس لـpicker@rawaea.com.

إذن المشكلة العامة أصبحت:

Data Integrity / Identity Mapping Debt في public.users

وليست مشكلة picker.html.

موقف خطة Inventory

start-picking لا ينفذ Physical Stock Movement، وهذا صحيح.
لكن إصلاح الهوية/الـtenant ما زال غير مكتمل System-wide بسبب الـ5 سجلات غير المرتبطة.

وهل أطلب منك تجربة؟

نعم. جرّب الآن picker@rawaea.com من picker.html على RS-2.

هذه هي أفضل تجربة مباشرة للحسم؛ لأن بيانات الحساب الآن صحيحة، واستدعاء picker.html نفسه سليم.

ولا تعدّل picker.html.

الحكم

العيب المحدد الذي اكتشفناه: تم إصلاحه ونشره.
لكن Data Integrity العامة للمستخدمين ليست 100% بعد.
