# MANDATORY CTO EXECUTION DIRECTIVE

## PHASE CURRENT — EXECUTE ONLY THIS SEQUENCE

من هذه اللحظة لا تعيد فتح Picker ولا تبدأ Closure Unit جديدة قبل إنهاء التسلسل التالي بالكامل:

**A — Governance / Artifacts Closeout**
↓
**B — Fresh Production Truth for `send-stock-voucher`**
↓
**C — Fresh Production HTTP E2E for `send-stock-voucher`**
↓
**D — 100% Closure**
↓
**E — `receive-stock-voucher`**
↓
ثم بقية الوحدات بالتتابع.

---

# 1. لا تعتمد على أي تقرير سابق

راجع Production مباشرة.

راجع:

* Live Edge Functions
* Live PostgreSQL Core
* Live migrations
* Current
* Historical
* Original
* Archive
* Git commits
* Consumers

Production هو المرجع التنفيذي الحالي، وليس التقرير أو version number.

---

# 2. GOVERNANCE / ARTIFACT CLOSEOUT أولًا

احصر كل:

* Temporary Edge Functions
* Canary Functions
* E2E Fixtures
* Test Users
* Test Workflows
* Test Triggers
* Test artifacts

ولا تحذف شيئًا إلا بعد:

`IDENTIFY`
→ `PROVE TEST-ONLY`
→ `CHECK REFERENCES`
→ `REMOVE`
→ `VERIFY ABSENCE`

قاعدة إلزامية:

**ACTIVE + 410 ≠ DELETED**

إذا كانت أداة Supabase لا توفر Delete:

* حدد أسماء الكائنات بدقة.
* حدد إجراء الحذف اليدوي المطلوب من المالك.
* لا توقف بقية التنفيذ.
* أكمل كل Governance cleanup الممكن آليًا.
* لا تعلن Governance = PASS قبل التحقق من غياب الكائنات فعلًا.

---

# 3. لا تلمس الوظائف المغلقة

لا تعيد فتح:

* `release_stock_reservation`
* `setup_van_stock`
* أي Closure Unit ثبتت صحتها

إلا إذا ظهر **Defect مباشر جديد من Production/Core**.

ولا تعكس إصلاحًا صحيحًا لمجرد أن تقريرًا قديمًا اختلف معه.

---

# 4. `send-stock-voucher` — إعادة فتح Fresh Verification فقط

هذه هي **Closure Unit الوحيدة النشطة بعد Governance cleanup**.

لا تفترض أن Production v19 صحيحة لأنها ظهرت في تقرير سابق.

افحص الآن مباشرة:

```text
send-stock-voucher
version
status
verify_jwt
package SHA
actual deployed source
```

ثم:

```text
send_stock_voucher_atomic
post_stock_movement
```

وأثبت:

```text
HTTP
→ Auth
→ public.users.auth_id
→ public.users.company_id
→ send_stock_voucher_atomic
→ post_stock_movement
→ stock_branches
→ inventory_log
→ response
```

---

# 5. المقارنة الإلزامية

افتح:

### Historical

`rawaie-erp-review/Edge_Functions/original/`

### Archive

`rawaie-erp-review/Edge_Functions/archive/`

### Current

`rawaie-erp-New/Current/Edge_Functions/send-stock-voucher`

### Original

`rawaie-erp-New/Original/` إن وجد

### Production

Supabase Live

وقارن:

```text
Historical
vs
Original
vs
Current
vs
Production
vs
Target Contract
```

أنشئ:

## LOSS / GAIN MATRIX

لكل مسؤولية:

`RETAINED`
`MOVED`
`HARDENED`
`ADDED`
`INTENTIONALLY REMOVED`
`MISSING`
`CONFLICT`

لا يجوز فقد أي مسؤولية صحيحة دون إثبات مكان انتقالها.

---

# 6. Fresh Production E2E — لا تعيد استخدام Evidence قديمة

نفذ **اختبارًا جديدًا** على Production الحالي.

استخدم Fixture/بيانات اختبار معزولة موجودة أصلًا، أو أنشئ بيانات مؤقتة بوسائل آمنة ثم نظفها بالكامل.

اختبر:

### Normal Send

Draft → Sent

### Retry

نفس الحدث مرة أخرى.

### Duplicate

نفس event identity.

### Invalid State

Voucher غير Draft.

### Wrong Company

Company mismatch.

### Insufficient Stock

المخزون غير كافٍ.

### Atomic Rollback

عدة خطوط: بعضها صحيح وبعضها يفشل.

### Inventory

تحقق من:

```text
source qty delta
allocated_qty unchanged
inventory_log exactly once
```

### Accounting

تأكد من الأثر المحاسبي الصحيح.

### Consumer

اختبر المسار الفعلي:

`create-stock-voucher → send-stock-voucher`

### Baseline

بعد الاختبارات:

```text
Test Users = 0
Test Vouchers = 0
Test Runsheets = 0
Test Orders = 0
Test Logs = 0
```

أثبت الاستعادة الفعلية.

---

# 7. CONCURRENCY

لا تدّعِ Concurrent PASS بناءً على:

* FOR UPDATE وحده.
* تحليل نظري.
* Sequential Retry.

إما أن تنفذ جلسات مستقلة فعلية، أو تصنف الدليل بوضوح أنه **Concurrency Contract Verified** فقط وليس Runtime Concurrency Evidence.

---

# 8. لا تحول العائق إلى STOP

عند ظهور عائق:

ممنوع:

`FOUND BLOCKER → STOP`

المطلوب:

`FOUND BLOCKER`
→ `ROOT CAUSE`
→ `SEARCH ALL SOURCES`
→ `FIND SOLUTION`
→ `IMPLEMENT`
→ `TEST`
→ `DEPLOY`
→ `VERIFY`

إذا احتاج الأمر إجراءً يدويًا من المالك:

**حدده بدقة ولا توقف بقية العمل.**

---

# 9. Current Source of Truth

أي نسخة نهائية لـ`send-stock-voucher` يجب أن تكون موجودة في:

`rawaie-erp-New/Current/Edge_Functions/send-stock-voucher`

ثم:

`Current`
↔ `Production`

يجب توثيق:

* Current SHA
* Production version
* Production package SHA
* Semantic parity
* Deployment parity
* أي Drift

لا تعتبر اختلاف الـSHA دليل Drift تلقائيًا إذا كان السبب packaging، لكن يجب إثبات ذلك.

---

# 10. SELF-AUDIT إلزامي

## مقدمة التقرير

```text
Business Understanding:
Architecture Understanding:
Database Understanding:
Historical Understanding:
Production Understanding:
Current Understanding:
Execution Confidence:

Confirmed Facts:
Unknowns:
Conflicts:
Unverified Claims:

Historical Opened:
Original Opened:
Production Opened:
Current Opened:
Schema Checked:
Triggers Checked:
Dependencies Checked:
Consumers Checked:
```

## نهاية التقرير

```text
What I Proved
What I Did Not Prove
What I Fixed
What I Initially Missed
What Could Still Be Wrong
Final Confidence
Final Closure Status
```

---

# 11. 100% CLOSED RULE

لا تستخدم:

`95%`
`98%`
`READY`
`RELEASE-READY`

كبديل عن الإغلاق.

الحكم الوحيد:

### `100% CLOSED`

عندما تثبت:

* Historical comparison
* Current final artifact
* Production parity
* Core correctness
* Dependencies
* Consumers
* Staging
* Fresh Production HTTP E2E
* Production runtime
* Baseline restoration
* Governance cleanup

أي نقص واحد:

### `INCOMPLETE`

لكن **INCOMPLETE ليست حالة توقف**؛ بل حالة إصلاح حتى الإغلاق.

---

# 12. بعد إغلاق SEND

فقط بعد:

### `send-stock-voucher = 100% CLOSED`

ابدأ:

`receive-stock-voucher`

ثم:

`receive-purchase`

ثم:

`bulk-stock-adjustment`

ثم:

`save-sales-invoice`

ثم:

`complete-return`

ثم:

`complete-order-delivery`

ثم:

# GLOBAL INVENTORY WRITER SWEEP

ثم:

# FINAL INVENTORY RESCUE CLOSEOUT

---

# FINAL COMMAND

نفّذ الآن.

**لا أريد Reconnaissance جديدًا بلا نتيجة.**
**لا أريد تقريرًا بدل التنفيذ.**
**لا أريد إعادة استخدام Evidence قديمة لإغلاق Production الحالي.**
**لا أريد Block.**

أريد:

`CLEAN GOVERNANCE`
→ `REVERIFY PRODUCTION`
→ `COMPARE`
→ `REPAIR IF NEEDED`
→ `FRESH E2E`
→ `RESTORE BASELINE`
→ `VERIFY`
→ `100% CLOSE`

ثم فقط:

`receive-stock-voucher`

وهكذا حتى:

# ZERO-DEBT INVENTORY RESCUE — 100% PRODUCTION VERIFIED
