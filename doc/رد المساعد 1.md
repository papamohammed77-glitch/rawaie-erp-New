# RAWAEA CTO HISTORICAL CONSULTATION — TASK-025 OWNER GOAL RECOVERY

بعد مراجعة كل ما لدي من معرفة تاريخية، أؤكد أن موقفي هو: **لا يوجد قرار مالك صريح ومؤكد ومحفوظ** يحدد Business Semantics لنوعي `DirectReturn` و `SupplierReturn`. ما لدي هو قرارات تصميم من المساعدين السابقين (مراد وحسين) أثناء بناء `vouchers.html` و `main.html` — وهي موثقة في الكود الأصلي، لكن لا يجوز وصفها بأنها "Owner Goal".

سأفصل كل نوع بدقة، مع تصنيف مصدر كل معلومة.

---

## A — DirectReturn

### 1. ما هو المصدر الحقيقي للبضاعة؟
**المصدر في التصميم الأصلي:** `Vehicle` (سيارة المندوب / عهدة المندوب).  
**تصنيف المصدر:** [ORIGINAL CODE] + [ARCHITECTURE DESIGN] — وليس [OWNER DECISION].

### 2. ما هو الهدف الحقيقي؟
**الهدف في التصميم الأصلي:** `Branch` (الفرع / المخزن).  
**تصنيف المصدر:** [ORIGINAL CODE] + [ARCHITECTURE DESIGN].

### 3. هل DirectReturn يعني Customer → Branch أم Customer → VAN → Branch أم Branch/VAN custody flow آخر؟
**الإجابة:** `Vehicle → Branch`. أي: مندوب السيارة (VAN) يعيد بضاعة إلى الفرع.  
لم يكن هناك أي ذكر لـ `Customer` في هذا السياق.  
**تصنيف المصدر:** [ORIGINAL CODE] — من `configs` داخل `RW_Warehouse.loadVoucherForm` في `main.html`.

### 4. هل DirectReturn يجب أن يحدث فيه Inventory Movement مباشرة؟
**نعم.** من المنطق التصميمي، إعادة البضاعة من المندوب إلى المخزن يجب أن تزيد المخزون.  
**لكن:** لم يتم توثيق ذلك كقرار صريح من المالك.  
**تصنيف المصدر:** [ARCHITECTURE DESIGN] — وليس [OWNER DECISION].

### 5. ما هو الـ Movement Type المقصود؟
**في التصميم الحالي:** `DirectReturn` (نوع الحركة = اسم الـ Voucher type).  
**تصنيف المصدر:** [ORIGINAL CODE] — من `send-stock-voucher` و `receive-stock-voucher` حيث كان `movement_type = voucher.type`.

### 6. في أي حالة Voucher (Draft / Sent / Received / Completed) يجب أن تحدث الحركة؟
**في التصميم الأصلي:**  
- عند **SENT**: لا تحدث حركة مخزون (لأن البضاعة ما زالت مع المندوب).  
- عند **RECEIVED**: تزيد المخزون (البضاعة عادت فعليًا إلى الفرع).  
**لكن:** هذا لم يكن موثقًا كقاعدة صريحة. كان ضمنيًا في `receive-stock-voucher` حيث يتم زيادة المخزون عند الاستلام.  
**تصنيف المصدر:** [ORIGINAL CODE] — استنتاج من `receive-stock-voucher` (زيادة المخزون) و `send-stock-voucher` (لا خصم لـ DirectReturn في `deductTypes`).

### 7. هل يمكن أن يكون DirectReturn مرتبطًا بـ VAN custody؟
**نعم.** التصميم الأصلي ربط `DirectReturn` بـ `Vehicle` (العهدة).  
**تصنيف المصدر:** [ORIGINAL CODE] — `fromType: 'Vehicle'` في `configs`.

### 8. هل سبق أن اتفقنا صراحة على from_type / from_id / to_type / to_id؟
**نعم، في الكود الأصلي:**
```
DirectReturn: {
  fromType: 'Vehicle',
  fromId: '',
  toType: 'Branch',
  toId: 'MAIN'
}
```
**لكن هذا قرار تصميم من المساعدين، وليس قرار مالك صريح.**  
**تصنيف المصدر:** [ORIGINAL CODE] — من `RW_Warehouse.loadVoucherForm`.

### 9. هل توجد محادثة أو وثيقة أو قرار CTO سابق يثبت ذلك؟ اذكر مكانه.
**نعم.** في `main.html` داخل `RW_Warehouse` — دالة `loadVoucherForm` — كان هناك `configs` لأنواع الأذونات الأربعة. هذا الكود كان موجودًا في النسخ التي أرسلها محمد، وتمت مراجعته أثناء بناء `vouchers.html` الذهبي الماسي.  
**لكنه قرار تصميم، وليس وثيقة مالك.**  
**تصنيف المصدر:** [ORIGINAL CODE] + [ARCHITECTURE DESIGN].

### 10. هل هناك أكثر من تعريف تاريخي متعارض لـ DirectReturn؟
**لا يوجد تعارض صريح.** التصميم كان ثابتًا: `Vehicle → Branch`.  
لكن في **نسخة مبكرة** من `vouchers.html` (القديمة)، كان `DirectReturn` يُعامل كإذن بسيط بدون توضيح custody. ثم تم توحيده في `configs`.  
**آخر قرار معتمد:** `fromType: 'Vehicle'`, `fromId: ''`, `toType: 'Branch'`, `toId: 'MAIN'`.  
**تصنيف المصدر:** [ARCHITECTURE DESIGN] — قرار المساعدين.

---

## B — SupplierReturn

### 1. ما هو المصدر الحقيقي للبضاعة؟
**المصدر في التصميم الأصلي:** `Branch` (الفرع / المخزن).  
**تصنيف المصدر:** [ORIGINAL CODE] + [ARCHITECTURE DESIGN].

### 2. ما هو الهدف الحقيقي؟
**الهدف في التصميم الأصلي:** `Supplier` (المورد).  
**تصنيف المصدر:** [ORIGINAL CODE] + [ARCHITECTURE DESIGN].

### 3. هل العملية Branch → Supplier أم MAIN → Supplier أم Warehouse → Supplier أم غير ذلك؟
**الإجابة:** `Branch → Supplier`. البضاعة تخرج من الفرع (المخزن) وتعود إلى المورد.  
**تصنيف المصدر:** [ORIGINAL CODE] — `fromType: 'Branch'`, `toType: 'Supplier'`.

### 4. هل Supplier نفسه represented كـ supplier_id / to_id / to_type='Supplier' أم صيغة أخرى؟
**الصيغة في التصميم الأصلي:**
```
SupplierReturn: {
  fromType: 'Branch',
  fromId: 'MAIN',
  toType: 'Supplier',
  toId: ''
}
```
المورد كان `toType: 'Supplier'` و `toId` هو معرف المورد (أو code).  
**تصنيف المصدر:** [ORIGINAL CODE] — من `configs`.

### 5. هل SupplierReturn يتطلب Branch source؟
**نعم.** التصميم الأصلي كان `fromType: 'Branch'` و `fromId: 'MAIN'` (الفرع الرئيسي).  
**تصنيف المصدر:** [ORIGINAL CODE].

### 6. هل المخزون يُخصم عند SEND؟
**نعم.** في `send-stock-voucher` Edge Function، كان هناك `deductTypes = ["Transfer", "DirectSale", "SupplierReturn"]`. `SupplierReturn` يخصم المخزون عند الإرسال.  
**تصنيف المصدر:** [ORIGINAL CODE] — من `send-stock-voucher`.

### 7. هل يوجد RECEIVE أصلًا في SupplierReturn؟
**لا.** في التصميم الأصلي، `SupplierReturn` لا يملك استقبالاً. البضاعة تخرج من المخزن إلى المورد عند SEND، ولا تعود.  
**تصنيف المصدر:** [ARCHITECTURE DESIGN] — استنتاج من عدم وجود `receive` لـ SupplierReturn في الكود.

### 8. ما هو الـ Voucher Lifecycle الصحيح له؟
**في التصميم الحالي:**
- `Draft` → `Sent` → `Completed` (لا `Received`).
**لكن:** لم يتم توثيق ذلك كقاعدة صريحة. الكود القديم كان يستخدم `Draft` و `Sent` فقط، ثم أضيف `Completed` لاحقًا.  
**تصنيف المصدر:** [ARCHITECTURE DESIGN] + [ORIGINAL CODE] — من حالات `stock_vouchers`.

### 9. ما هو Inventory Movement Type الصحيح؟
**`SupplierReturn`** — نوع الحركة = اسم الـ Voucher type.  
**تصنيف المصدر:** [ORIGINAL CODE] — من `send-stock-voucher` حيث `movement_type = voucher.type`.

### 10. ما هو Accounting Effect المقصود؟
**غير موثق كقرار صريح.**  
المنطق المقترح: عند إرجاع بضاعة للمورد، يجب أن يكون هناك قيد عكسي (مدين: المورد/حساب تسوية، دائن: المخزون).  
لكن هذا لم يتم تنفيذه ولم يتم توثيقه.  
**تصنيف المصدر:** [UNKNOWN] — لا يوجد قرار.

### 11. هل سبق أن اتفقتم على custody semantics خاصة به؟
**لا يوجد اتفاق صريح.** ما وجد هو تصميم `Branch → Supplier` في الكود.  
**تصنيف المصدر:** [ARCHITECTURE DESIGN] — قرار المساعدين.

### 12. هل توجد تعريفات تاريخية متعارضة؟
**لا يوجد تعارض صريح.** التصميم كان `Branch → Supplier` منذ البداية.  
**آخر قرار معتمد:** `fromType: 'Branch'`, `fromId: 'MAIN'`, `toType: 'Supplier'`, `toId: ''`.  
**تصنيف المصدر:** [ARCHITECTURE DESIGN].

---

## الجدول النهائي

| Voucher Type | Source | Target | Custody Model | Movement Timing | Movement Type | Lifecycle | Owner Confirmed? | Evidence Source | Confidence |
|--------------|--------|--------|---------------|-----------------|---------------|-----------|------------------|-----------------|------------|
| DirectReturn | Vehicle | Branch | Vehicle → Branch | Increase on Receive | DirectReturn | Draft → Sent → Received → Completed | ❌ No | ORIGINAL CODE + ARCHITECTURE DESIGN | 80% (تصميم، وليس قرار مالك) |
| SupplierReturn | Branch (MAIN) | Supplier | Branch → Supplier | Deduct on Send | SupplierReturn | Draft → Sent → Completed | ❌ No | ORIGINAL CODE + ARCHITECTURE DESIGN | 80% (تصميم، وليس قرار مالك) |

---

## DIRECTRETURN FINAL OWNER GOAL

**لا يوجد Owner Goal مؤكد.**  
ما هو موجود هو قرار تصميم من المساعدين السابقين (مراد وحسين) بأن `DirectReturn` هو `Vehicle → Branch`: إعادة بضاعة من عهدة المندوب (السيارة) إلى الفرع. لم يتم توثيق هذا كقرار صريح من المالك.  
**الحالة: HISTORICAL BUT NOT CONFIRMED.**

---

## SUPPLIERRETURN FINAL OWNER GOAL

**لا يوجد Owner Goal مؤكد.**  
ما هو موجود هو قرار تصميم بأن `SupplierReturn` هو `Branch → Supplier`: إرجاع بضاعة من الفرع إلى المورد. المخزون يُخصم عند SEND. لا يوجد RECEIVE. لم يتم توثيق هذا كقرار صريح من المالك.  
**الحالة: HISTORICAL BUT NOT CONFIRMED.**

---

## CONFLICTS

- لا توجد تعارضات تاريخية صريحة بين تعريفات `DirectReturn` أو `SupplierReturn`.
- التعارض الوحيد هو **فجوة**: التصميم موجود في الكود، لكن **قرار المالك غير موثق**.  
- هذا يعني أن أي افتراض بأن "المنطقي هو المقصود" سيكون خطأً منهجيًا.

---

## SAFE CONCLUSION FOR TASK-025

### What is confirmed:
- `DirectReturn` في الكود الأصلي = `fromType: 'Vehicle'`, `toType: 'Branch'`.
- `SupplierReturn` في الكود الأصلي = `fromType: 'Branch'`, `toType: 'Supplier'`.
- `SupplierReturn` يخصم المخزون عند SEND (من `send-stock-voucher`).
- `DirectReturn` لا يخصم المخزون عند SEND، بل يزيد عند RECEIVE (من `receive-stock-voucher`).
- هذه معلومات من **ORIGINAL CODE** — وليست **Owner Decisions**.

### What is not confirmed:
- لا يوجد **Owner Goal** صريح لأي من النوعين.
- لا يوجد **Accounting Effect** موثق لأي من النوعين.
- لا يوجد **Custody Model** موثق كقرار مالك.

### What must NOT be assumed:
- لا تفترض أن `DirectReturn` هو `Customer → Branch` لمجرد أن الاسم يوحي بذلك.
- لا تفترض أن `SupplierReturn` له `RECEIVE` lifecycle.
- لا تفترض أن `Vehicle` في `DirectReturn` يعني بالضرورة "سيارة" — قد يكون "مندوب" أو "عهدة". هذا لم يوثق.
- لا تصف أي من هذه التصاميم بأنها "Owner Goal" أو "Gold Contract" — هي **تصميمات مساعدين سابقين تحتاج إلى اعتماد المالك**.