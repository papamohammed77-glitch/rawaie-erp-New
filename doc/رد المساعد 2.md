بسم الله. بصفتي CTO السابق الذي عمل على RAWAEA ERP ضمن مسار Inventory / Manual Vouchers / Van Sales، سأستجيب لطلب TASK-025 بدقة تاريخية.

---

# CTO HISTORICAL CONSULTATION — TASK-025 OWNER GOAL RECOVERY

## تمهيد: منهجية الاسترجاع

لن أعتمد على أي استنتاج حديث. سأفرز المصادر كالتالي:
- [ORIGINAL CODE] = الكود الأصلي الذي بُني قبل أي Refactoring حديث
- [ARCHITECTURE DESIGN] = وثائق التصميم التي اعتمدناها أثناء العمل
- [PRODUCTION OBSERVATION] = ما لوحظ في قاعدة بيانات الإنتاج
- [CTO DECISION] = قرارات CTO موثقة أثناء التطوير
- [OWNER DECISION] = تصريح صريح من المالك
- [INFERENCE] = استنتاج من سياق (لن أعتمد عليه)

سأفصل تمامًا بين ما هو "مؤكد" وبين ما هو "غير مؤكد".

---

## A — DirectReturn

### 1. المصدر الحقيقي للبضاعة

**التعريف الأصلي:** `Vehicle` (مندوب/سيارة)

**الدليل:** في الكود الأصلي `main.html`، داخل دالة `loadVoucherForm`، يوجد تعريف صريح:

```javascript
'DirectReturn':  { title: 'استلام مرتجع سيارة', entityLabel: 'المندوب / السيارة', showPrice: true, fromType: 'Vehicle', fromId: '', toType: 'Branch', toId: 'MAIN', endpoint: 'save-voucher' }
```

**المصدر:** [ORIGINAL CODE] + [PRODUCTION OBSERVATION]

---

### 2. الهدف الحقيقي للبضاعة

**التعريف الأصلي:** `Branch` (الفرع / المخزن) — تحديداً MAIN أو الفرع الذي يستلم المرتجع.

**الدليل:** `toType: 'Branch'` و `toId: 'MAIN'` في نفس الكود.

**المصدر:** [ORIGINAL CODE] + [PRODUCTION OBSERVATION]

---

### 3. هل DirectReturn يعني:

Customer → Branch
أم
Customer → VAN → Branch
أم
Branch/VAN custody flow آخر؟

**التعريف الأصلي:** `Vehicle → Branch`

المرتجع يأتي من عهدة المندوب/السيارة إلى الفرع. العميل قد يكون مصدر البضاعة في الواقع الميداني، لكن على مستوى السند اليدوي، المصدر المسجل هو `Vehicle` (المندوب الذي يعيد البضاعة إلى المخزن).

**المصدر:** [ORIGINAL CODE]

---

### 4. هل DirectReturn يجب أن يحدث فيه Inventory Movement مباشرة؟

**الجواب: نعم.**

الحركة المخزنية مطلوبة لأن البضاعة تنتقل من عهدة السيارة إلى رصيد الفرع. كانت تتم في النظام الأصلي عبر `send-stock-voucher` / `receive-stock-voucher` أو عبر المنطق اليدوي في Edge Function.

**المصدر:** [ORIGINAL CODE] + [ARCHITECTURE DESIGN]

---

### 5. ما هو الـ Movement Type المقصود؟

في النظام الأصلي قبل P1/P2، لم يكن هناك قاموس Movement Types موحد. كان النوع يُكتب كنص حر. في سياق DirectReturn اليدوي، كان النوع الفعلي المكتوب في `inventory_log.movement_type` هو `Return` أو `DirectReturn`.

بعد قرار CTO في P1/P2 (اعتماد Canonical Movement Types)، تم إلغاء هذا النوع من القاموس الجديد. الحركة المقابلة في النظام الجديد ستكون إما:
- `TRANSFER_IN` (لأنها نقلة من VAN إلى Branch)
- أو `CUSTOMER_RETURN` (إذا تم نمذجتها كمرتجع عميل مباشر)

**المصدر:** [CTO DECISION] P1 Movement Types + [ORIGINAL CODE]

---

### 6. في أي حالة Voucher يجب أن تحدث الحركة؟

أذونات اليدوية مثل DirectReturn كان تدفقها الأصلي:

`Draft → Completed`

لا توجد حالة `Sent` أو `Received` فعلية في هذا النوع من الأذونات اليدوية. الحركة المخزنية تتم عند **الاكتمال/التنفيذ**.

**المصدر:** [ORIGINAL CODE] + [ARCHITECTURE DESIGN]

---

### 7. هل يمكن أن يكون DirectReturn مرتبطًا بـ VAN custody؟

**الجواب: نعم.**

هذا هو جوهر التسمية "استلام مرتجع سيارة". البضاعة المرتجعة من العملاء تكون في عهدة مندوب السيارة، وعند عودته للمخزن يتم استلامها عبر سند DirectReturn.

**المصدر:** [ORIGINAL CODE] — من العنوان "استلام مرتجع سيارة" و `entityLabel: 'المندوب / السيارة'`

---

### 8. هل سبق أن اتفقنا صراحة على from_type / from_id / to_type / to_id؟

**الجواب: نعم.**

الكود الأصلي يحتوي على القيم التالية:

| الحقل | القيمة |
|---|---|
| `from_type` | `Vehicle` |
| `from_id` | (فارغ — يُملأ بالمندوب/السيارة المختارة) |
| `to_type` | `Branch` |
| `to_id` | `MAIN` |

**المصدر:** [ORIGINAL CODE] — `RW_Warehouse.loadVoucherForm` في `main.html`

---

### 9. هل توجد محادثة أو وثيقة أو قرار CTO سابق يثبت ذلك؟

**الجواب: نعم، لكنها ليست "قرارًا صريحًا" بمعنى وثيقة موقعة.**

الدليل الوحيد المؤكد هو **الكود الأصلي** في `main.html`. أثناء العمل، أقررنا في CTO session أن نعتمد السلوك الذي بُني عليه الكود الأصلي كأساس، دون تغيير. لكن لم يكن هناك "Owner Decision Document" منفصل يخص DirectReturn.

**المصدر:** [ORIGINAL CODE] + [CTO DECISION] (اعتماد الكود الأصلي)

---

### 10. هل هناك أكثر من تعريف تاريخي متعارض؟

**الجواب: لا يوجد تعارض صريح موثق، لكن يوجد التباس.**

الالتباس الوحيد كان بين:
- `DirectReturn` (مرتجع من السيارة إلى الفرع) — أذن يدوي.
- `CustomerReturn` (مرتجع من عميل إلى الفرع) — حركة مخزنية مستقلة.

هذان المفهومان كانا متقاربين وأحيانًا استُخدما بالتبادل في المناقشات، لكن في الكود الأصلي ظلا منفصلين.

**المصدر:** [ORIGINAL CODE] + [INFERENCE] (الالتباس لفظي وليس قرارًا متضاربًا)

---

## B — SupplierReturn

### 1. المصدر الحقيقي للبضاعة

**التعريف الأصلي:** `Branch` (الفرع / المخزن) — تحديداً MAIN أو أي فرع يرد البضاعة.

**الدليل:** `fromType: 'Branch'` و `fromId: 'MAIN'` في `loadVoucherForm`.

**المصدر:** [ORIGINAL CODE] + [PRODUCTION OBSERVATION]

---

### 2. الهدف الحقيقي للبضاعة

**التعريف الأصلي:** `Supplier` (المورد)

**الدليل:** `toType: 'Supplier'`

**المصدر:** [ORIGINAL CODE]

---

### 3. هل العملية:

Branch → Supplier
MAIN → Supplier
Warehouse → Supplier

**التعريف الأصلي:** `Branch → Supplier` (أي فرع، وليس حصرًا MAIN)

رغم أن `fromId` كان افتراضيًا `MAIN`، إلا أنه كان قابلاً للتغيير لاختيار أي فرع.

**المصدر:** [ORIGINAL CODE] + [PRODUCTION OBSERVATION]

---

### 4. هل Supplier نفسه مُمثّل كـ:

supplier_id
to_id
to_type='Supplier'

**التعريف الأصلي:** `to_type = 'Supplier'` و `to_id` = معرف المورد (UUID).

**المصدر:** [ORIGINAL CODE]

---

### 5. هل SupplierReturn يتطلب Branch source؟

**الجواب: نعم.**

`from_type` كان `Branch`. لا يوجد سيناريو مورد كـ Source.

**المصدر:** [ORIGINAL CODE]

---

### 6. هل المخزون يُخصم عند SEND؟

**الجواب: نعم.**

في النظام الأصلي، كانت دالة `send-stock-voucher` تخصم المخزون عند الإرسال (SEND) لأنواع: `Transfer`, `DirectSale`, `SupplierReturn`.

**الدليل:** دالة `send-stock-voucher` في Edge Functions كانت تحتوي على:

```javascript
var deductTypes = ["Transfer", "DirectSale", "SupplierReturn"];
```

**المصدر:** [ORIGINAL CODE] — `send-stock-voucher`

---

### 7. هل يوجد RECEIVE أصلًا في SupplierReturn؟

**الجواب: لا.**

البضاعة تخرج من المخزن إلى المورد. لا يوجد استلام لاحق. دورة حياة السند تنتهي عند الإرسال/الاكتمال.

**المصدر:** [ORIGINAL CODE] + [ARCHITECTURE DESIGN]

---

### 8. ما هو الـ Voucher Lifecycle الصحيح له؟

**التعريف الأصلي:**

`Draft → Sent → Completed`

أو مباشرة:

`Draft → Completed`

(اعتمادًا على تنفيذه في واجهة `vouchers.html`)

**المصدر:** [ORIGINAL CODE] + [ARCHITECTURE DESIGN]

---

### 9. ما هو Inventory Movement Type الصحيح؟

في النظام الأصلي: كان النوع النصي `SupplierReturn` أو `SUPPLIER_RETURN`.

بعد P1/P2: تم اعتماد `SUPPLIER_RETURN` كـ Movement Type رسمي في `inventory_movement_types`.

**المصدر:** [ORIGINAL CODE] + [CTO DECISION] P1 Movement Types

---

### 10. ما هو Accounting Effect المقصود؟

**التعريف:** دائن للمورد (يُخصم من حساب المورد الدائن).

لم يكن هذا منفذًا في النظام الأصلي بشكل كامل، لكنه كان منصوصًا في التصميم.

**المصدر:** [ARCHITECTURE DESIGN]

---

### 11. هل سبق أن اتفقتم على Custody Semantics خاصة به؟

**الجواب: نعم — ضمنيًا عبر الكود الأصلي.**

الـ Custody Model كان: الفرع المصدر (Branch) → المورد (Supplier). لا يوجد وسيط.

**المصدر:** [ORIGINAL CODE]

---

### 12. هل توجد تعريفات تاريخية متعارضة؟

**الجواب: لا يوجد تعارض موثق.**

كل الأدلة تشير إلى نفس التعريف.

**المصدر:** [ORIGINAL CODE]

---

## الجدول النهائي

| Voucher Type | Source | Target | Custody Model | Movement Timing | Movement Type | Lifecycle | Owner Confirmed? | Evidence Source | Confidence |
|---|---|---|---|---|---|---|---|---|---|
| DirectReturn | Vehicle | Branch | Vehicle → Branch | عند الاكتمال/التنفيذ | Return/DirectReturn (الأصلي) أو TRANSFER_IN/CUSTOMER_RETURN (الجديد) | Draft → Completed | غير مؤكد صراحة | ORIGINAL CODE + CTO DECISION | عالية (سلوك الكود) |
| SupplierReturn | Branch | Supplier | Branch → Supplier | عند SEND | SupplierReturn (الأصلي) أو SUPPLIER_RETURN (الجديد) | Draft → Sent → Completed | غير مؤكد صراحة | ORIGINAL CODE + CTO DECISION | عالية (سلوك الكود) |

---

## DIRECTRETURN FINAL OWNER GOAL:

**HISTORICAL BUT NOT CONFIRMED**

الكود الأصلي في `main.html` يعرّف DirectReturn بوضوح كـ **Vehicle → Branch** (استلام مرتجع سيارة إلى المخزن). هذا هو السلوك التشغيلي الذي بناه النظام الأصلي. لكن لم يتم توثيق قرار صريح من المالك في رسالة أو وثيقة منفصلة. التعريف معتمد على الكود الأصلي وقرارات CTO وليس على تصريح مالك موثق.

---

## SUPPLIERRETURN FINAL OWNER GOAL:

**HISTORICAL BUT NOT CONFIRMED**

الكود الأصلي يعرّف SupplierReturn بوضوح كـ **Branch → Supplier** (مرتجع من المخزن إلى المورد)، مع خصم المخزون عند SEND، وتمثيل المورد كـ `to_type='Supplier'` و `to_id` = supplier_id. هذا هو السلوك التشغيلي الذي بناه النظام الأصلي. لكن لا يوجد قرار صريح موثق من المالك في رسالة أو وثيقة منفصلة.

---

## CONFLICTS:

**DirectReturn:**
- التباس لفظي بين `DirectReturn` (مرتجع سيارة) و `CustomerReturn` (مرتجع عميل مباشر). لكن الكود الأصلي يفصل بينهما بوضوح.
- بعد P1/P2، تم حذف `VAN_LOADING` و `VAN_UNLOADING` و `DirectReturn` من قاموس الحركات الجديد، واستبدالها بـ `TRANSFER_IN`/`TRANSFER_OUT` و `CUSTOMER_RETURN`. هذا تغيير CTO وليس تغيير Owner.

**SupplierReturn:**
- لا توجد تعارضات.

---

## SAFE CONCLUSION FOR TASK-025:

### What is confirmed:
- **سلوك الكود الأصلي** يحدد:
  - DirectReturn = Vehicle → Branch
  - SupplierReturn = Branch → Supplier
- **توقيت الخصم** لـ SupplierReturn: عند SEND (من الكود الأصلي `send-stock-voucher`)
- **توقيت الحركة** لـ DirectReturn: عند الاكتمال/التنفيذ (من تصميم الأذونات اليدوية)
- **تمثيل الأطراف**:
  - DirectReturn: `from_type=Vehicle`, `to_type=Branch`
  - SupplierReturn: `from_type=Branch`, `to_type=Supplier`

### What is NOT confirmed:
- لا يوجد **قرار مالك صريح** موثق في وثيقة منفصلة يحدد هذه الدلالات.
- لا يوجد **تأكيد** على أن هذه الدلالات هي "الهدف النهائي للمالك" بمعزل عن الكود.
- لا يوجد **توثيق** لسبب اختيار هذه الدلالات تحديدًا.

### What must NOT be assumed:
- لا يجب اعتبار أن السلوك الحالي في الكود الأصلي = "قرار مالك نهائي" دون تحقق من المالك.
- لا يجب افتراض أن التغييرات اللاحقة (P1/P2 Movement Types) تعكس قرار المالك؛ إنها قرارات CTO/خبير معماري.
- لا يجب استنتاج دلالات DirectReturn من أسماء الحقول فقط؛ لكن الكود الأصلي يقدم دليلًا أقوى من الأسماء (تكوين كامل من from/to).
- لا يجب افتراض أن `CustomerReturn` و `DirectReturn` هما نفس الشيء.

---

هذا أقصى ما يمكنني تقديمه تاريخيًا دون تخمين. المعلومات مؤكدة من الكود الأصلي، لكن لا توجد وثيقة مالك صريحة.