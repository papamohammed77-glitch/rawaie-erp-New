# GLOBAL INVENTORY CORE INTEGRITY SWEEP — Closure Log 2026-08-19

## Source Governance
- Governing principles: `doc/Draft/medhat/تقرير مبادئ حاكمة`
- Execution directive: `doc/Draft/medhat/برومبت استكمال مهام`
- Prompt 6 retrospective: `doc/Draft/medhat/تقرير تنفيذ برومبت 6`

## Closed Before This Unit
- `receive_purchase_atomic`: Production + Edge + PWA operation identity verified by Prompt 6 E2E.
- `create-stock-voucher`: canonical RPC path and tenant context closed.
- `post_inventory_adjustment_atomic`: Physical movement delegated to `post_stock_movement`.
- `save_sales_invoice_atomic`: PR #14 merged into `main` as commit `3e7ff26ecfacc153878adb9cd96f977e472206d9`.

## Closure Unit: complete-return
### Production Facts
- Production Edge Function `complete-return` version 24 is a thin authenticated wrapper around `complete_return_atomic`.
- Production RPC signature: `(uuid,text,text,boolean,text,jsonb)` and `SECURITY DEFINER`.
- Physical Stock mutation is delegated to `post_stock_movement`.
- Idempotency is keyed by `erp_operation_registry` operation identity.

### Defects Found and Fixed
1. Runsheet-only return path referenced `v_order.id` when `v_order` was never assigned. Fixed by using `p_order_code IS NOT NULL` guards.
2. Historical `driver_liabilities` responsibility had disappeared from the refactored Core. Restored for runsheet shortages inside the same atomic transaction.
3. Rescue audit action initially violated the existing `audit_log_action_check`; changed to the existing `update` action contract.

### Verification
- Transactional Runsheet Return test used real Production `RS-1` and real order detail `1003`, with temporary fixture changes rolled back.
- Test path: loaded 1, returned 0.5, condition `missing`.
- Liability was created inside the transaction.
- Retry of the identical operation returned `duplicate=true`.
- Final Production baseline after rollback: `RS-1` returned to `Picked`, test order detail returned to `qty_loaded=0`, `qty_returned=0`, `driver_liability=0`.
- No persistent `driver_liabilities` rows remained from the test.

## Status
`complete-return` = Production implemented + transactionally verified.

## Remaining Sweep
Proceed to `complete_order_delivery`, then global Physical Writer discovery and final Production snapshot. No percentage is valid before that snapshot.
