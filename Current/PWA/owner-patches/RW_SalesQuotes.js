// ============================================================
// RW_SalesQuotes — إدارة دورة عروض الأسعار
// Production contract: /functions/v1/sales-quotes
// Owner-side module: paste into the authoritative parent main.html
// immediately before the exact // EVENTS & BOOT marker.
// ============================================================
var RW_SalesQuotes = (function () {
    'use strict';
    var state = { rows: [], kpi: {}, catalog: { customers: [], items: [], branches: [], sales_reps: [] }, timer: null, editCode: null, conversionOps: {} };
    function esc(value) { return String(value == null ? '' : value).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;').replace(/'/g, '&#039;'); }
    function money(v) { return Number(v || 0).toLocaleString('ar-SA', { minimumFractionDigits: 2, maximumFractionDigits: 2 }); }
    function today() { return new Date().toISOString().slice(0, 10); }
    function plusDays(n) { var d = new Date(); d.setDate(d.getDate() + n); return d.toISOString().slice(0, 10); }
    function apiBase() { if (typeof RW_SUPABASE_URL !== 'undefined' && RW_SUPABASE_URL) return RW_SUPABASE_URL; if (typeof SUPABASE_URL !== 'undefined' && SUPABASE_URL) return SUPABASE_URL; if (typeof supabaseUrl !== 'undefined' && supabaseUrl) return supabaseUrl; throw new Error('SUPABASE_URL غير متاح'); }
    async function api(action, payload) {
        var sessionRes = await supabase.auth.getSession();
        if (!sessionRes.data || !sessionRes.data.session || !sessionRes.data.session.access_token) throw new Error('انتهت الجلسة');
        var res = await fetch(apiBase() + '/functions/v1/sales-quotes', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + sessionRes.data.session.access_token }, body: JSON.stringify(Object.assign({ action: action }, payload || {})) });
        var data = await res.json().catch(function () { return {}; });
        if (!res.ok || data && data.success === false) throw new Error((data && (data.msg || data.message)) || 'فشل تنفيذ عملية عروض الأسعار');
        return data;
    }
    async function loadCatalog() { var data = await api('catalog'); state.catalog = { customers: data.customers || [], items: data.items || [], branches: data.branches || [], sales_reps: data.sales_reps || [] }; }
    async function loadData() {
        try { await api('expire_due'); } catch (e) {}
        var statusEl = byId('sq-filter-status'); var queryEl = byId('sq-filter-query');
        var result = await Promise.all([api('summary'), api('list', { status: statusEl ? statusEl.value || null : null, query: queryEl ? queryEl.value.trim() || null : null, limit: 100, offset: 0 })]);
        state.kpi = result[0] && result[0].kpi || {}; state.rows = result[1] && result[1].rows || []; renderKPIs(); renderRows();
    }
    function statusLabel(s) { var m = { Draft: 'مسودة', Sent: 'مرسل', Accepted: 'مقبول', Rejected: 'مرفوض', Expired: 'منتهي', Cancelled: 'ملغى', Converted: 'محوّل لأوردر' }; return m[s] || s || ''; }
    function statusClass(s) { var m = { Draft: 'bg-slate-100 text-slate-700', Sent: 'bg-blue-100 text-blue-700', Accepted: 'bg-emerald-100 text-emerald-700', Rejected: 'bg-rose-100 text-rose-700', Expired: 'bg-amber-100 text-amber-700', Cancelled: 'bg-gray-100 text-gray-600', Converted: 'bg-violet-100 text-violet-700' }; return m[s] || 'bg-slate-100 text-slate-700'; }
    function renderKPIs() { var el = byId('sq-kpis'); if (!el) return; var k = state.kpi || {}; safeHTML(el, '<div class="rw-kpi-card"><div class="text-slate-500 text-sm font-bold">إجمالي العروض</div><div class="text-3xl font-black mt-2">' + esc(k.total || 0) + '</div><div class="text-xs text-slate-400 mt-2">كل حالات دورة العرض</div></div><div class="rw-kpi-card"><div class="text-slate-500 text-sm font-bold">العروض المرسلة</div><div class="text-3xl font-black mt-2">' + esc(k.sent || 0) + '</div><div class="text-xs text-slate-400 mt-2">بانتظار قرار العميل</div></div><div class="rw-kpi-card"><div class="text-slate-500 text-sm font-bold">العروض المقبولة</div><div class="text-3xl font-black mt-2">' + esc(k.accepted || 0) + '</div><div class="text-xs text-slate-400 mt-2">جاهزة للتحويل</div></div><div class="rw-kpi-card"><div class="text-slate-500 text-sm font-bold">قيمة المبيعات المحولة</div><div class="text-3xl font-black mt-2">' + esc(money(k.won_value || 0)) + '</div><div class="text-xs text-slate-400 mt-2">من العروض المقبولة/المحولة</div></div>'); }
    function renderRows() {
        var body = byId('sq-table'); if (!body) return;
        if (!state.rows.length) { safeHTML(body, '<tr><td colspan="8" class="p-10 text-center text-slate-500">لا توجد عروض أسعار مطابقة</td></tr>'); return; }
        var html = '';
        for (var i = 0; i < state.rows.length; i++) {
            var q = state.rows[i];
            var actions = '<button class="px-3 py-2 rounded-lg bg-slate-100 font-black" data-sq-action="detail" data-code="' + esc(q.quote_code) + '">عرض</button>';
            if (q.status === 'Draft') {
                actions += ' <button class="px-3 py-2 rounded-lg bg-blue-600 text-white font-black" data-sq-action="send" data-code="' + esc(q.quote_code) + '">إرسال</button>';
                actions += ' <button class="px-3 py-2 rounded-lg bg-white border font-black" data-sq-action="edit" data-code="' + esc(q.quote_code) + '">تعديل</button>';
                actions += ' <button class="px-3 py-2 rounded-lg bg-rose-50 text-rose-700 font-black" data-sq-action="cancel" data-code="' + esc(q.quote_code) + '">إلغاء</button>';
            } else if (q.status === 'Sent') {
                actions += ' <button class="px-3 py-2 rounded-lg bg-emerald-600 text-white font-black" data-sq-action="accept" data-code="' + esc(q.quote_code) + '">قبول</button>';
                actions += ' <button class="px-3 py-2 rounded-lg bg-rose-600 text-white font-black" data-sq-action="reject" data-code="' + esc(q.quote_code) + '">رفض</button>';
                actions += ' <button class="px-3 py-2 rounded-lg bg-slate-100 font-black" data-sq-action="cancel" data-code="' + esc(q.quote_code) + '">إلغاء</button>';
            } else if (q.status === 'Accepted') actions += ' <button class="px-3 py-2 rounded-lg bg-violet-600 text-white font-black" data-sq-action="convert" data-code="' + esc(q.quote_code) + '">تحويل لأوردر</button>';
            html += '<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black">' + esc(q.quote_code) + '</td><td class="p-3">' + esc(q.quote_date) + '</td><td class="p-3">' + esc(q.customer_name || '—') + '</td><td class="p-3">' + esc(q.sales_rep_name || '—') + '</td><td class="p-3">' + esc(q.item_count || 0) + '</td><td class="p-3 font-black">' + esc(money(q.total_amount)) + '</td><td class="p-3"><span class="px-3 py-1 rounded-full text-xs font-black ' + statusClass(q.status) + '">' + esc(statusLabel(q.status)) + '</span></td><td class="p-3"><div class="flex flex-wrap gap-2">' + actions + '</div></td></tr>';
        }
        safeHTML(body, html);
    }
    function options(list, selected, placeholder, labelFn) { var h = '<option value="">' + esc(placeholder) + '</option>'; for (var i = 0; i < list.length; i++) { var x = list[i]; h += '<option value="' + esc(x.id) + '"' + (selected === x.id ? ' selected' : '') + '>' + esc(labelFn(x)) + '</option>'; } return h; }
    function customerOptions(s) { return options(state.catalog.customers, s, 'اختر العميل', function (c) { return c.name + (c.phone ? ' — ' + c.phone : ''); }); }
    function branchOptions(s) { return options(state.catalog.branches, s, 'الفرع الرئيسي', function (b) { return b.branch_code + ' — ' + b.name; }); }
    function repOptions(s) { return options(state.catalog.sales_reps, s, 'اختر المندوب', function (r) { return r.name || r.email; }); }
    function itemOptions(s) { return options(state.catalog.items, s, 'اختر الصنف', function (i) { return i.item_code + ' — ' + i.name; }).replace(/value="([^"]+)"/g, 'value="$1"'); }
    function readFormItems() { var rows = document.querySelectorAll('[data-sq-line]'), items = []; for (var i = 0; i < rows.length; i++) { var r = rows[i]; var code = r.querySelector('[data-sq-item]'), qty = r.querySelector('[data-sq-qty]'), price = r.querySelector('[data-sq-price]'), disc = r.querySelector('[data-sq-disc]'), tax = r.querySelector('[data-sq-tax]'); var x = { item_code: code ? code.value : '', qty: Number(qty ? qty.value : 0), unit_price: Number(price ? price.value : 0), discount_percent: Number(disc ? disc.value : 0), tax_rate: Number(tax ? tax.value : 0) }; if (x.item_code && x.qty > 0) items.push(x); } return items; }
    function addLine(item) { var host = byId('sq-lines'); if (!host) return; item = item || {}; var row = document.createElement('div'); row.setAttribute('data-sq-line', '1'); row.className = 'grid grid-cols-12 gap-2 items-center border-b pb-3'; row.innerHTML = '<div class="col-span-5"><select data-sq-item class="w-full p-3 border rounded-xl">' + itemOptions(item.item_code || '') + '</select></div><div class="col-span-2"><input data-sq-qty type="number" min="0.01" step="0.01" class="w-full p-3 border rounded-xl" value="' + esc(item.qty || '') + '" placeholder="الكمية"></div><div class="col-span-2"><input data-sq-price type="number" min="0" step="0.01" class="w-full p-3 border rounded-xl" value="' + esc(item.unit_price || '') + '" placeholder="السعر"></div><div class="col-span-1"><input data-sq-disc type="number" min="0" max="100" step="0.01" class="w-full p-3 border rounded-xl" value="' + esc(item.discount_percent || 0) + '" placeholder="%"></div><div class="col-span-1"><input data-sq-tax type="number" min="0" step="0.01" class="w-full p-3 border rounded-xl" value="' + esc(item.tax_rate || 0) + '" placeholder="%"></div><div class="col-span-1"><button type="button" class="w-full p-3 rounded-xl bg-rose-50 text-rose-700 font-black" data-sq-remove>×</button></div>'; row.querySelector('[data-sq-remove]').addEventListener('click', function () { row.remove(); }); host.appendChild(row); }
    async function openNew() { state.editCode = null; await loadCatalog(); openEditor(null); }
    async function openEditor(q) {
        q = q || {}; if (!q.items && q.quote_code) { var d = await api('detail', { quote_code: q.quote_code }); q = Object.assign({}, d.quote, { items: d.items || [] }); }
        var modal = document.createElement('div'); modal.id = 'sq-modal'; modal.className = 'fixed inset-0 z-[1000] bg-black/50 flex items-center justify-center p-4'; modal.innerHTML = '<div class="bg-white rounded-3xl shadow-2xl w-full max-w-6xl max-h-[95vh] overflow-auto p-6"><div class="flex items-center justify-between mb-5"><div><h3 class="text-2xl font-black">' + (q.quote_code ? 'تعديل العرض ' + esc(q.quote_code) : 'عرض سعر جديد') + '</h3><p class="text-sm text-slate-500 mt-1">العرض التجاري لا يحجز ولا يصرف مخزونًا.</p></div><button id="sq-close" class="px-4 py-2 rounded-xl bg-slate-100 font-black">إغلاق</button></div><div class="grid grid-cols-1 md:grid-cols-4 gap-3 mb-5"><select id="sq-customer" class="p-3 border rounded-xl">' + customerOptions(q.customer_id || '') + '</select><select id="sq-branch" class="p-3 border rounded-xl">' + branchOptions(q.branch_id || '') + '</select><select id="sq-rep" class="p-3 border rounded-xl">' + repOptions(q.sales_rep_id || '') + '</select><input id="sq-valid" type="date" class="p-3 border rounded-xl" value="' + esc(q.valid_until || plusDays(7)) + '"></div><div class="grid grid-cols-1 md:grid-cols-3 gap-3 mb-5"><input id="sq-name" class="p-3 border rounded-xl" placeholder="اسم عميل غير مسجل" value="' + esc(q.customer_name || '') + '"><input id="sq-phone" class="p-3 border rounded-xl" placeholder="هاتف العميل" value="' + esc(q.customer_phone || '') + '"><input id="sq-area" class="p-3 border rounded-xl" placeholder="المنطقة" value="' + esc(q.area || '') + '"></div><div class="grid grid-cols-1 md:grid-cols-3 gap-3 mb-5"><input id="sq-discount" type="number" min="0" step="0.01" class="p-3 border rounded-xl" placeholder="خصم عام" value="' + esc(q.discount_amount || 0) + '"><input id="sq-delivery" type="number" min="0" step="0.01" class="p-3 border rounded-xl" placeholder="رسوم توصيل" value="' + esc(q.delivery_fee || 0) + '"><input id="sq-reference" class="p-3 border rounded-xl" placeholder="مرجع العميل/العرض" value="' + esc(q.reference || '') + '"></div><div class="mb-5"><textarea id="sq-notes" class="w-full p-3 border rounded-xl" rows="2" placeholder="ملاحظات">' + esc(q.notes || '') + '</textarea></div><div class="mb-4"><div class="flex items-center justify-between mb-3"><h4 class="font-black text-lg">الأصناف</h4><button id="sq-add-line" class="px-4 py-2 rounded-xl bg-slate-900 text-white font-black">+ إضافة صنف</button></div><div class="grid grid-cols-12 gap-2 text-xs font-black text-slate-500 pb-2"><div class="col-span-5">الصنف</div><div class="col-span-2">كمية</div><div class="col-span-2">سعر</div><div class="col-span-1">خصم%</div><div class="col-span-1">ضريبة%</div><div class="col-span-1"></div></div><div id="sq-lines" class="space-y-3"></div></div><div class="grid grid-cols-1 md:grid-cols-2 gap-3"><input id="sq-terms" class="p-3 border rounded-xl" placeholder="الشروط" value="' + esc(q.terms || '') + '"><div class="flex justify-end gap-2"><button id="sq-save" class="px-6 py-3 rounded-xl bg-blue-600 text-white font-black">حفظ مسودة العرض</button></div></div></div>';
        document.body.appendChild(modal); var lines = q.items || []; for (var i = 0; i < lines.length; i++) addLine(lines[i]); if (!lines.length) addLine({});
        byId('sq-close').addEventListener('click', function () { modal.remove(); }); byId('sq-add-line').addEventListener('click', function () { addLine({}); });
        byId('sq-save').addEventListener('click', async function () {
            var items = readFormItems(); if (!items.length) return showToast('أضف صنفًا صالحًا واحدًا على الأقل', 'warning');
            try { showLoader('جاري حفظ العرض...'); var customer = byId('sq-customer').value || null; var customerObj = state.catalog.customers.find(function (x) { return x.id === customer; }) || null; var payload = { quote_date: q.quote_date || today(), valid_until: byId('sq-valid').value || null, customer_id: customer, customer_name: byId('sq-name').value.trim() || (customerObj && customerObj.name) || null, customer_phone: byId('sq-phone').value.trim() || (customerObj && customerObj.phone) || null, area: byId('sq-area').value.trim() || (customerObj && customerObj.area) || null, branch_id: byId('sq-branch').value || null, sales_rep_id: byId('sq-rep').value || null, currency: 'SAR', discount_amount: Number(byId('sq-discount').value || 0), delivery_fee: Number(byId('sq-delivery').value || 0), reference: byId('sq-reference').value.trim() || null, notes: byId('sq-notes').value.trim() || null, terms: byId('sq-terms').value.trim() || null, items: items }; if (q.quote_code) { payload.quote_code = q.quote_code; await api('update', payload); } else { payload.operation_id = crypto.randomUUID(); await api('create', payload); } hideLoader(); modal.remove(); showToast('تم حفظ العرض بنجاح', 'success'); await loadData(); } catch (e) { hideLoader(); showToast(e.message || 'فشل حفظ العرض', 'error'); }
        });
    }
    async function detail(code) { try { showLoader('جاري تحميل تفاصيل العرض...'); var data = await api('detail', { quote_code: code }); hideLoader(); var q = data.quote || {}, lines = data.items || [], history = data.history || [], modal = document.createElement('div'); modal.className = 'fixed inset-0 z-[1000] bg-black/50 flex items-center justify-center p-4'; var lh = '', hh = ''; for (var i = 0; i < lines.length; i++) lh += '<tr class="border-b"><td class="p-3">' + esc(lines[i].item_code) + '</td><td class="p-3">' + esc(lines[i].item_name) + '</td><td class="p-3">' + esc(lines[i].qty) + '</td><td class="p-3">' + esc(money(lines[i].unit_price)) + '</td><td class="p-3 font-black">' + esc(money(lines[i].line_total)) + '</td></tr>'; for (var h = 0; h < history.length; h++) hh += '<div class="flex items-center justify-between border-b py-2"><div><span class="font-black">' + esc(statusLabel(history[h].to_status)) + '</span><span class="text-xs text-slate-500 mr-2">' + esc(history[h].action) + '</span></div><div class="text-xs text-slate-500">' + esc(history[h].actor_email) + ' · ' + esc(history[h].created_at) + '</div></div>'; modal.innerHTML = '<div class="bg-white rounded-3xl shadow-2xl w-full max-w-5xl max-h-[95vh] overflow-auto p-6"><div class="flex items-center justify-between mb-5"><div><h3 class="text-2xl font-black">' + esc(q.quote_code) + '</h3><p class="text-sm text-slate-500">' + esc(q.customer_name || '—') + ' · ' + esc(statusLabel(q.status)) + '</p></div><button class="px-4 py-2 rounded-xl bg-slate-100 font-black" data-sq-close>إغلاق</button></div><div class="grid grid-cols-2 md:grid-cols-5 gap-3 mb-5"><div class="p-4 bg-slate-50 rounded-2xl"><div class="text-xs text-slate-500">التاريخ</div><div class="font-black mt-1">' + esc(q.quote_date) + '</div></div><div class="p-4 bg-slate-50 rounded-2xl"><div class="text-xs text-slate-500">الصلاحية</div><div class="font-black mt-1">' + esc(q.valid_until || 'بدون') + '</div></div><div class="p-4 bg-slate-50 rounded-2xl"><div class="text-xs text-slate-500">الأصناف</div><div class="font-black mt-1">' + esc(lines.length) + '</div></div><div class="p-4 bg-slate-50 rounded-2xl"><div class="text-xs text-slate-500">الإجمالي</div><div class="font-black mt-1">' + esc(money(q.total_amount)) + '</div></div><div class="p-4 bg-slate-50 rounded-2xl"><div class="text-xs text-slate-500">الأوردر</div><div class="font-black mt-1">' + esc(q.converted_order_id ? 'تم التحويل' : '—') + '</div></div></div><div class="border rounded-2xl overflow-hidden mb-5"><table class="w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-3 text-right">الكود</th><th class="p-3 text-right">الصنف</th><th class="p-3 text-right">الكمية</th><th class="p-3 text-right">السعر</th><th class="p-3 text-right">الإجمالي</th></tr></thead><tbody>' + lh + '</tbody></table></div><div class="border rounded-2xl p-4"><h4 class="font-black mb-2">سجل دورة الحياة</h4>' + (hh || '<div class="text-slate-500">لا يوجد سجل</div>') + '</div></div>'; document.body.appendChild(modal); modal.querySelector('[data-sq-close]').addEventListener('click', function () { modal.remove(); }); } catch (e) { hideLoader(); showToast(e.message || 'فشل تحميل التفاصيل', 'error'); } }
    async function action(code, actionName) { try { showLoader('جاري تنفيذ العملية...'); var result = await api(actionName, { quote_code: code }); hideLoader(); showToast(result && result.status ? 'تم تحديث الحالة إلى ' + statusLabel(result.status) : 'تم التنفيذ', 'success'); await loadData(); } catch (e) { hideLoader(); showToast(e.message || 'فشل تنفيذ العملية', 'error'); } }
    async function convert(code) { if (!state.conversionOps[code]) state.conversionOps[code] = crypto.randomUUID(); try { showLoader('جاري تحويل العرض إلى أوردر...'); var result = await api('convert', { quote_code: code, operation_id: state.conversionOps[code] }); hideLoader(); if (result && result.order_code) showToast('تم التحويل إلى ' + result.order_code, 'success'); await loadData(); } catch (e) { hideLoader(); showToast(e.message || 'فشل تحويل العرض', 'error'); } }
    function bind() { var root = byId('rw-page-container') || document; root.addEventListener('click', function (e) { var btn = e.target.closest('[data-sq-action]'); if (!btn) return; var code = btn.getAttribute('data-code'), a = btn.getAttribute('data-sq-action'); if (a === 'detail') return detail(code); if (a === 'edit') return api('detail', { quote_code: code }).then(async function (d) { await loadCatalog(); openEditor(Object.assign({}, d.quote, { items: d.items || [] })); }).catch(function (err) { showToast(err.message || 'فشل فتح العرض', 'error'); }); if (a === 'convert') return convert(code); if (a === 'send' || a === 'accept' || a === 'reject' || a === 'cancel') return action(code, a); }); }
    function render() { var c = byId('rw-content') || byId('rw-page-container') || document.querySelector('.rw-page-container'); if (!c) return; safeHTML(c, '<div class="space-y-6"><div class="flex items-center justify-between gap-4 flex-wrap"><div><div class="text-sm text-slate-500 font-bold">المبيعات</div><h2 class="text-3xl font-black mt-1">عروض الأسعار</h2><p class="text-sm text-slate-500 mt-2">دورة تجارية متكاملة من المسودة حتى التحويل إلى أوردر، بدون أي حركة مخزنية في مرحلة العرض.</p></div><button id="sq-new" class="px-5 py-3 rounded-2xl bg-blue-600 text-white font-black shadow-lg">+ عرض سعر جديد</button></div><div id="sq-kpis" class="rw-kpi-grid"></div><div class="bg-white rounded-3xl border shadow-sm p-4"><div class="flex flex-col md:flex-row gap-3"><select id="sq-filter-status" class="p-3 border rounded-xl"><option value="">كل الحالات</option><option value="Draft">مسودة</option><option value="Sent">مرسل</option><option value="Accepted">مقبول</option><option value="Rejected">مرفوض</option><option value="Expired">منتهي</option><option value="Cancelled">ملغى</option><option value="Converted">محوّل لأوردر</option></select><input id="sq-filter-query" class="flex-1 p-3 border rounded-xl" placeholder="بحث برقم العرض أو العميل أو الهاتف"><button id="sq-refresh" class="px-5 py-3 rounded-xl bg-slate-900 text-white font-black">تحديث</button></div></div><div class="bg-white rounded-3xl border shadow-sm overflow-hidden"><div class="p-4 border-b"><h3 class="font-black text-xl">سجل عروض الأسعار</h3><p class="text-xs text-slate-500 mt-1">العرض Snapshot تجاري مستقل؛ المخزون لا يتغير قبل تحويل الأوردر وتنفيذ دورة الوفاء.</p></div><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-3 text-right">العرض</th><th class="p-3 text-right">التاريخ</th><th class="p-3 text-right">العميل</th><th class="p-3 text-right">المندوب</th><th class="p-3 text-right">الأصناف</th><th class="p-3 text-right">الإجمالي</th><th class="p-3 text-right">الحالة</th><th class="p-3 text-right">الإجراءات</th></tr></thead><tbody id="sq-table"><tr><td colspan="8" class="p-10 text-center text-slate-500">جاري التحميل...</td></tr></tbody></table></div></div></div>'); byId('sq-new').addEventListener('click', function () { openNew().catch(function (e) { showToast(e.message || 'فشل فتح نموذج العرض', 'error'); }); }); byId('sq-refresh').addEventListener('click', function () { loadData().catch(function (e) { showToast(e.message || 'فشل التحديث', 'error'); }); }); byId('sq-filter-status').addEventListener('change', function () { loadData().catch(function () {}); }); byId('sq-filter-query').addEventListener('keydown', function (e) { if (e.key === 'Enter') loadData().catch(function () {}); }); loadCatalog().then(loadData).catch(function (e) { showToast(e.message || 'فشل تحميل عروض الأسعار', 'error'); }); clearInterval(state.timer); state.timer = setInterval(function () { if (byId('sq-table')) loadData().catch(function () {}); }, 30000); bind(); }
    return { render: render };
})();
window.RW_SalesQuotes = RW_SalesQuotes;
