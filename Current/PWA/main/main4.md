/* RAWAEA MAIN4 — governed rebuild: POS / Roles / TeleSales */
(function(){'use strict';
if(!window.RW_ShellContext||typeof RW_ShellContext.getCompanyId!=='function')throw Error('MAIN4_REQUIRES_RW_SHELL_CONTEXT');
var $=id=>document.getElementById(id), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])), cid=()=>{var c=RW_ShellContext.getCompanyId();if(!c)throw Error('TENANT_CONTEXT_UNAVAILABLE');return c};
async function auth(){var r=await supabase.auth.getSession(),t=r?.data?.session?.access_token;if(!t)throw Error('انتهت الجلسة');return t}
async function call(fn,body){var t=await auth(),r=await fetch(RW_SUPABASE_URL+'/functions/v1/'+fn,{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+t},body:JSON.stringify(body||{})}),j=await r.json().catch(()=>({}));if(!r.ok||j.success===false)throw Error(j.error||j.msg||('فشل '+fn));return j}
function toast(e,t){var s=e?.message||String(e||'خطأ');if(typeof showToast==='function')showToast(s,t||'error');else console.error(s)}
async function cfg(f='*'){var r=await supabase.from('app_settings').select(f).eq('company_id',cid()).maybeSingle();if(r.error)throw r.error;if(!r.data)throw Error('إعدادات الشركة غير موجودة');return r.data}
async function getBranches(){var r=await supabase.from('branches').select('id,branch_code,name').eq('company_id',cid()).order('branch_code');if(r.error)throw r.error;return r.data||[]}
async function getStock(ids){if(!ids.length)return{};var r=await supabase.from('stock_branches').select('item_id,branch_id,qty,allocated_qty').in('branch_id',ids);if(r.error)throw r.error;var o={};(r.data||[]).forEach(x=>(o[x.item_id]??={},o[x.item_id][x.branch_id]={qty:+x.qty||0,allocated:+x.allocated_qty||0}));return o}
function item(code){return(RW_STATE.data.items||[]).find(x=>x.item_code===code)||null} function customer(code){return(RW_STATE.data.customers||[]).find(x=>x.customer_code===code||x.code===code)||null}

var RW_POS=(function(){var cart=[],fee=0,min=0,taxRate=0,branchCode='';
function draw(){var t=$('pos-cart-body');if(!t)return;var sub=0;safeHTML(t,cart.length?cart.map((x,i)=>{var v=+x.price*+x.qty;sub+=v;return`<tr><td>${esc(x.name)}<small>${esc(x.code)}</small></td><td>${(+x.price).toLocaleString()} EGP</td><td><input min="1" type="number" value="${x.qty}" onchange="RW_POS._updateQty(${i},this.value)"></td><td>${v.toLocaleString()} EGP</td><td><button onclick="RW_POS._removeItem(${i})">✕</button></td></tr>`}).join(''):'<tr><td colspan="5">لا توجد أصناف</td></tr>');var v=sub+fee,t=Math.round(v*taxRate)/100;['pos-subtotal','pos-delivery','pos-tax','pos-total','pos-count'].forEach((id,i)=>safeText($(id),[sub,fee,t,v+t,cart.length][i].toLocaleString?.()??String([sub,fee,t,v+t,cart.length][i])));if($('pos-del-row'))$('pos-del-row').classList.toggle('hidden',fee<=0);if($('pos-tax-row'))$('pos-tax-row').classList.toggle('hidden',taxRate<=0||sub<=0)}
async function render(){var c=$('rw-page-container');if(!c)return;safeText($('rw-header-title'),'نقطة البيع');try{var s=await cfg('delivery_fee,min_invoice_amount,tax_rate,main_branch_id');fee=+s.delivery_fee||0;min=+s.min_invoice_amount||0;taxRate=+s.tax_rate||0;var b=await supabase.from('branches').select('branch_code').eq('id',s.main_branch_id).eq('company_id',cid()).maybeSingle();if(b.error)throw b.error;if(!b.data)throw Error('MAIN_BRANCH_NOT_FOUND');branchCode=b.data.branch_code;if(!RW_STATE.data.items?.length)await RW_Data.loadItems();if(!RW_STATE.data.customers?.length)await RW_Data.loadCustomers()}catch(e){toast(e);return}safeHTML(c,'<div><h2>نقطة البيع</h2><select id="pos-customer"></select><input id="pos-area" readonly><input id="pos-search" placeholder="بحث" oninput="RW_POS._search(this.value)"><div id="pos-dropdown" class="hidden"></div><table><tbody id="pos-cart-body"></tbody></table><b id="pos-subtotal">0</b><div id="pos-del-row">رسوم التوصيل <b id="pos-delivery">0</b></div><div id="pos-tax-row">الضريبة <b id="pos-tax">0</b></div><b id="pos-total">0</b> <span id="pos-count">0</span><button onclick="RW_POS._clearCart()">مسح الكل</button><button onclick="RW_POS._save()">حفظ الفاتورة</button></div>');safeHTML($('pos-customer'),'<option value="">-- اختر عميلاً --</option>'+(RW_STATE.data.customers||[]).map(x=>`<option value="${esc(x.customer_code||x.code)}">${esc(x.name)}</option>`).join(''));$('pos-customer').onchange=onCust;cart=[];draw()}
function onCust(){var c=customer($('pos-customer')?.value);if($('pos-area'))$('pos-area').value=c?.area||''}
function search(q){var d=$('pos-dropdown');if(!d)return;q=String(q||'').trim().toLowerCase();if(!q)return d.classList.add('hidden');var f=(RW_STATE.data.items||[]).filter(x=>String(x.name||'').toLowerCase().includes(q)||String(x.item_code||'').toLowerCase().includes(q)||String(x.barcode||'').toLowerCase().includes(q)).slice(0,20);safeHTML(d,f.map(x=>`<div onclick="RW_POS._addItem('${esc(x.item_code)}')">${esc(x.name)} — ${(+x.sales_price||0).toLocaleString()} EGP</div>`).join('')||'<div>لا توجد نتائج</div>');d.classList.remove('hidden')}
function addItem(code){var x=item(code);if(!x)return;var e=cart.find(y=>y.code===code);e?e.qty++:cart.push({code:x.item_code,name:x.name,price:+x.sales_price||0,unit:x.unit||'حبة',qty:1});if($('pos-dropdown'))$('pos-dropdown').classList.add('hidden');if($('pos-search'))$('pos-search').value='';draw()}
function updateQty(i,v){var q=+v;if(!Number.isFinite(q)||q<=0)cart.splice(i,1);else cart[i].qty=q;draw()} function removeItem(i){cart.splice(i,1);draw()} function clearCart(){if(!cart.length)return toast('السلة فارغة','info');Swal.fire({title:'تأكيد مسح السلة',showCancelButton:true,confirmButtonText:'نعم',cancelButtonText:'إلغاء'}).then(r=>{if(r.isConfirmed){cart=[];draw()}})}
async function ensureQRCode(){if(typeof QRCode!=='undefined')return true;return await new Promise((res,rej)=>{var s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/qrcodejs@1.0.0/qrcode.min.js';s.onload=()=>res(true);s.onerror=rej;document.head.appendChild(s)})}
async function save(){await ensureQRCode().catch(()=>{});var code=$('pos-customer')?.value;if(!code)return toast('اختر عميلاً','warning');if(!cart.length)return toast('أضف أصنافاً','warning');var c=customer(code),sub=cart.reduce((a,x)=>a+x.price*x.qty,0),before=sub+fee,t=Math.round(before*taxRate)/100,total=before+t;if(min>0&&total<min)return toast('الحد الأدنى للفاتورة: '+min,'warning');showLoader('جاري حفظ الفاتورة...');try{var r=await call('save-sales-invoice',{orderHeader:{operation_id:crypto.randomUUID(),customer_code:code,custId:code,custName:c?.name||'',area:c?.area||'',total,deliveryFees:fee,status:'Invoiced',paymentType:'نقدي',taxAmount:t,taxRate,source:'pos'},itemsList:cart.map(x=>({code:x.code,name:x.name,price:x.price,qty:x.qty,unit:x.unit})),branchCode});if(typeof RW_Audit_log==='function')RW_Audit_log('create','orders',r.orderID||'',null,{customer_code:code,total});cart=[];draw();toast('تم حفظ الفاتورة بنجاح','success')}catch(e){toast(e)}finally{hideLoader()}}
return{render,_search:search,_addItem:addItem,_updateQty:updateQty,_removeItem:removeItem,_clearCart:clearCart,_onCustChange:onCust,_save:save}})();window.RW_POS=RW_POS;

var RW_Roles=(function(){var roles=[];var AP='pos,telesales,orders,van-sales,sales_supervisor,warehouse_supervisor,warehouse,delivery,delivery_supervisor,purchases,purchases_supervisor,finance,online-store,sales_manager,warehouse_manager,finance_manager,general_manager,hr'.split(','),ERP='dash,items,stock_adjustment,customers,suppliers,branches,runsheets,receiving,picking,loading,return,unloading,vouchers,transfer,direct-sale,direct-return,supplier-return,vehicle-count,branch-count,general-count,reports,users,roles,settings,settlement'.split(',');
async function load(){var r=await supabase.from('roles').select('*').eq('company_id',cid()).order('role_name');if(r.error)throw r.error;roles=r.data||[]}
function table(){var w=$('roles-table-wrapper');if(!w)return;safeHTML(w,roles.map(r=>`<div><b>${esc(r.role_name)}</b> — ${esc(r.description||'')} <button onclick="RW_Roles._openModal('${esc(r.id)}')">✎</button></div>`).join('')||'<div>لا توجد أدوار</div>')}
function tabs(){return'<div><button id="role-tab-apps" onclick="RW_Roles._switchRoleTab(\'apps\')">التطبيقات</button><button id="role-tab-erp" onclick="RW_Roles._switchRoleTab(\'erp\')">النظام الأم</button></div><div id="role-panel-apps">'+AP.map(x=>`<label><input class="role-perm" type="checkbox" value="${esc(x)}">${esc(x)}</label>`).join('')+'</div><div id="role-panel-erp" class="hidden">'+ERP.map(x=>`<label><input class="role-perm" type="checkbox" value="${esc(x)}">${esc(x)}</label>`).join('')+'</div>'}
async function render(){var c=$('rw-page-container');if(!c)return;safeText($('rw-header-title'),'إدارة الأدوار');safeHTML(c,'<button id="btn-seed-roles">تهيئة الأدوار</button><button id="btn-add-role">إضافة دور</button><div id="roles-table-wrapper"></div>');try{await load();table()}catch(e){toast(e);return}$('btn-add-role').onclick=()=>openModal();$('btn-seed-roles').onclick=seed}
function openModal(id){var role=id?roles.find(r=>r.id===id):null,h=`<div><input id="role-name" value="${esc(role?.role_name||'')}" placeholder="اسم الدور"><input id="role-desc" value="${esc(role?.description||'')}" placeholder="الوصف">${tabs()}<button onclick="Swal.close()">إلغاء</button>${role?'<button id="delete-role">حذف</button>':''}<button id="save-role">حفظ</button></div>`;Swal.fire({html:h,showConfirmButton:false,width:850,didOpen:()=>{var p=Array.isArray(role?.permissions)?role.permissions:[];document.querySelectorAll('.role-perm').forEach(x=>x.checked=p.includes(x.value));$('save-role').onclick=async()=>{try{showLoader('جاري الحفظ...');await call('save-role',{id:role?.id||null,role_name:$('role-name').value.trim(),description:$('role-desc').value.trim(),permissions:[...document.querySelectorAll('.role-perm:checked')].map(x=>x.value),is_system:!!role?.is_system});await load();table();Swal.close();toast('تم الحفظ','success')}catch(e){toast(e)}finally{hideLoader()}};if(role&&$('delete-role'))$('delete-role').onclick=async()=>{try{showLoader('جاري الحذف...');await call('delete-role',{roleId:role.id});await load();table();Swal.close();toast('تم الحذف','success')}catch(e){toast(e)}finally{hideLoader()}}}})}
function switchRoleTab(t){var a=$('role-panel-apps'),e=$('role-panel-erp');if(t==='apps'){a.classList.remove('hidden');e.classList.add('hidden')}else{a.classList.add('hidden');e.classList.remove('hidden')}} async function seed(){try{showLoader('جاري التهيئة...');await call('seed-roles',{});await load();table();toast('تمت التهيئة','success')}catch(e){toast(e)}finally{hideLoader()}}
return{render,_openModal:openModal,_switchRoleTab:switchRoleTab}})();window.RW_Roles=RW_Roles;

var RW_TeleSales=(function(){var cart=[],sel=null,fee=0,taxRate=0,branches={},stock={};
function avail(id){var code=$('ts-branch-select')?.value,b=code&&branches[code];if(b&&stock[id]?.[b]){var s=stock[id][b];return Math.max(0,s.qty-s.allocated)}return Object.values(stock[id]||{}).reduce((a,s)=>a+Math.max(0,s.qty-s.allocated),0)}
function draw(){var t=$('ts-cart-body');if(!t)return;var sub=0;safeHTML(t,cart.length?cart.map((x,i)=>{var v=x.price*x.qty;sub+=v;return`<tr><td>${esc(x.name)}</td><td>${x.price.toLocaleString()} EGP</td><td><input type="number" min="1" value="${x.qty}" onchange="RW_TeleSales._updateQty(${i},this.value)"></td><td>${v.toLocaleString()}</td><td><button onclick="RW_TeleSales._removeItem(${i})">✕</button></td></tr>`}).join(''):'<tr><td colspan="5">السلة فارغة</td></tr>');var b=sub+fee,t=Math.round(b*taxRate)/100;safeText($('ts-subtotal'),sub.toLocaleString());safeText($('ts-delivery'),fee.toLocaleString());safeText($('ts-tax'),t.toLocaleString());safeText($('ts-total'),(b+t).toLocaleString());safeText($('ts-cart-count'),String(cart.length));if($('ts-del-row'))$('ts-del-row').classList.toggle('hidden',fee<=0);if($('ts-tax-row'))$('ts-tax-row').classList.toggle('hidden',taxRate<=0||sub<=0)}
async function render(){var c=$('rw-page-container');if(!c)return;safeText($('rw-header-title'),'التلي سيلز');try{var s=await cfg('delivery_fee,tax_rate');fee=+s.delivery_fee||0;taxRate=+s.tax_rate||0;if(!RW_STATE.data.items?.length)await RW_Data.loadItems();if(!RW_STATE.data.customers?.length)await RW_Data.loadCustomers();var bs=await getBranches();branches={};bs.forEach(b=>branches[b.branch_code]=b.id);window._teleBranches=bs;window._teleBranchMap=branches;stock=await getStock(bs.map(b=>b.id));window._teleStockCache=stock}catch(e){toast(e);return}safeHTML(c,'<div><input id="ts-customer-search" oninput="RW_TeleSales._searchCustomers(this.value)" placeholder="بحث عميل"><div id="ts-customer-results" class="hidden"></div><button id="ts-add-customer-btn">+ عميل</button><div id="ts-customer-info" class="hidden"><b id="ts-cust-name"></b><div id="ts-cust-details"></div></div><select id="ts-branch-select"><option value="">-- اختر الفرع --</option></select><input id="ts-item-search" oninput="RW_TeleSales._searchItems(this.value)" placeholder="بحث صنف"><div id="ts-item-results" class="hidden"></div><table><tbody id="ts-cart-body"></tbody></table><div id="ts-del-row">التوصيل <b id="ts-delivery">0</b></div><div id="ts-tax-row">الضريبة <b id="ts-tax">0</b></div><div>المجموع <b id="ts-subtotal">0</b></div><b id="ts-total">0</b><span id="ts-cart-count">0</span><button onclick="RW_TeleSales._clearCart()">مسح</button><button onclick="RW_TeleSales._saveOrder()">حفظ</button></div>');loadBranches();bind();draw()}
function bind(){if($('ts-add-customer-btn'))$('ts-add-customer-btn').onclick=showCustomer}
function loadBranches(){safeHTML($('ts-branch-select'),'<option value="">-- اختر الفرع --</option>'+Object.keys(branches).map(k=>`<option value="${esc(k)}">${esc(window._teleBranches.find(b=>b.branch_code===k)?.name||k)}</option>`).join(''))}
function searchCustomers(q){var d=$('ts-customer-results');if(!d)return;q=String(q||'').trim().toLowerCase();if(!q)return d.classList.add('hidden');var f=(RW_STATE.data.customers||[]).filter(c=>String(c.name||'').toLowerCase().includes(q)||String(c.customer_code||'').toLowerCase().includes(q)||String(c.phone||'').includes(q)||String(c.area||'').toLowerCase().includes(q)).slice(0,15);safeHTML(d,f.map(c=>`<div onclick="RW_TeleSales._selectCustomer('${esc(c.customer_code)}')">${esc(c.name)} <small>${esc(c.customer_code)}</small></div>`).join('')||'<div>لا توجد نتائج</div>');d.classList.remove('hidden')}
function selectCustomer(code){sel=customer(code);if(!sel)return;$('ts-customer-search').value=sel.name+' ('+sel.customer_code+')';$('ts-customer-results').classList.add('hidden');$('ts-customer-info').classList.remove('hidden');safeText($('ts-cust-name'),sel.name);safeHTML($('ts-cust-details'),`<div>المنطقة: ${esc(sel.area||'')}</div><div>الدفع: ${esc(sel.payment_type||'')}</div><div>الهاتف: ${esc(sel.phone||'')}</div><div>الرصيد: ${(+sel.debt||0).toLocaleString()}</div>`)}
function showCustomer(){var ids=['cust-code','cust-name','cust-phone','cust-area','cust-location','cust-contact','cust-notes'];Swal.fire({html:'<div><input id="cust-code" value="جديد" readonly>'+ids.filter(id=>id!=='cust-code').map(id=>`<input id="${id}" placeholder="${id}">`).join('')+'<select id="cust-type"><option>عادي</option><option>جملة</option><option>VIP</option></select><select id="cust-payment"><option>نقدي</option><option>أجل</option></select><input id="cust-debt" type="number" value="0"><select id="cust-visit"><option value="">يوم الزيارة</option><option>السبت</option><option>الأحد</option><option>الإثنين</option><option>الثلاثاء</option><option>الأربعاء</option><option>الخميس</option></select><button id="save-new-customer">حفظ</button></div>',showConfirmButton:false,didOpen:()=>{$('save-new-customer').onclick=async()=>_saveNewCustomer({name:$('cust-name').value.trim(),phone:$('cust-phone').value.trim(),area:$('cust-area').value.trim(),location:$('cust-location').value.trim(),contact_person:$('cust-contact').value.trim(),notes:$('cust-notes').value.trim(),customer_type:$('cust-type').value,payment_type:$('cust-payment').value,debt:+$('cust-debt').value||0,visit_day:$('cust-visit').value})}})}
async function _saveNewCustomer(c){try{showLoader('جاري الحفظ...');var r=await call('save-customer',{customer:c,isEdit:false,customer_code:null});await RW_Data.loadCustomers();selectCustomer(r.customer_code);Swal.close();toast('تمت إضافة العميل','success')}catch(e){toast(e)}finally{hideLoader()}}
function searchItems(q){var d=$('ts-item-results');if(!d)return;q=String(q||'').trim().toLowerCase();if(!q)return d.classList.add('hidden');var f=(RW_STATE.data.items||[]).filter(x=>String(x.name||'').toLowerCase().includes(q)||String(x.item_code||'').toLowerCase().includes(q)||String(x.barcode||'').toLowerCase().includes(q)).slice(0,20);safeHTML(d,f.map(x=>`<div onclick="RW_TeleSales._addToCart('${esc(x.item_code)}')">${esc(x.name)} <small>${esc(x.item_code)} | ${avail(x.id)}</small></div>`).join('')||'<div>لا توجد نتائج</div>');d.classList.remove('hidden')}
function addToCart(code){var x=item(code);if(!x)return;var n=(cart.find(c=>c.code===code)?.qty||0)+1;if(n>avail(x.id))return toast('الرصيد المتاح غير كافٍ','warning');if(+x.max_qty>0&&n>+x.max_qty)return toast('تجاوز الحد الأقصى','warning');var e=cart.find(c=>c.code===code);e?e.qty++:cart.push({code:x.item_code,name:x.name,price:+x.sales_price||0,unit:x.unit||'حبة',qty:1});$('ts-item-results').classList.add('hidden');$('ts-item-search').value='';draw()}
function updateQty(i,v){var q=+v,x=item(cart[i].code);if(!Number.isFinite(q)||q<=0)return cart.splice(i,1),draw();if(x&&(q>avail(x.id)||+x.max_qty>0&&q>+x.max_qty))return toast('الكمية تتجاوز المتاح','warning');cart[i].qty=q;draw()} function removeItem(i){cart.splice(i,1);draw()} function clearCart(){cart.length?Swal.fire({title:'تأكيد مسح السلة',showCancelButton:true,confirmButtonText:'نعم',cancelButtonText:'إلغاء'}).then(r=>{if(r.isConfirmed){cart=[];draw()}}):toast('السلة فارغة','info')}
async function saveOrder(){if(!sel)return toast('اختر العميل','warning');var bc=$('ts-branch-select')?.value;if(!bc)return toast('اختر الفرع','warning');if(!cart.length)return toast('أضف أصنافاً','warning');for(var x of cart){var m=item(x.code);if(!m||x.qty>avail(m.id)||+m.max_qty>0&&x.qty>+m.max_qty)return toast('الرصيد أو الحد المسموح غير كافٍ للصنف '+x.code,'warning')}var sub=cart.reduce((a,x)=>a+x.price*x.qty,0),b=sub+fee,t=Math.round(b*taxRate)/100,total=b+t;showLoader('جاري حفظ الأوردر...');try{var r=await call('save-sales-invoice',{orderHeader:{operation_id:crypto.randomUUID(),customer_code:sel.customer_code,custName:sel.name,area:sel.area||'',total,deliveryFees:fee,status:'Confirmed',paymentType:sel.payment_type||'أجل',taxAmount:t,taxRate,source:'telesales'},itemsList:cart.map(x=>({code:x.code,name:x.name,price:x.price,qty:x.qty,unit:x.unit})),branchCode:bc});if(typeof RW_Audit_log==='function')RW_Audit_log('create','orders',r.orderID||'',null,{customer_code:sel.customer_code,total});cart=[];sel=null;$('ts-customer-search').value='';$('ts-customer-info').classList.add('hidden');draw();toast('تم حفظ الأوردر بنجاح','success')}catch(e){toast(e)}finally{hideLoader()}}
return{render,_searchCustomers:searchCustomers,_selectCustomer:selectCustomer,_searchItems:searchItems,_addToCart:addToCart,_updateQty:updateQty,_removeItem:removeItem,_clearCart:clearCart,_saveOrder:saveOrder}})();window.RW_TeleSales=RW_TeleSales;
})();