/* RAWAEA ERP — main3 governed reconstruction v4
   Modules: Customers / Suppliers / Branches / Settings / Users.
   Tenant: RW_ShellContext.getCompanyId(); Physical Stock: forbidden here. */
(function(){
'use strict';
if(!window.RW_ShellContext||typeof RW_ShellContext.getCompanyId!=='function')throw Error('MAIN3_REQUIRES_RW_SHELL_CONTEXT');
const $=id=>document.getElementById(id),C=()=>RW_ShellContext.getCompanyId(),esc=x=>String(x??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const attr=esc,msg=x=>typeof showToast==='function'?showToast(String(x?.message||x),'error'):console.error(x);
async function edge(fn,body){const s=await supabase.auth.getSession(),t=s?.data?.session?.access_token;if(!t)throw Error('انتهت الجلسة');const r=await fetch(RW_SUPABASE_URL+'/functions/v1/'+fn,{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+t},body:JSON.stringify(body||{})}),j=await r.json().catch(()=>({}));if(!r.ok||j.success===false)throw Error(j.error||j.msg||'فشل '+fn);return j}
function M(h,w=900){Swal.fire({html:h,width:w,showConfirmButton:false,showCancelButton:false,customClass:{popup:'!bg-transparent !shadow-none !p-0'}})}
function fields(spec,obj){return spec.map(f=>f[3]==='textarea'?`<textarea id="${f[0]}" class="p-2.5 border rounded-lg w-full ${f[4]||''}" placeholder="${f[1]}">${esc(obj?.[f[2]]??'')}</textarea>`:`<input id="${f[0]}" type="${f[3]||'text'}" class="p-2.5 border rounded-lg w-full ${f[4]||''}" placeholder="${f[1]}" value="${attr(obj?.[f[2]]??'')}">`).join('')}
function values(spec){const o={};spec.forEach(f=>o[f[2]]=f[3]==='number'?(Number($(f[0])?.value)||0):($(f[0])?.value?.trim()||''));return o}
const search=(arr,q,ks)=>arr.filter(x=>ks.some(k=>String(x?.[k]||'').toLowerCase().includes(q)));
/* ---------- Customers ---------- */
var RW_Customers=(function(){let d=[],f='customer_code',up=true,spec=[['cust-name','اسم العميل *','name'],['cust-phone','الهاتف','phone'],['cust-area','المنطقة','area'],['cust-location','العنوان','location'],['cust-debt','الرصيد','debt','number'],['cust-contact','مسؤول التواصل','contact_person'],['cust-notes','ملاحظات','notes','textarea']];
async function load(){d=await RW_Data.loadCustomers();return d}
function table(r){let w=$('cust-table-wrapper');if(!w)return;if(!r.length)return safeHTML(w,'<div class="text-center p-10">لا يوجد عملاء</div>');let cs=[['customer_code','الكود'],['name','الاسم'],['phone','الهاتف'],['area','المنطقة'],['contact_person','مسؤول التواصل'],['visit_day','يوم الزيارة'],['debt','الرصيد']];r=r.slice().sort((a,b)=>{let x=f==='debt'?+a.debt||0:String(a[f]||'').toLowerCase(),y=f==='debt'?+b.debt||0:String(b[f]||'').toLowerCase();return x<y?(up?-1:1):x>y?(up?1:-1):0});safeHTML(w,'<table class="w-full text-sm"><thead><tr>'+cs.map(c=>`<th class="p-3" data-f="${A(c[0])}" onclick="RW_Customers._sort(this.dataset.f)">${E(c[1])}</th>`).join('')+'</tr></thead><tbody id="cust-tbody"></tbody></table><div id="cust-tbody-controls"></div>');RW_Table.paginate('cust-tbody',r,1,50,c=>`<tr data-c="${A(c.customer_code)}" onclick="RW_Customers._openModal(this.dataset.c)" class="cursor-pointer"><td class="p-3">${E(c.customer_code)}</td><td class="p-3">${E(c.name)}</td><td class="p-3">${E(c.phone)}</td><td class="p-3">${E(c.area)}</td><td class="p-3">${E(c.contact_person)}</td><td class="p-3">${E(c.visit_day)}</td><td class="p-3">${(+c.debt||0).toLocaleString()}</td></tr>`)}
function _sort(x){f===x?up=!up:(f=x,up=true);table(d)}
function _openModal(code){let c=code?d.find(x=>x.customer_code===code):null,e=!!c,sp=spec.slice();let h='<div class="p-6" dir="rtl"><h3 class="text-xl font-bold mb-4">'+(e?'تعديل العميل':'إضافة عميل جديد')+'</h3><div class="grid md:grid-cols-2 gap-3">'+fields(sp,c)+'</div><select id="cust-type" class="p-2.5 border rounded-lg w-full mt-3"><option>عادي</option><option>جملة</option><option>VIP</option></select><select id="cust-payment" class="p-2.5 border rounded-lg w-full mt-3"><option>نقدي</option><option>أجل</option></select><select id="cust-visit" class="p-2.5 border rounded-lg w-full mt-3"><option value="">اختر يوم الزيارة</option>'+['السبت','الأحد','الإثنين','الثلاثاء','الأربعاء','الخميس'].map(x=>'<option value="'+A(x)+'">'+E(x)+'</option>').join('')+'</select><div class="flex justify-end gap-3 mt-4">'+(e?'<button id="btn-delete-cust" class="bg-red-600 text-white px-5 py-2 rounded-xl">حذف</button>':'')+'<button onclick="Swal.close()" class="border px-5 py-2 rounded-xl">إلغاء</button><button id="btn-save-cust" class="bg-emerald-600 text-white px-6 py-2 rounded-xl">حفظ</button></div></div>';M(h);if(e){$('cust-type').value=c.customer_type||'عادي';$('cust-payment').value=c.payment_type||'نقدي';$('cust-visit').value=c.visit_day||''}$('btn-save-cust').onclick=()=>_handleSave(c,e);if(e)$('btn-delete-cust').onclick=()=>_handleDelete(c)}
async function _handleSave(c,e){let p=values(spec);p.customer_type=$('cust-type').value;p.payment_type=$('cust-payment').value;p.visit_day=$('cust-visit').value;if(!p.name)return msg('اسم العميل مطلوب');showLoader('جاري الحفظ...');try{let j=await edge('save-customer',{customer:p,isEdit:e,customer_code:c?.customer_code||null});if(window.RW_Audit_log)RW_Audit_log(e?'update':'create','customers',j.customer_code||c?.customer_code||'',e?c:null,p);hideLoader();Swal.close();showToast(e?'تم التعديل':'تمت الإضافة','success');table(await load())}catch(x){hideLoader();msg(x)}}
async function _handleDelete(c){if(!(await Swal.fire({title:'تأكيد الحذف',text:'حذف هذا العميل؟',icon:'warning',showCancelButton:true,confirmButtonText:'حذف',cancelButtonText:'إلغاء'})).isConfirmed)return;showLoader('جاري الحذف...');try{await edge('delete-customer',{customer_code:c.customer_code});hideLoader();Swal.close();showToast('تم الحذف','success');table(await load())}catch(x){hideLoader();msg(x)}}
async function render(){let c=$('rw-page-container');if(!c)return;safeText($('rw-header-title'),'العملاء');safeHTML(c,'<div class="p-4"><div class="flex justify-between mb-4"><h2 class="text-xl font-bold">العملاء</h2><button id="btn-add-cust" class="bg-emerald-600 text-white px-4 py-2 rounded-xl">+ عميل جديد</button></div><input id="cust-search" class="w-full p-3 border rounded-xl mb-4" placeholder="بحث..."><div id="cust-table-wrapper"></div></div>');table(await load());$('cust-search').oninput=()=>{let q=$('cust-search').value.toLowerCase().trim();table(search(d,q,['name','customer_code','phone']))};$('btn-add-cust').onclick=()=>_openModal(null)}return{render,_sort,_openModal,_handleSave,_handleDelete}})();window.RW_Customers=RW_Customers;
/* ---------- Suppliers ---------- */
var RW_Suppliers=(function(){let d=[],f='supplier_code',up=true;
async function load(){let r=await supabase.from('suppliers').select('*').eq('company_id',C()).order('supplier_code');if(r.error)throw r.error;return d=r.data||[]}
function table(r){let w=$('supp-table-wrapper');if(!w)return;if(!r.length)return safeHTML(w,'<div class="text-center p-10">لا يوجد موردين</div>');let cs=[['supplier_code','الكود'],['name','الاسم'],['phone','الهاتف'],['area','المنطقة'],['contact_person','جهة الاتصال'],['accounts_payable','الرصيد']];safeHTML(w,'<table class="w-full text-sm"><thead><tr>'+cs.map(c=>`<th class="p-3" data-f="${A(c[0])}" onclick="RW_Suppliers._sort(this.dataset.f)">${E(c[1])}</th>`).join('')+'</tr></thead><tbody id="supp-tbody"></tbody></table><div id="supp-tbody-controls"></div>');RW_Table.paginate('supp-tbody',r.slice().sort((a,b)=>{let x=f==='accounts_payable'?+a[f]||0:String(a[f]||'').toLowerCase(),y=f==='accounts_payable'?+b[f]||0:String(b[f]||'').toLowerCase();return x<y?(up?-1:1):x>y?(up?1:-1):0}),1,50,s=>`<tr data-c="${A(s.supplier_code)}" onclick="RW_Suppliers._openModal(this.dataset.c)" class="cursor-pointer"><td class="p-3">${E(s.supplier_code)}</td><td class="p-3">${E(s.name)}</td><td class="p-3">${E(s.phone)}</td><td class="p-3">${E(s.area)}</td><td class="p-3">${E(s.contact_person)}</td><td class="p-3">${(+s.accounts_payable||0).toLocaleString()}</td></tr>`)}
function _sort(x){f===x?up=!up:(f=x,up=true);table(d)}
function _openModal(code){let s=code?d.find(x=>x.supplier_code===code):null,e=!!s,h='<div class="p-6" dir="rtl"><h3 class="text-xl font-bold mb-4">'+(e?'تعديل المورد':'إضافة مورد جديد')+'</h3><div class="grid md:grid-cols-2 gap-3">'+fields([['supp-name','اسم المورد *','name'],['supp-phone','الهاتف','phone'],['supp-area','المنطقة','area'],['supp-address','العنوان','address'],['supp-contact','جهة الاتصال','contact_person'],['supp-rep','مسؤول المشتريات','purchase_rep'],['supp-balance','الرصيد','accounts_payable','number'],['supp-notes','ملاحظات','notes','textarea']],s)+'</div><select id="supp-type" class="p-2.5 border rounded-lg w-full mt-3"><option>مورد عام</option><option>مصنع</option><option>مورد خارجي</option></select><select id="supp-payment" class="p-2.5 border rounded-lg w-full mt-3"><option>نقدي</option><option>أجل</option></select><div class="flex justify-end gap-3 mt-4">'+(e?'<button id="btn-delete-supp" class="bg-red-600 text-white px-5 py-2 rounded-xl">حذف</button>':'')+'<button onclick="Swal.close()" class="border px-5 py-2 rounded-xl">إلغاء</button><button id="btn-save-supp" class="bg-orange-600 text-white px-6 py-2 rounded-xl">حفظ</button></div></div>';M(h);if(e){$('supp-type').value=s.supplier_type||'مورد عام';$('supp-payment').value=s.payment_type||'نقدي'}$('btn-save-supp').onclick=async()=>{let p=values([['supp-name','اسم المورد','name'],['supp-phone','الهاتف','phone'],['supp-area','المنطقة','area'],['supp-address','العنوان','address'],['supp-contact','جهة الاتصال','contact_person'],['supp-rep','مسؤول المشتريات','purchase_rep'],['supp-balance','الرصيد','accounts_payable','number'],['supp-notes','ملاحظات','notes','textarea']]);p.supplier_type=$('supp-type').value;p.payment_type=$('supp-payment').value;if(!p.name)return msg('اسم المورد مطلوب');showLoader('جاري الحفظ...');try{let j=await edge('save-supplier',{supplier:p,isEdit:e,supplier_code:s?.supplier_code||null});if(window.RW_Audit_log)RW_Audit_log(e?'update':'create','suppliers',j.supplier_code||s?.supplier_code||'',e?s:null,p);hideLoader();Swal.close();showToast(e?'تم التعديل':'تمت الإضافة','success');table(await load())}catch(x){hideLoader();msg(x)}};if(e)$('btn-delete-supp').onclick=async()=>{if(!(await Swal.fire({title:'تأكيد الحذف',text:'حذف هذا المورد؟',icon:'warning',showCancelButton:true,confirmButtonText:'حذف',cancelButtonText:'إلغاء'})).isConfirmed)return;showLoader('جاري الحذف...');try{await edge('delete-supplier',{supplier_code:s.supplier_code});hideLoader();Swal.close();showToast('تم الحذف','success');table(await load())}catch(x){hideLoader();msg(x)}}}
async function render(){let c=$('rw-page-container');if(!c)return;safeText($('rw-header-title'),'الموردين');safeHTML(c,'<div class="p-4"><div class="flex justify-between mb-4"><h2 class="text-xl font-bold">الموردين</h2><button id="btn-add-supp" class="bg-orange-600 text-white px-4 py-2 rounded-xl">+ مورد جديد</button></div><input id="supp-search" class="w-full p-3 border rounded-xl mb-4" placeholder="بحث..."><div id="supp-table-wrapper"></div></div>');table(await load());$('supp-search').oninput=()=>{let q=$('supp-search').value.toLowerCase().trim();table(search(d,q,['name','supplier_code','phone']))};$('btn-add-supp').onclick=()=>_openModal(null)}
return{render,_sort,_openModal,_handleSave:(s,e)=>_openModal(s?.supplier_code||null),_handleDelete:s=>_openModal(s?.supplier_code||null)}})();window.RW_Suppliers=RW_Suppliers;
/* ---------- Branches ---------- */
var RW_Branches=(function(){let d=[];async function render(){let c=$('rw-page-container');if(!c)return;safeText($('rw-header-title'),'المخازن والفروع');d=await RW_Data.loadBranches();safeHTML(c,'<div class="p-4"><div class="flex justify-between mb-4"><h2 class="text-xl font-bold">الفروع والمخازن</h2><button id="btn-add-branch" class="bg-indigo-600 text-white px-4 py-2 rounded-xl">+ إضافة فرع</button></div><input id="branch-search" class="w-full p-3 border rounded-xl mb-4" placeholder="بحث..."><div id="branch-table-wrapper"></div></div>');paint();$('branch-search').oninput=()=>paint(search(d,$('branch-search').value.toLowerCase(),['name','branch_code','location']));$('btn-add-branch').onclick=()=>_openModal(null)}
function paint(r=d){let w=$('branch-table-wrapper');safeHTML(w,r.length?'<table class="w-full text-sm"><thead><tr><th class="p-3">الكود</th><th class="p-3">الاسم</th><th class="p-3">الموقع</th><th class="p-3">المدير</th><th class="p-3">الهاتف</th><th class="p-3">الحالة</th></tr></thead><tbody>'+r.map(b=>`<tr data-c="${A(b.branch_code)}" onclick="RW_Branches._openModal(this.dataset.c)" class="cursor-pointer"><td class="p-3">${E(b.branch_code)}</td><td class="p-3">${E(b.name)}</td><td class="p-3">${E(b.location)}</td><td class="p-3">${E(b.manager)}</td><td class="p-3">${E(b.phone)}</td><td class="p-3">${b.is_active?'🟢':'🔴'}</td></tr>`).join('')+'</tbody></table>':'<div class="text-center p-10">لا توجد فروع</div>')}
async function _openModal(code){let b=code?d.find(x=>x.branch_code===code):null,e=!!b,h='<div class="p-6" dir="rtl"><h3 class="text-xl font-bold mb-4">'+(e?'تعديل الفرع':'إضافة فرع جديد')+'</h3>'+fields([['branch-name','اسم الفرع *','name'],['branch-location','الموقع','location'],['branch-manager','المدير','manager'],['branch-phone','الهاتف','phone']],b)+'<select id="branch-status" class="p-2.5 border rounded-lg w-full mt-3"><option value="active">نشط</option><option value="inactive">غير نشط</option></select><div class="flex justify-end gap-3 mt-4">'+(e?'<button id="btn-delete-branch" class="bg-red-600 text-white px-5 py-2 rounded-xl">حذف</button>':'')+'<button onclick="Swal.close()" class="border px-5 py-2 rounded-xl">إلغاء</button><button id="btn-save-branch" class="bg-indigo-600 text-white px-6 py-2 rounded-xl">حفظ</button></div></div>';M(h,800);if(e)$('branch-status').value=b.is_active?'active':'inactive';$('btn-save-branch').onclick=async()=>{let p=values([['branch-name','الاسم','name'],['branch-location','الموقع','location'],['branch-manager','المدير','manager'],['branch-phone','الهاتف','phone']]);p.is_active=$('branch-status').value==='active';if(!p.name)return msg('اسم الفرع مطلوب');showLoader('جاري الحفظ...');try{await edge('save-branch',{branch:p,isEdit:e,branch_code:b?.branch_code||null});hideLoader();Swal.close();showToast(e?'تم التعديل':'تمت الإضافة','success');d=await RW_Data.loadBranches();paint(d)}catch(x){hideLoader();msg(x)}};if(e)$('btn-delete-branch').onclick=async()=>{if(!(await Swal.fire({title:'تأكيد الحذف',text:'حذف هذا الفرع؟',icon:'warning',showCancelButton:true,confirmButtonText:'حذف',cancelButtonText:'إلغاء'})).isConfirmed)return;showLoader('جاري الحذف...');try{await edge('delete-branch',{branch_code:b.branch_code});hideLoader();Swal.close();showToast('تم الحذف','success');d=await RW_Data.loadBranches();paint(d)}catch(x){hideLoader();msg(x)}}}
return{render,_openModal}})();window.RW_Branches=RW_Branches;
/* Settings */
var RW_Settings=(function(){let cur={};async function render(){let c=$('rw-page-container');if(!c)return;safeText($('rw-header-title'),'إعدادات النظام');showLoader('جاري تحميل الإعدادات...');try{let r=await supabase.from('app_settings').select('*').eq('company_id',C()).order('created_at',{ascending:true}).order('id',{ascending:true}).limit(1).maybeSingle();if(r.error)throw r.error;let s=r.data||{};cur=s;safeHTML(c,'<div class="p-6 max-w-2xl mx-auto"><div class="grid md:grid-cols-2 gap-3">'+[['settings-delivery-fee','delivery_fee'],['settings-min-invoice','min_invoice_amount'],['settings-tax-rate','tax_rate']].map(x=>'<input id="'+x[0]+'" type="number" class="p-2.5 border rounded-lg" placeholder="'+x[1]+'" value="'+A(s[x[1]]||0)+'">').join('')+'<select id="settings-currency" class="p-2.5 border rounded-lg">'+['SAR','EGP','AED','KWD','QAR','BHD','OMR','USD'].map(x=>'<option'+(s.currency===x?' selected':'')+'>'+x+'</option>').join('')+'</select><input id="settings-company-name" class="p-2.5 border rounded-lg md:col-span-2" placeholder="اسم الشركة" value="'+A(s.company_name||'')+'"></div><input id="settings-logo-file" type="file" accept="image/*" class="mt-4"><img id="settings-logo-preview" class="w-20 h-20 object-contain mt-2 border rounded-xl" src="'+A(s.company_logo||'')+'"><button id="btn-save-invoice-settings" class="mt-4 bg-blue-600 text-white px-6 py-2 rounded-xl">حفظ</button><hr class="my-5"><input id="settings-vat-number" class="w-full p-2.5 border rounded-lg mb-3" placeholder="الرقم الضريبي" value="'+A(s.vat_number||'')+'"><input id="settings-registered-name" class="w-full p-2.5 border rounded-lg mb-3" placeholder="الاسم التجاري" value="'+A(s.registered_name||'')+'"><textarea id="settings-business-address" class="w-full p-2.5 border rounded-lg">'+E(s.business_address||'')+'</textarea><button id="btn-save-zatca-settings" class="mt-3 bg-emerald-600 text-white px-6 py-2 rounded-xl">حفظ الفوترة</button></div>');$('settings-logo-file').onchange=e=>{let f=e.target.files?.[0];if(f){let r=new FileReader;r.onload=x=>$('settings-logo-preview').src=x.target.result;r.readAsDataURL(f)}};$('btn-save-invoice-settings').onclick=()=>save({delivery_fee:+val('settings-delivery-fee'),min_invoice_amount:+val('settings-min-invoice'),tax_rate:+val('settings-tax-rate'),currency:val('settings-currency'),company_name:val('settings-company-name'),company_logo:cur.company_logo||''});$('btn-save-zatca-settings').onclick=()=>save({vat_number:val('settings-vat-number'),registered_name:val('settings-registered-name'),business_address:val('settings-business-address')})}catch(x){msg(x)}finally{hideLoader()}}async function save(p){showLoader('جاري الحفظ...');try{let f=$('settings-logo-file')?.files?.[0];if(f){let u=await supabase.storage.from('product-images').upload('logos/'+Date.now()+'-'+f.name,f,{upsert:true});if(u.error)throw u.error;p.company_logo=supabase.storage.from('product-images').getPublicUrl(u.data.path).data.publicUrl}await edge('save-settings',p);hideLoader();showToast('تم حفظ الإعدادات','success')}catch(x){hideLoader();msg(x)}}return{render}})();window.RW_Settings=RW_Settings;
/* Users */
var RW_Users=(function(){let emps=[],roles=[],branches=[],perms=[['pos','نقطة البيع'],['telesales','التلي سيلز'],['orders','الأوردرات'],['van-sales','فان سيلز'],['sales_supervisor','مشرف المبيعات'],['warehouse_supervisor','مشرف المخازن'],['warehouse','عمال المخازن'],['delivery','مندوب التوصيل'],['delivery_supervisor','مشرف التوصيل'],['purchases','المشتريات'],['purchases_supervisor','مشرف المشتريات'],['finance','المحاسب'],['online-store','المتجر الإلكتروني'],['sales_manager','مدير المبيعات'],['warehouse_manager','مدير المخازن'],['finance_manager','المدير المالي'],['general_manager','المدير العام'],['hr','الموارد البشرية'],['dash','لوحة التحكم'],['items','الأصناف والمخزون'],['customers','العملاء'],['suppliers','الموردين'],['branches','الفروع والمخازن'],['runsheets','الرانشيتات'],['receiving','الاستلام'],['picking','التحضير'],['loading','التحميل'],['return','المرتجعات'],['unloading','التفريغ'],['vouchers','الأذونات'],['transfer','تحويل مخزني'],['direct-sale','صرف سيارة'],['direct-return','مرتجع سيارة'],['supplier-return','مرتجع مورد'],['vehicle-count','جرد سيارة'],['branch-count','جرد فرع'],['general-count','جرد عام'],['reports','التقارير'],['users','المستخدمين'],['roles','الأدوار'],['settings','الإعدادات'],['settlement','إغلاق اليومية']];
const L=v=>Array.isArray(v)?v.map(String):typeof v==='string'?(()=>{try{let x=JSON.parse(v);return Array.isArray(x)?x.map(String):[]}catch(_){return v.split(',').map(x=>x.trim()).filter(Boolean)}})():[];
async function load(){let c=C();let [u,r,b]=await Promise.all([supabase.from('users').select('*').eq('company_id',c).order('name'),supabase.from('roles').select('*').eq('company_id',c).order('role_name'),RW_Data.loadBranches()]);if(u.error)throw u.error;emps=u.data||[];roles=r.error?[]:(r.data||[]);branches=b||[]}
function table(){let w=$('emp-table-wrapper'),q=($('emp-search')?.value||'').toLowerCase().trim(),r=emps.filter(e=>!(e.role==='مالك'||e.role==='Owner'||e.is_owner===true));if(q)r=search(r,q,['name','email','phone']);safeHTML(w,r.length?'<table class="w-full text-sm"><thead><tr><th class="p-3">الاسم</th><th class="p-3">البريد</th><th class="p-3">الدور</th><th class="p-3">الحالة</th><th class="p-3">الهاتف</th><th class="p-3">الفروع</th></tr></thead><tbody>'+r.map(e=>`<tr data-e="${A(e.email)}" onclick="RW_Users._openModal(this.dataset.e)" class="cursor-pointer"><td class="p-3">${E(e.name)}</td><td class="p-3">${E(e.email)}</td><td class="p-3">${E(e.role)}</td><td class="p-3">${e.status==='Active'?'🟢':'🔴'}</td><td class="p-3">${E(e.phone)}</td><td class="p-3">${E(L(e.allowed_branch_ids).join(', ')||'*')}</td></tr>`).join('')+'</tbody></table>':'<div class="text-center p-10">لا يوجد مستخدمون</div>')}
function emp(email){return emps.find(e=>e.email===email)||emps.find(e=>e.email===window._currentEmpEmail)||null}
async function assignments(){let e=emp();if(!e)return;let r=await supabase.from('customer_assignments').select('customer_id,customers!inner(customer_code,name,area,phone,company_id)').eq('user_id',e.id).eq('is_active',true).eq('customers.company_id',C());if(r.error)throw r.error;window._assignedCustomerIds=(r.data||[]).map(x=>x.customer_id);safeHTML($('assigned-customers-list'),(r.data||[]).map(a=>`<div class="flex justify-between p-2 border-b"><span>${E(a.customers.name)} <small>${E(a.customers.customer_code)}</small></span><button data-id="${A(a.customer_id)}" onclick="RW_Users._removeAssignedCustomer(this.dataset.id)">✕ إزالة</button></div>`).join('')||'<div class="text-center p-3">لا يوجد عملاء معينون</div>')}
function _switchEmpTab(t){['basic','perms','field','assignments'].forEach(k=>{$('emp-panel-'+k)?.classList.add('hidden');$('tab-'+k)?.classList.remove('border-blue-600','text-blue-600')});$('emp-panel-'+t)?.classList.remove('hidden');$('tab-'+t)?.classList.add('border-blue-600','text-blue-600');if(t==='assignments')assignments().catch(msg)}
function _openModal(email){let e=emp(email),is=!!e,cp=L(e?.permissions),ab=L(e?.allowed_branch_ids),ro=roles.map(r=>`<option value="${A(r.role_name)}"${e?.role===r.role_name?' selected':''}>${E(r.role_name)}</option>`).join(''),br=branches.map(b=>{let x=String(b.branch_code||b.id);return`<option value="${A(x)}"${ab.includes(x)?' selected':''}>${E(b.name)} (${E(x)})</option>`}).join(''),pc=perms.map(p=>`<label class="text-xs"><input type="checkbox" class="emp-custom-perm" value="${A(p[0])}"${cp.includes(p[0])?' checked':''}> ${E(p[1])}</label>`).join('');let h='<div class="p-6" dir="rtl"><h3 class="text-xl font-bold mb-4">'+(is?'تعديل مستخدم':'إضافة مستخدم جديد')+'</h3><div class="flex border-b mb-4"><button id="tab-basic" onclick="RW_Users._switchEmpTab(\'basic\')" class="px-3 py-2 border-b-2 border-blue-600 text-blue-600">البيانات</button><button id="tab-perms" onclick="RW_Users._switchEmpTab(\'perms\')" class="px-3 py-2">الصلاحيات</button><button id="tab-field" onclick="RW_Users._switchEmpTab(\'field\')" class="px-3 py-2">الميدان</button>'+(is?'<button id="tab-assignments" onclick="RW_Users._switchEmpTab(\'assignments\')" class="px-3 py-2">العملاء</button>':'')+'</div><div id="emp-panel-basic"><div class="grid md:grid-cols-2 gap-3"><input id="emp-name" class="p-2.5 border rounded-lg" placeholder="الاسم *" value="'+A(e?.name||'')+'"><input id="emp-email" type="email" class="p-2.5 border rounded-lg" placeholder="البريد *" value="'+A(e?.email||'')+'"><input id="emp-phone" class="p-2.5 border rounded-lg" placeholder="الهاتف" value="'+A(e?.phone||'')+'"><input id="emp-password" type="password" class="p-2.5 border rounded-lg" placeholder="'+(is?'اتركه فارغًا':'كلمة المرور *')+'"><select id="emp-role" class="p-2.5 border rounded-lg"><option value="">اختر دورًا</option>'+ro+'</select><select id="emp-status" class="p-2.5 border rounded-lg"><option value="Active"'+(!is||e.status==='Active'?' selected':'')+'>نشط</option><option value="Inactive"'+(is&&e.status==='Inactive'?' selected':'')+'>غير نشط</option></select><input id="emp-expiry" type="date" class="p-2.5 border rounded-lg" value="'+A(e?.expiry_date||'')+'"><select id="emp-branches" multiple class="p-2.5 border rounded-lg" style="min-height:100px">'+br+'</select></div></div><div id="emp-panel-perms" class="hidden"><label><input id="emp-custom-perms-toggle" type="checkbox" '+(cp.length?'checked':'')+'> صلاحيات مخصصة</label><div id="emp-custom-perms-section" class="'+(cp.length?'':'hidden ')+'grid grid-cols-2 md:grid-cols-4 gap-2 mt-3">'+pc+'</div></div><div id="emp-panel-field" class="hidden space-y-3"><label><input id="emp-allow-all-customers" type="checkbox" '+(e?.allow_all_customers?'checked':'')+'> السماح بكل العملاء</label><label><input id="emp-restrict-visit-day" type="checkbox" '+(!is||e?.restrict_to_visit_day!==false?'checked':'')+'> تقييد بيوم الزيارة</label><input id="emp-device-id" class="p-2.5 border rounded-lg w-full" placeholder="Device ID" value="'+A(e?.device_id||'')+'"></div>'+(is?'<div id="emp-panel-assignments" class="hidden"><input id="assignment-search" class="w-full p-2.5 border rounded-lg" placeholder="بحث عن عميل"><div id="assignment-results" class="border mt-2 p-2"></div><div id="assigned-customers-list" class="border mt-3 p-2">جاري التحميل...</div></div>':'')+'<div class="flex justify-end gap-3 mt-5 border-t pt-4">'+(is?'<button id="btn-delete-emp" class="bg-red-600 text-white px-5 py-2 rounded-xl mr-auto">حذف</button>':'')+'<button onclick="Swal.close()" class="border px-5 py-2 rounded-xl">إلغاء</button><button id="btn-save-emp" class="bg-indigo-600 text-white px-6 py-2 rounded-xl">حفظ</button></div></div>';M(h,950);$('emp-custom-perms-toggle').onchange=()=>$('emp-custom-perms-section').classList.toggle('hidden');$('assignment-search')?.addEventListener('input',x=>_searchAssignmentCustomers(x.target.value));$('btn-save-emp').onclick=_save;if(is){$('btn-delete-emp').onclick=_del;assignments().catch(msg)}}
async function _save(){let n=val('emp-name'),m=val('emp-email');if(!n||!m)return msg('الاسم والبريد مطلوبان');if(!window._currentEmpEmail&&!val('emp-password'))return msg('كلمة المرور مطلوبة');let p={name:n,email:m,phone:val('emp-phone'),password:val('emp-password')||null,role:val('emp-role'),status:val('emp-status'),expiry_date:val('emp-expiry'),allowed_branch_ids:[...$('emp-branches').selectedOptions].map(x=>x.value),permissions:[...document.querySelectorAll('.emp-custom-perm:checked')].map(x=>x.value),allow_all_customers:$('emp-allow-all-customers').checked,restrict_to_visit_day:$('emp-restrict-visit-day').checked,device_id:val('emp-device-id')};showLoader('جاري الحفظ...');try{await edge('save-employee',{employee:p,isEdit:!!window._currentEmpEmail,originalEmail:window._currentEmpEmail||''});hideLoader();Swal.close();showToast(window._currentEmpEmail?'تم التعديل':'تمت الإضافة','success');await load();table()}catch(x){hideLoader();msg(x)}}
async function _del(){let e=emp();if(!e)return;if(!(await Swal.fire({title:'تأكيد الحذف',text:'حذف هذا الموظف؟',icon:'warning',showCancelButton:true,confirmButtonText:'حذف',cancelButtonText:'إلغاء'})).isConfirmed)return;showLoader('جاري الحذف...');try{await edge('delete-employee',{email:e.email});hideLoader();Swal.close();showToast('تم الحذف','success');await load();table()}catch(x){hideLoader();msg(x)}}
function _searchAssignmentCustomers(q){let w=$('assignment-results');if(!w)return;q=String(q||'').toLowerCase().trim();if(q.length<2)return safeHTML(w,'<div class="text-center p-2">اكتب حرفين على الأقل</div>');let r=search(RW_STATE.data.customers||[],q,['name','customer_code']).slice(0,20);safeHTML(w,r.map(c=>`<div class="flex justify-between p-2 border-b"><span>${E(c.name)} <small>${E(c.customer_code)}</small></span><button data-id="${A(c.id)}" onclick="RW_Users._toggleAssignmentCustomer(this.dataset.id,this)">إضافة</button></div>`).join('')||'<div>لا توجد نتائج</div>')}
async function _toggleAssignmentCustomer(id,btn){let e=emp(),c=(RW_STATE.data.customers||[]).find(x=>x.id===id);if(!e||!c)return msg('العميل غير موجود ضمن الشركة الحالية');let a=window._assignedCustomerIds||[];if(!a.includes(id)){let r=await supabase.from('customer_assignments').upsert({user_id:e.id,customer_id:id,assigned_by:RW_STATE.app.userId||null,is_active:true},{onConflict:'user_id,customer_id'});if(r.error)return msg(r.error);a.push(id)}else{let r=await supabase.from('customer_assignments').update({is_active:false}).eq('user_id',e.id).eq('customer_id',id);if(r.error)return msg(r.error);a.splice(a.indexOf(id),1)}window._assignedCustomerIds=a;btn&&(btn.textContent=a.includes(id)?'✓ مضاف':'إضافة');await assignments()}
async function _removeAssignedCustomer(id){let e=emp();if(!e)return;let r=await supabase.from('customer_assignments').update({is_active:false}).eq('user_id',e.id).eq('customer_id',id);if(r.error)return msg(r.error);await assignments()}
async function render(){let c=$('rw-page-container');if(!c)return;safeText($('rw-header-title'),'المستخدمين والصلاحيات');safeHTML(c,'<div class="p-4"><div class="flex justify-between mb-4"><h2 class="text-xl font-bold">إدارة المستخدمين والصلاحيات</h2><button id="btn-add-emp" class="bg-indigo-600 text-white px-4 py-2 rounded-xl">+ إضافة مستخدم</button></div><input id="emp-search" class="w-full p-3 border rounded-xl mb-4" placeholder="بحث..."><div id="emp-table-wrapper"></div></div>');await load();table();$('emp-search').oninput=table;$('btn-add-emp').onclick=()=>_openModal(null)}
return{render,_openModal,_switchEmpTab,_searchAssignmentCustomers,_toggleAssignmentCustomer,_removeAssignedCustomer}})();window.RW_Users=RW_Users;
window.MAIN3_GOVERNED_RECONSTRUCTED='v4';
})();